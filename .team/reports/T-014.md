# T-014 — M2 shared self-service and governance domain

STATUS: ACCEPTED_CHECKPOINT

## Immutable result

The tested implementation is `a9e64276273fc3698105c2bf8b0b7bc833444057`, based on merged M1 main `b8dae76034caf63bf7d0721cba99a58a0586ae85`. It is pushed over SSH on `agent/dim-gate/mainline/m2-governance` and opened as Draft [PR #13](https://github.com/fallrising/newclear/pull/13). This is a shared-slice checkpoint, not M2 milestone acceptance, merge or deployment.

## Delivered behavior

- `dim-gate-m2-v1` preserves the 60-CI M1 inventory and explicitly rejects M1 snapshots without rewriting their bytes before recovery.
- Catalog, request, job, capacity and pool reads share one scoped snapshot. Request commands enforce state/version/idempotency and immutable catalog template snapshots.
- Approval atomically reserves capacity and deterministic environment/CI identity. AWS, Aliyun and on-prem success paths create exactly one environment, CI and placement.
- Configure failure terminates, releases reservation and exposes bounded job logs. Retry increments attempt while reusing environment and planned CI IDs.
- Manual CI onboarding accounts for queued/running and scenario reservations, so it cannot overbook approved work.
- Access, user, navigation, catalog revision and CMDB custom-field commands enforce self/last-admin, recovery-route, published-revision and core-field refusal rules.
- Request/provisioning audit scope is project/pool precise; Admin governance, demo scenarios and plain clock events do not leak through generic RD audit fallback.
- The generated 72-operation OpenAPI marks 52 M0–M2 operations implemented while keeping M3/M4 planned.

## Verification

- `pnpm install --frozen-lockfile` — passed with repository Node 24.18.0 and pnpm 11.18.0.
- `pnpm lint`, `pnpm typecheck`, `pnpm test` — passed; 15 files and 132/132 tests.
- `pnpm check:docs` — passed; 70 Markdown files and 147 repository links.
- `pnpm check:contracts` — passed; 72 operations, 166 schemas and all local references resolved.
- `pnpm check:ci`, `pnpm check:architecture`, actionlint 1.7.12 and `git diff --check` — passed.
- `pnpm build --mode demo` — passed; production JS 419.77 kB gzip.
- Initial `pnpm test:e2e` could not launch Chromium because the container lacks `libasound.so.2`; no product assertion ran. The documented temporary Ubuntu ALSA extraction was then supplied only through `LD_LIBRARY_PATH=/tmp/dim-gate-playwright-libs/usr/lib/x86_64-linux-gnu`. The corrected final bundle passed 11/11 production Chromium tests in 37.8 seconds, including M0/M1 responsive, axe, focus, persistence, subpath, CMDB, topology and AC-20 regressions.

## Review findings closed in-cycle

The lead review added three regressions beyond the initial green suite: manual onboarding now respects outstanding reservations; a disabled catalog cannot re-expose an older published revision while a replacement draft exists; and non-provisioning clock audit is Admin-only. The final 132-test and 11-E2E results include those corrections.

## Remote CI reconciliation

Metadata-head run 35534559281 passed every native gate but failed the AC-20 browser test because its observer started before the later Playwright persona-switch command; the 150 ms Data search could legally finish in that inter-command gap. This was a test timing defect: the DOM result appeared before scope transition began. No product delay or assertion was weakened.

Test-only commit `c35147cba7e6d52343456019e9975b78d9ef2fa3` installs the observer in advance but activates leak recording from the capture phase of the real persona-select change event. It also asserts the Data request was still pending at that exact event. The focused production-browser race passed 5/5 locally, then the full 132-test and 11-E2E suites passed. Replacement GitHub Actions run [35535062404](https://github.com/fallrising/newclear/actions/runs/35535062404) passed every gate on the corrected head.

## Remaining work

RD catalog/request UI, Ops approvals/capacity/jobs UI, Admin governance UI, production browser walkthroughs for those routes, independent fixed-commit review and successful current-head remote CI remain mandatory. M2 is not ACCEPTED. Draft PR #13 must not be merged yet. No cloud credentials, provider APIs, paid services or deployment were used. The 419.77 kB gzip bundle remains above the future M5 300 kB budget.
