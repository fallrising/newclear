# T-015 — M2 RD/Ops self-service UI

STATUS: ACCEPTED_CHECKPOINT

## Immutable result

The tested product commit is `3eaea290de563818af27b240bff73e02f47513d0` on `agent/dim-gate/mainline/m2-governance`, based on the accepted T-014 shared checkpoint. This is an RD/Ops slice checkpoint, not M2 milestone acceptance, merge or deployment.

## Delivered behavior

- A typed client now exposes scoped catalog, pool, capacity, request and job reads plus versioned request commands. Demo failure injection uses the existing scoped `/scenarios` command; React never mutates the snapshot directly.
- RD routes `/rd/catalog`, `/rd/catalog/:itemId/request`, `/rd/requests` and `/rd/requests/:requestId` provide catalog browsing, capacity-aware request submission, request history, eligible cancellation and failed-request retry.
- Ops routes `/ops/requests`, `/ops/requests/:requestId`, `/ops/jobs`, `/ops/jobs/:jobId` and `/ops/capacity` provide filtering, approve/reject/provision commands, attempts/logs, a job-specific configure-failure control and provider capacity projections.
- A successful journey keeps one request/correlation identity, reserves capacity on approval, converts reservation to active CI usage, produces a ready environment plus one canonical CI/placement, and exposes five bounded job log entries.
- The failure journey releases reservation at configure, then creates attempt 2 with a new job ID while retaining the same environment ID, correlation ID and planned CI ID. The second delivery creates only one active CI/placement.
- Route registry permission guards back every visible M2 entry. RD job history keeps readable IDs without offering an Ops-only deep link. Empty/error/404 states remain scope-safe.

## Production browser evidence

- Built with `pnpm build --mode demo` and served by `pnpm preview --host 127.0.0.1 --port 4173 --strictPort` at `http://127.0.0.1:4173/dim-gate/`.
- Real headless Chromium executed the UI controls for RD Commerce catalog/wizard/submission, Ops approval/capacity/provision/job, Guide clock, ready RD environment placement, job-specific failure injection, RD retry and second Ops delivery.
- The successful AC-09 journey recorded zero browser console errors, page errors and failed requests. The repository browser run passed 14/14 journeys, retaining all M0/M1 tests.
- M2 catalog was checked at 1440×900, 768×1024 and 390×844. Document overflow was zero and axe serious/critical findings were zero at each width. Ops request/job/capacity routes also had zero serious/critical findings.
- Browser screenshots are Playwright run artifacts, not product assets. No real provider call, credential, deployment, paid service or global permission was used.

## Verification

- `pnpm install --frozen-lockfile` — passed with repository Node 24.18.0 and pnpm 11.18.0.
- `pnpm lint`, `pnpm typecheck`, `pnpm test` — passed; 16 files and 135/135 tests.
- `pnpm check:docs` — passed before this evidence update with 72 Markdown files and 149 repository links.
- `pnpm check:contracts` — passed; 72 operations, 166 schemas and all local references resolved.
- `pnpm check:ci`, `pnpm check:architecture`, actionlint 1.7.12 and `git diff --check` — passed.
- `pnpm build --mode demo` — passed; production JS is 426.27 kB gzip.
- `pnpm test:e2e` — passed 14/14 in production Chromium using only the documented temporary runtime library path.
- GitHub Actions run [35548907839](https://github.com/fallrising/newclear/actions/runs/35548907839) passed the same frozen install, native gates, production build and 14/14 Chromium suite on evidence head `26199b681e9c2fb3f08c78c621c7aa8833d77f43`; browser artifacts were preserved.

## Evaluation notes

The first browser attempt exposed an ambiguous test locator, and later attempts exposed missing test waits around asynchronous user commands plus an invalid 3-tick test option. Captured DOM showed the product remained in its valid pre-command/queued state. The tests were corrected to target exact accessible controls and wait for visible command receipts; no product delay, domain shortcut or weakened assertion was introduced.

One UX finding was corrected before the immutable checkpoint: the RD request timeline no longer links to the Ops-only job route. Full local gates and all 14 browser journeys were repeated after that product change.

## Remaining work

Admin access/navigation/catalog/model/audit UI (AC-21–23), final integrated browser walkthrough and independent fixed-commit review remain mandatory. M2 is not ACCEPTED and Draft PR #13 must not be merged yet. The 426.27 kB gzip bundle remains above the future M5 300 kB budget.
