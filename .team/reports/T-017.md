# T-017 — M2 integrated adversarial review and correction

STATUS: LOCAL_ACCEPTED_REMOTE_CI_PENDING

## Fixed result

- Branch: `agent/dim-gate/mainline/m2-governance`
- Base/main: `b8dae76034caf63bf7d0721cba99a58a0586ae85`
- Rejected review head: `5dd74e8a3f94596be2a1c5abe7b08d1165a437d2`
- Primary correction: `83a2e81432eb48c8e9dfbfce6b9e10e7ae4052be`
- Final product/test correction: `513e6cc2f3ff9d1fc8228805c366ce4f9d732925`
- PR: #13 remains OPEN, Draft, CLEAN and unmerged while current-head remote CI is pending.

This is the integrated M2 acceptance review for AC-09–12 and AC-21–23. It preserves the accepted M0/M1 behavior and does not represent deployment.

## Independent review chronology

The fresh-context read-only review rejected `5dd74e8` with three blockers and one high finding:

1. Admin could read full provisioning job logs through `/api/v1/jobs/:id` even though logs are scoped to RD/Ops.
2. A manually onboarded CI could take an approved job's deterministic canonical identity and strand provisioning.
3. Admin catalog UI could change only name/description, while the RD wizard hard-coded `2 vCPU / 2048 MiB`; AC-23's new-spec behavior was not operable.
4. Multi-role users saw navigation for only `session.centers[0]`.

Correction `83a2e81` closed those findings, but independent re-review found a second planned-identity window: a failed job stopped reserving its identity before retry. Final correction `513e6cc` keeps identities reserved for queued, running and failed attempts; cancelled attempts release them, succeeded attempts are protected by the materialized CI, and rejected requests have no job. The final read-only verdict reported no blocking, high or medium product finding and stated that M2 acceptance is no longer blocked by the review.

## Delivered corrections

- Added `canReadJob`: Admin-only scope can still read governed requests, but job list/detail and logs require an actual scoped RD/Ops grant.
- Manual CI onboarding compares canonical identity against both materialized CIs and planned identities for queued, running and failed jobs. Both pre-provision and post-failure/pre-retry takeover attempts return `409 DUPLICATE_RESOURCE`; the original delivery remains recoverable.
- Admin catalog draft editing now exposes allowed project IDs, pool IDs, providers, stages, defaults and limits. Published revisions remain immutable.
- The RD wizard derives stage/provider/spec values from the active published revision; an older request keeps its immutable template snapshot.
- AppShell queries and merges persisted navigation for every effective center, preserving the union of valid multi-role grants while static route/action guards remain authoritative.

## Production browser walkthrough

- Built with `pnpm build --mode demo` and served by Playwright through `pnpm preview --host 127.0.0.1 --port 4173 --strictPort` at `http://127.0.0.1:4173/dim-gate/`.
- Real headless Chromium operated AWS and on-prem success delivery, Aliyun configure failure/retry, reject, approved cancellation, invalid transition, capacity reservation/release, job logs and ready environment/CI deep links.
- Browser UI attempted canonical-identity takeover both while a job was queued and after configure failure; both rendered the explicit `409 DUPLICATE_RESOURCE`, and the failure path then retried to fulfillment with the original environment and CI identity.
- Browser requests proved Admin job list is empty and job detail/log is concealed as 404 while Ops can open the same job.
- Admin created catalog revision 2, edited defaults to `4 vCPU / 4096 MiB`, published it, and RD saw those values in a new wizard/request while a revision-1 request remained `2 / 2048`; disabling the item blocked only new requests.
- Admin added Ops project/pool grants to a Commerce RD user; the same persona displayed RD and Ops navigation together. After AWS pool revocation, the captured CI metadata mutation failed closed with visible 403 and did not commit.
- Existing M0/M1 browser coverage revalidated `/dim-gate/` and subpath refresh, route guards, global/scope isolation, 60-CI CMDB, provider fields and filters, manual onboarding, topology cycles/truncation/table fallback/invalidation, persistence/reset/copied tab/corrupt recovery, responsive layout, theme, keyboard/focus/dialog and axe assertions.
- The suite visited 1440×900, 768×1024 and 390×844 layouts. Serious/critical axe findings and document overflow were zero in the covered light/dark journeys. Page errors and failed transport requests were zero; deliberate 403/404/409/422 HTTP refusals rendered explicit DOM errors and only their expected browser resource-console entries.

## Verification

- `pnpm install --frozen-lockfile` had already passed for this unchanged lockfile with repository Node 24.18.0 and pnpm 11.18.0.
- `pnpm lint`, `pnpm typecheck`, `pnpm test` — passed; 16 files and 138/138 tests.
- `pnpm check:docs` — passed before this report; 76 Markdown files and 154 repository links. It must be repeated after committing this evidence.
- `pnpm check:contracts` — passed; 72 operations, 166 schemas, all local references resolved.
- `pnpm check:ci`, `pnpm check:architecture`, actionlint 1.7.12 and `git diff --check` — passed.
- `pnpm build --mode demo` — passed; production JavaScript is 431.39 kB gzip.
- `pnpm test:e2e` — passed 22/22 against the final correction in real production-preview Chromium.
- Final independent fixed-head review of `513e6cc` — no blocking, high or medium product finding.

## Remaining non-blocking limits

- The AC-21 browser journey reopens the same CI dialog after the demo persona cycle; it proves the captured mutation is rejected after revocation, while the exact already-mounted-dialog race remains covered at the domain/client boundary rather than by one persistent React instance.
- Catalog Chromium changes and verifies defaults; the additional project/pool/provider/stage/limit controls are present and schema-backed but not every control is mutated in E2E.
- Production JavaScript is 431.39 kB gzip, above the future M5 300 kB budget.
- No real AWS/Aliyun/IDC operation, deployment, paid service or global permission change occurred.

## Pending acceptance action

Commit and SSH-push this evidence with the product corrections, wait for exact-head GitHub Actions, then update this report/PLAN/STATUS with the run and decide final M2 acceptance and PR readiness. Do not treat prior green runs as current-head evidence.
