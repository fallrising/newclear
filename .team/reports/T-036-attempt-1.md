STATUS: PARTIAL

## Summary

T-036 attempt 1, run DG-W2-20260923-01, independently reviews W2 against WS-SDD revision 1 and W2 integration contract revision 2. Verdict: REWORK recommended for three MEDIUM findings below. No additional HIGH/CRITICAL finding was confirmed in the reviewed scope. Passing automated tests do not close these independently reproduced behavior/specification gaps, and this report does not accept W2.

Fixed candidate: `a05491ce6ee9883f7d3e41a5e0c8d227aa739292`. Actual accepted W1 merge base: `b4ef57f1e15082f3e980b2eb0d8451b1f1f4433d`. Product source is identical to `740a2bca6c8ce657c185c276b61dcf661488ce3c`; the intervening component diff contains STATUS plus governance/isolation tests. Detached review checkout: `/home/ckc/test/codex/newclear-dim-gate-w2-review`. The reviewer did not implement W2 and made no product, task, PLAN, dependency, Git, remote or acceptance mutation. Only these two explicitly authorized review reports are written. No delegation was used. Route is the available built-in uninvolved Codex reviewer; the exact runtime model slug is not independently exposed, and no Claude or multi-model claim is made.

### F-01 — MEDIUM — Parent-wide incomplete impact incorrectly blocks authorized object maintenance

Location: `platform/dim-gate/src/features/resources/InventoryPages.tsx:26`; related consumer list at line 31. The Redis maintenance button uses `inventory.impactIncomplete`, which describes every allocation on the physical parent, instead of the selected allocation's affected consumers and canonical action authority.

Reproduction on the fixed production build at port 4223: select seeded `w2-user-multi` through the visible persona control and open `/ops/caches/ci-idc-redis-01`. This actor has the pool and all Store consumer project/stage grants required for `w2-object-redis-commerce`; the separate Data allocation on the same Redis is hidden. Commerce maintenance is nevertheless disabled. A diagnostic canonical POST with that same actor/session, `kind=resource.resize`, catalog `w2-catalog-redis` revision 1, CI/object version 1 and quota 2048 returns 201 with `change-0001`. Inventory reports `impactIncomplete=true`, visible object `[w2-object-redis-commerce]`, and visible consumers `[env-checkout-dev, env-storefront-dev]`. This diagnostic request is evidence of a UI/domain mismatch, not UI-success acceptance setup; no storage mutation was used.

The dialog also lists all parent consumers, rather than the chosen object's affected consumers. SDD09 action-specific authorization, SDD11 shared maintenance and AC-WS-08 require the target pool plus all affected object consumers, not every unrelated allocation's consumers. Provide an object-specific canonical impact/capability projection and preserve redaction of parent-wide totals and hidden identities/counts. Test both a qualified actor with an unrelated hidden allocation and revocation of one genuinely affected consumer grant. Lead acknowledged REWORK.

### F-02 — MEDIUM — Approved work disappears from the waiting-for-execution filter

Location: `platform/dim-gate/src/domain/resource-views.ts:151`; UI phase label at `platform/dim-gate/src/features/resources/WorkItemsPage.tsx:35`. The exclusive phase mapping assigns `approved` only to `decided`, while the execution option promises waiting-for-execution/executing work.

Independent canonical domain reproduction: create a 512 MiB Redis binding as `user-rd-commerce`, submit, then approve as `user-ops`. `/work-items?center=ops&view=all` contains the same `change-0001` with `rawState=approved` and `actionRequired=true`; adding `phase=execution` removes it, while `phase=decided` finds it. Its canonical detail has `availableActions=[execute]`. The same shared phase mapping affects approved environment Requests awaiting provision.

This breaks SDD11 section 4's waiting-for-execution classification and the common work-item discovery path (REQ-WS-05, AC-WS-04/17). Preserve original source states; phase membership may overlap so an approved decision can also be waiting for execution. Add source-specific Request/Change domain/HTTP and visible list-to-execute regression. Lead acknowledged REWORK.

### F-03 — MEDIUM — Approval surfaces omit required typed delta and risk context

Locations: `platform/dim-gate/src/features/resources/WorkItemsPage.tsx:42`, `platform/dim-gate/src/features/resources/ChangeDetailPage.tsx:41`, and its approval confirmation at line 59. Work-item rows contain source type/ID, target references, actors, state and time, but omit the resource request kind, unit-bearing delta and risk. Change detail exposes desired settings and parent capacity, but does not show canonical `riskClass` or the selected object's current-to-desired delta before approval.

For a shared allocation resize from 1024 to 3072 MiB, an approver must find the current object on another page and calculate the 2048 MiB delta manually. The list cannot distinguish bind, resize and topic-create beyond the generic resource-change source type. The source fields and UI code confirm the omission; the native phase reproduction also produced a WorkItem with no such summary.

SDD11 section 4 explicitly requires request kind, delta and risk in the approval list and current capacity/reservation/delta before decision; section 3 also requires capacity delta for high-impact operations (REQ-WS-04/05, AC-WS-04/06/08/17). The shorter integration-contract DTO must not silently reduce those requirements. Derive a shared, typed, source-specific summary for list/detail/approval confirmation. Preserve units and redaction; missing/stale historical bases must be unknown rather than inferred from whole-parent private usage. Request and Release should retain their own semantics, with explicit not-applicable values when appropriate. The domain reservation arithmetic itself was not found incorrect. Lead acknowledged REWORK.

## Verification

All executed native commands used component cwd `platform/dim-gate`, Node 24.18.0 on `/home/ckc/test/codex/.toolchains/node-v24.18.0-linux-x64/bin` and pinned pnpm 11.18.0. Shell calls used the authorized elevated execution route because the default bwrap sandbox cannot initialize loopback. Browser environment used `LD_LIBRARY_PATH=/tmp/dim-gate-m5-webkit-libs/usr/lib/x86_64-linux-gnu:/tmp/dim-gate-playwright-libs/usr/lib/x86_64-linux-gnu`, `FONTCONFIG_FILE=/tmp/dim-gate-m3-fonts-kaBaS8/fonts.conf` and isolated `DIM_GATE_TEST_PORT=4223`.

- `git rev-parse HEAD`, `git status --short`, fixed-base diff inspection and `git diff --check`: clean immutable product candidate before report writes — passed
- `pnpm exec vitest run src/domain/w2.test.ts src/domain/engine-loading.test.ts src/demo/migrations.test.ts src/demo/resource-persistence.test.ts src/demo/w2-handlers.test.ts`: independent 65/65 across five files — passed
- `pnpm test`: independent complete 314/314 across 29 files, 9.84 s; `/tmp/dim-gate-w2-evidence/t036-review-native.log` — passed
- `pnpm build --mode demo`: independent production build and TypeScript build; only ignored build artifacts produced — passed
- `pnpm check:contracts`: independent 91 operations/209 schemas with resolved references — passed
- `pnpm check:architecture`: independent feature import boundaries — passed
- `pnpm check:docs`: independent 170 Markdown files/395 repository links on fixed candidate — passed
- `pnpm check:ci`: independent workflow scope, pinned actions, authority and native gates — passed
- `pnpm exec playwright test e2e/w2-resources.spec.ts e2e/w2-governance.spec.ts e2e/w2-migration.spec.ts e2e/w2-isolation.spec.ts --output=/tmp/dim-gate-w2-evidence/t036-review-browser`, with `PLAYWRIGHT_HTML_OUTPUT_DIR=/tmp/dim-gate-w2-evidence/t036-review-html`: independent 11/11 production Chromium tests, retries 0, 3.4 minutes — passed
- Parsed the independent HTML report's 11 unique browser-health attachments: 2366 network responses, zero page errors, zero failed requests; expected denial cases include SELF_APPROVAL_DENIED, FORBIDDEN, NOT_FOUND, DUPLICATE_RESOURCE, CATALOG_CHANGED and CATALOG_DISABLED — passed
- Manual screenshot inspection of Redis typed editor at 768 dark and Kafka typed editor at 390 light: readable labels, bounded form layout, retained tablet navigation and mobile shell. Default `view_image` helper could not launch bwrap; equivalent read-only PNG loading through the authorized shell enabled actual visual inspection — passed
- Independent F-01 production Chromium diagnostic (`node --input-type=module`, local Playwright import, visible persona selection, read-only inventory plus canonical diagnostic POST): qualified Commerce maintenance button remains disabled despite canonical 201 — failed
- Independent F-02 `node --input-type=module` diagnostic using Vite `createServer({server:{middlewareMode:true},appType:'custom'})` and `ssrLoadModule` for seed/engine: canonical create/submit/approve followed by all/execution/decided work-item reads confirms approved item missing from execution — failed
- SDD11 approval-list/detail requirements compared with actual WorkItem schema/projection and rendered component source: F-03 delta/risk context missing — failed
- Inspected fixed `740a2bc` raw benchmark attachments: all five cold initial loads 303924 gzip bytes, median LCP 724 ms, 5000-CI query P95 0.5 ms, persisted HTTP mutation P95 168.600000024 ms. The recorded tested commit and empty tracked-diff hash match the source-equivalent baseline; unchanged benchmark includes startup validation, Mock bootstrap and service worker — passed
- Lead a054 complete native log independently read: 314 tests plus lint/typecheck/docs/contracts/CI/architecture completed. Lead full 76 Chromium run was still active at report cutoff, with test 52 and the 1000-real-UI-command case completed; its final Chromium/8 Firefox-WebKit/2 live-isolation results and exact-head CI were not yet inspected as completed — skipped
- Rework product changes and final closeout documents are not part of fixed a054; a new immutable candidate and affected/full evidence are required — skipped

Independent browser log: `/tmp/dim-gate-w2-evidence/t036-review-browser.log`; HTML and PNG/canonical-readback/health artifacts: `/tmp/dim-gate-w2-evidence/t036-review-html/`. Successful cases do not produce failure traces. Fixed lead logs are `/tmp/dim-gate-w2-evidence/a05491c-native.log`, `a05491c-build.log`, and `a05491c-full-browser.log`. Source-equivalent performance attachments are under `/tmp/dim-gate-w2-evidence/740a2bc-performance/`. These local paths are evidence locations, not claims of remote artifact durability.

Reviewed coverage and conclusions:

1. AC-WS-03/04/05/06: one canonical CI/Object/Binding/Placement identity, unit-specific quota counted once per object, approval/execution reservations, same-name same-parent conflict and cross-parent distinct identities, binding-existing zero allocation demand, last-quota race, cancellation and atomic terminal materialization. Independent UI stories cover old staging Request into Redis, Kafka create/bind, failure and independently reapproved retry, with canonical IDs/readback and reload. F-03 remains.
2. AC-WS-07/08: current policy before idempotency replay; lazy command-module import occurs inside the serialized queue, then authorization/replay uses current state; input copying, failed persistence and continued queue execution have passing regressions. Non-self approval, pool/project intersections, Admin isolation, immutable catalog/spec snapshots, catalog replacement/disable, stale target versions, execution locks, failure/no ghost and append-only retry history are implemented and tested. F-01/F-02/F-03 remain.
3. AC-WS-09/15: resource object/consumer visibility differs from physical CI visibility; private parent totals become null for incomplete scope, observed usage remains unknown without samples, K8s is read-only. Resource policy is applied to new list/detail/search/audit/notification/home projections. Existing bounded physical topology remains a CI projection and does not expose new object payload. Held successful old-persona response and search isolation passed independently.
4. AC-WS-16: strict genuine W1 fixture provenance references accepted b4ef57f and records canonical controller commands. Migration validates the original schema/relations before adding only metadata and empty business collections; original history, receipts, ownership/identity and active Release/ProvisionJob survive. Independent migration browser resumes both old active operations once; native tests cover corrupt/incompatible/collision/quota-failure original-byte preservation and atomic retry. Complete native regression passed.
5. AC-WS-17/18: actual production base path, responsive/theme/keyboard dialogs and Admin typed variants passed the bounded browser suite; screenshots were inspected. Typed API/OpenAPI, no arbitrary catalog execution payload, Mock labels, read-only K8s and explicit W3-W5 exclusions remain. Complete cross-browser/final fixed-head gates are still a lead-owned pending condition at this cutoff.

## Documentation

Read applicable AGENTS, DEVELOPMENT_PROMPT/DEVELOPMENT_PROTOCOL, task T-036, the relevant current dim-gate PLAN, W2 contract revision 2, SDD09-14 and affected 01-07 baseline/deltas, contributor reports T-033 attempt 2 and T-034 attempt 3, and the W2 validation record. No private kernel source or credentials were copied. The existing pinned report validator is used only for this report's format.

The fixed candidate still contains stale pre-W2 headings in README/SDD09-14 alongside newer implementation checkpoints. The lead identified this and provided a separate closeout tree. The requested read-only inspection of those README/SDD/STATUS wording changes confirms they distinguish W1 accepted/merged, W2 implemented but not accepted, and W3-W5 unimplemented, and correct the operation count to 91. That tree is now also the active rework tree, so this is a preliminary documentation-only assessment, not a claim that its changing full diff or a new final commit has passed review. Recheck its fixed final wording, links, test counts and evidence references on the next candidate. No specification requirement was waived in this report.

## Risks and Follow-ups

Lead has acknowledged all three findings as REWORK. Fix them together behind a contract revision recorded before implementation, preserve this attempt and immutable a054 evidence, then provide one new fixed candidate for independent delta review. Reproduce F-01 with both qualified and revoked object consumers, F-02 for original Request and new Change waiting-execution discovery, and F-03 with unit-specific current/desired/delta/risk in list and approval confirmation. Maintain current authorization and unknown/redacted values.

Rerun affected native/HTTP/browser tests and unchanged full gates on that source, including benchmark: the reviewed initial-load headroom is only 3276 bytes below the 307200-byte budget, and new projection/schema code must be measured. Complete and inspect the final cross-browser, isolation and exact-head CI results. Do not mark W2 accepted or open W3 implementation based on this PARTIAL review. Product, PLAN and acceptance remain lead-owned. After report validation/hash handoff, this reviewer stops writes and remains available for an explicitly assigned fixed-candidate re-review.
