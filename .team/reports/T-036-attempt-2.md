STATUS: DONE

## Summary

T-036 attempt 2, run DG-W2-20260923-01, completes the bounded independent read-only W2 review against WS-SDD revision 1 and W2 integration contract revision 3. The three MEDIUM findings from attempt 1 and the additional LOW readability finding are closed on the source and evidence identified below. No open HIGH, MEDIUM or LOW finding remains from this review. This is reviewer completion, not W2 acceptance, merge approval, or a claim that the latest PR-head CI has completed.

Fixed references:

- Actual accepted W1 merge base: `b4ef57f1e15082f3e980b2eb0d8451b1f1f4433d`.
- Attempt 1 candidate: `a05491ce6ee9883f7d3e41a5e0c8d227aa739292`.
- Attempt 2 corrected domain/API/UI baseline: `4a69e07233fb31a90d2abafa664c39d3b91c4f23`.
- Intermediate narrow layout/test correction: `d07166c93d600211454074c91b1bfa2e50526adc`.
- Final reviewed narrow correction: `bf3f168a09c2b4f5ae442703588f2a3dd4d5cb20`.

The detached review checkout remains `/home/ckc/test/codex/newclear-dim-gate-w2-review-2` at 4a69. Fixed subsequent deltas were inspected with Git and exercised from the clean lead checkout `/home/ckc/test/codex/newclear-dim-gate-w2`. Between 4a69 and bf3f168 the only product files changed are `src/features/resources/WorkItemsPage.tsx` and `resources.css`; there are no additional domain, API, schema, persistence, dependency, lockfile or workflow changes. Additional changes are bounded browser tests and task/report/status documentation. Complete 77-test Chromium and live-isolation evidence belongs to 4a69; final layout, Firefox/WebKit smoke and fresh performance evidence belongs to bf3f168. These are deliberately distinguished, not presented as a local complete 77-test run of bf3f168.

The reviewer did not implement W2, did not delegate, and made no product, task, PLAN, dependency, Git, remote or acceptance changes. Only the two explicitly authorized attempt-2 review report files are written. The route is the available built-in uninvolved Codex reviewer; the exact runtime model slug is not independently exposed. No Claude or multi-model review is claimed.

### Findings and closure

1. **F-01 — MEDIUM — Closed.** Previously, parent-wide incomplete visibility disabled authorized maintenance of an individual allocation and the dialog listed unrelated parent consumers. `platform/dim-gate/src/domain/resource-views.ts:39` now derives one canonical object impact per visible object, including only that object's authorized consumers; `resource-policy.ts:23` shares the pool/all-affected-environment predicate with commands. `src/features/resources/InventoryPages.tsx:27` uses that object's `canResize`, and line 32 renders its consumers. Independent canonical reproduction confirms qualified `w2-user-multi` may maintain the Commerce object while a separate Data allocation remains hidden and parent used/reserved totals remain null. Revoking the pool or hiding an actually affected consumer produces denial and unchanged business state. The visible shared-resize browser journey, readback and both-consumer outcome pass.
2. **F-02 — MEDIUM — Closed.** `platform/dim-gate/src/domain/resource-views.ts:172` now treats approved work as both decided and waiting for execution, without changing source states. Independent domain checks and source-specific domain/HTTP regressions cover approved Request and Change membership. The production browser suite creates and approves each source, finds the same source ID through the waiting-execution filter, opens its detail, and executes it.
3. **F-03 — MEDIUM — Closed.** `platform/dim-gate/src/domain/resource-views.ts:54` supplies the same typed kind/risk/unit-bearing summary to WorkItem and ChangeDetail; `src/features/resources/shared.tsx:38` renders it in list/detail/approval. Independent diagnostic results include 1024 to 3072 MiB, signed delta +2048, and 3072 to 512 MiB, delta -2560. After execution changes the target version, historical current/delta become null rather than inventing a prior baseline. Frozen source snapshots remain unchanged. The UI regression confirms matching shared risk and +1024 MiB summary in list and approval confirmation. New allocation/topic, existing binding, Request CPU/memory and Release not-applicable semantics remain distinct; private parent usage is not used as a baseline.
4. **F-04 — LOW — Closed on bf3f168.** The 4a69 work list compressed target IDs and state text at narrow widths. The first d071 rule was overridden by `.table-scroll table`; independent DOM inspection measured table min-width 680px and target cell 57.6px despite the intended 58rem. Final `platform/dim-gate/src/features/resources/resources.css:34` has sufficient specificity, a target-column minimum, single-line target codes, a state-column minimum and block metadata. Independent production measurement at 768/390 in both themes now reports min-width 812px, target width 252px and four single-line references. Actual screenshots, Tab-to-region, ArrowRight horizontal scrolling, no document overflow and axe checks pass. This closes the actual readability issue, not merely an automated axe result.

Historical failures remain failures. The old a054 and 4a69 eight-test cross-browser runs each finished 6/8: immediate consecutive full navigations aborted Firefox bootstrap and WebKit service-worker loading. d071 removed the redundant first navigation but finished 7/8 because Firefox switched persona after dialog dismissal while sequential detail/audit readback was still active. Independent d071 trace inspection found submit 200 at monotonic 65707.292, persona selection at 65749.589, old detail readback at 65872.683 and old-persona audit 409 STALE_IDENTITY at 66033.295. The final smoke waits for the first submit's new completion notice, submitted state and visible `change.submit` audit before switching. Browser-health policy, retries, timeouts and application authorization were not relaxed. Final bf3f168 full eight-test smoke and independent two-engine W2 smoke pass. The original logs/traces and attempt-1 report remain preserved.

Reviewed scope continues to cover AC-WS-03–09/15–18. Unchanged full-W2 conclusions from attempt 1 are carried forward after delta inspection and repeated native/browser coverage: canonical CI/Object/Binding/Placement IDs; quota counted once per object; current authorization before replay and inside the serialized lazy-import command queue; atomic reservation/materialization/cancel/failure with no ghost binding; independent reapproval and immutable attempt/catalog/spec history; partial-consumer redaction and unknown observed usage; typed Admin variants; held old-persona response isolation; genuine W1 snapshot provenance and migration preserving active old Release/ProvisionJob and completing each once. Existing Request/Release authorization and state machines remain unchanged. W3–W5 are outside this review.

## Verification

Commands used component cwd `platform/dim-gate`, Node 24.18.0 from `/home/ckc/test/codex/.toolchains/node-v24.18.0-linux-x64/bin`, pinned pnpm 11.18.0 and authorized elevated shell execution because default bwrap cannot initialize loopback. Independent browser runs used port 4223, `LD_LIBRARY_PATH=/tmp/dim-gate-m5-webkit-libs/usr/lib/x86_64-linux-gnu:/tmp/dim-gate-playwright-libs/usr/lib/x86_64-linux-gnu`, `FONTCONFIG_FILE=/tmp/dim-gate-m3-fonts-kaBaS8/fonts.conf`, and `DIM_GATE_WEBKIT_EXECUTABLE=/tmp/dim-gate-m5-webkit`.

- `git rev-parse`, `git status --short`, `git diff a05491c..4a69e07`, `git diff 4a69e07..d07166c`, `git diff d07166c..bf3f168` and path-bounded source/config comparison: fixed references and clean product sources reconciled — passed
- Independent `pnpm test` on 4a69: 321/321 across 29 files, 8.09s; `/tmp/dim-gate-w2-evidence/t036-attempt2-native.log` — passed
- Independent `pnpm build --mode demo`, `pnpm lint` and `pnpm typecheck` on 4a69; build log `/tmp/dim-gate-w2-evidence/t036-attempt2-build.log` — passed
- Independent `pnpm check:contracts` (91 operations/210 schemas), `pnpm check:docs` (171 Markdown files/404 links on 4a69), `pnpm check:architecture` and `pnpm check:ci` — passed
- Independent canonical domain diagnostic through `node --input-type=module`, Vite `createServer({server:{middlewareMode:true},appType:'custom'})` and seed/engine `ssrLoadModule`: qualified hidden sibling allowed; selected-object hidden consumer and revoked pool denied with unchanged state; positive/negative delta, overlapping approved phases and historical unknown baseline verified — passed
- Independent `pnpm exec playwright test e2e/w2-resources.spec.ts e2e/w2-governance.spec.ts e2e/w2-migration.spec.ts e2e/w2-isolation.spec.ts --output=/tmp/dim-gate-w2-evidence/t036-attempt2-browser`, HTML directory `t036-attempt2-html`: 12/12 production Chromium tests, 3.7m, including representative success/denial, Request/Change filter-to-execute, typed Admin revisions, genuine migration and held old-response cases — passed
- Parsed those 12 unique browser-health attachments: 2518 responses, zero page errors and failed requests; HTTP refusals are the asserted CATALOG_CHANGED, CATALOG_DISABLED, DUPLICATE_RESOURCE, FORBIDDEN, NOT_FOUND and SELF_APPROVAL_DENIED cases — passed
- Independent final `pnpm exec playwright test e2e/w2-governance.spec.ts --grep 'Ops shared Redis resize' --output=/tmp/dim-gate-w2-evidence/t036-bf3f168-layout-artifacts`: 1/1, 25.2s; actual geometry, keyboard scroll, both themes, 1440/768/390 and list/approval accessibility; 217 responses, only expected SELF_APPROVAL_DENIED — passed
- Actual visual inspection of independent bf3f168 768-dark and 390-light screenshots in `t036-bf3f168-layout-html`; target references are separated and readable inside the keyboard-scroll region — passed
- Independent final `pnpm exec playwright test --config playwright.smoke.config.ts e2e/w2-browser-smoke.spec.ts --grep 'W2 extra browser' --output=/tmp/dim-gate-w2-evidence/t036-bf3f168-smoke-artifacts`: Firefox and WebKit 2/2, 39.9s; two health attachments total 334 responses, zero HTTP/page/failed-request errors — passed
- Inspected lead fixed 4a69 `pnpm test:e2e` terminal log and HTML: 77/77 Chromium, 24.8m, including 1000 genuine visible UI commands; 43 health attachments, 24589 responses, zero page errors/failed requests; `4a69e07-full-browser.log`, `4a69e07-full-html`, `4a69e07-full-health.json` — passed
- Inspected lead fixed 4a69 `pnpm test:isolation`: two production demo/live and sibling isolation cases, 2/2 in 3.0s; `/tmp/dim-gate-w2-evidence/4a69e07-isolation.log` — passed
- Inspected lead final bf3f168 `pnpm test:smoke`: full Firefox/WebKit suite 8/8, 2.5m; four health attachments, 1115 responses, zero page errors/failed requests; `bf3f168-smoke.log`, `bf3f168-smoke-html`, `bf3f168-smoke-health.json` — passed
- Inspected lead fresh final `pnpm benchmark` and decoded raw attachments: 3/3; testedCommit bf3f168 and empty tracked-diff SHA256 match; five cold initial-JS samples each 304477 gzip bytes, LCP median 756ms, 5000-CI query P95 0.599999905ms, persisted HTTP mutation P95 169.200000048ms; `/tmp/dim-gate-w2-evidence/bf3f168-performance/` — passed
- `git diff --check` plus pinned `teamctl.py validate-report` on both authorized attempt-2 report files — passed

Independent log and artifact prefixes are `/tmp/dim-gate-w2-evidence/t036-attempt2-*` and `t036-bf3f168-{layout,smoke}*`. These are local evidence locations, not remote-durability claims. Intermediate d071 independent two-test Chromium evidence is preserved under `t036-final-delta-*`; it is not substituted for final geometry or cross-browser results. Historical smoke failures are explicitly described above and retained under the a054/4a69/d071 logs and failed-smoke artifacts.

## Documentation

Read applicable component AGENTS, DEVELOPMENT_PROMPT/DEVELOPMENT_PROTOCOL, T-036, relevant dim-gate PLAN decisions DG-D067–069 and subsequent bounded corrections, W2 contract revision 3, affected SDD09–14/01–07 and source/tests, contributor reports, and the prior review. The read-only reassessment includes the closeout README/SDD/status wording changes: W1 is accepted/merged, W2 is implemented but not accepted, W3–W5 remain unimplemented, and typed API counts are corrected. Revision 3 records object capability, overlapping phases and shared typed summaries without reducing SDD acceptance requirements or changing persisted state machines.

The bf3f168 STATUS is a historical checkpoint accurately distinguishing pending full gates and preserved smoke failures at its writing time. The lead must incorporate the now-completed evidence and latest exact-head CI in the next metadata checkpoint. This reviewer has not rewritten PLAN, task acceptance or status. No private kernel source, credential or large raw artifact is copied into the repository.

Attempt-1 content remains preserved in the original review checkout and in the lead's integrated history. Its SHA256 is `c006bb83c9c5c0537367b22c5d8347c2c91ec2c91d099a0f0b55363fa361f591`; the attempt-2 report does not erase the original three findings or historical failed gates. This attempt writes `.team/reports/T-036-attempt-2.md` and an identical canonical `.team/reports/T-036.md` only.

## Risks and Follow-ups

No unresolved implementation finding from this review remains. Latest exact PR-head CI is still a lead-owned acceptance requirement at report cutoff; this report does not call it passed, accept W2, authorize merge, or open W3. The lead must check the final metadata diff, required exact-head CI and preserved evidence before its own acceptance decision. If product/tests/config change beyond the reviewed fixed delta, re-evaluate affected evidence rather than attributing the earlier 77-test run to the new head.

Performance remains within the unchanged 307200-byte initial-JS budget with only 2723 bytes of headroom. Future changes need fresh measurements. Unknown observed usage and unavailable historical resize baselines are intentional and must remain explicit; hidden consumer IDs/counts and private parent totals must remain redacted. No real infrastructure or live adapter behavior is claimed.

After validation and hash handoff, the reviewer stops writes and releases ownership of these two report files to the lead. Any further product work or independent review requires a new explicit assignment.
