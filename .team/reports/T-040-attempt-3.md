STATUS: PARTIAL

## Summary

T-040 attempt 3, run DG-W3-20260923-01. Independent source audit is complete for fixed candidate `84089270fed1f9c145901de5220599706cab4107`, relative to accepted W2 `91626851fb17df7ab31c96dee9353b9ee4d42c92`. Verdict: REWORK for one confirmed MEDIUM finding, T040-F1. Passing normal-flow tests do not close the corrupted-history startup invariant below. This report does not accept the product or authorize merge.

Isolated checkout: `/home/ckc/test/codex/newclear-dim-gate-w3-review-3`; component cwd `platform/dim-gate`. This reviewer had no implementation participation and performed no delegation, product/test/doc/PLAN edits, Git mutations or remote actions. Only this report and authorized generated test/evidence artifacts were written. Built-in Codex reviewer route was used; the exact runtime model slug is not independently exposed. No Claude or multi-model review is claimed. Dependencies were already installed by the lead.

T040-F1 — MEDIUM: startup accepts a traffic timeout whose own samples already prove success. At `src/domain/service-delivery-integrity.ts:125–148`, validation establishes sample shape/cadence, validates existing checkpoints and the timeout duration, but does not derive the first terminal decision from every step's sample prefix. Reproducer: create two real successful releases; create/validate/start a traffic policy with a 60-second health window and 120-second timeout; inject the supported missing-health scenario and advance 120 seconds, yielding a valid failed timeout with four null 30-second samples and no checkpoint. Clone that persisted snapshot and change only each sample's metrics to the deterministic healthy pair `(120, 0.002)`. `integrityErrors` returns `[]` for both snapshots and `createEngine(corruptSnapshot)` succeeds. The healthy prefix required the 10% step to succeed at 60 seconds; it cannot continue sampling that step through 120 seconds and time out. This violates the eager strict snapshot/evidence integrity requirement in AC-WS-11/16. It is a persisted-history validation defect, not a demonstrated normal-flow execution or authorization bypass.

Evidence: `/tmp/dim-gate-w3-resume-evidence/t040-timeout-corruption.json`, `t040-repro.log`, and runnable `t040-traffic-integrity-repro.mjs` (SHA256 `f5e897381017c8dc1ad3ed8e1da32f88758e89bcd76e8645b96ed07f2ed171d8`). Run the script with Node 24 from the component cwd; exit 1 means contradictory history was accepted. The lead acknowledged the finding and is correcting it in a later commit. No later correction is covered by this attempt's verdict.

Completed source audit beyond the three-line deferred callback correction:

- Domain models, inputs, policy, validation, commands, views and integrity: definition affected targets include the actual active baseline, including the active-prod R1 / unapproved-nonprod R2 / R3 replacement sequence; independent production decisions and current requester/approver grants; immutable per-environment run snapshots and retries; configuration typed values/registered secrets, activation failure retention and new-revision restore; traffic release qualification, ordered source-time samples, unknown resets, 10/50/100 checkpoints and failed-50 retention. F1 is the sole confirmed finding.
- Shared locks cover legacy nonterminal pipeline/release operations and W3 executions, while metadata approvals remain lock-free. Source visibility, Admin restrictions, all-affected-environment grants, filtered pagination, deep links, search, home/work-item projections, audit and notifications were traced to common policy.
- Engine startup validates seed/migration/snapshot integrity eagerly. Lazy command loading remains inside the single queue; call-time input/identity capture and execution-time policy/version checks precede replay. Persist succeeds before memory swap. Deferred client calls check actor/session/generation/policy/epoch after import. The explicit callback wrapper at 840 removes the bare mutation callback argument boundary.
- Schema extraction audit compared 146 baseline declarations: 135 bodies identical after pure IIFE normalization, with 11 inspected structural/W3 extension deltas and no missing baseline export. This is AST evidence, not a claim that a separate 172-schema runtime comparison was rerun. Recursive JSON fields retain the existing Zod schema identity. Delivery extraction had 28/31 unchanged bodies; the three changes implement shared locks, the effect rename and definition retry metadata. Observation extraction had 21/21 unchanged bodies. Gate scripts, dependency lock, benchmark budgets/exclusions and browser-health settings were not relaxed.
- Strict frozen W1/W2 shapes validate legacy relationships before additive migration; existing IDs, original run shapes, users/grants and active tasks are preserved, with empty W3 business collections and no invented legacy evidence. W1 fixture/capture provenance is unchanged from accepted W2. W2 fixture source is accepted merge 9162685; recorded SHA256 is `2180e098e84bdcccaa35c6573d623577985b2480b780e30a1302965078e6607b`. Capture commands, hashes, native migration/atomic-write tests and actual browser upgrade were inspected; fixtures were not regenerated in an old checkout.
- Typed editors, detail/evidence/actions/controls, confirmation focus, command/readback ownership, retry-only readback, configuration comparison, current legal actions, router/registry/AppShell integration and typed Ops triage were inspected. Actual screenshots show readable light/dark layouts, distinct desired/effective weights and independent Ops decisions without an apply shortcut. Unsupported capabilities remain outside interactive routes.

## Verification

- Exact fixed HEAD verified; checkout initially clean; no source mutation by reviewer — passed
- Independent `pnpm test`: 34 files, 372 tests, exit 0; `/tmp/dim-gate-w3-resume-evidence/t040-native.log` — passed
- Independent fresh `pnpm build --mode demo` at 840, exit 0; `t040-build.log` — passed
- Independent `pnpm check:contracts` and `pnpm check:architecture`, exit 0; `t040-contracts.log` and `t040-architecture.log` — passed
- Independent `pnpm test:e2e e2e/w3-definitions.spec.ts e2e/w3-service-delivery.spec.ts e2e/w3-governance.spec.ts e2e/w3-migration.spec.ts e2e/w3-isolation.spec.ts --output=/tmp/dim-gate-w3-resume-evidence/t040-browser-results`: 11/11 Chromium on the fresh 840 build; `t040-browser.log`, `t040-browser-report/index.html` — passed
- Independent `pnpm test:smoke e2e/w3-browser-smoke.spec.ts --output=/tmp/dim-gate-w3-resume-evidence/t040-smoke-results`: 2/2 Firefox/WebKit on the same fresh 840 build; `t040-smoke.log`, `t040-smoke-report/index.html` — passed
- Browser reproduction covers immutable definition/retry snapshots, separate prod release approval, approver/requester grant changes, configuration failure/restore/reload and independent Ops decisions, shared locks, missing-health timeout and healthy rollout, failed-50 retention, real held old-persona HTTP 200 isolation, command/readback ownership and genuine W2 active-execution migration/resumption — passed
- Actual reviewer screenshots inspected for definition editor, confirmation and failed-50 traffic in desktop/mobile light/dark; axe/keyboard checks in the executed specs passed. Supplemental 6c19fe7 config/Ops/run-dialog PNGs and `/tmp/t038-attempt2-evidence/summary.json` were inspected only after confirming service-delivery UI source bytes unchanged through 840 — passed
- Readonly source extraction comparison and baseline gate comparison: evidence `t040-schema-extraction.json`, `t040-schema-normalized.json`; deliberate W3 deltas inspected — passed
- Independent saved corruption reproducer at 840: validErrors `[]`, corruptErrors `[]`, startupAcceptedCorruption `true`, exit 1; required rejection of impossible healthy-window timeout — failed
- Lead full 89-case Chromium, complete 10-case Firefox/WebKit, final fresh benchmark/isolation and latest exact-head CI have not all been independently inspected at a terminal state in this attempt — skipped
- Pinned kernel 237aa277 `teamctl.py validate-report` and `git diff --check` for this report — passed

Test runtime: Node 24.18.0 / pnpm 11.18.0 via `/home/ckc/test/codex/.toolchains/node-v24.18.0-linux-x64/bin`; port 4355. Browser environment used the supplied local WebKit library/executable and font configuration. Shell commands required escalation because the default local bwrap wrapper fails; commands remained within the authorized read/test/report scope. The lead separately ran affected M3 tests on this fresh 840 dist (18/18 reported), which is not counted as a reviewer-executed gate.

Source binding: `fixed-gates.json` is explicitly bound to 25ba5ca, and its full browser step was RUNNING when inspected. `source-bindings.json` distinguishes that unchanged older dist from fresh 840 tests. This report's native/build/13 browser results are directly bound to 840. Neither a pending gate nor an older build is relabeled as passing on a later commit.

## Documentation

Read applicable component AGENTS, task T-040, DEVELOPMENT_PROMPT, development protocol, W3 integration contract revision 3, SDD and workspace SDD 09–14 plus affected baseline 01–08; reconciled routes, authorization, strict schemas/migration, source-time traffic, API and exit-gate requirements. Attempt 1's unfinished UI, queue/schema, migration/provenance and focused browser checklist is now covered as described above. The prior attempt 2 speculation that QueryClient cannot be structured-cloned is not adopted as a finding: it was not factually established, and actual M3 evidence is tracked separately by the lead.

Only this report was changed. PLAN/STATUS/canonical acceptance and requirement closure belong to the lead. Historical attempt reports and evidence remain intact. The contract describes specified behavior; it does not turn pending verification into accepted W3 delivery.

## Risks and Follow-ups

1. Correct F1 by deriving each traffic step's first terminal sample prefix using the same abnormal / complete consecutive healthy window / timeout precedence as execution. Reject inconsistent outcome/time, continued samples after that decision and already-complete running steps. Preserve legitimate unknown-window resets. Add corruption regressions and rerun the independent reproducer.
2. Review the fixed correction in a new immutable checkout/attempt. Reuse this explicitly bounded source audit, inspect every delta, and rerun affected native/browser flows. The lead has assigned attempt 4 for `46e3a557fcd1ff40221ce6881a7565b73c5daa7e`; this attempt intentionally retains F1 at 840.
3. Reconcile terminal full regression, browser-health, fresh decoded benchmark/isolation and latest exact-head CI evidence with their actual commits before a final readiness recommendation. Lead acceptance/merge remain separate decisions.

Review ownership of attempt 3 is released after validation and SHA256 handback. No product acceptance, merge, deployment or external notification was performed.
