STATUS: DONE

## Summary

T-040 attempt 4, continuation run `DG-W3-20260923-02`. Fixed reviewed product commit: `46e3a557fcd1ff40221ce6881a7565b73c5daa7e`; accepted W2 base: `91626851fb17df7ab31c96dee9353b9ee4d42c92`. Independent source verdict: T040-F1 is CLOSED, with no remaining confirmed source findings. Local reproduction, reviewed local release gates and terminal product CI pass. The independent product review is complete and ready for lead acceptance review. The lead separately owns acceptance, later metadata-head CI and merge; no worker self-acceptance is made.

This is the bounded correction review following the complete independent source audit in T-040 attempt 3 at `84089270fed1f9c145901de5220599706cab4107`. That report remains intact with its historical failed reproducer; its opening run ID `DG-W3-20260923-01` should be read as historical task identity, not the continuation run ID. Attempt 4 uses the corrected continuation identifier above. Attempt 3's report SHA256 is `7ea43f6deff22007c01c7113df18b24351f064fb879a77fc63d6c700317f444f`.

Isolated fixed checkout: `/home/ckc/test/codex/newclear-dim-gate-w3-review-4`, component cwd `platform/dim-gate`. The reviewer remained independent of implementation, made no product/test/doc/PLAN/Git/remote changes and did not delegate. Only authorized reports and generated verification evidence were written. The lead installed frozen dependencies offline. Built-in Codex review route was used; exact model slug is not independently exposed, and no Claude/multi-model claim is made.

The complete attempt-3 audit is explicitly reused for unchanged code: all W3 domain/API/policy/commands/projections, immutable definition snapshots and active-baseline target union, current requester/approver grants, config activation/failure/new-revision restore, source-time traffic and shared locks, eager startup/schema extraction, lazy command queue and call-time identity, strict genuine W1/W2 migration, typed editors/Ops triage/keyboard/readability, and actual held old-persona 200 isolation. Attempt 3 independently passed 372 native tests, 11 Chromium W3 cases and two Firefox/WebKit cases on a fresh 840 build. No whole-source re-review on 46 is implied: every delta from 840 was inspected, and only three files changed.

T040-F1 closure: `src/domain/service-delivery-integrity.ts:125–143` now counts consecutive known healthy samples per step and derives terminal decisions using abnormal, complete healthy window, then timeout precedence. Recorded step state/completion time/failure code must agree with the first terminal prefix, and that step cannot retain further samples. This matches the actual executor ordering, including success for a window first completed exactly at timeout. Existing cadence, checkpoint and terminal checks still apply. The new contract paragraph states this eager rejection requirement; three regression variants cover already-complete running history, an earlier healthy window in a timeout history, and a healthy window completed at the timeout boundary, while preserving an incomplete healthy suffix after unknown samples. The original independent saved reproducer now returns `INVALID_SNAPSHOT` for corruption and accepts its unmodified valid timeout.

## Verification

- `git rev-parse HEAD` matched 46e3a55; clean source before reports; exact `840..46` diff contains only 10 integrity lines, 21 regression-test lines and a two-line contract clarification — passed
- Independent `pnpm test`: 34 files, 375 tests, exit 0; `/tmp/dim-gate-w3-resume-evidence/t040-attempt4-native.log` — passed
- Independent fresh `pnpm build --mode demo` and `pnpm check:docs`, exit 0; `t040-attempt4-build.log`, `t040-attempt4-docs.log` — passed
- Original independent `node /tmp/dim-gate-w3-resume-evidence/t040-traffic-integrity-repro.mjs` executed in fixed 46 cwd, exit 0; validErrors empty, corruptErrors identify terminal-prefix contradictions and startupAcceptedCorruption false; `t040-attempt4-corruption.json`, `t040-attempt4-repro.log` — passed
- Independent generated evidence script `t040-attempt4-prefix-matrix.mjs`: actual healthy and missing-health commands for every supported 60/120/180-second window, with valid integrity/reload after every 30-second advance; six early/boundary corruption variants rejected without changing input bytes; three incomplete healthy suffixes after unknown accepted; `t040-attempt4-prefix-matrix.json` and `.log` — passed
- Independent `pnpm test:e2e e2e/w3-service-delivery.spec.ts --output=/tmp/dim-gate-w3-resume-evidence/t040-attempt4-browser-results`: 4/4 Chromium on fresh 46 dist; config failure/restore/reload, independent prod Ops decisions, missing timeout/complete checkpoints, failed-50 retaining verified10 and unchanged active release; `t040-attempt4-browser.log`, `t040-attempt4-browser-report/index.html` — passed
- Independent `pnpm test:smoke e2e/w3-browser-smoke.spec.ts --output=/tmp/dim-gate-w3-resume-evidence/t040-attempt4-smoke-results`: 2/2 Firefox/WebKit on the same fresh 46 dist; definition run, config activation, complete traffic and refresh; `t040-attempt4-smoke.log`, `t040-attempt4-smoke-report/index.html` — passed
- Independently decoded own fresh browser-health payloads: Chromium four attachments/1530 responses, zero page errors/failed requests/unexpected HTTP, four declared negative HTTP responses; Firefox/WebKit two attachments/836 responses, zero page/console/request/HTTP errors. Fresh 768px dark traffic PNG inspected; 1440/768/390 light/dark axe/layout checks passed; `t040-attempt4-{browser,smoke}-health.json` — passed
- Inspected lead `46-native.json` and logs: lint, typecheck, 375 native tests, docs, OpenAPI contracts (127 operations/259 schemas), CI configuration and architecture checks all exit 0 and bind 46 — passed
- Inspected terminal lead `fixed-gates.json` and actual logs: 89/89 Chromium and 10/10 Firefox/WebKit on unchanged 25ba5ca dist; 18/18 affected M3 cases separately on fresh 840. This older-build coverage is combined with inspected deltas and fresh 46 affected checks, not relabeled as a full 46 browser run — passed
- Independently decoded lead browser health: full Chromium 55 health attachments/29310 responses, zero page/request/unexpected HTTP errors and 42 declared negative HTTP responses; smoke six health attachments/2007 responses with zero errors; isolation three page-health attachments/39 responses with zero errors. Not every older test emits a health attachment; test totals come from terminal reports — passed
- Inspected fresh lead 46 benchmark metadata, empty tracked-source-diff hash and decoded payloads: five cold contexts, each 306447 gzip bytes, median LCP 752ms, no cold errors; 5000-CI/100-read p95 about 0.6ms; 100 persisted HTTP commands all 200, p95 about 170.4ms with +100 each audit/events/idempotency; all three tests pass. Evidence `performance-results.json`, `t040-attempt4-lead-performance.json` — passed
- Inspected lead fresh 46 `pnpm test:isolation` build/log/report: 2/2 actual sibling document/API worker isolation and unavailable live mode without demo fallback; `fixed-test-isolation.log`, `test-isolation-report/index.html` — passed
- Inspected terminal product [CI 35862758449](https://github.com/fallrising/newclear/actions/runs/35862758449) run.json/run.log: success, completed 2026-09-23T13:27:54Z, PR head 46e3a55; 375 native tests, 89 Chromium (32.5 minutes), 10 Firefox/WebKit (4.3 minutes), three benchmark and two isolation tests; all workflow gates succeeded — passed
- Independently verified actual CI checkout `928b563aeabe909528bd6e16f355463a11d635d0` is the synthetic merge of `f4a233dc3357cdeb30cb561b9c0a2a9207f6b10c` and reviewed head 46e3a55. Git shows both complete `platform/dim-gate` trees equal `f4dc14f9ecafa926cab771a5c068979d1dd16766`, with no component/workflow diff. Performance payloads name the synthetic commit and have an empty tracked diff; they are not relabeled as literal 46 checkouts — passed
- Independently decoded formal CI browser artifacts: HTML totals 89/10/2, all expected, zero flaky/skipped/unexpected; Chromium 55 health attachments/29316 responses, zero page/request/unexpected HTTP errors and 42 declared negatives; smoke six attachments/2008 responses and isolation three page attachments/39 responses with zero errors. CI 390px dark traffic screenshot inspected with readable controls and distinct desired/verified weights; `t040-ci-{chromium,smoke,isolation}-health.json` — passed
- Independently decoded formal CI benchmark payloads: five isolated cold samples each 306447 gzip bytes, median LCP 1324ms, zero cold errors; 5000-CI/100-read p95 about 1.1ms; 100 persisted HTTP commands all 200, p95 about 184.6ms with +100 audit/events/idempotency. All budgets unchanged and passed; `t040-ci-performance.json` — passed
- Verified formal artifact metadata and downloaded contents: artifact 10751674277, 85900919 bytes, named for actual synthetic checkout, expires 2026-10-23T13:27:47Z; metadata-reported archive digest `sha256:0498b5cc2adc9ca6f9d61d5b041a438a3811bcad3ef8241790f8fefca1f80059`. Artifact report counts and payloads match the terminal log; no independent archive checksum calculation is claimed — passed
- Inspected the lead factual matrix `.team/reports/dim-gate-w3-validation.md`; source labels/counts/metrics match the examined evidence, and all four HTML-report SHA256s plus the historical checkpoint SHA256 match actual files — passed
- Pinned kernel 237aa277 `teamctl.py validate-report` and `git diff --check` for this report and identical canonical T-040 handback — passed

Independent commands ran with Node 24.18.0/pnpm 11.18.0 via the supplied toolchain PATH; browser tests used port 4355, supplied local WebKit executable/libraries and font configuration. Local read/test/report commands used required escalation because default bwrap fails. The matrix and reproducer load actual source with Vite SSR, use real commands to create releases/executions, and mutate only independent cloned snapshots to test rejection; they never fabricate successful UI state.

Local source binding is explicit: `25ba5ca9a4e5d7e8cb3b2aa4448165801390a89c` covers the lead's full Chromium/smoke run; 840 covers the complete prior independent audit and affected M3 rerun; 46 covers this correction/native/browser reproduction and fresh benchmark/isolation. `source-bindings.json` overrides the continuing runner's original top-level testedCommit for the later rebuilding steps. Local benchmark payloads themselves independently report 46 and an empty tracked diff. Formal CI separately names synthetic merge 928b563, whose complete component tree is proven identical to 46 as above. Raw terminal CI evidence is under `/tmp/dim-gate-w3-resume-evidence/ci-35862758449/{run.json,run.log,artifacts.json,artifacts/}`. The earlier local source-bindings manifest still listed product CI pending at the time of the first terminal read; the terminal artifacts and tree comparison close that historical pending item, and lead was notified to update its metadata.

## Documentation

Applicable AGENTS/task/protocol, W3 revision-3 contract, SDD09–14 and affected01–08 were fully audited in attempt 3. This attempt inspected the entire new contract paragraph alongside the corresponding implementation and tests; it clarifies the existing eager integrity requirement without weakening gates or expanding scope. The only difference from 25 to 840 is the explicit pipeline mutation callback; the only difference from 840 to46 is the three-file F1 correction above. There are no dependency, architecture, browser-health, budget or exclusion changes.

The superseded attempt-3 finding and raw evidence remain available. This report corrects its continuation run identifier rather than rewriting history. Canonical T-040, PLAN, STATUS and acceptance integration are lead-owned except for the explicitly assigned canonical report handback. No W4/W5 delivery, live integration, external notification or deployment is claimed.

## Risks and Follow-ups

No confirmed source finding remains after the correction and actual reproduction. The complete source audit, bounded correction coverage and terminal product CI establish readiness for lead acceptance review. Acceptance, later metadata-head CI, merge, remote durability and post-merge checks remain separate lead actions; no worker self-acceptance occurred.

Initial-route JavaScript is within the unchanged 300KiB budget by only 753 bytes (306447 / 307200). This passes the current gate but leaves little growth margin. Preserve the current startup/import boundaries and budget rather than relaxing the measurement.

All local product and verification results here bind product commit 46. Later report-only commits can reuse product evidence only after confirming no product-source delta; the latest actual PR head must still receive the required CI result. The historical 25/840 results are explicitly limited and do not replace future regression for unrelated source changes.

The initial bounded review ended PARTIAL solely for running CI. Its exact bytes were preserved before this authorized evidence-only follow-up at `/tmp/dim-gate-w3-resume-evidence/t040-attempt4-partial-checkpoint.md`, SHA256 `7048ea1989c5d96bde39926eaee4d96e0902151df6a27a8a8f57b1ce49071a52`. This final attempt-4 report and canonical T-040 are identical DONE handbacks. Source HEAD remains 46; no product changes occurred during the evidence follow-up. Report ownership is released after validation and final SHA256 delivery.
