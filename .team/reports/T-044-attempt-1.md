STATUS: PARTIAL

## Summary

T-044 attempt 1, run `DG-W4-20260923-01`, is an independent read-only review of fixed product commit `65a4c363689aeefb2fdc03e132c6aa3b1f10a43b` against base `d80028c64c2d359d6a44bbe699a09d1d1d2bfe8a`. Verdict: **REWORK for one confirmed blocking W4 finding, T044-F1**. This review does not accept W4 or authorize a merge. The lead owns correction, new-source verification and acceptance. The reviewed checkout is `/home/ckc/test/codex/newclear-dim-gate-w4-review-65a4`, detached at the exact SHA. I did not implement T041–043 or lead integration, did not delegate, and made no product, test, configuration, documentation, PLAN, commit, push, PR or browser-port changes. The only checkout write is this authorized report. The available uninvolved reviewer route is built-in Codex; the exact runtime model slug is not independently exposed, and no Claude or multiple-model claim is made.

**T044-F1 — MEDIUM, blocking AC-WS-12 and the idempotent command contract.** At `platform/dim-gate/src/domain/monitoring-commands.ts:132–139`, Silence creation checks the rule's editable `status === 'active'` and checks `startAt >= current demo time` while `prepareMonitoring` runs. `platform/dim-gate/src/domain/engine-commands.ts:172–176` calls that preparation before the identical-key replay at lines 279–281. A revised rule correctly preserves `activeRevision = 1` but sets editable `status = draft`; the old active rule continues to evaluate, opens a CI incident and offers Silence in the UI, yet both a fresh Silence and a same-key retry return 409 `INVALID_STATE`. Separately, after a successful Silence, advancing the demo clock by just one second makes an exact body/key retry return 422 `VALIDATION_ERROR` because its original `startAt` is now in the past. A true replay should return the original receipt after current-scope reauthorization; a fresh Silence should be authorized against the still-active revision and validated against the current time at apply. The independent actual-command reproducer in `/tmp/dim-gate-t044-source-65a4/platform/dim-gate/src/domain/t044-repro.test.ts` (SHA256 `0153f347bf3a24b3579cbe45a891d653e4f14bb74ae73218acbb29b68e5613c7`) demonstrates all three outcomes. It also verifies that revoking the actor's pool grant makes a same-key retry return 403, so the correction must retain scope reauthorization before replay. Add fixed-source regressions for these four cases, including the preserved active revision and incident evidence. No unauthorized delivery or data leak was observed.

Beyond F1, I inspected the W4 91-file source/spec/contract diff by area: strict v1/v2/v3 to v4 migration and seed identity, models and revision integrity, service versus pool policy and independent prod approval, source-time evaluation/incident/delivery state, scoped list/detail/audit, typed Mock/OpenAPI/runtime registry, UI route and command readback, incident variant and navigation, and the unchanged gates. The baseline SDD09–14 and affected01–07 deltas remain consistent with the W4 integration contract. The W4 navigation, API, canonical CI-only incident path and Demo-only boundaries appear coherent in the inspected paths. This is a completed source review with a finding, not a claim that the product gate passed.

## Verification

- `git rev-parse HEAD` exactly `65a4c363689aeefb2fdc03e132c6aa3b1f10a43b`; isolated checkout initially clean, with only this report added afterward — passed
- Full W4 changed-file inventory and source/spec/contract inspection against base `d80028c`; `git diff --check d80028c...65a4` — passed
- Exact-commit `git archive` copied `platform/dim-gate` to `/tmp/dim-gate-t044-source-65a4`; tests used that copy and the existing canonical dependency directory, leaving the reviewed checkout source untouched — passed
- Independent native full suite via Node 24.18.0/Vitest 5.0.1, excluding only the separate temporary T044 reproducer: 37 files, 397/397 tests, exit 0 — passed
- Independent focused W4 native suites `src/domain/w4.test.ts`, `src/demo/w4-handlers.test.ts`, `src/demo/w4-migrations.test.ts`: 21/21, exit 0 — passed
- Temporary actual-command T044 repro: 1/1 test passed while asserting the fixed product returns 422 for a one-second clock-advanced same-key Silence replay, 409 for fresh Silence after an active-rule draft revision, 409 for the same-key revision replay, and 403 for a scope-revoked replay; active infra breach still creates one incident — passed
- Independent generated OpenAPI check on the exact-source archive: 161 operations, 313 schemas, all local references resolved — passed
- Independent TypeScript `tsc -b --pretty false` and architecture boundary check on the exact-source archive — passed
- Independent `check-docs` on the fixed checkout: 206 Markdown files, 452 repository links; `check-ci` workflow authority/path/action pin checks — passed
- Pinned private `teamctl.py validate-task .team/tasks/T-044.md` and report validator plus report whitespace checks — passed
- Independent browser execution was omitted while the lead's corrected 92-case Chromium run used port 4350; this report does not relabel the lead's in-progress browser/CI as independent or passed — skipped

## Documentation

Read the applicable `platform/dim-gate/AGENTS.md`, `DEVELOPMENT_PROTOCOL.md`, T-044 task, W4 integration contract revision 1, `SDD.md`, SDD09–14 and changed SDD01–07, PLAN D091, STATUS, handoff and W4 validation report. The fixed-source report in the commit still says final product SHA, full corrected Chromium, T044, exact PR-head CI and merge were pending; its historical first complete Chromium was 89/92, with two stale v3 migration assertions and one W4 read-before-completion test, and the contract-only CI35894949839 failed an M2 Admin axe 30-second timeout. Those earlier failures remain historical and are not treated as green or as failures of the present F1 reproducer. The lead separately reported a newer complete run and PR-head CI in progress; their outcomes were not independently observed here. W4 is therefore not accepted. No real collector, external notification, cloud action or deployment was exercised or claimed.

## Risks and Follow-ups

T044-F1 needs a new product commit. Authorize new Silence against the existing active revision even when a newer editable draft/submission exists; keep current actor/target scope checks before replay, and move fresh time-window/state checks behind replay into the apply path. Test a fresh Silence after revision, exact same-key replay after time advance and revision, a changed-body same-key conflict, and a revoked-grant retry. Then run focused native/API/browser gates on the corrected SHA and obtain an uninvolved fixed-delta review. This report's source verdict cannot be transferred to a changed product SHA without inspecting that delta.

The lead's running complete Chromium, fresh benchmark/smoke/isolation evidence and exact latest PR-head CI remain separate acceptance gates. The earlier 89/92 run and contract-only CI failure are explicitly historical, not combined with focused reruns into a claimed complete pass. This review did not run a browser workload or inspect terminal PR-head CI artifacts, and it does not self-accept. The saved temporary reproducer is machine-local; its SHA and exact observations are retained here for correction and repeatability. No other confirmed blocking source finding is asserted from this bounded review.
