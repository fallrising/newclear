STATUS: PARTIAL

## Summary

T-044 attempt 2, run `DG-W4-20260923-01`, independently reviews fixed correction commit `460424b12e27bffdea44912ba6101b6fd90b8a89` against the first reviewed product commit `65a4c363689aeefb2fdc03e132c6aa3b1f10a43b`. Verdict: **T044-F1 CLOSED; REWORK for newly confirmed blocking T044-F2**. This is not W4 acceptance or merge authorization. The detached review checkout is `/home/ckc/test/codex/newclear-dim-gate-w4-review-4604`; no product/test/config/docs/PLAN/Git/remote files were changed by this reviewer, and no delegation or independent browser workload was used. The only checkout write is this authorized attempt-2 report. Built-in Codex was the available uninvolved reviewer route; the exact runtime model slug is not independently exposed. No Claude or multi-model review is claimed.

**T044-F1 closed on this SHA.** The delta in `platform/dim-gate/src/domain/monitoring-commands.ts:132–147` retains `requireTarget` before engine replay and moves the active-revision and time-window checks into `apply`. A fresh Silence can now bind still-active rule revision 1 while editable revision 2 is draft and the rule continues to create a CI incident. Exact same-key/body replay after a one-second clock advance and after the draft revision returns the original receipt without adding rows. A changed body under the same key returns 409 `IDEMPOTENCY_CONFLICT`, and a replay after revoking the actor's pool grant returns 403. The new checked-in domain regression covers the time, draft, current-grant and fresh Silence paths; an independent version of the original actual-command reproducer covers those paths plus a live breach and changed-body conflict. The failure evidence from attempt 1 remains intact and historical.

**T044-F2 — MEDIUM, blocking AC-WS-15 and the idempotent command contract.** `monitoring-commands.ts:61–69` calls `checkSpec` before current-scope authorization and before the replay branch at `engine-commands.ts:279–281`. For AlertRule, `checkSpec` validates against the referenced MonitorPolicy's latest editable `spec` (`monitoring-commands.ts:31–35`, `monitoring.ts:57–68`), not necessarily the still-active monitor revision. Independent actual commands create and activate an infrastructure CPU monitor/rule under monitor revision 1, then revise the monitor to an inactive draft revision 2 that lists only memory; the active rule remains valid. Replaying the original `/alert-rules` create body and key now returns 422 `VALIDATION_ERROR` instead of its original receipt, although actor and target scope remain valid. The same early validation is an authorization oracle: a Data RD actor guessing a Store monitor ID gets 403 when the proposed metric is supported but 422 when it is unsupported, revealing a hidden monitor's metric allowance. This violates current-scope filtering and scope-first denial. Resolve the immutable target/reference and current actor scope before replay; perform mutable metric/spec compatibility, self-approval and state checks only in the fresh-command apply path. Preserve 404/403 masking, same-key changed-body conflict and current-scope checks on every retry. Add both draft-drift replay and hidden-metric differential regressions for AlertRule, with the analogous SLO path inspected too.

The source delta from 65a to 460 consists of the 11-line Silence fix, 31 lines of focused domain regression and lead-owned evidence/PLAN/STATUS updates. The W4 feature implementation and unchanged gates otherwise match the first complete source audit; this attempt inspected the full delta and adjacent command preparation rather than claiming a new full 91-file audit. The 65a source verdict does not transfer to this changed SHA except as explicitly verified above.

## Verification

- `git rev-parse HEAD` exactly `460424b12e27bffdea44912ba6101b6fd90b8a89`; isolated checkout initially clean and only this report added — passed
- Complete `65a..460` delta inspection, focused source trace, and scoped `git diff --check` on dim-gate/lead W4 records — passed
- Exact-commit `git archive` copied `platform/dim-gate` into `/tmp/dim-gate-t044-source-4604`; existing canonical dependency directory was linked only in that temporary copy, leaving reviewed source untouched — passed
- Independent full native suite via Node 24.18.0/Vitest 5.0.1 on that exact-source copy, excluding only the separate temporary T044 reproducer: 37 files, 398/398 tests — passed
- Independent fixed-source `src/domain/w4.test.ts` plus adapted original T044 reproducer: 7/7 across two files; F1 closure includes fresh Silence under active revision, clock/revision same-key receipt replay, no duplicate row, changed-body conflict, revoked-grant 403 and CI breach — passed
- Additional temporary actual-command F2 tests on 460: active CPU rule's same-key create replay fails 422 after the monitor receives a memory-only draft; guessed hidden Store monitor shows supported metric 403 versus unsupported metric 422; full temporary reproducer 3/3 — passed
- Independent `tsc -b --pretty false`, OpenAPI check (161 operations, 313 schemas), and architecture boundary check on the exact-source copy — passed
- Independent fixed-checkout docs check (207 Markdown files, 455 links) and CI workflow authority/path/pin check — passed
- Pinned private task/report validator and report whitespace check — passed
- Browser and exact PR-head CI gates were left to the lead after the first-review finding; no success is claimed for cancelled or in-progress jobs — skipped

## Documentation

Re-read applicable component AGENTS, T-044, W4 integration contract revision 1, SDD09–14 and affected01–07, PLAN D091, STATUS and W4 validation after comparing the 65a→460 delta. Attempt-1 report `T-044-attempt-1.md` is present unchanged in this checkout and records the original three failing Silence behaviors, 397/397 native baseline, historical 89/92 first full Chromium, and contract-only CI35894949839 timeout. The lead intentionally cancelled the obsolete 65a/899 complete Chromium and PR-head CI after F1 and is restarting them on corrected source; they are not relabeled as passing 460 evidence. Product acceptance, exact latest PR-head CI, merge and post-merge checks remain lead-owned. No real telemetry collector, external notification, cloud action or deployment was performed or claimed.

## Risks and Follow-ups

T044-F2 requires another product correction and fixed-SHA review. In preparation, retain only immutable target/reference lookup and current-scope authorization, then defer all mutable compatibility and state validation to `apply`, which is reached only after exact-body replay check. Re-run the independent three-case temporary reproducer (SHA256 `1eaae17c89b5b22d1d84ac275603dbcec5a3a9957c66f77deafca2043ad04fd9`) with expectations updated to original receipt and uniform scope denial, plus focused checked-in AlertRule and SLO tests. Inspect changed-body replay and requester/approver separation on the corrected SHA. The temporary file is machine-local evidence and was not added to the product tree.

The previous full browser and CI evidence is either historical or was cancelled before completion; this reviewer did not consume a browser port or inspect final CI artifacts. A subsequent fixed candidate must have its own source-bound checks and independent review before the lead can accept W4. No other new blocking finding is asserted from this bounded delta review.
