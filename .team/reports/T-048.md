STATUS: DONE

## Summary

Applied only the four T-042 blocking design repairs to the candidate contract: T-043 module/port ownership, nested internal rehydration authority, immutable approval consumption, and bare storage versus prefixed HTTP digest codecs.

## Verification

- Contract consistency inventory confirms T-043 exclusively owns `go.mod`/`go.sum`, persistence port, SQLite and rehydration paths while later child scopes exclude them — passed
- Proposed Go package-path inventory confirms only `internal/domain` and `internal/domain/sqlite` may import `internal/domain/internal/rehydrationcap`, excluding application, transport, command root and external callers — passed
- Wire/storage/domain digest examples confirm `sha256:` plus lowercase 64-hex decodes to bare `domain.Digest` storage and re-encodes exactly while bare, doubled and uppercase spellings reject — passed
- `python3 /home/ckc/.codex/plugins/cache/fallrising-private/codex-team-superpowers/0.1.0+codex.20260904040057/scripts/teamctl.py validate-report .team/reports/T-048.md` exits 0 — passed
- `git diff --check` exits 0 and scope audit identifies only the authorized candidate contract and new T-048 report as this task's changes — passed

## Documentation

Updated only the candidate implementation contract and added this validator-compatible rework report; T-042 remains an unchanged FAIL and existing Fake/SSE/NotRun content is retained.

## Risks and Follow-ups

This is design repair only. T-049 must independently verify the four repairs; all implementation and runtime oracles remain NotRun until their assigned child executes.
