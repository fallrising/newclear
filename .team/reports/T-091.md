STATUS: DONE

## Summary

Completed the bounded T-091 worker assignment: the accepted Fake-only backend now has a loopback-only executable runtime with controlled SQLite and artifact storage, explicit local-session and exact-Origin enforcement, stable health/readiness, graceful signal shutdown, restart durability, copyable local-development instructions, and the four required tests. This worker report is not self-acceptance; the orchestrator must independently review the candidate and evidence.

## Verification

- Before production Go changes, the worker read the applicable task-worker and Modern Go skills, task/contract/PLAN/design/API/predecessor/backend inputs, wrote `docs/sdd/local-fake-runtime.md` and `docs/local-development.md`, then ran the complete unfiltered `use-modern-go ... list --go-version 1.27` command at exit 0 and applied its Go 1.27 guidance — passed
- Runtime configuration validates an absolute clean controlled data root, explicit loopback-IP/non-zero-port listener, matching exact HTTP Origin, 43-256 byte base64url token, bounded token-form actor and shutdown window before the production listener is created; wildcard, hostname, non-loopback, zero-port, malformed/mismatched/HTTPS Origin, traversal and weak-token cases are rejected — passed
- Local authority retains only the configured token's SHA-256 digest and compares fixed-size digests with `subtle.ConstantTimeCompare`; the sole accepted cookie remains `__Host-hai_session`, the sole principal is the configured actor, and logs/errors do not interpolate token or cookie bytes — passed
- Artifact storage confines lowercase SHA-256 object names beneath a private non-symlink root, rejects traversal/absolute/alternate names and symlink objects, seals through a private temporary file, and re-hashes pre-existing objects before idempotent success; corrupt regular objects fail closed — passed
- `GET /healthz` returns HTTP 200 and stable 123-byte JSON declaring commands/persistence ready while automatic execution/projections remain unavailable; all other routes delegate to the accepted HTTP server and the projection/specification sources fail closed — passed
- Exact pinned command `go test -count=1 -v ./cmd/taskboard -run '^(TestRuntimeConfig_RejectsUnsafeExposure|TestLocalArtifactStore_RejectsEscape|TestLocalSessionAuthority_EnforcesTokenAndOrigin|TestRuntime_HTTPPersistenceAcrossRestart)$'` exited 0 with all four required names passing and no skip — passed
- Exact pinned command `go test -count=10 ./cmd/taskboard -run '^TestRuntime_HTTPPersistenceAcrossRestart$'` exited 0; each run used real loopback HTTP and SQLite, canceled cleanly, rebound the same address, reopened the same root, preserved lookup bytes and replayed without duplicate mutation — passed
- The required built-process smoke used `/tmp/hai-taskboard-t091.j9CnFK`, loopback `127.0.0.1:18091`, the documented valid IDs and a 43-byte non-production test token withheld from this report: health returned 200, authenticated exact-Origin CreateProject returned 200, lookup returned the canonical 350-byte result, SIGTERM exited 0, the same port/root restarted, and post-restart lookup equaled idempotent replay byte-for-byte at SHA-256 `4f447e269250d1c7210a43ad29990a5e9639d0acad86129efde9f4c882e25626` — passed
- Both process sessions emitted only `taskboard: listening on 127.0.0.1:18091`; neither runtime output contained the withheld token, cookie, request body or result, and both exact-PID SIGTERM operations exited 0 — passed
- The first host smoke launch inside the restricted no-network sandbox exited 1 with the deliberately generic `taskboard: startup failed` because bind was denied; the same immutable binary then ran under explicitly approved loopback permission and completed every required process assertion, retaining this non-acceptance tooling diagnostic without substituting a check — passed
- The complete authoritative gate used `golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`, `--network none`, read-only candidate/module mounts, read-only container root, executable temporary build caches, `GOTOOLCHAIN=local`, `GOPROXY=off` and `GOSUMDB=off`; `go version` reported `go1.27.1 linux/amd64`, `go mod verify` reported `all modules verified`, complete `gofmt -l .` was empty and `go vet ./...` passed — passed
- In that same fail-fast gate, `go test -count=1 ./...` passed every backend package including SQLite in 7.022s, and `go test -count=1 -race ./...` passed every backend package including SQLite in 31.932s with no failure, skip or race — passed
- In that same gate, `go build -o /tmp/taskboard ./cmd/taskboard`, complete `go test -list . ./...`, direct import inventory and `go list -m all` all exited 0; the command package inventory contains only standard library and accepted internal application/service/domain/SQLite/Fake/HTTP seams and adds no module — passed
- `python3 .../teamctl.py validate-task .team/tasks/T-091.md` exited 0 with `validate-task validation passed: .team/tasks/T-091.md`; `python3 .../teamctl.py validate-report .team/reports/T-091.md` exited 0 with report validation passed — passed
- Git identity remained branch `agent/hai-taskboard-p0a`, local HEAD and `origin/agent/hai-taskboard-p0a` both `c37a57d147bdfeb3ef43e6faae681f81a42d00b6`; task identity is `4ee76ec51997b8eff787bc420cf4d14379d8d5a773856093c3e632c16733e957` — passed
- Protected predecessor identities remained go.mod/go.sum `8c66a51fe39944f0a30e0952756dfd4b35824483af60a826ab9620d751691226`/`59d6f9f0590f841c826c0c2003f906b038b16397a886eaef7a562ac031b68d6b`, wiring `532121121fa48326349ce952f50489248546f6f4f30cdb5de2a39d62d638d332`, SQLite `3b000590cc720f5da715fe0064ba9341f584dbf8b131ab389979b1a250cf1741`, Fake adapter `bb5cbcb6f176f5abd450c4b288f4222b94c837fe5e2b93730b81e4e0d2aed1cc`, HTTP server `ae5adb54f1c2ae6cdaa665c920e404ab816cae8e348c3a52f29071b3eb21c71a` and migration `57d96955d3351de47b1a81696398cf9ddb843394eb5c004c6f79841117c7745c` — passed
- Final T-091 candidate identities are mini-SDD/local-development `496001110a1ac9dcd955694297df8b8a79edb7358766571495d12ebc4d87f97d`/`0a5c71e708e5017ba962a07f5987d4687d5f5cabb89f314c8228c41b0f4a5a0e`, main/config `f25a822b3e9bab04ed41f23cbd7aa9e67dfde528542628e013eabce5283bd487`/`f073c9ceced83505ed4eb5d55e005464a2b2341dd04aa05a10bbe55ebbd2f032`, local dependencies/runtime `60ace9d38217b1fc43a19c04fb7ed942607144b8879addf51758a463b9ca4854`/`1e848a2ca8f8b4b1594b94ae080dd493f10835fe1fc933e713163322ef92b6bc`, and runtime test `d587c9a42cdc8adf32c4159ab2e9ff57bb9179c5d1bb351fb3dfcd6074cd6755` — passed
- Worker-created durable paths are exactly the two authorized docs, `backend/cmd/taskboard/main.go`, four new authorized command-package files and this report; inherited `.team/PLAN.md` and `.team/tasks/T-091.md` changes were not edited by the worker, no predecessor/module/API/status/Git-ref path was changed, and `git diff --check` exited 0 — passed
- The exact task-owned smoke processes were confirmed stopped before `rm -rf /tmp/hai-taskboard-t091.j9CnFK`; cleanup exited 0 and removed only that disposable binary/data root — passed
- Automatic persistent outbox/Fake execution, persisted board/SSE projections, restore/backup, browser/live UI, root CI, real provider/credential/agent execution and wider G1/G2 acceptance are recorded as `NotRun/Deferred` boundaries and were not represented as runtime-ready or counted as T-091 acceptance checks — passed

## Documentation

Added `products/hai-taskboard/docs/sdd/local-fake-runtime.md` before production behavior and added `products/hai-taskboard/docs/local-development.md` with build, private data/token setup, health, authenticated command, lookup, shutdown and restart instructions using valid Crockford IDs. Implemented runtime code and tests only under `products/hai-taskboard/backend/cmd/taskboard/**`. Added this authorized worker report.

## Risks and Follow-ups

NotRun/Deferred by design: no automatic persistent worker or Fake scenario execution; no persisted board/SSE projection runtime; no restore/backup; no browser/live UI or root CI; no real provider, credential, agent, shell or ambient-network execution; and no wider G1/G2, deployment or release claim. Draft-to-Ready also fails closed because this local runtime lacks accepted repository specification bindings. A later bounded task and independent review must implement each surface before readiness changes; the orchestrator must independently evidence-gate T-091 before acceptance.
