STATUS: DONE

## Summary

Delivered portfolio documentation tiers (A/B/C/D): policy, taxonomy, templates,
updated root README catalog (all 26 components), PORTFOLIO documentation-tiers
table, README banners on every component, A-tier `docs/quickstart.md` files,
`products/goku/README.md`, and a `specs/fleet` path-clarification note.
Documentation-only; no application source changes. Tutorials (T-010) skipped by
policy for non-A and deferred for A (existing local-dev docs linked instead).

## Verification

- Inventory script: 26 components; 0 missing root README rows; 0 missing component READMEs; 0 missing A-tier quickstarts; 0 missing banners — passed
- `cd platform/ice-maker && PYTHONPATH=src python3 -m ice_maker status --json` — passed
- `git diff --name-only` scoped to Markdown / docs / .team task — passed
- `products/kith` package.json present (`node` probe) — passed

## Documentation

- Added: `docs/portfolio-doc-tiers.md`, `docs/taxonomy.md`, `docs/templates/*`
- Updated: root `README.md`, `PORTFOLIO.md` § Documentation tiers
- Added A-tier quickstarts under ice-maker, kith, agent-platform, hai-taskboard, dim-gate, eru-vps-mvp
- Added `products/goku/README.md`
- Banner prepended to 25 component READMEs (goku already included banner)

## Risks and Follow-ups

- `make check` in ice-maker: 303 tests, 1 failure (`test_github_command_timeout_and_partial_evidence_are_stable`) due to missing SSH agent socket in this environment — pre-existing env sensitivity, not introduced by docs; not claimed as pass.
- `go` and `pytest` unavailable on this host — A-tier quickstart commands for hai-taskboard / eru-vps-mvp not executed here; recipes point at existing authoritative docs.
- No directory rename for `specs/fleet` in this PR (documented only).
- dim-gate `.team/PLAN.md` program block left untouched; portfolio-docs program appended separately.
