# dim-gate W4 validation

Run `DG-W4-20260923-01` · [draft PR50](https://github.com/fallrising/newclear/pull/50) · [contract revision1](../../platform/dim-gate/docs/W4-INTEGRATION-CONTRACT.md) · base `d80028c64c2d359d6a44bbe699a09d1d1d2bfe8a`.

Status: **IN PROGRESS / NOT ACCEPTED**. T041–T043 source is integrated locally, with additional lead fixes for W4 navigation, CMDB route loading, infrastructure Incident audit, and initial JS size. The fixed product SHA and final browser, review, PR-head CI and merge outcomes remain pending. W3 PR37 is merged and its post-merge workflow35873299419 completed/SUCCESS. W4 contract checkpoint `9fd3279ead6aa9e776c56c9fb3066e670631484a` is SSH saved. Its CI35894949839 failed on the existing M2 Admin axe test's 30-second timeout after 88/89 Chromium cases passed; it is historical evidence for a contract-only head, not a result for the integrated W4 product.

| AC | Required evidence | Current result |
| --- | --- | --- |
| AC-WS-12 | Service/infra rules, independent prod decision, sample-time/revision lineage, no duplicate incident, Silence expiry and delivery statuses | Integrated domain, typed API and UI; 397 native, 2 W4 Chromium journeys and both W4 extra-browser paths pass. Complete corrected Chromium/review pending. |
| AC-WS-15 | Current-scope list/detail/evaluation/delivery/audit and stale persona response; 404/403 | Direct HTTP/native and focused W4 Chromium pass; scoped infrastructure Incident audit positive/negative regression passes. Complete corrected Chromium/review pending. |
| AC-WS-16 | Strict W1/W2/W3→v4 migration, active work, corrupt/quota retry and no ghost writes | Native 397/397, corrected W1→v4 and W2→v4 browser upgrade cases, and isolation 2/2 pass; full corrected Chromium pending. |
| AC-WS-17 | Keyboard, axe light/dark 1440/768/390, deep refresh, Firefox/WebKit, unchanged performance gates | W4 Chromium journey/a11y, Firefox/WebKit smoke 12/12, unchanged fresh benchmark 3/3 and isolation 2/2 pass; full corrected Chromium pending. |
| AC-WS-18 | Honest capability map, Demo marker, no external collector/message/cloud request | Home copy updated to describe local demo; final independent content/network review pending. |

Integrated precommit native gate: `pnpm install --frozen-lockfile`, `generate:contracts`/`check:contracts` (161 operations, 313 schemas, all local references resolved), `typecheck`, `lint`, `check:architecture`, `check:ci`, `check:docs` (206 Markdown files, 452 repository links), `test` (397/397 in 37 files), and `build --mode demo` all passed on the current local source. `git diff --check` passed. This is not a substitute for source-bound final CI.

CI35894949839's sole failure is a 30-second timeout in the existing M2 Admin responsive/axe case, which performs 18 full scans. Its per-test timeout is now 60 seconds with the same routes, viewports, assertions and 30-second default for all other cases. A fixed-source focused rerun and new exact-head CI are still required; the old CI failure is not relabeled as success.

First complete Chromium run on the integrated product was **89 passed/3 failed in 29.1 minutes**. The failed W2 and W3 browser migration cases still asserted schema v3 although the correct persisted result was v4; both now assert v4 and W4 additive metadata and pass their focused browser rerun. The W4 extra-browser scenario read the persisted snapshot while its command button was still disabled; it now awaits visible evaluation evidence before readback and scopes the incident link locator. This case passes in focused Chromium and Firefox/WebKit. The complete corrected 92-case Chromium command is running; no combined-green claim is made from the separate reruns.

`pnpm test:smoke` passed **12/12** (six Firefox, six WebKit), including W4 service monitoring→Ops incident evidence on each engine. Fresh `pnpm benchmark` passed **3/3** on five isolated cold starts: 302874 initial gzip JS/worker bytes versus 307200 budget, median LCP 732ms versus 2500ms, 5000-CI query p95 0.6ms versus 150ms, persisted Mock HTTP p95 171ms versus 500ms. `pnpm test:isolation` passed **2/2** for sibling API/document boundaries and unavailable live mode. These runs used current integrated source and unchanged benchmark/isolation scripts; the local Playwright JSON attachments provide raw samples but are not a remote durable artifact.

Fixed-product gate plan: frozen install, lint, typecheck, native test, docs, contracts, CI/architecture, demo build; complete Chromium, Firefox/WebKit smoke, fresh unchanged benchmark and sibling/live isolation; uninvolved T044 fixed-source review; exact latest PR-head CI; then authorized normal merge and actual merge/tree/post-merge checks. Historical failed or partial attempts remain explicit. CI artifacts expire and `/tmp` raw logs are machine-local, so versioned summaries must preserve source SHA, command, result and artifact URL. Do not call a draft or intermediate branch ACCEPTED.
