ROLE: WORKER

AGENT: sqlite-selection-reviewer

ID: T-004

Goal: Execute a bounded Go 1.27.1 SQLite driver spike and recommend one P0-A driver/engine/backup
contract for ADR-005.

Why: Persistence production code is blocked until the actual embedded SQLite version and correctness,
backup and reproducibility behavior are evidenced rather than inferred from a module version.

Inputs to read first: `.team/PLAN.md`; `products/hai-taskboard/AGENTS.md`;
`products/hai-taskboard/docs/adr/ADR-005-sqlite-and-local-deployment.md`;
`.team/reports/T-002.md`; the Modern Go Guidelines skill and its required full CLI `list` for Go 1.27.

Scope (may touch): `.team/reports/T-004.md` only. Temporary spike files may be created under a unique
`/tmp` directory and disposable containers may be run; do not copy them into the repository.

Out of scope (must not touch): Product code/docs; every other report; Git refs; credentials; live
databases; real adapters; external mutations other than read-only module downloads.

Definition of done:

- [ ] Compare current pinned `modernc.org/sqlite` and `github.com/mattn/go-sqlite3` versions under
      Go 1.27.1, recording module versions, license/CGO trade-off and actual `sqlite_version()`.
- [ ] Run reproducible tests for foreign keys, WAL, synchronous mode, busy timeout/concurrent writers,
      consistent backup/restore and race compatibility, distinguishing passed/failed/not-run.
- [ ] Confirm the selected engine includes the SQLite WAL-reset fix (3.51.3+ or verified backport).
- [ ] Recommend a driver plus exact DSN/startup/backup policy, or return BLOCKED with evidence.

Verify with: actual commands, image/module/engine versions, UTC timing and exit codes in the report;
re-read the report and verify no repository path outside the report changed.

Budget: One bounded executable spike; no production implementation, commit or nested delegation.
