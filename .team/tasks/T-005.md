ROLE: WORKER

AGENT: transport-contract-reviewer

ID: T-005

Goal: Specify the smallest versioned REST/OpenAPI, SSE cursor and artifact envelope contract required
for the first Fake vertical slice.

Why: Domain and UI implementation must share stable command/result/error shapes without making the
transport or generated schema a second source of business invariants.

Inputs to read first: `.team/PLAN.md`; `products/hai-taskboard/AGENTS.md`;
`products/hai-taskboard/docs/SDD.md`; all `products/hai-taskboard/docs/sdd/*.md`;
`.team/reports/T-002.md`; `.team/reports/T-003.md`.

Scope (may touch): `.team/reports/T-005.md` only.

Out of scope (must not touch): Product code/docs/schema; every other report; Git refs; dependencies;
runtime processes; real adapters; external systems.

Definition of done:

- [ ] Recommend exact P0-A endpoints for Project/WorkItem queries, commands, command lookup and SSE.
- [ ] Define command, success, structured error, version conflict, Done rejection, stream cursor/reset,
      Candidate/Evidence and immutable artifact locator envelopes with example JSON.
- [ ] State validation/canonicalization and forward-compatibility rules, including unknown fields.
- [ ] Map contract operations to named tests and SDD clauses without moving invariants into OpenAPI.

Verify with: re-read `.team/reports/T-005.md`; check every P0 endpoint has an authority/permission and
idempotency/version rule; verify no file outside the report changed.

Budget: One bounded contract review; no code/schema generation, commit or nested delegation.
