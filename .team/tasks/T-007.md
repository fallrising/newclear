ROLE: REVIEWER

AGENT: g0-evidence-reviewer

ID: T-007

Goal: Independently audit whether the HAI Taskboard candidate design is sufficient and internally
consistent for G0 acceptance and the first deterministic production-code tasks.

Why: Reports and authored specs cannot accept themselves; code must not begin from contradictory or
unverified authority, persistence, transport, UI or toolchain contracts.

Inputs to read first: the codex-team-superpowers:codex-evidence-gate skill; `.team/PLAN.md`;
`products/hai-taskboard/AGENTS.md`; `products/hai-taskboard/docs/SDD.md`;
all six `products/hai-taskboard/docs/adr/*.md`; all five `products/hai-taskboard/docs/sdd/*.md`;
`products/hai-taskboard/docs/traceability.md`; `products/hai-taskboard/docs/reproducibility.md`;
`products/hai-taskboard/docs/spec-graph.yaml`; `products/hai-taskboard/api/openapi.yaml`. For reports
T-001 through T-006, verify file presence/hash and read their `## Summary`, `## Verification` and
`## Risks and Follow-ups` sections; only chase another section when a gate claim depends on it.

Scope (may touch): `.team/reports/T-007.md` only.

Out of scope (must not touch): Every product/API/task/plan file; other reports; Git refs;
dependencies; implementation; credentials; runtime/external systems.

Definition of done:

- [ ] Verify repository placement/isolation and one execution-status authority.
- [ ] Check clauses, ADR/mini-SDD precedence, WorkItem/Run/Review/QA/Done, subject identity,
      idempotency, epochs/generations, old+new invalidation and Fake-only boundary for contradictions.
- [ ] Validate OpenAPI/YAML and operation/payload/error/cursor/artifact coverage with read-only tools.
- [ ] Verify UI IA maps every required state and non-drag/focus contract.
- [ ] Verify Go/web tasks can begin without an unresolved driver, version, CI or test-oracle choice.
- [ ] Return PASS, PASS_WITH_EXPLICIT_FOLLOWUPS or FAIL; list failed/skipped/not-run checks exactly.

Verify with: actual read-only commands, paths, hashes/exit codes where relevant, scope audit and a
re-read of the report. No fixing during review.

Budget: One bounded independent evidence-gate turn; no edits outside the report and no delegation.
