ROLE: REVIEWER

AGENT: g0-rework-verifier

ID: T-008

Goal: Independently verify only that every T-007 blocking finding is repaired without regressing the
previously passing G0 areas.

Why: The orchestrator's rework cannot accept itself, and a prior failed gate must remain visible.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/reports/T-007.md`; `products/hai-taskboard/api/openapi.yaml`;
`products/hai-taskboard/docs/SDD.md`; `products/hai-taskboard/docs/sdd/ui-interaction.md`;
`products/hai-taskboard/docs/traceability.md`; `products/hai-taskboard/docs/tasks/HTB-P0A-001*` and
`HTB-P0A-002*`.

Scope (may touch): `.team/reports/T-008.md` only.

Out of scope (must not touch): Product/API/spec/task/plan files; other reports; dependencies;
implementation; Git refs; credentials; runtimes/external systems.

Definition of done:

- [ ] Confirm T-010 no longer runs before its G0 dependency and stale open-decision text is gone.
- [ ] Confirm exact Attention and Impact queries/schemas plus complete WorkItem detail fields.
- [ ] Confirm typed SSE projection/reset data includes epoch/sequence/reset fields.
- [ ] Confirm all command operations are conditionally/discriminator-bound to strict payload schemas;
      validate at least one legal and one mismatched operation/payload instance if available tools permit.
- [ ] Re-run available YAML/OpenAPI/spec-graph checks without installing tools; retain NotRun evidence.
- [ ] Return PASS or FAIL; do not use conditional prose to hide a blocker.

Verify with: read-only commands, exact exits and scope audit; re-read the report. No fixes.

Budget: One narrow re-review turn; no implementation, install, commit or delegation.
