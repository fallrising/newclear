ROLE: REVIEWER

AGENT: reviewer-contract-verifier

ID: T-013

Goal: Independently verify T-012's forward-only reviewer-report contract and decide whether it closes
the current compatibility debt without changing historical evidence.

Why: A documentation worker cannot accept its own compatibility inventory or future reviewer rules.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/tasks/T-012.md`; `.team/reports/T-012.md`; `.team/REVIEWER_REPORT_CONTRACT.md`;
the complete initial plugin report contract; every existing `.team/reports/T-*.md`; and the exact
source-identified `teamctl.py` implementation.

Scope (may touch): `.team/reports/T-013.md` only. Read-only probes and isolated `/tmp` fixtures are
allowed; remove temporary fixtures before the scope audit.

Out of scope (must not touch): The candidate contract; existing task/report bytes; `.team/PLAN.md`;
product files; validator/plugin code; dependencies; Git refs; credentials; providers/external state.

Definition of done:

- [ ] Recompute all 19 historical report hashes and current validator outcomes; compare every row
      with the candidate manifest and visible decision.
- [ ] Confirm the contract never makes syntax compatibility an acceptance oracle and never rewrites,
      relabels or hides historical failed/skipped evidence.
- [ ] Prove with isolated representative PASS and FAIL reviewer reports that the forward-only profile
      is accepted by the exact current validator and preserves non-DONE status for failed/skipped gates.
- [ ] Validate T-012 and the final T-013 report with the exact current validator.
- [ ] Audit report-only scope and return an unconditional PASS or FAIL for accepting T-012.

Verify with: hashes, full validator inventory, isolated fixtures, exact validator commands/exits,
`git diff --check`, temporary cleanup and final scope audit.

Budget: One narrow independent review; no fixes, delegation, commits, Git refs or external mutation.
