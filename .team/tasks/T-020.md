ROLE: WORKER
AGENT: Independent in-environment Codex reviewer, fresh context, inherited runtime model (exact identifier unavailable)
ID: T-020
Goal: Independently review fixed M3 delivery candidate f7905bbab0c3032b28387d4596795c3a64800c58 for AC-13–16 and AC-24, with M0–M2 and AC-20 regression safety.
Why: A fresh reviewer who did not implement the change is a required milestone gate.
Inputs to read first: platform/dim-gate/AGENTS.md; platform/dim-gate/DEVELOPMENT_PROMPT.md; platform/dim-gate/docs/DEVELOPMENT_PROTOCOL.md; platform/dim-gate/docs/M3-INTEGRATION-CONTRACT.md; platform/dim-gate/docs/sdd/03-workflows.md; platform/dim-gate/docs/sdd/04-permissions-admin.md; platform/dim-gate/docs/sdd/06-api-mock.md; platform/dim-gate/docs/sdd/07-delivery-validation.md; .team/PLAN.md; platform/dim-gate/docs/STATUS.md.
Scope (may touch): Read-only inspection of fixed candidate and its diff against e760d8e988c0e2a837b226c600805a659a362c10. Return review findings to the lead; no file writes.
Out of scope (must not touch): Editing files, mutating shell commands, network access, delegation, commit/push, PR changes, merge, deployment, credentials or host configuration.
Definition of done:
- [ ] Report reproducible blocking/high/medium findings with exact code references, inputs, expected/actual behavior and relevant requirement; explicitly state if none remain.
- [ ] Inspect authorization before replay, stage scope and audits, immutable artifacts, atomic environment locks, health-gated activation, approval actor separation, cancel/retry/rollback history, scheduler integrity, persistence recovery and UI action guards.
Verify with: Read fixed source/tests and `git diff e760d8e988c0e2a837b226c600805a659a362c10..f7905bbab0c3032b28387d4596795c3a64800c58 -- platform/dim-gate` using read-only tools/commands. Read-only shell escalation is permitted if the sandbox cannot start; no writes, installs or dynamic execution. The lead reproduces all findings and runs tests/Chromium separately.
Budget: One initial independent review and at most one same-approach rework; three overall lead cycles. No subagents.

Project/variant: dim-gate/mainline; revision 2; attempt 2. Candidate worktree: /home/ckc/test/codex/newclear-m3-review2. Revision 1 and its fixed checkout remain historical and unchanged. No acceptance authority; the orchestrator owns evidence and the final decision.

Role clarification: WORKER is the kernel task-envelope value; the assigned function is independent read-only reviewer, with no implementation authority.

Routing note: Claude preflight replied READY and identified claude-sonnet-5, but the environment rejected sending unpublished product source to that external service. No source review was executed through Claude and the rejection was not bypassed. The fallback is a fresh read-only reviewer inside the existing collaboration environment; this is independent multi-agent review, not a claimed multi-model review.

Attempt 2 input: `.team/reports/T-020-attempt-1.md` records the four original findings. Inspect their closure and all changed authorization/scheduler/playback paths independently, including regressions introduced by the correction. Lead owns dynamic validation; a green test count is not a substitute for this review. Return the report in your final message only; the lead will preserve it without upgrading your verdict. Do not send source to any external model.
