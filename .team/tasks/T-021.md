ROLE: REVIEWER

AGENT: domain-kernel-verifier

ID: T-021

Goal: Independently inspect and verify the T-020 pure-Go domain/reconciliation kernel candidate.

Why: A worker report is evidence, not acceptance. Completion authority, immutable subjects,
independent test oracles and old+new closure semantics are safety boundaries for all later slices.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/tasks/T-020.md`; `.team/reports/T-020.md`; `products/hai-taskboard/AGENTS.md`;
the accepted SDD, ADR-002, ADR-004, domain/reconciliation mini-SDDs, traceability and every file
under `products/hai-taskboard/backend/`.

Scope (may touch): `.team/reports/T-021.md` only. Read-only temporary external-package probes may be
created under `/tmp`, never inside the candidate module.

Out of scope (must not touch): Backend source/module; web/spec/API/plan/tasks/other reports; Git refs;
dependencies, credentials or external systems.

Definition of done:

- [ ] Trace the source against HTB-P0A-001 and named test skeletons; confirm strong identities,
      immutable copy boundaries, canonical valid JSON and deterministic rejection codes.
- [ ] Prove ordinary/public transition APIs cannot bypass subject-bound `CompleteWorkItem` to enter
      Done, including from another package in the module. Confirm failed gates return neither Done
      state nor CompletionRecord and successful results bind the exact subject/version.
- [ ] Audit exhaustive nonpassing/unavailable/stale/missing evidence, review independence, blockers,
      active/unknown Runs, approvals, idempotency conflicts and subject TOCTOU. Check historical
      evidence/reviews cannot accidentally invalidate or satisfy a different/current subject.
- [ ] Audit typed graph validation, stable-ID diffs, old+new reverse closure, deterministic shortest
      cause paths, hard caps, plan identity/stale activation and reuse dimensions. Tests must use
      independent expected values rather than production closure/transition logic as oracle.
- [ ] Independently run pinned Go 1.27.1 gofmt, vet, test, race, test-list and stdlib/authority audits;
      confirm no go.sum, third-party module, DB/network/filesystem/shell/executor/provider import.
- [ ] Audit report-only scope and return unconditional PASS or FAIL for T-020 acceptance.

Verify with: complete source inspection, exact read-only commands/versions/exits/hashes and optional
`/tmp` compile probes. No fixes, commits, installs, delegation or candidate edits.

Budget: One bounded independent domain-kernel review.
