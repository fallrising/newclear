ROLE: WORKER

AGENT: done-authority-reworker

ID: T-022

Goal: Close the sole T-021 public Done-construction bypass without widening the domain kernel.

Why: Only `CompleteWorkItem` may create a Done WorkItem, and every successful entry to Done must
return the exact-subject CompletionRecord in the same value result.

Inputs to read first: codex-team-superpowers:codex-task-worker; Modern Go skill; `.team/PLAN.md`;
`.team/reports/T-020.md`; `.team/reports/T-021.md`; `products/hai-taskboard/AGENTS.md`;
ADR-002; domain mini-SDD; `backend/internal/domain/workitem.go`, `completion.go`, and domain tests.

Scope (may touch): `products/hai-taskboard/backend/internal/domain/workitem.go`;
`products/hai-taskboard/backend/internal/domain/*_test.go`; `.team/reports/T-022.md`.

Out of scope (must not touch): Other backend source/module/reconciliation code; web/spec/API/plan/
tasks/other reports; dependencies, Git refs, credentials or external systems.

Definition of done:

- [ ] Before editing Go, run/read the complete Modern Go CLI list for the exact Go 1.27 module/file.
- [ ] Public `NewWorkItem` and `Transition` cannot create/enter Done for any caller-controlled input.
- [ ] Add an external-package regression test proving direct public construction/transition cannot
      yield Done, while the existing successful `CompleteWorkItem` test still binds the exact
      subject digest and returns a non-zero CompletionRecord together with the Done item.
- [ ] Preserve existing transition/reopen tests without adding a public rehydration bypass. Durable
      persistence rehydration is outside this slice and requires its own guarded contract.
- [ ] Re-run pinned gofmt, vet, test, race and authority audits; no dependency/go.sum/authority import.
- [ ] Write exact evidence and scope audit; do not weaken Done predicates or test oracles.

Verify with: pinned Go 1.27.1 read-only checks plus external-package tests and exact diff inspection.

Budget: One narrow authority-boundary repair; no delegation, commit or Git ref mutation.
