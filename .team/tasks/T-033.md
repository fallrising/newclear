ROLE: REVIEWER

AGENT: web-shell-rework-verifier

ID: T-033

Goal: Independently verify the narrow T-032 repairs and decide T-030 web-shell acceptance.

Why: T-031's failures must be rerun without erasing them, while already-passing UI behavior should
not be needlessly recomputed beyond a regression gate.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/tasks/T-030.md`; `.team/tasks/T-032.md`; `.team/reports/T-030.md`;
`.team/reports/T-031.md`; `.team/reports/T-032.md`; `products/hai-taskboard/AGENTS.md`;
`docs/reproducibility.md`; and every non-generated file under `products/hai-taskboard/web/`.

Scope (may touch): `.team/reports/T-033.md` only. Perform installs/builds in an isolated temporary
copy and remove it before scope audit.

Out of scope (must not touch): Candidate web/backend/spec/API/plan/tasks/other reports; dependencies
outside an isolated temp copy; Git refs; credentials, live API/provider or image pulls.

Definition of done:

- [ ] Confirm exact manifest/lock pins for Playwright 1.62.1 and axe-playwright 4.13.0, plus no
      workspace/generated path in the candidate.
- [ ] Confirm shadcn registry/base/slate/lucide/CLI/no-response provenance and that all recorded
      source hashes exactly match.
- [ ] In pinned Node 24.20.0/pnpm 11.25.0, independently perform frozen install and rerun format,
      lint, typecheck, all 8 Vitest tests/full fixture axe and Vite build.
- [ ] Reconfirm no live mutation/provider path and that T-031's passing surface/transition findings
      were not regressed by the manifest-only rework.
- [ ] Keep Playwright/browser/contrast/zoom/coarse-pointer checks explicit NotRun; under T-030/T-032
      their absent image does not block this fixture-shell slice, but it does block broader G1 claims.
- [ ] Audit report-only scope and return unconditional PASS or FAIL for T-030/T-032 acceptance.

Verify with: exact versions/commands/exits/hashes, read-only inspection and temp-copy cleanup.
No fixes, installs in candidate, pulls, commits, delegation or Git refs.

Budget: One narrow independent web-shell re-review.
