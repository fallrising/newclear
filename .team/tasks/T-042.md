ROLE: REVIEWER

AGENT: fake-vertical-slice-design-verifier

ID: T-042

Goal: Independently decide whether T-041 is executable and safe enough to authorize its first Go
child without weakening accepted domain, authority or evidence contracts.

Why: A compact cross-module design can look complete while hiding an impossible Go dependency,
non-atomic SQLite invariant or scope that cannot add its pinned dependency.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041 task/report; the candidate implementation contract;
product AGENTS; SDD; ADR-001/002/003/005/006; persistence/executor/domain mini-SDDs; traceability,
reproducibility and spec graph; HTB-P0A-003; backend `go.mod` and all domain code/tests; full OpenAPI.

Scope (may touch): `.team/reports/T-042.md` only. Read-only probes and removable `/tmp` fixtures
are allowed.

Out of scope (must not touch): Candidate documents/code; task/plan files; existing reports;
dependencies; Git refs; credentials; provider/external state.

Definition of done:

- [ ] Check package direction and every proposed child scope, including whether the child authorized
      to add SQLite may also update the required `go.mod`/`go.sum` and whether serial interfaces can
      compile without editing earlier accepted scopes.
- [ ] Check guarded Done rehydration is implementable across Go package visibility and cannot expose
      an ordinary public construction bypass; require executable missing/mismatch cases.
- [ ] Check SQL constraints/triggers are internally consistent, use the accepted digest wire/domain
      representation deliberately, and preserve atomic Done/idempotency/audit/outbox/event behavior.
- [ ] Check Fake fencing and SSE snapshot/replay/backpressure algorithms against accepted clauses and
      OpenAPI without silently claiming restore/provider/runtime evidence.
- [ ] Re-run graph/YAML/path/traceability and report-validator checks; audit report-only scope.
- [ ] Return unconditional PASS or FAIL. Any ambiguity that would require a child to invent authority
      or widen writable scope is a blocking design failure.

Verify with: complete source/contract inspection, deterministic probes where useful, exact validator,
`git diff --check`, temp cleanup and scope audit.

Budget: One independent design gate; no fixes, delegation, commits, Git refs or external mutation.
