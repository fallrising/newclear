ROLE: WORKER

AGENT: deterministic-fake-executor-implementer

ID: T-045

Goal: Implement and prove the accepted deterministic `fake/v1` executor, immutable scripted
scenario model and fenced worker seam entirely within the T-045-owned Fake package.

Why: T-044/T-066 are accepted by T-067, so the next serial T-040 child must make start, heartbeat,
checkpoint, lookup, terminal, cancellation and unknown-outcome behavior deterministic before any
HTTP/SSE or vertical integration work begins.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; product AGENTS; `docs/SDD.md` HAI-BOUNDARY,
HAI-EXEC and HAI-SEC clauses; ADR-003; `docs/sdd/executor-and-security.md`,
`docs/sdd/persistence-and-recovery.md` and accepted `docs/sdd/fake-vertical-slice-implementation.md`
sections 1, 5 and 7; `docs/reproducibility.md`; `docs/traceability.md` AC-P0A-005/006 and AC-12..16,
AC-20/42/53; `docs/tasks/HTB-P0A-003-fake-vertical-slice.md`; T-044/T-065/T-066/T-067 tasks and
reports; backend `go.mod`; application `port/executor.go`, `port/clock.go` and `port/artifact.go`;
domain identity/digest/Run source; application service executor-declaration validation/tests. Treat
accepted persistence/application source and tests as read-only predecessor evidence.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27. Apply every relevant rule and use `explain` before skipping
one that appears applicable.

Scope (may touch): `products/hai-taskboard/backend/internal/executor/fake/**`;
`.team/reports/T-045.md`.

Out of scope (must not touch): Every existing backend file outside `internal/executor/fake/**`,
including application ports/services, persistence, domain, SQLite and module files; HTTP/SSE,
composition root and T-046; integration and T-047; web/OpenAPI/specification/traceability/plan/task
files and prior reports; backup/restore implementation; Git refs; real providers, credentials,
host shell, network, repository mutation, PR, merge, deployment, release or external state.

Definition of done:

- [ ] Implement an adapter satisfying the accepted `port.Executor` declaration boundary with exact
      adapter ID `fake/v1`, a nonempty fixed version and an immutable, sorted, duplicate-free subset
      of `start_ack`, `heartbeat`, `lookup`, `cancel_ack` and `durable_checkpoint`. Construction
      rejects unknown/duplicate capabilities and caller mutation cannot change the declaration.
- [ ] Define and validate an immutable scenario envelope containing stable scenario identity,
      declared capabilities and an ordered finite observation script. The script is data only and
      supports dispatch receipt, start acknowledgement/loss, heartbeat, checkpoint, terminal
      success/failure, timeout, late/stale publication, lookup result, cancellation acknowledgement
      and unknown outcome without executable callbacks, host paths or network locations.
- [ ] Drive observations only through an injected clock or explicit monotonically increasing test
      tick. No wall-clock sleeps, ambient randomness, goroutine scheduling, map iteration or caller
      aliases may influence the emitted order or bytes. Repeated construction/execution of the same
      scenario must return an alias-safe identical transcript.
- [ ] Require every dispatch/cancel/lookup/poll request and every emitted observation to bind Run ID,
      immutable input digest, lease holder/epoch and restore generation. The worker seam rejects and
      records stale epoch, wrong holder/generation/input/Run and terminal-after-expiry publication
      before accepted-state mutation; rejected or duplicate observations cannot advance the script.
- [ ] Fail unsupported dispatch operations with `capability_unsupported` and zero scenario/worker
      mutation. Lost start acknowledgement uses lookup only when declared; absent/unknown lookup,
      lease expiry or timeout preserves `NeedsReconcile` plus `OutcomeUnknown` and never schedules,
      emits or implies an automatic redispatch. Cancellation becomes confirmed only after a current
      fenced cancel acknowledgement; lost/unconfirmed cancellation remains unknown.
- [ ] Permit Fake artifact output only through an explicitly supplied bounded staging-writer
      interface using validated relative names and copied bytes. Production Fake files must import
      no `os`, `os/exec`, `net`, `net/http`, `syscall`, provider/client package or concrete SQLite/
      transport package, and must expose no shell, Git, credential, repository or external-write
      capability. This is a Fake-boundary denial claim only, not real OS isolation.
- [ ] Add and pass exact tests `TestFakeAdapter_CapabilitiesFailClosed`,
      `TestFakeAdapter_ScriptedStartHeartbeatCheckpointAndUnknown`,
      `TestRunRecovery_ExpiryDoesNotImplyStoppedOrRetry`,
      `TestAdapterObservation_RejectsStaleEpochOrRestoreGeneration` and
      `TestFakeAdapter_HasNoShellNetworkOrExternalWriteCapability`, plus deterministic repetition,
      alias-mutation, duplicate/late/cancel/lookup and invalid-scenario negative cases.
- [ ] Prove the Fake implements the existing public executor declaration without changing that port;
      keep application persistence/observation integration, HTTP/SSE, T-047, restore, real provider,
      browser and broader G1/G2 evidence explicit NotRun. Write a validator-compatible T-045 report
      with exact commands/exits, source/import inventory, scope audit and no skipped required check.

Verify with: complete Modern Go list; digest-pinned network-disabled Go 1.27.1 container and existing
read-only module cache; module/format/vet; exact named Fake tests; repeated Fake package tests; full
backend and race tests; test/import/API/scope inventory; current task/report validators; `git diff
--check`.

Budget: One bounded implementation turn; no delegation, commit, Git ref, dependency/port/domain/
persistence/application change, HTTP/SSE/T-047, real executor, credential, deployment or external
mutation.
