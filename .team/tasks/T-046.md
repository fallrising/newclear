ROLE: WORKER
AGENT: Codex orchestrator
ID: T-046
Goal: Integrate strict v1–v4→v5 migration, safe W5 seed, typed Mock HTTP clients/routes/OpenAPI and contract tests.
Why: W5 governance must use the same persisted canonical state through every API and upgrade path.
Inputs to read first: platform/dim-gate/AGENTS.md; docs/W5-INTEGRATION-CONTRACT.md; SDD.md; docs/sdd/09–14 and affected 01–07; .team/PLAN.md latest dim-gate block; T-045 domain interfaces; current migration/HTTP tests.
Scope (may touch): platform/dim-gate/src/demo/**, src/api/**, scripts/openapi.ts, owned T-046 report; lead shared schema composition after T-045.
Out of scope (must not touch): Other components, historical worktrees, real external request or credential, force/main push, deployment, self-acceptance.
Definition of done:
- [ ] Strict old-snapshot proof before atomic additive v5 migration, preserving active W4 work/history and corrupt/quota recovery.
- [ ] Typed routes, clients, runtime manifest/OpenAPI, scoped HTTP positive/negative/replay tests and safe seeded Mock defaults; record actual checks.
Verify with: focused migration/HTTP tests; pnpm typecheck; pnpm check:contracts; pnpm check:docs; git diff --check
Budget: Three bounded cycles, one same-cause rework before changing approach; checkpoint incomplete work honestly.

Project/variant dim-gate/mainline; run DG-W5-20260923-01; task revision1/attempt1; base actual W4 merge 55ce00a; W5 contract revision1. Lead owns this slice and final integration; no separate implementation worker is claimed.
