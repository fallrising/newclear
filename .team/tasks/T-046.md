ROLE: WORKER

AGENT: http-sse-boundary-implementer

ID: T-046

Goal: Implement and prove the accepted `/api/v1` HTTP, SHA256 wire-codec, stored-command-result
lookup, resumable SSE and sole composition-root boundary over the accepted T-044/T-066 application,
T-043/T-063 persistence and T-045/T-069/T-075 deterministic Fake checkpoints.

Why: T-045 is accepted by the complementary T-073/T-076 independent reviews, so the next serial
T-040 child must expose the already-accepted application behavior through a strict authenticated
transport and close the durable snapshot-to-stream boundary before any T-047 vertical integration.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; `products/hai-taskboard/AGENTS.md`;
`docs/HANDOFF.md`; `docs/SDD.md` HAI-BOUNDARY-001/003, HAI-AUTH-002, HAI-API-001..005 and
HAI-SEC-001/003/005; ADR-001/003/005; `docs/sdd/persistence-and-recovery.md`,
`docs/sdd/executor-and-security.md` and accepted `docs/sdd/fake-vertical-slice-implementation.md`
sections 1, 2, 4, 6 and 7; `docs/reproducibility.md`; `docs/traceability.md` AC-P0A-009/010/015 and
AC-07/10/35..37/46; `docs/tasks/HTB-P0A-003-fake-vertical-slice.md`; complete OpenAPI; T-041/T-052,
T-043/T-063/T-064, T-044/T-065/T-066/T-067 and T-045/T-068/T-069/T-070/T-073/T-074/T-075/T-076
tasks/reports; backend `go.mod`/`go.sum`; every current application command/service/port file; public
SQLite Store construction/UnitOfWork methods; public Fake construction/declaration; and all current
backend tests. Treat every predecessor byte as read-only accepted evidence.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27. Apply every relevant rule, including method-aware ServeMux
patterns and `Request.PathValue`; call `explain` before skipping a returned rule that appears
applicable.

Scope (may touch): `products/hai-taskboard/backend/internal/transport/httpapi/**`;
`products/hai-taskboard/backend/cmd/taskboard/**`; `.team/reports/T-046.md`.

Out of scope (must not touch): Every existing backend file outside the two T-046 directories,
including `go.mod`, `go.sum`, application commands/services/ports, domain/reconciliation, SQLite and
Fake source/tests; `backend/integration/**` and T-047; web/OpenAPI/specification/traceability/
reproducibility/HANDOFF/PLAN/task files and every prior report; restore/backup implementation; CI;
Git refs; real providers, credentials, real agent execution, shell/network egress from Fake, PR,
merge, deployment, release or external state. Do not add a dependency or weaken an accepted
application/persistence/Fake gate.

Definition of done:

- [ ] Implement a transport package whose handlers depend only on injected application-facing
      interfaces and application/domain values; production `httpapi` imports neither concrete
      SQLite nor Fake. The sole `cmd/taskboard` composition root may import and wire concrete
      adapters, but no adapter imports another adapter and no transport path writes authoritative
      tables or reconstructs stored command results from mutable state.
- [ ] Use method-aware standard-library routing under canonical `/api/v1` paths. Reject duplicate or
      noncanonical path encodings, malformed IDs/cursors, wrong route-project payloads, unsupported
      V1 operations, wrong media type, oversized bodies and unknown/duplicate JSON members with a
      bounded OpenAPI-shaped error. Decode only the five accepted application operations
      `CreateProject`, `CreateWorkItem`, `MarkReady`, `DispatchRun` and `CompleteWorkItem`; do not
      fabricate implementation for later command families.
- [ ] Resolve the authenticated principal only from the local session cookie/configured session
      authority. Reject unauthenticated requests before reading a sensitive body or resolving a
      project/cross-project identifier; require configured same-origin validation on every state
      change; never accept actor/principal authority in a payload. Map application canonical result
      bytes byte-for-byte and preserve stored domain failure payloads without relabelling them.
- [ ] Implement authorized project-scoped `GET /api/v1/projects/{project_id}/commands/{command_id}`
      lookup that returns the original immutable stored success/failure bytes and never executes a
      command. Missing, corrupt, wrong-project or unauthorized material must fail closed without
      disclosing whether another project or actor owns it.
- [ ] Implement the production SHA256 codec so only `sha256:` plus exactly 64 lowercase hexadecimal
      characters decodes to a bare `domain.Digest`, and encoding adds exactly one prefix. Reject
      missing, doubled, uppercase-prefix, uppercase-suffix, short and non-hex spellings and preserve
      an identical round trip through domain/storage representation.
- [ ] Define an injected projection snapshot/replay authority and an in-process post-commit hub that
      implements `port.ProjectionSink`. A snapshot and its `(stream_epoch,event_sequence)` high-water
      come from one consistent source read. SSE parses query `cursor` and `Last-Event-ID` as
      `epoch:sequence`, requires equality when both exist, begins after current high-water when
      absent, replays only durable same-project events with sequence greater than the cursor, then
      serializes subscriber attachment and live publication without holding a SQLite write
      transaction or writing a client socket under the hub mutex. Duplicate replay/live events are
      discarded and delivered IDs remain strictly monotonic.
- [ ] On wrong epoch or a sequence below `minimum_retained_sequence-1`, emit exactly one
      `projection_reset_required` payload containing current epoch, minimum sequence and canonical
      snapshot URL, then close. Each connection permits at most 128 queued events and 1,048,576
      queued payload bytes; enqueue never blocks an application command. Overflow attempts one
      `slow_consumer` reset and closes, while session revocation similarly closes with
      `authorization_revoked`. Do not claim restore rotation itself; only test old/wrong-epoch reset
      behavior with injected source state.
- [ ] Add and pass exact tests
      `TestHTTP_AuthorizationAndCanonicalRouteBeforeSensitiveLookup`,
      `TestHTTPDigestCodec_WireRoundTripAndRejectsInvalidSpellings`,
      `TestSSE_SnapshotGapReplayAndRetentionReset` and
      `TestSSE_SlowConsumerCannotBlockCommand`, plus adversarial strict-JSON/path/cursor,
      byte-exact command replay, revoked-session, per-project isolation, replay/live deduplication
      and import/composition tests. Tests use injected deterministic clocks/sources and bounded
      synchronization, not wall-clock sleeps or scheduling-dependent expected output.
- [ ] Keep Fake-to-application worker persistence wiring, T-047 end-to-end completion, backup/
      restore, real provider/credentials, browser, deployment and broader G1/G2 evidence explicit
      NotRun or Deferred. Write a validator-compatible T-046 worker report with exact commands,
      exits, image digest, test/import/scope inventories and no failed or skipped required entry;
      the report must state that it is worker evidence and cannot accept itself.
- [ ] Pass the complete digest-pinned network-disabled Go 1.27.1 gate with candidate and existing
      module cache mounted read-only: `go version`, `go mod verify`, empty `gofmt -l`, `go vet ./...`,
      the four exact named tests, all transport/composition tests, repeated SSE/transport tests,
      full backend and full race. Also pass task/report validators, production import/API inventory,
      predecessor-hash and exact-scope audit, `git diff --check`, and retain every setup failure or
      NotRun result visibly rather than converting it into a pass.

Verify with: complete Modern Go list; current `teamctl.py validate-task`/`validate-report`;
digest-pinned `golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
with `--network none`, read-only candidate/module mounts, `GOPROXY=off` and `GOSUMDB=off`;
uncached named/adversarial/repeated/full/race tests; source/import/API/predecessor/scope inventory;
`git diff --check`.

Budget: One bounded implementation turn; no delegation, commit, Git ref, dependency or predecessor
edit, T-047, restore/backup implementation, real executor/provider/credential, network access,
deployment, release or external mutation.
