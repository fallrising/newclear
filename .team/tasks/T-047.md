ROLE: WORKER

AGENT: fake-vertical-integration-evidence-implementer

ID: T-047

Goal: Implement deterministic, exported-seam integration evidence for the accepted Fake-only
create → dispatch → candidate → review/evidence → approval → Done path and its response-loss,
stale-epoch and cancel-unknown boundaries, without changing any accepted predecessor byte.

Why: T-046/T-078/T-080 is accepted by the independent T-081 review and the orchestrator's pinned
evidence gate, so the next serial T-040 child must now prove how the accepted application, real
SQLite Store, deterministic Fake and HTTP/SSE boundaries compose. Integration evidence must expose,
not silently repair, any missing predecessor seam.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; `products/hai-taskboard/AGENTS.md`;
`docs/HANDOFF.md`; `docs/SDD.md`; all six `docs/adr/ADR-*.md`; all `docs/sdd/*.md`, especially
`docs/sdd/fake-vertical-slice-implementation.md`; `docs/reproducibility.md`;
`docs/traceability.md`; `docs/spec-graph.yaml`; `docs/tasks/HTB-P0A-003-fake-vertical-slice.md`;
complete `api/openapi.yaml`; T-041/T-045/T-046/T-048/T-049/T-051/T-052/T-053/T-054/T-056/T-057/
T-058/T-061/T-062/T-063/T-064/T-065/T-066/T-067/T-068/T-069/T-070/T-071/T-072/T-073/T-074/
T-075/T-076/T-077/T-078/T-079/T-080/T-081 task envelopes and every existing corresponding report,
including historical FAIL/PARTIAL evidence and the absent T-071/T-072 reports; backend `go.mod`,
`go.sum` and every current backend Go source/test/migration file. Treat all predecessor inputs as
read-only. Confirm from PLAN/HANDOFF/T-081 that the HTTP/SSE checkpoint is accepted before editing.

Mandatory Go preflight: before editing any Go file, run
`sh /home/ckc/.codex/plugins/cache/goland-codex-marketplace/modern-go-guidelines/1.1.1/skills/use-modern-go/scripts/run-tool.sh list --go-version 1.27`
without piping, filtering or truncation, read the complete output, apply every relevant rule, and
use `explain <guideline-id>` before skipping any returned rule that appears applicable.

Scope (may touch): `products/hai-taskboard/backend/integration/**`; `.team/reports/T-047.md`.

Out of scope (must not touch): Every existing file outside `backend/integration/**`, including
application command/service/port, persistence/domain/SQLite, deterministic Fake, HTTP/SSE,
`cmd/taskboard`, module/dependency, migration, web, OpenAPI, specification, traceability,
reproducibility, HANDOFF, PLAN, task and prior-report bytes; direct SQL/table writes from integration
tests; test-only copies or reimplementations of accepted production decisions; automatic persistent
worker/restore behavior not exposed by accepted production seams; Git refs, commit, push, PR, merge,
real providers/credentials/agent execution, network, deployment, release or external mutation. If a
named oracle requires an out-of-scope predecessor change, retain the failing/NotRun evidence and
report PARTIAL or BLOCKED; do not repair it here.

Definition of done:

- [ ] Add exact test `TestVerticalFake_CompletionAndResponseLoss` under an external
      `integration_test` package. Use a controlled temporary root, injected deterministic clock/IDs,
      the real SQLite Store/UnitOfWork, accepted application Service, deterministic `fake/v1`, and
      accepted HTTP/result boundary where that operation exists. Exercise CreateProject,
      CreateWorkItem, MarkReady and DispatchRun; recover a deliberately lost dispatch response by
      the original command/idempotency identity and prove byte-exact replay, exactly one Run,
      outbox/audit/projection group and no duplicate Fake execution.
- [ ] Continue the same deterministic subject through Fake dispatch/start/success and bounded staged
      artifact bytes, then use only existing exported fixture/application/persistence seams to bind
      the exact Candidate, required AC revisions, graph/policy/recipe/environment, independent
      Review and passing Evidence, exact human Approval and QA subject. Complete through the real
      application/SQLite transaction and assert Done/version, one immutable CompletionRecord, exact
      joins, exactly one Approval consumption, byte-exact result replay and no duplicate mutation.
      Do not directly write SQLite tables or manufacture a parallel Done predicate in the test.
- [ ] Add exact test `TestRunLease_RejectsStaleEpochPublication`. Drive a persisted dispatched Run
      and deterministic Fake worker/fence through the accepted exported seams; advance ownership to
      a newer epoch, attempt old-epoch terminal/candidate publication, and prove the stale publisher
      is rejected, visible/auditable as supported by the accepted boundary, and cannot mutate Run,
      Candidate, WorkItem, completion material, audit/projection authority or trigger redispatch.
- [ ] Add exact test `TestCancel_UnknownStopIsNotCanceled`. Persist cancellation intent for a
      dispatched Fake Run, exercise lost/unconfirmed cancel or lookup-unknown through deterministic
      exported seams, and prove the durable/result projection remains CancelRequested and/or
      OutcomeUnknown/NeedsReconcile rather than Canceled, Done, confirmed stopped or automatically
      retried. A current fenced cancel acknowledgement may be a positive control only.
- [ ] Make every expected value deterministic: no wall-clock sleep, ambient randomness, network,
      shell, scheduling-dependent ordering or unbounded wait. Use `t.Context`, fake time, fixed valid
      identities/digests and bounded synchronization. Test artifacts remain beneath `t.TempDir()`;
      Fake writes only through its bounded staging writer.
- [ ] Keep the evidence claim exact. If accepted exported seams do not provide candidate/review/
      evidence/approval publication, durable lease/audit publication, cancellation intent or other
      required authority, create the strongest compiling failing/NotRun evidence possible inside
      `backend/integration/**`, name the missing seam precisely, and return PARTIAL/BLOCKED. Do not
      substitute direct SQL, reflection, copied production logic or a mock-only happy path.
- [ ] Pass, with no required failure or skip for DONE, the complete digest-pinned network-disabled
      Go 1.27.1 gate: `go version`, `go mod verify`, empty `gofmt -l`, `go vet ./...`, the three exact
      named integration tests, ten integration repetitions, full backend, full race, and test/import
      inventory. Also validate the task/report, run `git diff --check`, bind predecessor and new-file
      hashes, and prove exact scope.
- [ ] Report Fake-to-application automatic persistent worker execution, restore/backup, browser,
      real provider/credentials, deployment and all broader acceptance families not actually
      exercised as explicit out-of-envelope NotRun/Deferred boundaries, not as passing T-047
      evidence. Produce a validator-compatible report; worker evidence cannot accept itself.

Verify with: complete unfiltered Modern Go 1.27 list; current `teamctl.py validate-task` and
`validate-report`; exact digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
with `--network none`, read-only module cache, `GOPROXY=off` and `GOSUMDB=off`; uncached exact named,
ten-repeat, full and race tests; source/test/import/predecessor-hash/exact-scope inventory;
`git diff --check`.

Budget: One bounded implementation/evidence turn; no delegation, predecessor/module/spec/status
document edit, direct table write, commit, push, Git ref, real executor/provider/credential, network,
deployment, release or external mutation.
