ROLE: WORKER

AGENT: fake-vertical-slice-design-reworker

ID: T-048

Goal: Repair only the four blocking T-042 findings in the T-041 implementation contract without
widening T-040 behavior or erasing the failed review.

Why: Fake/SSE and the overall decomposition passed; module ownership, guarded rehydration, immutable
approval consumption and digest representation need one executable resolution before Go work.

Inputs to read first: codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-042 tasks and reports; the candidate implementation
contract; product AGENTS; ADR-001/002/005/006; domain/persistence mini-SDDs; backend `go.mod`, domain
identity/completion/workitem code and external Done-boundary test; OpenAPI SHA256 schema.

Scope (may touch): `products/hai-taskboard/docs/sdd/fake-vertical-slice-implementation.md`;
`.team/reports/T-048.md`.

Out of scope (must not touch): Other specs/traceability/graph; production Go/TS/OpenAPI;
dependencies; `.team/PLAN.md`; tasks/existing reports/contracts; Git refs; credentials; providers,
shell/network execution or external state.

Definition of done:

- [ ] Give T-043 exact authority for `backend/go.mod`, `backend/go.sum`, the persistence port file
      and its SQLite/rehydration implementation paths so it can resolve `modernc.org/sqlite@v1.58.0`
      and compile independently; later children cannot edit those owned files.
- [ ] Define a compile-time non-bypassable rehydration capability using a nested Go `internal`
      package boundary: only the SQLite adapter located beneath the authorized domain subtree may
      obtain the opaque authority; application/transport/external domain callers cannot name or
      construct it. The domain factory accepts no raw phase and returns matching Done item/record.
- [ ] Replace mutable `approvals.consumed_at_ns` with an immutable, unique
      `approval_consumptions` record committed atomically with the authorized command.
- [ ] Store domain digests as bare lowercase 64-hex matching `domain.Digest`; add/remove the
      OpenAPI-required `sha256:` prefix only at HTTP wire codec boundaries, reject double/missing/
      uppercase prefixes, and name a round-trip oracle.
- [ ] Preserve T-041's already-passing trigger order, Fake/SSE and NotRun language. Write a
      validator-compatible report with exact diff/scope evidence.

Verify with: contract consistency inventory; proposed Go import-visibility/package-path analysis;
wire/storage/domain codec examples; report validator; `git diff --check`; exact scope audit.

Budget: One narrow design rework; no delegation, code, commit, Git ref or external mutation.
