ROLE: REVIEWER

AGENT: fake-vertical-slice-design-reverifier

ID: T-049

Goal: Independently verify the narrow T-048 repairs and decide whether T-041/T-048 now safely
authorizes the T-043 SQLite foundation child.

Why: T-042's four failures must be closed with executable package/storage semantics while preserving
the already-passing trigger, Fake/SSE and explicit NotRun boundaries.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-042/T-048 tasks/reports; candidate implementation
contract; product AGENTS; relevant accepted ADR/mini-SDDs; backend `go.mod` and domain authority
code/tests; OpenAPI SHA256 schema.

Scope (may touch): `.team/reports/T-049.md` only. Read-only probes and removable `/tmp` fixtures
are allowed.

Out of scope (must not touch): Candidate documents/code; task/plan files; other reports;
dependencies; Git refs; credentials; providers/external state.

Definition of done:

- [ ] Confirm T-043 can add/pin SQLite and compile its persistence port/adapter/rehydration scope
      without editing a later child, and later children cannot rewrite those files.
- [ ] Confirm the nested `internal` capability paths are legal and exclude application/transport/
      external domain callers from the Done rehydration authority while allowing domain+SQLite use.
- [ ] Confirm approval consumption is append-only/unique and the command order remains atomic.
- [ ] Confirm the OpenAPI-prefixed versus bare domain/storage digest codec is unambiguous and has
      positive/negative round-trip oracles.
- [ ] Reconfirm trigger order, Fake/SSE design and implementation evidence remain correctly NotRun.
- [ ] Validate report syntax, diff and report-only scope; return unconditional PASS or FAIL.

Verify with: package-visibility reasoning or disposable compile fixture, contract/source inspection,
exact validator, `git diff --check`, temp cleanup and scope audit.

Budget: One narrow independent re-review; no fixes, delegation, commits, Git refs or external mutation.
