ROLE: WORKER

AGENT: sqlite-foundation-repairer

ID: T-054

Goal: Repair only the independently proven T-043 blockers so the SQLite foundation preserves
history, supplies T-044's application-owned atomic transaction seam, and fails closed under the
required storage attacks.

Why: T-053 passed the pinned happy-path gates but proved that the candidate loses re-completion
history, cannot support the next command slice through its public port, classifies SQLite busy by
message text, and lacks required project-scope/determinism/attack evidence.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-043/T-048/T-051/T-052/T-053 tasks and
reports; product AGENTS; SDD clauses HAI-DOMAIN/STATE/DONE/EXEC; ADR-001/002/003/005;
`domain-and-gates.md`, `persistence-and-recovery.md`, accepted
`fake-vertical-slice-implementation.md`; reproducibility/traceability; every existing backend Go,
module, migration and T-043 test file.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27. Apply all relevant guidance; use `explain` for any returned
rule that appears relevant but would be skipped.

Scope (may touch): `products/hai-taskboard/backend/go.mod`;
`products/hai-taskboard/backend/go.sum`;
`products/hai-taskboard/backend/internal/application/port/persistence.go`;
`products/hai-taskboard/backend/internal/domain/rehydration.go`;
`products/hai-taskboard/backend/internal/domain/rehydration_test.go`;
`products/hai-taskboard/backend/internal/domain/internal/rehydrationcap/**`;
`products/hai-taskboard/backend/internal/domain/sqlite/**`; `.team/reports/T-054.md`.

Out of scope (must not touch): Existing non-rehydration domain/reconcile source/tests; application
command/service implementation; Fake/HTTP/SSE/composition/web/OpenAPI; specs/traceability/plan/tasks/
other reports; CI/deploy; Git refs; credentials; shell/network/provider execution or external state.

Definition of done:

- [ ] Replace the single-WorkItem completion uniqueness with exact `(project_id,work_item_id,
      result_work_item_version)` identity so immutable CompletionRecords from prior Done states remain
      queryable after reopen/re-completion, while Done load requires exactly one record for the
      current project/item/version. Prove two historical completions survive and the current one loads.
- [ ] Define an application-owned `UnitOfWork`/transaction port, using only domain values or port DTOs,
      through which T-044 can atomically read/mutate aggregate state and write idempotency, canonical
      command result, Run, completion inputs/joins/consumption, audit group/entries, outbox intent and
      projection event. Implement it in SQLite with one bounded `BEGIN IMMEDIATE`; T-044 must need no
      concrete SQLite import and no edit to this file or `domain/sqlite`. Add a public-port-only
      transaction/rollback oracle that detects any partial category write.
- [ ] Replace message matching with typed `modernc.org/sqlite.Error` code inspection using Go 1.27
      error APIs; classify base/extended `SQLITE_BUSY` deterministically, perform no unbounded retry,
      and prove a real bounded lock conflict returns `ErrBusy` while unrelated text does not.
- [ ] Add the complete guarded-rehydration matrix: missing and duplicate current records, wrong
      project/item/version/subject, corrupt evidence/review/approval/artifact joins, and no partial
      aggregate. Preserve the nested internal capability and add an architecture inventory oracle;
      the independent reviewer will repeat an external compile-denial probe.
- [ ] Close composite project scoping for every project-owned association used by this V1 schema.
      Composite FKs must prevent cross-project WorkItem/AC/Run/completion/command/audit/outbox/event
      bindings even when individual IDs or digests otherwise exist; add direct negative SQL probes.
- [ ] Make controlled-path authority explicit with an absolute configured root plus a normalized DB
      path contained under it; reject relative paths, escapes and DSN/PRAGMA injection spellings.
      Remove ambient `time.Now` from mutations by accepting an injected clock or explicit UTC
      nanosecond values, and assert deterministic timestamps.
- [ ] Make blocker mutations fully optimistic: both the aggregate row and exact unresolved blocker
      row must affect one row in the same transaction or the whole mutation rolls back with a stable
      error/version result. Prove missing/already-resolved and stale-version cases.
- [ ] Retain exact `modernc.org/sqlite@v1.58.0`, linked SQLite >=3.51.3, per-connection asserted PRAGMAs,
      bare digest behavior, existing named tests and no provider/network/shell/runtime behavior.
- [ ] Pass pinned format/vet/unit/race, all existing and added SQLite/rehydration tests, module/license/
      engine/import inventories, validator-compatible report, exact scope audit and `git diff --check`.

Verify with: digest-pinned Go 1.27.1 container with network disabled after module verification;
existing module cache; exact named/adversarial tests; disposable lock/import probes; current report
validator; temp cleanup; exact scope audit.

Budget: One bounded repair turn; no delegation, commits, Git refs, application command behavior,
real executor, credential, HTTP call, deployment or external mutation.
