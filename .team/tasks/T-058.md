ROLE: WORKER

AGENT: approval-consumption-repairer

ID: T-058

Goal: Close the sole T-057 blocker by making every Approval joined to a CompletionRecord require
exactly one immutable, project/subject-correct consumption before Done can load.

Why: The final candidate otherwise passes all independent gates, but currently treats absence of an
approval consumption as valid while only rejecting malformed consumptions that happen to exist.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-053/T-054/T-056/T-057 tasks/reports; accepted
fake vertical-slice contract; SDD HAI-DONE-001/004; ADR-002/005; current persistence port, migration,
store and all SQLite/rehydration tests.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 and apply all relevant guidance.

Scope (may touch): `products/hai-taskboard/backend/internal/application/port/persistence.go`;
`products/hai-taskboard/backend/internal/domain/sqlite/migrations/0001_v1.sql`;
`products/hai-taskboard/backend/internal/domain/sqlite/store.go`;
`products/hai-taskboard/backend/internal/domain/sqlite/store_test.go`; `.team/reports/T-058.md`.

Out of scope (must not touch): Modules, other domain/application source, specs/traceability/plan/
tasks/prior reports, Fake/HTTP/SSE/web/CI/deploy, Git refs, credentials/providers/external state.

Definition of done:

- [ ] Model `port.Completion` with zero or more approval consumptions so there is a one-to-one set
      equality with `ApprovalIDs`; reject duplicates, missing/extra IDs, wrong project or wrong subject
      before any write. Do not silently support many approvals with a single consumption pointer.
- [ ] Keep each consumption INSERT-only and project-bound to its Approval and command result. The
      completion write, all joins, all consumptions and Done aggregate update remain usable inside one
      `port.UnitOfWork` transaction; any failure rolls everything back.
- [ ] During Done rehydration, count consumptions by the CompletionRecord's joined Approval set and
      require exactly `approval_count`, one per joined Approval. Missing, duplicate, unjoined, wrong-
      project or wrong-subject consumption must return typed `domain.ErrStorageCorruption` and a zero
      WorkItem. Approval-free CompletionRecords require zero relevant consumptions.
- [ ] Add `missing-approval-consumption` to the corrupt Done matrix and repair every other fixture so
      it includes a valid command result/consumption when its intended attack is unrelated. Add a
      successful multi-Approval write/load or explicitly constrain V1 to exactly zero-or-one Approval
      in both port validation and schema/tests.
- [ ] Prove failure injection at the consumption seam rolls back CompletionRecord, joins, command
      result and aggregate. Preserve all prior 14+ corrupt cases, project/FK/immutability attacks,
      public-port replay, real busy, path/import/digest/blocker/history evidence.
- [ ] Pass the exact pinned network-disabled Go 1.27.1 module/format/vet/named/full/race/engine/
      license/import gates, report validator, exact scope audit and `git diff --check`.

Verify with: existing digest-pinned Go 1.27.1 image and read-only module cache; uncached SQLite named
tests; full/race; disposable T-057 missing-consumption probe semantics; current report validator.

Budget: One very narrow repair; no delegation, commit, Git ref, new dependency, application command,
Fake/transport behavior, real executor, credential, deployment or external mutation.
