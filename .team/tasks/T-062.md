ROLE: REVIEWER

AGENT: sqlite-forward-contract-final-verifier

ID: T-062

Goal: Independently decide whether the final T-043/T-054/T-056/T-058/T-061 SQLite persistence
candidate closes every T-053/T-057 finding and is a safe predecessor for T-044.

Why: T-058 repaired approval-consumption cardinality and T-061 repaired canonical command-result
replay, verifier-role persistence and immutable identities. Both are worker results and cannot accept
themselves; one fresh review must attack the combined final bytes before application commands begin.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-043/T-048/T-051/T-052/T-053/T-054/
T-056/T-057/T-058/T-061 tasks and reports; product AGENTS; SDD; ADR-001/002/003/005; domain,
persistence and Fake vertical-slice mini-SDDs; reproducibility/traceability; backend go.mod/go.sum
and every backend source/test/migration file.

Scope (may touch): `.team/reports/T-062.md` only. Read-only candidate mounts plus removable `/tmp`
compile, lock and corruption probes are permitted; remove them before scope audit.

Out of scope (must not touch): Candidate code/dependencies/specs/tasks/plan/prior reports; Git refs;
credentials/providers/external state. Do not fix findings and do not accept your own report.

Definition of done:

- [ ] Re-run pinned Go 1.27.1 module verify, format, vet, the complete named SQLite/adversarial suite,
      full unit and race against the final candidate; verify modernc v1.58.0/license and linked
      SQLite >=3.51.3.
- [ ] Compile or implement a disposable application-only probe using `port.UnitOfWork` and domain
      values, with no SQLite import or edit, proving T-044 can perform project-scoped idempotency
      replay and one atomic aggregate/result/idempotency/Run/completion/audit/outbox/event write.
- [ ] Attack canonical success and failure result replay after response loss, caller aliasing,
      digest/payload mismatch on write and stored payload corruption on read; require byte-identical
      replay or typed zero-result storage corruption without re-execution.
- [ ] Attack all nineteen corrupt Done cases, including balanced duplicate/missing Approval
      consumption cardinality; require typed storage corruption and zero aggregate. Reconstruct
      persisted Evidence through the public domain API and prove verifier-role independence remains
      enforced after reload.
- [ ] Inspect every V1 table/key/FK/CHECK/trigger and run `PRAGMA foreign_key_check`. Attack all
      append-only and immutable identity fields for command results, projection events, Runs,
      idempotency and outbox while proving their declared state/tombstone/claim transitions remain
      legal.
- [ ] Independently rerun the real-lock timeout and synthetic-message busy tests, project scoping,
      controlled path/symlink/DSN, injected timestamps, optimistic blocker race/rollback, sequence
      allocation and external rehydration-capability import denial checks.
- [ ] Confirm the source/import inventory adds no application command, Fake, HTTP/SSE, provider,
      network-client or shell behavior and keeps every later acceptance family explicitly NotRun.
- [ ] Validate a one-line-compatible unconditional PASS or FAIL report, exact report-only scope,
      temporary cleanup and `git diff --check`.

Verify with: digest-pinned network-disabled Go 1.27.1 container and existing read-only module cache;
SQL/source inspection; disposable external application/internal-import probes; exact report validator.

Budget: One independent combined code gate; no fixes, delegation, commits, Git refs, real executor,
credentials, HTTP calls, deployment or external mutation.
