ROLE: WORKER

AGENT: command-result-ordering-repairer

ID: T-063

Goal: Repair only T-062's proven transaction-order blocker so application code can allocate the
real audit sequence and projection cursor, then persist one immutable canonical success response in
the same transaction without predicting values, replacing a placeholder or importing SQLite.

Why: OpenAPI CommandSuccess includes allocator-owned audit/cursor values, while the current immediate
command-result foreign keys require result bytes before those allocators run. The final transaction
must retain referential integrity at commit while permitting the accepted completion/audit/event/
outbox/result ordering inside the transaction.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-043/T-048/T-051/T-052/T-053/T-054/
T-056/T-057/T-058/T-061/T-062 tasks and reports; product AGENTS; SDD HAI-AUTH-001 and HAI-DONE-001;
ADR-001/002/005; `domain-and-gates.md`, `persistence-and-recovery.md` and the accepted
`fake-vertical-slice-implementation.md` transaction ordering; OpenAPI CommandSuccess/CommandFailure;
reproducibility/traceability; current persistence port, V1 migration, SQLite store and all tests.

Mandatory Go preflight: before editing the Go test file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 and apply all relevant guidance.

Scope (may touch): `products/hai-taskboard/backend/internal/domain/sqlite/migrations/0001_v1.sql`;
`products/hai-taskboard/backend/internal/domain/sqlite/store_test.go`; `.team/reports/T-063.md`.

Out of scope (must not touch): Persistence port/store production Go, modules, other domain/
application/reconcile source/tests, specs/OpenAPI/traceability/plan/tasks/prior reports,
Fake/HTTP/SSE/web/CI/deploy, Git refs, credentials/providers/external state.

Definition of done:

- [ ] Preserve every command-result association and project boundary, but make only the transaction-
      local command-result foreign keys needed before final response construction commit-deferred.
      A transaction missing the referenced final command result MUST fail at commit and roll back;
      cross-project or permanently dangling references MUST remain impossible.
- [ ] Keep `command_results` nonempty, digest-verified and INSERT-only. Do not add a placeholder,
      update path, caller-owned audit/event sequence, concrete SQLite exposure or weaker FK.
- [ ] Add `TestUnitOfWork_CanonicalSuccessUsesAllocatedMetadataWithoutPrediction` (or an exactly
      equivalent named oracle) that operates through `port.UnitOfWork`/`port.Transaction` after
      fixture setup, first advances audit/event state beyond `1`, then writes completion material,
      immutable Approval consumptions, Done aggregate, audit, projection event and outbox before
      constructing canonical CommandSuccess bytes from the returned real sequence/cursor; only then
      stores the result/idempotency and proves byte-exact replay.
- [ ] In the same oracle, omit the final result in a separate transaction and require commit-time FK
      failure plus rollback of aggregate/completion/consumption/audit/outbox/event and
      `instance_state.next_event_sequence`; prove `PRAGMA foreign_key_check` remains clean.
- [ ] Preserve byte-exact success/failure replay, caller alias safety, stored corruption denial,
      all nineteen corrupt-Done/Approval cardinality cases, verifier-role reload, project/path/busy/
      blocker/history/trigger positive controls and all prior failure-injection gates.
- [ ] Pass pinned network-disabled Go 1.27.1 module/format/vet/new-named/all-SQLite/full/race,
      modernc v1.58.0/engine/license/import inventory, report validator, exact scope audit and
      `git diff --check`.

Verify with: existing digest-pinned Go 1.27.1 image and read-only module cache; uncached new named
oracle plus the full SQLite package; full/race/inventory; current report validator.

Budget: One minimal ordering repair; no delegation, commit, Git ref, new dependency, application
command/Fake/transport behavior, credential, deployment or external mutation.
