ROLE: REVIEWER

AGENT: application-command-final-verifier

ID: T-065

Goal: Independently decide whether T-044 implements the bounded application-command and atomic
result slice over the accepted persistence port without broadening into Fake, HTTP/SSE or integration.

Why: The T-044 worker reports a complete application-only service and test double, but worker
evidence cannot accept its own source. A fresh reviewer must attack the final bytes, including the
recorded-failure and artifact-availability repairs added during owner inspection.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-044/T-048/T-051/T-052/T-062/T-063/
T-064 tasks and reports; product AGENTS; SDD; ADR-001/002/003/005; domain, persistence, executor and
Fake vertical-slice mini-SDDs; OpenAPI command/result/error schemas; reproducibility/traceability;
backend go.mod/go.sum, all application/domain/SQLite source/tests and the T-044 report.

Scope (may touch): `.team/reports/T-065.md` only. Read-only candidate mounts plus removable isolated
copies and `/tmp` probes are permitted; remove every probe before the scope audit.

Out of scope (must not touch): Candidate code/dependencies/specs/OpenAPI/tasks/plan/prior reports;
Git refs; credentials/providers/external state. Do not fix findings and do not accept your own report.

Definition of done:

- [ ] Re-run digest-pinned, network-disabled Go 1.27.1 module verify, format, vet, the three required
      named T-044 tests, every application test, the accepted SQLite suite, full backend and full
      race; verify exact modernc v1.58.0/license and no unexpected module drift.
- [ ] Inspect every T-044 source/test byte. Confirm package direction, no SQLite/Fake/HTTP/shell/
      network imports, no actor override in command DTOs, authenticated principal as sole audit/
      completion/consumption actor, trusted MarkReady policy and no executor call in a transaction.
- [ ] Independently decode/compare canonical V1 request/success/failure bytes against OpenAPI:
      deterministic project/operation scope, normalized time and sorted revisions; recorded status,
      typed result, real audit sequence/cursor, correlation bound; bounded stable failure fields;
      bare application digests and strict stored-result corruption rejection.
- [ ] Attack idempotency with same request, different bytes, expiry/tombstone, mismatched persisted
      scope/identity/digest/payload, alias mutation, response loss and bounded concurrent same-key
      requests. Prove exactly one Run/audit/event/result/idempotency/outbox and no executor call.
- [ ] Attack every transaction seam and allocator ordering. Prove state/result/audit/event/outbox/
      completion/consumption and cursor all commit or all roll back; success bytes use returned real
      sequence/cursor, never prediction or placeholder. Prove recordable guard failures atomically
      persist and byte-replay with zero authoritative aggregate/audit/event/outbox mutation, while
      invalid/auth/storage/infrastructure failures fail closed.
- [ ] Attack MarkReady/Dispatch/Complete lifecycle inputs: missing/stale/nonpassing evidence, actual
      missing/Missing/Quarantined artifacts, non-independent verifier, expired approval, active or
      unknown Run, blocker, version/current-subject/policy/recipe/environment/graph mismatch and
      storage corruption. Successful completion must persist exact joins and one consumption per
      selected Approval before Done.
- [ ] In an isolated disposable module copy, exercise at least one real `sqlite.Store` command-service
      success plus byte-exact replay through `port.UnitOfWork`, proving the application service and
      accepted deferred-FK ordering compose without modifying candidate source. Combine this with
      T-064's accepted direct Store completion/order evidence; do not claim T-047 integration.
- [ ] Verify exact worker scope, unchanged predecessor hashes, no report self-acceptance, explicit
      later-gate NotRun inventory, report validation/temp cleanup and `git diff --check`.
- [ ] Produce a one-line-compatible unconditional PASS or FAIL. Any required failed/skipped/NotRun
      review gate is an unconditional FAIL and remains visible.

Verify with: exact digest-pinned Go container/cache from reproducibility; independent disposable
application/real-Store/adversarial probes; source/OpenAPI inspection; report validator; scope audit.

Budget: One fresh independent application review; no fixes, delegation, commits, Git refs, Fake or
HTTP/SSE implementation, real executor, credentials, deployment or external mutation.
