ROLE: REVIEWER

AGENT: application-command-repair-final-verifier

ID: T-067

Goal: Independently decide whether T-066 closes every T-065 failure while preserving all passing
T-044 and accepted SQLite guarantees, so the bounded application-command slice can be accepted.

Why: T-066 reports a three-file repair and passing real-Store completion/replay, but the repairer
cannot accept its own bytes. A fresh report-only reviewer must reproduce the original failures and
attack the combined final candidate.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-044/T-052/T-062/T-063/T-064/T-065/
T-066 tasks and reports; product AGENTS; SDD; ADR-001/002/003/005; domain, persistence, executor and
Fake vertical-slice mini-SDDs; OpenAPI command/result/error schemas; reproducibility/traceability;
backend go.mod/go.sum, every application source/test and accepted persistence/domain/SQLite bytes.

Scope (may touch): `.team/reports/T-067.md` only. Read-only candidate mounts plus removable isolated
copies and `/tmp` probes are permitted; remove every probe/cache before scope audit.

Out of scope (must not touch): Candidate code/dependencies/specs/OpenAPI/tasks/plan/prior reports;
Git refs; credentials/providers/external state. Do not fix findings and do not accept your own report.

Definition of done:

- [ ] Re-run digest-pinned, network-disabled Go 1.27.1 module verify, empty format, vet, all six
      T-044/T-066 named tests, every application and accepted SQLite suite, full backend and full
      race; verify modernc v1.58.0/license/import inventory and unchanged predecessor hashes.
- [ ] In an independent disposable copy, seed exact QA/Run/Candidate/AC/graph/Evidence/Review/
      Approval/artifact material and exercise actual Service.CompleteWorkItem over sqlite.Store.
      Prove Done/version 6, exact immutable completion joins/one Approval consumption, real allocator
      success bytes, result/idempotency, byte-exact replay and no duplicate mutation; inject a later
      failure or missing final result and prove total rollback.
- [ ] Re-run T-065's blocking call-tracked executor attack. Declaration must be called exactly once
      during construction, outside UnitOfWork; mutation of executor-owned bytes cannot change the
      service snapshot; Dispatch performs no executor method call.
- [ ] Independently attack canonical success/failure encoding and stored decoding with unknown
      top-level and every nested member, reordered keys, whitespace, noncanonical number, missing/
      duplicate fields, malformed/mismatched identity/operation/result/status/replay/cursor,
      out-of-enum ErrorBody code and unknown/duplicate/oversized rejection reasons. Valid bytes for
      all five operations and all fifteen OpenAPI codes must remain byte-exact and replayable.
- [ ] Re-run idempotency same/conflict/expiry/tombstone/concurrency/response-loss/alias/digest/scope
      corruption; authenticated actor; trusted MarkReady; Dispatch blocker/active-unknown; completion
      subject/policy/recipe/environment/graph/evidence/artifact/review/approval/verifier gates; every
      transaction seam and allocator-not-predicted ordering. No required failure may mutate state.
- [ ] Inspect exact T-066 three-file scope and all final source/test bytes, confirm no application
      SQLite/Fake/HTTP/shell/network import or port/dependency drift, retain T-045/T-046/T-047 and
      broader families NotRun, validate report/temp cleanup and run `git diff --check`.
- [ ] Produce a one-line-compatible unconditional PASS or FAIL. Any required failed/skipped/NotRun
      review gate is unconditional FAIL and remains visible; the report never self-accepts.

Verify with: exact digest-pinned Go container/cache; fresh real-Store completion/failure probes;
executor and strict-codec attacks; source/OpenAPI inspection; report validator; exact scope audit.

Budget: One fresh independent final application review; no fixes, delegation, commit, Git refs,
Fake/HTTP/SSE implementation, real executor, credentials, deployment or external mutation.
