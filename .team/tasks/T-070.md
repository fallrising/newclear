ROLE: REVIEWER

AGENT: deterministic-fake-repair-final-verifier

ID: T-070

Goal: Independently decide whether T-069 closes all four T-068 failures while preserving every
passing deterministic Fake, fencing, recovery, artifact and scope guarantee required by T-045.

Why: T-069 reports a narrow repair, but its author evidence cannot accept T-045. A fresh reviewer
must reproduce the original failures against the repaired final bytes and attack the new lifecycle
and validation boundaries.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045/T-068/T-069 tasks and reports; product
AGENTS; SDD HAI-BOUNDARY/EXEC/SEC; ADR-003; executor/security, persistence/recovery and accepted Fake
implementation mini-SDDs; reproducibility/traceability; HTB-P0A-003; backend module, unchanged
application executor port/declaration validation and every final Fake source/test byte.

Mandatory Go preflight: before any disposable Go attack, run and read the complete unfiltered
Modern Go list for Go 1.27 and apply every relevant rule.

Scope (may touch): `.team/reports/T-070.md` only. Read-only candidate mounts plus removable `/tmp`
copies/probes/caches are permitted and must be deleted before handoff.

Out of scope (must not touch): Candidate source/tests, modules, application ports/services,
domain/persistence/SQLite, specifications/OpenAPI/traceability/plan/tasks/prior reports, HTTP/SSE/
T-047, Git refs, real providers, credentials, PR, merge, deployment, release or external state. Do
not fix findings and do not accept your own report.

Definition of done:

- [ ] Bind exact final candidate/report hashes and independently rerun digest-pinned,
      network-disabled Go 1.27.1 module verify, empty format, vet, all exact T-045 names, the T-068
      regression, repeated Fake, full backend and full race gates.
- [ ] Reproduce each T-068 failure independently: invalid MIME spellings reject; unsupported lookup
      and cancel with every stale fence dimension return `capability_unsupported` with zero session
      mutation; invalid artifact name/bytes/digest/media observations visibly reject without accepted
      mutation; and terminal success/failure reject from Queued and Starting but accept from Running.
- [ ] Attack the explicit lifecycle table across dispatch, start acknowledgement/loss, heartbeat,
      checkpoint, lookup running/unknown, timeout/unknown, success/failure and cancellation
      acknowledgement. Out-of-order/duplicate/regressed/late/expired cases must remain visible and
      state-preserving; valid lost-start lookup and current cancel flows must retain uncertainty until
      confirmation and never redispatch automatically.
- [ ] Reconfirm capability normalization/immutability, scenario/observation/artifact alias safety,
      finite ordered explicit-tick determinism under repetition/concurrency, staging-writer digest
      enforcement, supported stale-fence rejection and no wall clock/random/scheduling-dependent
      output.
- [ ] Reconfirm production Fake imports/API expose no host filesystem, shell, Git, network, HTTP,
      database/SQLite, transport, credential or provider/client capability; artifact output uses only
      validated relative names/copied bytes through the supplied staging writer. Keep the claim
      limited to Fake-boundary denial rather than real OS isolation.
- [ ] Confirm exact T-069 repair scope, unchanged predecessor and T-068 historical FAIL bytes,
      explicit later-gate NotRun inventory, report validation, temp cleanup, `git diff --check` and
      report-only scope audit.
- [ ] Return unconditional PASS or FAIL. Any required failure or skipped reviewer check makes the
      review FAIL and remains visible; never repair the candidate.

Verify with: exact pinned Go image/read-only cache; disposable T-068 reproduction plus lifecycle,
concurrency and artifact probes; source/API/import inspection; current report validator; cleanup and
report-only scope audit.

Budget: One fresh independent final review; no delegation, candidate fix, commit, Git ref, HTTP/SSE/
T-047, real executor, credential, deployment or external mutation.
