ROLE: REVIEWER

AGENT: deterministic-fake-complete-evidence-verifier

ID: T-071

Goal: Independently complete the reviewer evidence T-070 skipped and decide whether the exact
T-045/T-069 deterministic Fake candidate satisfies its bounded contract.

Why: T-070's standard suite and source inspection passed, but it correctly returned PARTIAL because
the mandatory fresh failure reproductions and lifecycle/concurrency/artifact attacks were skipped.
A new report-only reviewer must execute every missing check; no author or orchestrator evidence may
substitute for reviewer evidence.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045/T-068/T-069/T-070 tasks and reports;
product AGENTS; SDD HAI-BOUNDARY/EXEC/SEC; ADR-003; executor/security, persistence/recovery and
accepted Fake implementation mini-SDDs; reproducibility/traceability; HTB-P0A-003; backend module,
unchanged application executor port/declaration validation and every final Fake source/test byte.

Mandatory Go preflight: before writing any disposable Go attack, run and read the complete
unfiltered Modern Go list for Go 1.27 and apply every relevant rule.

Scope (may touch): `.team/reports/T-071.md` only. Read-only candidate mounts plus removable `/tmp`
copies/probes/caches are permitted and must be deleted before handoff.

Out of scope (must not touch): Candidate source/tests, modules, application ports/services,
domain/persistence/SQLite, specifications/OpenAPI/traceability/plan/tasks/prior reports, HTTP/SSE/
T-047, Git refs, real providers, credentials, PR, merge, deployment, release or external state. Do
not fix findings and do not accept your own report.

Definition of done:

- [ ] Bind exact final candidate and T-045/T-068/T-069/T-070 report hashes; independently rerun the
      digest-pinned, network-disabled Go 1.27.1 module verify, empty format, vet, every exact T-045
      named test, the T-068 regression, repeated Fake, full backend and full race gates.
- [ ] With fresh disposable external-package tests, reproduce all four T-068 findings: invalid MIME
      spellings reject; unsupported lookup/cancel with each stale fence dimension return
      `capability_unsupported` without session mutation; invalid artifact name/bytes/digest/media
      observations visibly reject without accepted mutation; terminal success/failure reject from
      Queued and Starting but accept from Running.
- [ ] Attack dispatch, start acknowledgement/loss, heartbeat, checkpoint, lookup running/unknown,
      timeout/unknown, success/failure and cancellation acknowledgement. Out-of-order, duplicate,
      regressed, late and expired observations must remain visible and state-preserving; lost-start
      lookup and current cancel flows retain uncertainty until confirmation and never redispatch.
- [ ] Independently attack capability normalization/immutability, scenario/step/observation/artifact
      alias safety, finite ordered explicit-tick determinism under repetition and concurrency,
      staging-writer digest enforcement, supported stale-fence rejection and hostile artifact names
      without sleeps, randomness or scheduling-dependent expected output.
- [ ] Reconfirm production Fake imports/API expose no host filesystem, shell, Git, network, HTTP,
      database/SQLite, transport, credential or provider/client capability; artifact output uses only
      validated relative names/copied bytes through the supplied staging writer. Limit the claim to
      the Fake boundary, not real OS isolation.
- [ ] Confirm exact T-069 repair scope, unchanged predecessor and historical reports, explicit
      later-gate NotRun inventory, current report validation, temp cleanup, `git diff --check` and
      report-only scope audit.
- [ ] Return unconditional PASS or FAIL. Any required failure or skipped reviewer check makes the
      review FAIL and remains visible; never repair the candidate.

Verify with: exact pinned Go image/read-only cache; fresh disposable external-package T-068,
lifecycle, concurrency and artifact attacks; source/API/import inspection; current report validator;
cleanup and report-only scope audit.

Budget: One fresh independent evidence review; no delegation, candidate fix, commit, Git ref,
HTTP/SSE/T-047, real executor, credential, deployment or external mutation.
