ROLE: REVIEWER

AGENT: deterministic-fake-repair-and-lifecycle-verifier

ID: T-073

Goal: Independently attack the four T-068 repairs and the complete fenced Worker lifecycle of the
unchanged T-045/T-069 candidate.

Why: T-070 skipped fresh attacks, while two all-in-one retry reviewers stalled before creating a
probe. This smaller report-only review owns the repair and lifecycle half of the required evidence.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045/T-068/T-069/T-070 tasks/reports; product
AGENTS; SDD HAI-EXEC/SEC; ADR-003; executor/security, persistence/recovery and accepted Fake
implementation mini-SDDs; reproducibility/traceability; HTB-P0A-003; final Fake source/test bytes.
Run and read the complete unfiltered Modern Go list for Go 1.27 before writing disposable Go.

Scope (may touch): `.team/reports/T-073.md` only. Read-only candidate mounts and removable `/tmp`
probes/caches are permitted and must be removed.

Out of scope (must not touch): Candidate/tests, modules, all other product/spec/plan/task/prior-report
files, Git refs, HTTP/SSE/T-047, real providers, credentials, PR, merge, deploy or external state.

Definition of done:

- [ ] Bind exact candidate/T-068/T-069 hashes and run a fresh disposable external-package test that
      proves malformed MIME rejection; unsupported lookup/cancel preceding every stale fence
      dimension without mutation; visible invalid artifact name/bytes/digest-shape rejection without
      accepted mutation; and terminal success/failure rejection from Queued and Starting plus
      acceptance from Running.
- [ ] In the same focused probe, exercise dispatch, start acknowledgement/loss, heartbeat,
      checkpoint, lookup running/unknown, timeout/unknown, success/failure, cancellation request/ack,
      duplicate, out-of-order, regressed, late/stale and expired paths. Rejections preserve accepted
      state, uncertainty remains until confirmation, and Redispatches stays zero.
- [ ] Rerun the five exact T-045 tests, T-068 regression, ten-repeat Fake and `go test -race` for the
      Fake package in the pinned network-disabled Go 1.27.1 image; inspect relevant lifecycle code.
- [ ] Validate an unconditional PASS/FAIL report, remove temp paths, run `git diff --check`, and
      prove report-only scope. Any failed/skipped required item is FAIL; never repair the candidate.

Budget: One focused independent reviewer, no delegation or candidate/Git/external mutation.
