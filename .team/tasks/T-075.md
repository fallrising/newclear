ROLE: WORKER

AGENT: deterministic-fake-parent-path-repairer

ID: T-075

Goal: Repair only T-074's exact-parent artifact-name bypass while preserving every passing T-069,
T-073 and T-074 deterministic Fake guarantee.

Why: T-074 independently proved that artifact name exactly `..` passes `validRelativeName`, allowing
a parent-directory staging token despite all other bounded Fake gates passing.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045/T-068/T-069/T-073/T-074 tasks/reports;
product AGENTS; SDD HAI-SEC; ADR-003; executor/security and accepted Fake implementation mini-SDDs;
current `types.go`/`fake_test.go`. Run and read the complete unfiltered Modern Go list for Go 1.27
before editing Go.

Scope (may touch): `products/hai-taskboard/backend/internal/executor/fake/types.go`;
`products/hai-taskboard/backend/internal/executor/fake/fake_test.go`; `.team/reports/T-075.md`.

Out of scope (must not touch): Every other file, including adapter/worker, modules, application/
domain/persistence/SQLite, specs/traceability/plan/tasks/prior reports, HTTP/SSE/T-047, Git refs,
providers, credentials, PR, merge, deploy, release or external state.

Definition of done:

- [ ] Reject artifact name exactly `..` in the shared relative-name validator before a scenario can
      reach any staging writer; preserve valid nested names and existing rejection behavior.
- [ ] Add an exact regression for `..` to the existing hostile artifact-name table without changing
      any previously named test.
- [ ] Pass focused regression, all Fake tests ten times, pinned Go 1.27.1 module/format/vet, full
      backend and full race, import/scope inventory, task/report validators and `git diff --check`.
- [ ] Bind repaired file hashes in a validator-compatible report; do not claim self-acceptance and
      keep HTTP/SSE/T-047, persistence integration, restore and real-provider work NotRun.

Verify with: complete Modern Go list; exact regression; pinned network-disabled Go 1.27.1
module/format/vet/Fake-repeat/full/race; import and exact-scope inventory; current task/report
validators; `git diff --check`.

Budget: One two-code-file repair plus one report; no delegation, commit, Git ref, dependency or
external mutation.
