ROLE: REVIEWER

AGENT: http-sse-boundary-final-verifier

ID: T-077

Goal: Independently attack-review the exact T-046 HTTP/SSE/composition candidate and decide whether
it satisfies its bounded transport contract without broadening into T-047, restore or predecessor
implementation.

Why: The T-046 worker reports a green pinned suite, but worker evidence cannot accept itself. A
fresh report-only reviewer must verify auth-before-read, strict OpenAPI translation, byte-exact
stored results and gap-free bounded SSE against the actual final bytes.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-046/T-052/T-064/T-067/T-068/T-069/
T-070/T-073/T-074/T-075/T-076 tasks/reports; product AGENTS; HANDOFF; SDD HAI-BOUNDARY/AUTH/API/SEC;
ADR-001/003/005; persistence/recovery, executor/security and accepted Fake implementation mini-SDDs;
reproducibility/traceability; HTB-P0A-003; complete OpenAPI; backend module; accepted application
command/service/port public APIs, public Store and Fake boundaries; and every final T-046 source/test
byte. Bind the candidate to T-046 report SHA-256
`ce50447a48405fddc1db79b2b9b277063f43befc572b38a0d2d03318cd711f00` and the nine source/test
hashes recorded there. Before writing disposable Go, run and read the complete unfiltered Modern Go
list for Go 1.27.

Scope (may touch): `.team/reports/T-077.md` only. Read-only candidate/module mounts and removable
isolated `/tmp` probes/copies/caches are permitted and must be removed before handoff.

Out of scope (must not touch): T-046 candidate source/tests; modules; application/port/domain/
reconciliation/SQLite/Fake; OpenAPI/specifications/traceability/reproducibility/HANDOFF/PLAN/task
files and prior reports; `backend/integration/**` and T-047; restore/backup implementation; Git refs;
real providers, credentials, PR, merge, deployment, release or external state. Do not fix findings
and do not accept your own report.

Definition of done:

- [ ] Confirm branch/local/remote/baseline identity, exact T-046 task/report/candidate hashes,
      preserved predecessor and historical T-068/T-070/T-074 report hashes, and absent T-071/T-072
      reports. Independently rerun the exact digest-pinned, network-disabled, read-only candidate/
      module Go 1.27.1 module/format/vet/four-name/transport+composition/ten-repeat/full/race gates.
- [ ] Freshly attack canonical/method-aware routes, percent/double/backslash/NUL path spellings,
      duplicate cookies/headers/JSON/query cursor members, wrong method/content type/origin, body
      limit and all five accepted command decoders. Authentication must precede body and resource
      resolution; same-origin and project/result authorization must precede sensitive mutation/read;
      no payload actor may be accepted and unsupported OpenAPI command families must not be
      fabricated.
- [ ] Freshly verify production SHA256 encode/decode round trip and all missing/double/uppercase/
      zero/length/non-hex forms. Attack stored-result lookup with unauthorized/wrong-project/missing,
      ID/digest/payload alias and digest mismatch plus digest-correct but malformed/noncanonical
      success/failure bytes; only an authorized immutable V1 canonical result may be returned and
      lookup must never invoke a command.
- [ ] Freshly validate SSE `data` as the OpenAPI `ProjectionEvent` shape, including a cursor equal to
      the SSE `id`, allowed event/resource values and exact project. Validate reset payload shape and
      reason. Corrupt payload, cursor disagreement, wrong project/epoch, non-monotonic/duplicate/
      gapped durable replay or live publication must fail closed or reset, never silently lose or
      reorder an authoritative event.
- [ ] Attack the snapshot-to-stream boundary with controlled commits before/during/after snapshot or
      replay reads, replay/live duplicates and deliberately out-of-order post-commit publications.
      Require one strictly ordered gap-free project stream without holding a SQLite write or socket
      write under the hub mutex. Confirm no-cursor begins after one consistent snapshot high-water;
      current same-epoch replay is strictly `> cursor`; exact `minimum_sequence-1` is resumable;
      older and wrong-epoch cursors emit one canonical reset then close.
- [ ] Attack both queue limits independently: the 129th queued event and a payload crossing
      1,048,576 queued bytes must make `PublishCommitted` return promptly, attempt exactly one
      `slow_consumer` reset and close without blocking another command/publication. Attack continuous
      revocation and cross-project isolation; require exactly one `authorization_revoked` reset and
      no further data. Use deterministic synchronization, no sleeps or scheduling-dependent expected
      order.
- [ ] Inspect all production imports and exported API. `httpapi` must import neither SQLite nor Fake;
      only `cmd/taskboard` may import both concrete adapters. Confirm Compose actually wires the
      accepted Store, Fake declaration, application Service, result source, projection source,
      session authority, Hub and HTTP server without changing predecessor authority or claiming
      listener/runtime/T-047/restore evidence it does not provide.
- [ ] Preserve the worker report's two initial repaired test failures and detached unauditable gate
      history. Confirm T-047, Fake-to-application persistence execution, backup/restore, browser,
      provider/credentials and broader G1/G2 remain NotRun/Deferred. Validate the reviewer report,
      remove every temp path, run `git diff --check` and prove report-only scope.
- [ ] Return an unconditional visible PASS or FAIL. Any required failed, skipped or NotRun reviewer
      check is FAIL and remains visible. Use `STATUS: DONE` only for PASS with no failed/skipped
      verification entries; otherwise use `STATUS: PARTIAL` or `BLOCKED`. Never repair the candidate
      or claim acceptance.

Verify with: complete Modern Go list; exact digest-pinned Go image/read-only module cache; fresh
external-package HTTP/SSE/OpenAPI/order/backpressure probes; source/import/API/hash inspection;
current `teamctl.py validate-report`; temp cleanup; `git diff --check`; exact report-only scope audit.

Budget: One fresh independent report-only review; no delegation, candidate fix, commit, Git ref,
T-047, restore/backup implementation, real provider/credential, deployment or external mutation.
