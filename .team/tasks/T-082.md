ROLE: REVIEWER

AGENT: fake-vertical-integration-gap-verifier

ID: T-082

Goal: Independently decide whether the exact T-047 integration candidate provides the three
required vertical/durable oracles, and adversarially verify every claimed unavailable predecessor
seam without modifying the candidate.

Why: T-047 correctly reports PARTIAL with required skips after executing legal SQLite/application/
HTTP/Fake prefixes. Worker evidence cannot accept itself, and a fresh report-only reviewer must
determine whether the skips are real predecessor defects or avoidable test omissions before the
orchestrator may route any production repair.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-047 task/report; T-041 decomposition; T-045/T-046 and
T-064/T-067/T-073/T-076/T-077/T-078/T-079/T-080/T-081 tasks/reports; product AGENTS;
`docs/HANDOFF.md`; `docs/SDD.md`; ADR-001/002/003/005; domain, persistence, executor and Fake
vertical-slice mini-SDDs; reproducibility/traceability; HTB-P0A-003; complete OpenAPI; backend
module/migration and every application command/service/port, domain/SQLite, Fake, HTTP/SSE,
composition and `backend/integration/**` source/test byte. Treat candidate and predecessors as
read-only. Before any disposable Go probe, run and read the complete unfiltered Modern Go list for
Go 1.27.

Scope (may touch): `.team/reports/T-082.md` only. Read-only candidate/module mounts and removable
`/tmp` probes/copies/caches are permitted and must be removed before handoff.

Out of scope (must not touch): T-047 integration candidate/report/task, every predecessor or product
source/test/module/migration, specs/OpenAPI/traceability/reproducibility/HANDOFF/PLAN, prior tasks/
reports, Git refs, real providers/credentials, network, PR, merge, deployment, release or external
state. Do not fix a finding and do not accept your own report.

Definition of done:

- [ ] Bind branch/local/remote/baseline identity; T-047 task/report and integration test hashes;
      accepted T-081 plus application/persistence/Fake/HTTP predecessor hashes; and exact candidate
      scope. Confirm no direct SQL, reflection, copied Done predicate, mock-only authority or edit
      outside `backend/integration/**` exists.
- [ ] Freshly run the digest-pinned network-disabled Go 1.27.1 module/empty-format/vet, all three
      exact T-047 names, full backend and integration race checks against read-only candidate bytes.
      Retain every SKIP/FAIL visibly even when the command exit is zero.
- [ ] Attack `TestVerticalFake_CompletionAndResponseLoss`: verify its real SQLite/service/HTTP prefix
      is sensitive and byte-exact, then inspect/compile-inventory accepted exported APIs for a legal
      path from persisted dispatch through Fake publication, Candidate, Review/Evidence, Approval,
      Review→QA and Complete. FAIL if the test could legally finish without predecessor modification;
      otherwise identify every missing authority and retain the required vertical NotRun.
- [ ] Attack `TestRunLease_RejectsStaleEpochPublication`: verify the in-memory Fake rejection is
      state-preserving, then determine whether accepted exports can claim/advance a persisted lease,
      publish a fenced observation/Candidate and query authoritative Run/audit/projection effects.
      Do not accept an unbound fabricated Fence as durable integration evidence.
- [ ] Attack `TestCancel_UnknownStopIsNotCanceled`: verify deterministic Fake uncertainty and zero
      redispatch, then determine whether accepted exports can persist cancellation intent, unknown
      observation/reconciliation state and query the durable result/audit projection. Do not treat
      in-memory Fake state as persisted cancellation evidence.
- [ ] Map each required T-047 Definition-of-done item to actual evidence. An unconditional PASS is
      forbidden if any required test still skips or proves only an unbound prefix. If gaps are
      predecessor defects, return an unconditional FAIL and recommend the smallest production repair
      envelope(s), without changing candidate bytes.
- [ ] Produce a forward-compatible report with one physical Verification line per result, run
      `teamctl.py validate-report`, remove every temporary path, run `git diff --check`, and prove
      report-only durable scope. Preserve T-047 PARTIAL and all historical FAIL/PARTIAL evidence.

Verify with: exact digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
with network disabled and read-only candidate/module mounts; exact named/full/integration-race tests;
source/export/API/hash/scope inspection; current report validator; temp cleanup; `git diff --check`.

Budget: One bounded independent report-only review; no delegation, candidate/predecessor fix,
commit, push, Git ref, provider/credential, network, deployment, release or external mutation.
