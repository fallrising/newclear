ROLE: WORKER

AGENT: pre-push-vertical-code-review-repairer

ID: T-090

Goal: Repair the three pre-push review findings in the accepted cumulative T-083/T-085/T-087
candidate: filesystem artifact verification inside SQLite write transactions, misclassified
Review/Evidence version conflicts, and Approval creation without a current subject/version recheck.

Why: A fresh source review found concrete violations of HAI-AUTH-002, HAI-DOMAIN-004 and
HAI-SEC-006 that prior green tests did not attack. The checkpoint must not be pushed while those
authority boundaries are known to be under-specified or incorrectly implemented.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-047/T-082 through T-089 tasks and reports;
`products/hai-taskboard/AGENTS.md`; `docs/SDD.md`; ADR-001/002/003/005/006;
`docs/sdd/domain-and-gates.md`; `docs/sdd/executor-and-security.md`;
`docs/sdd/fake-vertical-slice-implementation.md`; `docs/sdd/persistence-and-recovery.md`;
`docs/traceability.md`; `docs/reproducibility.md`; complete `api/openapi.yaml`; every current
modified/untracked backend Go source and test. Preserve every earlier PARTIAL/FAIL report unchanged.

Mandatory Go preflight: before editing Go, run the complete unfiltered Modern Go Guidelines list
for Go 1.27, read every returned rule and apply all relevant guidance.

Scope (may touch): `products/hai-taskboard/docs/sdd/fake-vertical-slice-implementation.md`;
`products/hai-taskboard/api/openapi.yaml`;
`products/hai-taskboard/backend/internal/application/command/{result.go,types.go,vertical.go}`;
`products/hai-taskboard/backend/internal/application/service/vertical.go`;
`products/hai-taskboard/backend/internal/domain/sqlite/{store.go,store_test.go}`;
`products/hai-taskboard/backend/integration/vertical_test.go`; `.team/reports/T-090.md`.

Out of scope (must not touch): deterministic Fake, HTTP/SSE implementation, composition root,
domain/reconciliation/rehydration, V1 migration, go.mod/go.sum, web, other specifications, prior
tasks/reports, automatic polling, backup/restore, real providers/credentials/agent execution, PR,
merge, deployment or release. PLAN/HANDOFF/reproducibility/traceability remain orchestrator-only and
may change only after the repair passes the final evidence gate.

Definition of done:

- [ ] Amend the accepted vertical contract and OpenAPI before production behavior so
      `ApproveSubjectPayload` identifies `work_item_id` and `candidate_id`; its `expected_version`
      is the current WorkItem version and the application recomputes the exact current completion
      subject inside the Approval transaction.
- [ ] Add exact failing tests before behavior:
      `TestVerticalAuthority_ArtifactVerificationOutsideWriteTransaction`,
      `TestVerticalAuthority_ReviewEvidenceVersionConflicts`, and
      `TestVerticalAuthority_ApprovalRequiresCurrentSubjectAndVersion`.
- [ ] Verify sealed Candidate/Evidence objects outside `UnitOfWork.Within`, but only after
      authentication and a replay lookup. Preserve byte-exact idempotent replay even if the object
      is subsequently unavailable, then recheck immutable database metadata inside the write
      transaction before binding it.
- [ ] Return `version_conflict`, with no immutable material/audit/projection mutation, when
      PublishFixtureReview or PublishFixtureEvidence carries a stale WorkItem version; keep true
      lease/restore mismatches classified as lifecycle/fence rejection.
- [ ] Reject Approval for a stale version, wrong WorkItem/Candidate or non-current subject before
      storing any Approval. A valid exact subject remains accepted and completes through the
      existing one-use Approval gate.
- [ ] Keep all T-047/T-087 exact integration oracles green and update their Approval calls to the
      repaired contract without weakening any assertion.
- [ ] Pass pinned OpenAPI lint plus Go 1.27.1 module verification, empty gofmt, vet, the three T-090
      tests, three exact integration tests, ten repetitions, complete backend, race, inventories,
      task/report validation and `git diff --check`, with no required failure or skip.
- [ ] Produce a validator-compatible evidence report. The report cannot accept itself; preserve
      prior hashes/findings and keep automatic polling, restore, browser, real provider and wider
      G1/G2 work explicit NotRun/Deferred.

Verify with: complete unfiltered Modern Go 1.27 list; teamctl task/report validators; pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under network-none/read-only module cache; pinned
`redocly/cli:2.51.2@sha256:2dcc3939c2180e1da96db06a40aa079cb32c4ef3bac8b35ff061f2140322da64`;
named/repeated/full/race/inventory checks; exact diff/hash inspection; `git diff --check`.

Budget: One bounded pre-push repair; no delegation, prior-evidence rewrite, migration/module/Fake/
HTTP/composition edit, real execution, PR, merge, deployment or release. Commit and push happen only
after the orchestrator evidence gate under the user's explicit authorization.
