ROLE: WORKER

AGENT: local-fake-runtime-bootstrapper

ID: T-091

Goal: Turn the accepted Fake-only command stack into a runnable loopback process with durable
SQLite state, explicit local-session security, health/readiness, graceful shutdown and a real HTTP
restart smoke test, without claiming the still-missing persistent worker or projection runtime.

Why: T-043 through T-090 established tested persistence, application, deterministic Fake and
HTTP/SSE components, but `cmd/taskboard/main.go` is empty and the accepted composition checkpoint
explicitly deferred environment, listener and lifecycle ownership. A green library suite therefore
does not yet produce a server a developer can start. This serial child closes that concrete T-040
runtime-bootstrap gap before reconciliation/restore work proceeds.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; `products/hai-taskboard/AGENTS.md`; `docs/HANDOFF.md`;
`docs/SDD.md`; ADR-001/002/003/005/006; `docs/sdd/domain-and-gates.md`;
`docs/sdd/executor-and-security.md`; `docs/sdd/fake-vertical-slice-implementation.md`;
`docs/sdd/persistence-and-recovery.md`; `docs/traceability.md`; `docs/reproducibility.md`;
complete `api/openapi.yaml`; T-041/T-043 through T-090 task envelopes and all corresponding
reports, including retained FAIL/PARTIAL evidence; complete current backend source/tests/migrations.
Treat accepted predecessor inputs as read-only except the explicitly writable paths below.

Mandatory Go preflight: before editing any Go file, run
`sh /home/ckc/.codex/plugins/cache/goland-codex-marketplace/modern-go-guidelines/1.1.1/skills/use-modern-go/scripts/run-tool.sh list --go-version 1.27`
without piping, filtering or truncation, read the complete output, apply every relevant rule, and
use `explain <guideline-id>` before skipping any returned rule that appears applicable.

Scope (may touch): `products/hai-taskboard/docs/sdd/local-fake-runtime.md`;
`products/hai-taskboard/docs/local-development.md`; `products/hai-taskboard/backend/cmd/taskboard/**`;
`.team/reports/T-091.md`.

Out of scope (must not touch): existing application/command/port, domain/SQLite, deterministic Fake,
HTTP/SSE adapter, migrations, module/dependency, web, OpenAPI, traceability, reproducibility,
HANDOFF, PLAN, task and prior-report bytes; direct SQL/table access; automatic persistent outbox or
worker polling; pretending unavailable board/SSE projections are ready; backup/restore; real
providers, credentials, agents, shell or network execution; Git refs, commit, push, PR, merge,
deployment, release or external mutation. If the accepted interfaces cannot support the required
runnable checkpoint within composition-root scope, preserve the exact failure and report
PARTIAL/BLOCKED rather than changing a predecessor.

Definition of done:

- [ ] Write `docs/sdd/local-fake-runtime.md` before production behavior. Specify environment/flag
      inputs, loopback-only bind, exact Origin, local session-token handling, data/artifact paths,
      readiness semantics, shutdown ordering, restart durability and explicit unsupported runtime
      surfaces. Add `docs/local-development.md` with copyable build/run/health/authenticated command
      and shutdown instructions that never print or commit a real token.
- [ ] Replace the empty `main` with a testable runtime entry point that opens the accepted SQLite
      Store under a controlled data root, composes only the accepted deterministic Fake adapter,
      supplies concrete clock/cryptographic ID/artifact/session/projection-boundary dependencies,
      listens only on an explicitly loopback address, and closes HTTP then SQLite cleanly on context
      cancellation or SIGINT/SIGTERM. Startup failure returns non-zero and no session secret appears
      in logs or HTTP errors.
- [ ] Require an explicit sufficiently strong local session token; enforce the accepted cookie,
      actor and exact-Origin boundary for commands. Reject wildcard/non-loopback exposure,
      malformed origins, path traversal/symlink escape in data or artifact handling, weak/missing
      tokens and insecure artifact names without starting a listener or partially accepting work.
- [ ] Add unauthenticated `GET /healthz` readiness with a bounded stable JSON response. All other
      paths remain owned by the accepted HTTP server. Do not label board/SSE or automatic execution
      ready: the local projection dependency must fail closed with a stable unavailable response
      until a later bounded task supplies persisted projections.
- [ ] Add exact unit tests `TestRuntimeConfig_RejectsUnsafeExposure`,
      `TestLocalArtifactStore_RejectsEscape`, and `TestLocalSessionAuthority_EnforcesTokenAndOrigin`.
      Add exact integration test `TestRuntime_HTTPPersistenceAcrossRestart` using a real loopback
      listener, real SQLite Store and accepted HTTP commands: create a Project, retrieve the stored
      canonical result, gracefully stop, restart from the same data root, and prove byte-exact
      command-result replay with no duplicate mutation. Use `t.Context`, bounded synchronization,
      `t.TempDir`, no wall-clock sleeps, ambient network or shell.
- [ ] Build the command and execute a process-level loopback smoke with a temporary data root:
      readiness becomes 200, an authenticated exact-Origin CreateProject command succeeds, a clean
      SIGTERM exits 0, restart preserves lookup bytes, and no token is emitted. Record every exact
      command and outcome in the report; clean up only the task-owned temporary process/data.
- [ ] Pass the complete digest-pinned network-disabled Go 1.27.1 gate: `go version`, `go mod verify`,
      empty `gofmt -l`, `go vet ./...`, the four exact T-091 tests, ten repetitions of the restart
      integration test, complete backend, complete race, build and test/import inventory. Also run
      task/report validation, `git diff --check`, bind predecessor/new-file hashes and prove exact
      scope, with no required failure or skip for DONE.
- [ ] Report automatic persistent worker/Fake execution, persisted board/SSE projections,
      restore/backup, browser/live UI, root CI, real provider/credential/agent execution and wider
      G1/G2 acceptance as explicit NotRun/Deferred. A worker report cannot accept itself.

Verify with: complete unfiltered Modern Go 1.27 list; current `teamctl.py validate-task` and
`validate-report`; exact digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
with `--network none`, read-only module cache, `GOPROXY=off` and `GOSUMDB=off`; uncached exact named,
ten-repeat, full and race tests; build/process smoke; source/test/import/predecessor-hash/exact-scope
inventory; `git diff --check`.

Budget: One bounded implementation/evidence turn; no delegation, predecessor/module/API/status
document edit, direct SQL, automatic worker, real executor/provider/credential, network, commit,
push, PR, merge, deployment, release or external mutation.
