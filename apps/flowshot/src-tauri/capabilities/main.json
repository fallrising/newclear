{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal capability set for the Flowshot main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
