# M2：故障恢復與 patched-core reapply（2026-09-23）

接續 PR #32 的 core 修補與 worker-4 連續三次重裝。先核對工作樹及全部既有 journal，82 項原測試通過；沒有重播舊失敗計畫。實機 inventory、credentials、backups 與 raw evidence 仍只在 private／遠端既有 recovery 目錄。

## 本輪實作

- `recovery.py`：新的 source-bound plan／execute，提供 core API 不可用時的 binary rollback、worker-4 精確 restore 與保留新 agent 狀態的 resume。
- 恢復綁定原 journal、遠端備份／現況與目前程式輸入；較新的操作使舊來源失效。已還原但回覆遺失時，新 plan 接續驗證／啟動，不重做還原。
- worker 恢復有連續 HTTP guards、空 target／配額／ownership／保留服務核對；node up 回覆不確定時做一次明確記錄的 corrective fence。恢復不改原失敗 run、不增加 component revision。
- patched reapply 明確指定已驗證 artifact，對照成功部署紀錄與 running SHA；installer 保留 patch，拒絕未完成更新或其他 payload 覆寫 core。
- `--fault-after quarantine|start` 只可綁定 worker-4 元件重裝 plan，供有界故障恢復驗收；沒有 OS、etcd 或 core 故障注入。

## 驗證

本機測試目前 116 項通過，包含真實暫存檔案上的部分 binary／manifest 更新、backup corruption、後來的變更、來源 journal 漂移、回覆遺失、新 plan 接續恢復、worker 保留新狀態、resume 失敗／corrective fence 不確定，以及 reapply SHA／health／invocation 綁定。

實機驗收前唯讀檢查：全群 workload 0、配額一致、worker-4 ownership 通過，core SHA／invocation 仍匹配前日部署紀錄，NRestarts=0。第一段 31 次／150 秒觀測的 etcd health 全數通過，WAL 60 筆樣本的 p99 bucket 上界 16 ms，backend 無新增樣本；沒有新的 timeout／panic。這不代表歷史磁碟停頓根因已解決。

## 實機驗收

| 新 plan/run | 操作／預期 | 結果 |
| --- | --- | --- |
| `20260923T062405Z-8e0fa486` | 保留 patched core 的四台同版本 reapply | COMPLETE；worker-2 nginx、HTTP、membership／usage、所有被觀察服務 invocation 與 runtime task 保留，最後清理回零 |
| `20260923T062856Z-b93a9f57` | worker-4 quarantine 後故意中斷 | 如計畫 FAILED 且保持 fenced；02／03 各 9 次 HTTP、零失敗 |
| `20260923T063115Z-819b7123` | 以新 plan 精確還原前項備份 | COMPLETE；還原、啟動、target nginx、配額回零、隔離與 resume 通過；02／03 各 45 次 HTTP、零失敗 |
| `20260923T063343Z-aee209cc` | worker-4 安裝／啟動後故意中斷 | 如計畫 FAILED 且保持 fenced；02／03 各 17 次 HTTP、零失敗 |
| `20260923T063732Z-93f8ac6b` | 仍 fenced 時跑 target smoke，產生真實新 CNI／agent 狀態並清理 workload | COMPLETE；之後舊備份 restore 顯示 5 項新資料衝突，拒絕覆寫 |
| `20260923T063925Z-cdd76d1b` | 新 plan 保留新狀態、驗證後 resume | COMPLETE；target nginx／配額／隔離通過；02／03 各 32 次 HTTP、零失敗 |

最初的 resume 草案 `20260923T063617Z-1a285ce6` 未執行：唯讀檢查發現單純啟動 agent 尚未寫出新狀態，因此先做上述 target smoke，再建立綁定新現況的 plan。沒有重播任何失敗 plan。source FAILED journal、quarantine 備份與新 recovery journal 均保留。

四個演練／恢復 guard 窗口，每台共 103 次 HTTP、零失敗，最大相鄰間隔約 1.001 秒；不包含窗口間空檔，也不是 24h soak。worker component revision 保持 3、cluster generation 保持 1。

06:26 UTC 起的 61 次／300 秒負載觀測：health 61/61，WAL 215 筆新 observations、p99 bucket 上界 8 ms；backend 27 筆、上界 32 ms。没有新的 timeout、panic 或 core／etcd invocation 改變；backend 性能警示保留。

core API 不可用的恢復使用本機真實備份與故障注入測試驗證；沒有刻意讓實機 core 停機或退回含 lock-context bug 的 binary。


最後以 `20260923T064140Z-19460ba2` 精確清理兩個 canaries。唯讀核對：全群 workload 0、CPU/memory/storage 配額全零、三台 worker available 且未 fencing、worker-4 ownership 通過、core SHA／invocation 與前日 patch 部署相同、NRestarts=0，etcd health 通過且無 alarm。原始證據：private/diagnostics/recovery-final-readonly-20260923.json。

core 的實機唯讀恢復規劃 `20260923T064713Z-e4b39184` 通過，涵蓋四台 host identity、空 runtime、etcd count-only metadata、原 backup 與配置 checksum；此 plan 未執行。探測時發現實際 etcdctl 的 count-only 僅接受 fields 輸出，已修正解析並補零／非零／異常格式測試；不把缺少 Count 視為空 metadata。

後續仍需獨立驗收 core 實機故障／完整停電邊界、非空 drain、其他 worker、snapshot restore／HA、OS reimage 與長時間 soak。此次沒有 provider API 或 OS 重灌。
