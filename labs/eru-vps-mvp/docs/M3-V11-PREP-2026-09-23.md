# ERU-007 本機準備：正式 V11 每秒私網探測

日期：2026-09-23。ERU-007 仍**進行中**，固定清單剩餘 17 項（近期 6、後續 11）。本輪完成每秒私網 HTTP 排程、raw evidence 與離線判讀的程式和本機驗證；尚未執行新的實機 pilot 或 24h 正式 run。

## 已交付

- `soak.py start --acceptance v11` 會為 02／03 各設一條每秒一個請求的背景排程，並保留 01／02／03 原本的服務、task／etcd 30 秒採樣。每一秒的結果、遲到、漏送都寫在當期 JSONL；單次請求超過一秒不算成功。原 30 秒模式無此選項，遠端已啟動的現有 source/config 不會改變。
- 每台 24h 預期 86400 個 slot；02、03 各須至少 85536 次成功，遺漏 slot／序號錯亂、HTTP 健康與服務失敗都不能被後續成功掩蓋。`report` 從 raw JSONL 重算 slot、成功率與 stored summary，並核對來源、長度與 SHA。試跑短於 24h 時，不會標成正式完成。
- 01 仍檢查 etcd health、alarm、服務／journal／histogram；三台 V11 模式另讀 kernel journal 中的 OOM 訊號與根檔案系統、etcd 資料、Docker/containerd 路徑的磁碟使用量。`df -P -B1` 允許 VPS 實際輸出的 `Capacity` 欄名，超過 80% 記失敗。
- 來源 SHA、固定 byte boundary、192 MiB raw 上限、一次性 run／systemd 保護及 private evidence 路徑沿用既有機制。報告仍是 `complete_needs_review`，不自動發 PASS。

## 驗證及限制

`PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -q` 通過 **175 tests**（前次 167，新增 8）。新增測試涵蓋真正執行三秒的背景排程與檔案、請求超時造成漏秒、99% 邊界、受中斷後的 status、離線回收資料缺 slot 但 hash／summary 自洽、短 pilot 不冒充 24h、OOM／磁碟門檻和 VPS 的 `Capacity` 欄名。原 30 秒模式與回收測試仍通過。

唯讀檢查 `ckc-disposable-01`～`03`：V11 要查的 `df` 路徑均存在，三台以 root 查 kernel journal 均可讀；當時所查磁碟使用均為 1%。這是讀取能力確認，非新的 VPS pilot／正式 V11 驗收。當前 30 秒 24h run 保持原 source、service 與起止時間，預計 2026-09-24 11:25:06 UTC 結束。

每 30 秒的磁碟、OOM／etcd 讀取仍是離散觀測；沒有聲稱看見取樣間的所有瞬時事件。下一步先完成 ERU-002 的全量回收、ERU-003 的精確清理，再建立全新 canaries，做短 pilot 與獨立 24h run；[操作命令](SOAK.md) 已記錄。若中途需要 VM mutation，沿用既有 soak 中止／紀錄規則。
