# TODO：回收 24 小時 VPS 觀測結果

任務編號：[ERU-002（本次觀測）、ERU-003（清理）、ERU-004（etcd 分析）](TASKS.md)。原 SDD 每秒一次的正式 V11 另列 ERU-007；目前 30 秒間隔觀測不直接算 V11 通過。

狀態：ERU-002 已完成。遠端 observer 在截止時間自然結束，完整回收與判讀見本文 2026-09-24 完成紀錄。操作細節與判讀界線見 [SOAK.md](SOAK.md)。

| 項目 | 紀錄（UTC） |
| --- | --- |
| Soak run | `20260923T112336Z-561e71e7` |
| 啟動登記 | `2026-09-23T11:23:36Z` |
| 共同觀測開始 | `2026-09-23T11:25:06Z` |
| 實際觀測截止 | `2026-09-24T11:25:06Z` |
| 取樣間隔 | 30 秒，含截止樣本，正常約 2881 筆／台 |
| 觀測主機 | `ckc-disposable-01`、`ckc-disposable-02`、`ckc-disposable-03` |
| Unit（各台同名） | `eru-mvp-soak-20260923T112336Z-561e71e7.service` |
| 遠端資料目錄 | `/var/lib/eru-mvp/soak/20260923T112336Z-561e71e7/` |
| 原 canary run（之後精確 cleanup 用） | `20260923T112207Z-99e9508c` |
| B 私有 manifest | `private/soak/20260923T112336Z-561e71e7/manifest.json` |

01 觀測 etcd／core／磁碟／服務；02、03 各保留一個 0.25 CPU／128 MiB 的 nginx 並定時 GET、核對 task／服務。04 保持空閒、available，component revision 3。觀測程序到期自行結束；canary 不會自動刪除。

本機 126 tests 通過；01 的 30 秒 pilot `20260923T111859Z-8eb82433` 已驗證 SSH 結束後仍取樣、截止後 MainPID 0／SubState exited、四筆 evidence 保留。實機 02、03 HTTP／task／服務 probe 無失敗，新的 canary-start journal complete。這些短測不替代 24 小時結果。

## 已結束的 observer 操作

不需要停止或重啟本次 run。到期後的收集、報告、末端狀態核對及精確 canary 清理均已執行；當前任務狀態以 [TASKS.md](TASKS.md) 為準。私人快照和 journal 保存在 `private/`，raw evidence、實機 inventory 與 credentials 不進 Git。

## 2026-09-23 12:14 UTC 中途回收

Collection `20260923T121401Z-2d3104ca`：三台各 98 筆，共同覆蓋約 2910 秒（48.5 分鐘），狀態均 `in_progress`。SHA、筆數、時間與摘要吻合；無 HTTP／health／服務變更錯誤、無 slow fdatasync 警告。WAL 212 observations，p99 桶上界 8 ms；backend observations 0，未能驗證 backend 寫入延遲。這是中途結果，24h TODO 保持未完成。

01 的 30 秒窗口峰值：iowait 約 0.513%、steal 約 0.061%、I/O PSI some 約 2.888%、read/write 平均耗時約 3.702 ms。沒有當期 etcd 故障可做事故對照，不能藉此確認歷史根因。原始資料與完整報告均保留在 private。回收後仍須以新的 status 查詢最新進度。

## 2026-09-24 00:36 UTC 唯讀進度

三台 `status` 各 1,583 筆，最後樣本 00:36:06 UTC；boot ID 與啟動時一致、systemd 仍 running，摘要無功能失敗或警告。01 的 WAL fsync 3,467 observations、p99 桶上界 8 ms；backend commit 仍只有 2 次。這是未到期的狀態快照，不能代替完整收集／末端 cluster 驗收；11:25:06 UTC 的回收 TODO 保持未勾選。私有 status 輸出在 `private/diagnostics/soak-status-20260924T0036Z.log`。

## 2026-09-24 04:09 UTC 唯讀進度

三台 `status` 均為 running、各 2,010 筆，最後樣本 04:09:36 UTC；failures 與 warnings_by_sample 摘要為空，預定截止仍為 11:25:06 UTC。這是未到期狀態快照；完整 collect/report、末端 cluster 核對與 ERU-003 canary cleanup 仍未完成。私有 status 輸出在 `private/diagnostics/soak-status-20260924T0406Z.log`。

## 2026-09-24 11:32 UTC 完成回收與判讀（ERU-002）

- 遠端 observer 自然結束於 `2026-09-24T11:25:06Z`；collection `20260924T112609Z-6af79a2f` 完整收回 01–03 evidence。每台 2,881 筆，首末樣本為 `2026-09-23T11:25:06Z`／`2026-09-24T11:25:06Z`，共同覆蓋約 86,400 秒，最大取樣間隔 30 秒。
- 離線 report assessment 為 `complete_needs_review`（工具不會自動宣告 PASS）；三台 integrity errors、功能 failures、warnings 均為零。worker HTTP probe、task membership 與四項必要服務在樣本中持續穩定；core／etcd 健康、alarms 與必要服務檢查無失敗。
- 01 的 WAL fsync histogram 增量為 6,308 observations，p99 桶上界 8 ms；backend commit 只有 2 observations，雖然桶上界也是 8 ms，樣本數不足以穩健描述尾端分佈。guest 30 秒窗口峰值：iowait 0.854%、steal 0.145%、I/O PSI some/full 5.032%／4.974%、磁碟 read/write 平均 8.46 ms／operation。這些是時間相關的 guest-side 指標，不證明 provider storage 的因果根因。
- 末端 live snapshot（2026-09-24 11:28 UTC）中 etcd health 成功、三個 worker 均 available，原 run 的兩個 nginx canaries 仍存在且 owner/run/node 配對一致。隨後 ERU-003 另以全新 hash-bound cleanup plan 處理，結果見 [TASKS.md](TASKS.md) 的完成紀錄。
- 這是 30 秒低頻觀測，不是正式 V11 的 1 req/s 24h 驗收。此期間沒有觀察到歷史 slow-fdatasync／timeout，不代表歷史事件已修復；backend 量測也很稀疏。ERU-004 仍需綜合歷史事件與本次結果，確認可支持的解釋、影響與下一步成本。
- 完整 snapshot、報告、status/collect 輸出均在本機 `private/soak/20260923T112336Z-561e71e7/collections/20260924T112609Z-6af79a2f/` 與 `private/diagnostics/`；未複製 raw evidence 到版本控制。

## 2026-09-24 11:33 UTC ERU-003 精確清理完成

- 依原 smoke run `20260923T112207Z-99e9508c` 建立新 plan `20260924T112838Z-4deeeff4`。plan 無 blocker，只列 worker-2／3 上 owner、run、node 標籤完全相符的兩個 canary；execute 前重新讀取 cluster、etcd health 及 workload ownership。
- 執行 journal 為 `complete`。清理後 workload 數為零，worker-2／3／4 都 available 且 resource usage 歸零；原 pod 保留。沒有刪除其他 workload，也沒有執行 blanket reset。
- 清理後透過 `ckc-disposable-01`～`04` aliases 核對 etcd endpoint health 成功；etcd、core、agents、Docker/containerd 均 active/running，firewall oneshot active/exited（RemainAfterExit），proxy socket 為 active 且 listener 已設定。必要服務保留，沒有非預期 restart。
- 完整 hash-bound plan、journal 及 host 檢查留在本機 `private/operations/`、`private/diagnostics/`；本文件僅留公開摘要，未包含 workload IDs、host inventory 或 raw evidence。
