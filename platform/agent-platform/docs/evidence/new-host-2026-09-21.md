# M0 第二台主機驗證 — 2026-09-21

**歷史紀錄：以下是 operator 安裝前的停止點。** 權限／依賴之後已修復，完整真實 KVM 結果見 [後續驗收](../KVM-VALIDATION.md)；不要將以下初期失敗當作目前狀態。

基準為 PR #15 合併後的 `aafd24d`，本機分支 `agent/agent-platform/m0-kvm`。本次重新驗證既有 probe 並準備 runtime 檔案；**M0 in progress、`full_m0_complete=false`**。沒有執行 sandbox claim 或啟動 MicroVM，也沒有將未執行項目判成支援／不支援。

## 主機與結果

- Linux x86_64，Ubuntu kernel `6.8.0-139-generic`。
- AMD Ryzen 7 PRO 8700GE，8 cores／16 threads、約 61 GiB RAM。
- Python 3.12.3、Docker Engine 29.1.3；依 `pyproject.toml` 安裝固定 probe 相依套件。
- `svm` 已暴露、沒有 `hypervisor` flag；`/dev/kvm` 存在且為 `root:kvm`、0660。執行帳號不在 `kvm` 群組，無法開啟裝置，因此未取得 KVM API version。
- `sudo -n true` 退出碼 1（需要密碼）；沒有執行主機套件安裝、權限／網路修改或 root daemon。

| 執行 | 結果 | 證據 |
| --- | --- | --- |
| `make check` | pass：38 tests、Ruff lint／format | 本次終端執行，exit 0 |
| `agent-platform-m0 preflight` | fail：`kvm_permission_denied`、exit 2 | [preflight JSON](host-preflight-2026-09-21.json) |
| `docker build -t newclear-agent-m0:local guest` | pass | guest report 的 `local_image_id` |
| `docker-smoke` | pass：14 checks | [原始映像 JSON](docker-new-host-2026-09-21.json) |
| `guest-image-smoke` | pass：14 checks | [guest JSON](guest-docker-new-host-2026-09-21.json) |
| `sandbox-smoke`、VM boot／TTL／isolation／egress／stop／failure injection | not run：等待管理員 runtime 準備 | 所有 KVM gates 保持 pending |

兩組 Docker report 均為 `docker_contract_passed=true`，但 `full_m0_complete=false`。測試容器與 network 已移除；額外提取 boot files 的未啟動容器也已移除。沒有清理其他專案資源。

## 已準備的本機檔案

開發 checkout 為 `/home/ckc/test/codex/newclear-agent-m0`，虛擬環境與原始 report 位於 `platform/agent-platform/.venv`、`.artifacts`（不提交）。Guest image 為 `newclear-agent-m0:local`；其 Docker image config ID **不是** OCI manifest digest，尚未設定 `GUEST_TEMPLATE`。

下載的 Cocoon 0.6.7 與 sandboxd 0.1.12 tarball 均通過各自發布的 `checksums.txt`。Cocoon 回報 revision `d7dd9a698c1d55c5685c5d4b743ac1413d86e87a`；sandboxd 回報 0.1.12。以下 SHA-256 為已提取檔案的本機量測：

| 暫存檔案 | SHA-256 |
| --- | --- |
| `/tmp/agent-platform-install/cocoon/cocoon` | `515590c7cdcd0034f63d790398253f06958c30f59241314a5d55c75635b9157b` |
| `/tmp/agent-platform-install/sandbox/sandboxd` | `d46adf71c028d428560a50a4e7ecfe08ac3c76c20ba0712497d82441d9efd6ca` |
| `/tmp/agent-platform-install/boot/vmlinuz-sandbox` | `20f7c74057451af14505730fa6865217437eaf856ff964094a6a087b7d7908bd` |
| `/tmp/agent-platform-install/boot/initrd.img-sandbox` | `8393e3421523131a04361a3489879380b608f9a846b8516be705fc6038471e43` |

Boot files 直接來自此次建置的 guest。上述路徑只是本機 staging，尚未安裝至 `/usr/local/bin` 或 `/boot`，重開機或更換主機後可能不存在。

## 管理員接續

1. 依 [KVM 主機準備](../KVM-HOST.md#裝置存在但權限不足) 修復裝置權限，重新登入後取得實際 `kvm_ready=true`／API version 12。這一步不能取代 Cocoon 的 root runtime 準備。
2. 安裝已驗證的 Cocoon／sandboxd、Cloud Hypervisor v54+、erofs-utils >=1.8、qemu-utils，以及 matching boot files；`mkfs.ext4` 已存在。Cocoon／sandboxd 使用新的專用資料目錄。
3. 完成 guest 的 registry／Cocoon image store 準備，取得真正 OCI manifest digest。配置單節點 `net=none`、`warm=0`、loopback listener，將隨機 API token 保存在 repo 外的 0600 檔案。
4. 管理員確認 guest 能開機並啟動 sandboxd 後，執行文件中的 `sandbox-smoke`，再補 TTL、VM 停止／資源回收、隔離、egress 與故障注入的獨立證據。

本次已執行固定 revision 的 Cocoon doctor **check-only**。它確認缺少上述主要 binaries 與主機目錄；完整原始輸出僅保留在 `.artifacts/host-preflight-20260921/cocoon-doctor.txt`。其通用檢查也包含 cloud-image firmware、CNI／iptables；這些結果需依選用的 OCI boot／network lane 解讀，非 root 下的 iptables 檢查也不能證明規則不存在。沒有執行 `--fix`／`--upgrade`，也沒有把 doctor 的一般性 fail 數量視為 MicroVM 契約測試結果。
