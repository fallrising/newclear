---
id: ACCEPT-T002
subject: T002
reviewer_context: fresh-context-claude-code-2.1.220
decision: accepted
date: 2026-07-27
---

# Scope

T002 is the coordination parent for:

- Accepted T002A / CU-AUTH-P0-02 / B+E / E1 credential scope and lease.
- Accepted T002B / CU-AUTH-P0-01 / D+F / E2 device-login lifecycle.

It intentionally owns no direct production module because TD §9.3 requires independently testable
boundaries with different atomicity models to remain separate tasks.

# Composition Evidence

The `codebox-agent-codex` public surface is exactly the union of the two child boundaries.
`LoginBroker::new` consumes a validated `CredentialScope`; internal owned-lease acquisition connects
the E1 serialization boundary to the E2 login lifecycle without adding a public general-purpose
process or credential interface.

`LoginBrokerError` preserves the T002A lease-contention disposition while wrapping all other
credential-scope failures as typed errors. The fixed invocation surface remains limited to version,
login status, and device login. The child environment is cleared and contains only `CODEX_HOME`.

# Security and Atomicity

Both child acceptance reports confirm that lock release after a crash is not treated as proof of a
provider outcome. T002B consults its durable ledger and requires reconciliation before retry.
Neither child offers a shell, repository-controlled command, `codex exec`, or Codex Cloud execution
surface. The P14 regression exists at both child boundaries and passes in the combined crate.

# Validation Evidence

The combined tree passed:

```text
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace --all-targets --all-features
cargo build --workspace --bins --all-features
cargo deny check
git diff --check
```

The focused crate suite passed 23 unit tests and 12 integration tests. No hosted CI run exists for
the current uncommitted implementation.

# Fresh Review Findings

Claude Code 2.1.220 reviewed the parent with fresh, read-only context and returned `ACCEPTED` with
no blocker. It independently confirmed:

- both child dependencies and their upstream T001/T010 dependencies are Accepted;
- E1 and E2 ownership is correctly decomposed and composes without a missing parent module;
- the public types, typed error conversion, fixed argv, and cleared environment compose;
- combined gate evidence is coherent; and
- INV-007 and P14 remain enforced.

Its only material documentation observation was that the combined INV-007 traceability row still
used the phrase “Future T002/T004 implementation.” That row is corrected to distinguish accepted
T002 credential isolation from pending T004 cloud execution.

# Decision

Accepted. Both independently accepted child CUs compose at the documented boundary, all combined
local gates pass, and the fresh parent review found no blocker.
