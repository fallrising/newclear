---
id: ACCEPT-T007
subject: T007
reviewer_context: fresh-context-codex-subagent-read-only
decision: accepted
date: 2026-07-28
---

# Normative Inputs Reviewed

- Both applicable `AGENTS.md` files
- TD §§1.6–1.7, 14, and 15.0
- ADR-0002, ADR-0003, and ADR-0004
- Every accepted T001–T006 task, specification, implementation, test suite, and acceptance report
- T007 task, SPEC-T007, deterministic implementation, test history, and CI configuration
- `docs/traceability.md` and `docs/HANDOFF.md`

# Scope

Acceptance composition only: the deterministic conjunction of the accepted T006 browser contract
suite and one wire-equivalent flow through the concrete T002–T005 stack, embedded T006 page route,
real loopback WebSocket, and a private fake Codex process. T007 adds no production API, provider,
repository, execution, credential, recovery, persistence, or browser authority.

# Spec-First and Test-First Evidence

The first fresh design review rejected an incorrect stdin-prompt assumption. Commit `58d644f`
repaired the fixture to match the accepted T003 final-argv prompt contract and the accepted T004A
null-stdin launcher contract. A second fresh-context read-only review returned
`DESIGN ACCEPTED`.

Commit `adf5d19` added the exact
`p0_subscription_e2e_fake_codex_reaches_final_diff` skeleton. The test compiled with `--no-run`
and then failed alone with the fixed `T007 skeleton: not implemented` assertion before production
test behavior was added. Commit `5462d35` implemented the deterministic path, and commit
`2e54567` guaranteed exact shutdown and cleanup even when the loopback bind itself fails.

# Deterministic Acceptance Evidence

The Linux-only test creates one exact private fixture root outside the repository and exercises:

- concrete credential scopes and login status through the pinned fake CLI;
- static page delivery, bootstrap authentication, secure application cookie, and initial snapshot;
- real WebSocket replay-begin, snapshot, replay-end, and contiguous live event delivery;
- one explicit prompt and exactly one Cloud submit with the prompt as the final argv item and null
  stdin;
- Pending-to-Ready monitoring, terminal snapshot agreement, high-water reconnect without duplicate
  events, and bounded provider-managed diff retrieval;
- disconnect without mutation, explicit logout, cookie rejection, lower-runtime shutdown, and
  exact-root cleanup; and
- bounded command classifications, zero unknown/apply/repository commands, and absence of bootstrap,
  cookie, credential, prompt, diff, provider-output, and private-path canaries outside their
  authorized boundaries.

The fresh final read-only reviewer returned `T007 ACCEPTED` at `2e54567`, finding the deterministic
composition, final-argv/null-stdin behavior, replay/reconnect ordering, authority isolation,
deadlines, shutdown, and cleanup conformant.

# Machine Evidence

```text
cargo test -p codebox-control-plane --test p0_subscription_e2e \
  --all-features -- --exact \
  p0_subscription_e2e_fake_codex_reaches_final_diff         passed
same exact deterministic E2E, 10 consecutive runs          passed
cargo test -p codebox-control-plane --all-features          passed: 37
node --test --test-isolation=none \
  apps/control-plane/web/p0-client.test.mjs                 passed: 10
cargo fmt --all -- --check                                  passed
cargo clippy --workspace --all-targets --all-features \
  -- -D warnings                                            passed
cargo test --workspace --all-targets --all-features         passed: 179
cargo build --workspace --bins --all-features               passed
cargo deny check                                             passed
                                                            advisories/bans/licenses/sources ok
git diff --check                                             passed
fresh read-only acceptance review                           T007 ACCEPTED
GitHub Actions run 30425447197                              passed
```

The exact workspace test was rerun outside the filesystem/network sandbox after four loopback
tests reported only `PermissionDenied`; the unrestricted run passed all 179 tests, including T007.

# Live-Smoke Record

`not run — credential/environment gate unavailable`

All nine required administrator variables were absent:
`CODEBOX_CODEX_EXECUTABLE`, `CODEBOX_CODEX_HOME`, `CODEBOX_STATE_DIR`,
`CODEBOX_WORKING_DIR`, `CODEBOX_CLOUD_ENVIRONMENT`, `CODEBOX_CLOUD_BRANCH`,
`CODEBOX_PUBLIC_ORIGIN`, `CODEBOX_BOOTSTRAP_TOKEN`, and `CODEBOX_LISTEN_ADDRESS`.
No credentials, provider task, or live-success evidence was fabricated.

# Decision

Accepted at `2e54567`. The deterministic fake-Codex gate proves the complete accepted P0
composition and passed hosted CI at
https://github.com/fallrising/fanzloud/actions/runs/30425447197. The separately gated live smoke was
honestly unavailable and is not represented as passed.
