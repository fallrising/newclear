---
id: ACCEPT-T010
subject: T010
reviewer_context: fresh-context-claude-code-2.1.220
decision: accepted
date: 2026-07-27
---

# Normative Inputs Reviewed

- `docs/TD.md` §§4.1, 8.2–8.10, 9.2–9.6, and 16.1
- `AGENTS.md`
- `docs/tasks/T010.task.md`
- `docs/specs/SPEC-T010-strong-ids-errors.md`
- `docs/acceptance/T000.acceptance.md`
- `docs/traceability.md`
- `.github/workflows/ci.yml` and `deny.toml`
- `Cargo.toml`, `Cargo.lock`, and `crates/codebox-domain/**`

# Scope

T010's value-only domain boundary: strong UUID identifiers, `EventSeq`, `WorkspacePath`, typed
validation errors, serde validation, and compile-time ID separation.

# Clause-to-Evidence Review

| Clause | Test or inspection | Evidence | Result |
|---|---|---|---|
| Every entity ID is non-nil | `ids_are_non_nil` | 7 ID types passed | Passed |
| Nil IDs are rejected | `nil_ids_are_rejected` | Typed `IdError::Nil` asserted for all 7 types | Passed |
| ID identity survives serde | `ids_round_trip_through_serde` | All 7 types round-tripped | Passed |
| Serde cannot create nil IDs | `nil_id_deserialization_is_rejected` | Nil UUID JSON rejected | Passed |
| ID types are compile-time distinct | Compile-fail doctest in `src/lib.rs` | `cargo test -p codebox-domain --all-features` | Passed |
| Workspace paths normalize idempotently | `workspace_path_normalizes_and_is_idempotent` | `./src//./lib.rs` becomes `src/lib.rs` and revalidates | Passed |
| Boundary path inputs are rejected | `workspace_path_rejects_boundary_inputs` | Empty, absolute, parent, NUL, backslash, and drive prefix cases | Passed |
| Path serde revalidates | `workspace_path_deserialization_revalidates` | Traversal JSON rejected; valid path round-tripped | Passed |
| Path size is bounded | `workspace_path_rejects_overlong_input` | 4097-byte input returns `TooLong` | Passed |
| Sequence overflow is typed | `event_seq_overflow_is_typed` | `u64::MAX` does not wrap | Passed |
| Malformed input does not panic | `malformed_values_return_errors_without_panicking` | Adversarial path table passed under `catch_unwind` | Passed |
| Workspace gates remain green | Repository validation commands | Tests, Clippy, build, fmt, and cargo-deny all passed | Passed |
| Hosted CI reproduces the workspace gates | GitHub Actions run | [Run 30262687153](https://github.com/fallrising/fanzloud/actions/runs/30262687153), commit `aa56b75`, conclusion `success`; Rust job completed in 2m23s | Passed |

# Public API Drift Check

The public domain values are re-exported from `codebox-domain/src/lib.rs`; public constructors and
projections carry the `CU-FS-00` rustdoc contract marker. No protocol or persisted schema was
introduced.

# Security and Recovery Review

The value boundary rejects absolute paths, parent traversal, NUL, backslash, drive prefixes, and
overlong values without filesystem or process access. It does not claim symlink safety, filesystem
containment, authorization, or persistence; those remain downstream boundary responsibilities.
The E0 contract has no external side effect to reconcile after a crash.

# Validation Evidence

```text
cargo fmt --all -- --check                                      passed
cargo test -p codebox-domain --all-features                    passed: 10 tests + 1 doctest
cargo clippy -p codebox-domain --all-targets --all-features -- -D warnings
                                                               passed
cargo test --workspace --all-targets --all-features             passed
cargo clippy --workspace --all-targets --all-features -- -D warnings
                                                               passed
cargo build --workspace --bins --all-features                   passed
cargo deny check                                                 passed
```

Hosted reproduction: [GitHub Actions run 30262687153](https://github.com/fallrising/fanzloud/actions/runs/30262687153)
passed all workflow gates on `aa56b75`.

# Review Limitation

The fresh Claude review was intentionally read-only and did not rerun shell commands or independently
query GitHub. It statically verified that the code and named tests cover every specification clause,
that CI contains the required workspace commands, and that the recorded hosted run is consistent
with the reviewed commit. Local executable validation is rerun by the primary agent before this
acceptance update is finalized.

# Fresh Review Findings

The reviewer found no correctness, completeness, feasibility, security, recovery, public API, or
test-coverage blocker. Its non-blocking observations were:

- The compile-fail doctest is identified by source location rather than the descriptive test name in
  the specification, but it functionally proves ID type separation.
- The drive-prefix check conservatively rejects values such as `a:b`; this stricter behavior is
  compatible with the portable-path contract.
- Hosted CI evidence was recorded rather than independently queried in the read-only review.

# Decision

Accepted. The implementation and executable acceptance clauses pass, and a fresh-context,
document-first Claude review found no blocking issue.
