---
id: T005
title: P0 session API and replayable live stream
status: accepted
milestone: P0
primary_module: control-plane
contract_units: [CU-SES-P0-01, CU-API-P0-01, CU-API-P0-02]
depends_on: [T005A, T005B, T005C]
risk: high
---

# Outcome

Accept the composition of the process-lifetime P0 session lifecycle, authenticated private HTTP
API, and replay-then-live WebSocket stream without claiming generic backend conformance or
crash-durable P1 replay.

# Inputs

- TD §§1.7, 2.3, 4.2, 4.7, 7, 8–10, 14, and 15.0
- ADR-0002, ADR-0003, and ADR-0004
- Accepted T002 login broker and T004 provider-specific Cloud orchestrator
- Accepted child reports for T005A, T005B, and T005C

# Deliverables

- Composition-only T005 acceptance report
- Combined login, session, reconnect, cancel, cleanup, redaction, P14, and P15 evidence
- Updated handoff and traceability records

# Non-scope

- New production behavior beyond the accepted children
- P1 event storage, canonical `AgentBackend`, approvals, artifacts, or crash-durable replay
- Multi-user/public authentication or repository-controlled local execution

# Invariants

- INV-004, INV-006, INV-007, INV-010, and INV-012 remain true.
- T005 owns no provider retry, repository command, patch application, or credential-copy surface.

# Dependency Evidence

T004, T005A, T005B, and T005C are Accepted. Their reports are the dependency evidence for this
composition-only task.

# Decomposition

| Child | CU | Atomicity | Outcome |
|---|---|---|---|
| T005A | CU-SES-P0-01 | E1 | In-process session lifecycle and bounded ordered fanout |
| T005B | CU-API-P0-01 | Per endpoint | Authenticated login/session HTTP trust boundary |
| T005C | CU-API-P0-02 | E0 | Replay-then-live WebSocket transport |

# Machine Acceptance

- Every child has an accepted fresh-context report.
- Full workspace format, Clippy, tests, binary build, dependency policy, and diff checks pass.
- Combined tests prove stale-instance rejection, explicit-only cancel/recovery, disconnect
  independence, no secret/diff leakage to events, and no local repository execution.
- A fresh read-only composition review returns `COMPOSITION ACCEPTED`.

# Required Documentation Updates

Update SPEC-T005, this task, traceability, HANDOFF, and `ACCEPT-T005`.

# Risks and Gaps

Resolved. Both exact combined-flow regressions and every child/workspace gate pass. A fresh
read-only parent review returned `COMPOSITION ACCEPTED`; see `ACCEPT-T005`.
