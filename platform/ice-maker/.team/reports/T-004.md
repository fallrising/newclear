STATUS: DONE

## Summary

Added a dependency-free Phase 0 repository gate. `make check` runs the standard-library checker and the repository test suite. The checker validates tracked JSON, SDD source hashes, trailing whitespace, secret-like content, protected-path CODEOWNERS coverage, and SHA-pinned GitHub Actions. Secret diagnostics report only file and line, never matched credential values. Tests are scanned; only lines with explicit synthetic markers under `tests/` or fixture-path components are exempt.

Added focused contract tests for the passing repository and malformed JSON, secret detection/redaction, path-scoped synthetic markers, production-marker rejection, unpinned actions, and SDD hash mismatch.

## Verification

- RED: Before any task files existed, `python3 -m unittest tests.test_phase0_contracts -v` — exit 1, with the expected missing `tests.test_phase0_contracts` module failure.
- Rework baseline: `python3 -m unittest tests.test_phase0_contracts -v` — exit 0; 6 tests passed.
- Final: `python3 -m unittest tests.test_phase0_contracts -v` — exit 0; 7 tests passed.
- `make check` — exit 0; repository gate passed and 7 tests passed.
- `git diff --check` — exit 0.

## Documentation

No documentation update was needed; the Make target and checker are the repository-native implementation requested by the existing CI workflow.

## Risks and Follow-ups

None known. The gate intentionally scans tracked files, matching CI behavior; newly created untracked files are included once tracked by the checkout/commit workflow. Tests are scanned; synthetic marker exemptions apply only to marked lines under `tests/` or fixture-path components, and both CODEOWNERS copies must remain identical and cover every protected policy path.
