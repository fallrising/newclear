STATUS: DONE

## Summary

Implemented the deterministic offline SDD lifecycle CLI and completed both
follow-up corrections. The CLI provides `init`, `new`, `validate`, and `status`
through `python -m ice_maker` and the `sdd` console entry point. `init`
preflights configuration and governed templates before writing and rejects
symlinked paths; `new` validates staged output and publishes only through
Linux `renameat2(RENAME_NOREPLACE)`, failing closed when unavailable.

## Verification

- REWORK 1 RED: `PYTHONPATH=src python3 -m unittest tests.test_sdd_cli.SddCliTest.test_init_rejects_symlinked_templates_before_any_write tests.test_sdd_cli.SddCliTest.test_init_preflights_divergence_without_partial_initialization tests.test_sdd_cli.SddCliTest.test_lifecycle_and_deterministic_status tests.test_sdd_cli.SddCliTest.test_new_rejects_invalid_title_without_publishing_target -v` — exited 1 as expected: symlinked template destination was accepted, divergent initialization left partial files, `<short feature title>` was unreplaced, and an invalid title was published.
- REWORK 1 GREEN: `PYTHONPATH=src python3 -m unittest tests.test_sdd_cli tests.test_sdd_validation -v` — exited 0; 16 tests passed.
- REASSIGN RED: `PYTHONPATH=src python3 -m unittest tests.test_sdd_cli.SddCliTest.test_publish_no_replace_preserves_collision_created_after_preflight -v` — exited 1 as expected: `_publish_no_replace` was absent.
- REASSIGN GREEN: `PYTHONPATH=src python3 -m unittest tests.test_sdd_cli.SddCliTest.test_publish_no_replace_preserves_collision_created_after_preflight -v` — exited 0; collision target was preserved.
- Final required verification: `PYTHONPATH=src python3 -m unittest tests.test_sdd_cli tests.test_sdd_validation -v && git diff --check` — exited 0; 17 tests passed and no whitespace errors.

## Documentation

Updated `README.md` with source-checkout commands, init preflight/symlink
safety, and Linux no-replace publication behavior.

## Risks and Follow-ups

Implementation files: `pyproject.toml`, `src/ice_maker/__init__.py`,
`src/ice_maker/__main__.py`, `src/ice_maker/cli.py`,
`tests/test_sdd_cli.py`, `README.md`, and `.team/reports/T-006.md`.

The constrained front-matter parser intentionally is not general YAML. An
unsupported Linux architecture or unavailable `renameat2` fails closed rather
than risking overwrite. No commit was created.
