STATUS: DONE

## Summary

Completed REWORK 2 after escalation to `codex-strong`. The task contract remains compatible with `task.schema.json`; provider selection is trusted input, data class and cost ceiling come from approved spec front matter, provider policy is structurally validated, and only current worktree `HEAD` is accepted. Bounded execution now retains a finite look-ahead window, redacts before retained-payload truncation, hides failed-start details, and uses exit code `125` for output limits. Changed-path inspection disables rename detection so deletion and addition sides are both gated, while conservative glob-prefix checks reject wildcard overlap with protected paths.

## Verification

- RED: `PYTHONPATH=src python3 -m unittest tests.test_execution -v` — failed as expected before implementation (exit 1): `ModuleNotFoundError: No module named 'ice_maker.execution'`; `Ran 1 test in 0.000s`.
- REWORK RED: `PYTHONPATH=src python3 -m unittest tests.test_execution -v` — failed as expected before the REWORK fixes (exit 1): schema-compatible contract tests failed with `TypeError: validate_task_contract() got an unexpected keyword argument 'provider'`; the forbidden-to-allowed rename test failed with `AssertionError: ContractError not raised`; the byte-cap test failed with `AssertionError: -9 != 125`. The child-process cleanup and recognizable-token regressions passed against the prior implementation.
- REWORK 2 RED: `PYTHONPATH=src python3 -m unittest tests.test_execution -v` — failed as expected: chunked cross-cap secret output exposed `bou`, an incomplete private-key block retained `PRIVATE KEY`, `**/*.txt` did not reject its overlap with wildcard protected paths, over-spec cost and invalid command collections passed, and failed process startup raised a raw `FileNotFoundError` containing the supplied secret.
- REWORK 2 GREEN: `PYTHONPATH=src python3 -m unittest tests.test_execution -v && python3 -m compileall -q src tests && git diff --check` — passed (exit 0): `Ran 16 tests in 3.890s`, `OK`; compile and diff checks produced no output.
- The final suite covers schema/provider/spec-budget policy, malformed and duplicate commands, old-but-valid base rejection, chunked supplied-secret and incomplete-private-key redaction, deterministic bounded evidence, safe start failure, timeout child cleanup, wildcard protected-path intersection, symlink escape, and both rename sides.

Machine-readable result evidence compatible with `result.schema.json`:

```json
{
  "task_id": "T-008",
  "base_sha": "f60819a18d4d4289854ba96e07979d39bdbb4e51",
  "status": "completed",
  "summary": "Offline policy-core and REWORK 2 regressions passed.",
  "changed_files": ["src/ice_maker/execution.py", "tests/test_execution.py"],
  "commands_run": [{"command": "PYTHONPATH=src python3 -m unittest tests.test_execution -v && python3 -m compileall -q src tests && git diff --check", "exit_code": 0, "output": "Ran 16 tests in 3.890s; OK; compile and diff checks produced no output."}],
  "acceptance": [{"criterion": "T-008 policy core and REWORK 2", "status": "pass", "evidence": "Focused offline suite passed."}],
  "tests": {"passed": 16, "failed": 0},
  "risks": [],
  "open_questions": [],
  "usage": {"provider": "local", "model": "not-invoked", "estimated_cost_usd": 0, "elapsed_seconds": 4}
}
```

## Documentation

No documentation was changed; documentation is outside the assigned scope.

## Risks and Follow-ups

None within T-008 scope. Disposable-worktree creation, OS sandbox construction, adapter invocation, and publication remain assigned to subsequent tasks.
