STATUS: DONE

## Summary

Replaced caller-asserted cleanup with a two-stage lifecycle boundary. Evidence
is staged only from the exact active, registered `WorktreeJob` at the contract
base; its worktree provides the only changed-path diff. Finalization requires
that same job to be inactive, absent, and unregistered after cleanup. Staged
result, command, adapter, and path evidence is immutable and protected by a
canonical sorted-JSON SHA-256 digest.

Result validation now checks every `CommandEvidence` before field access,
permits ordinary newline/tab output, and rejects malformed types, failed or
non-exact evidence, secrets, and non-finite/over-budget usage. Publication
revalidates direct requests before the callback. The manual read-only workflow,
protected-path policy, and both CODEOWNERS copies remain synchronized.

## Verification

- RED: `PYTHONPATH=src python3 -m unittest tests.test_orchestration -v` —
  exit 1 before canonical command-evidence serialization was added. Four
  staged-lifecycle tests failed with `TypeError: Object of type CommandEvidence
  is not JSON serializable`; the forbidden-diff, malformed-evidence, and
  governance tests already passed. This demonstrated that staged evidence could
  not yet be deterministically finalized.
- GREEN: `PYTHONPATH=src python3 -m unittest tests.test_orchestration -v && make check && git diff --check` — exit 0.
  Focused suite: 7 tests passed in 0.289s. `make check` passed the repository
  gate and all 66 offline tests in 5.526s. `git diff --check` produced no
  output.
- The focused suite uses temporary local Git repositories only; no provider,
  network, container runtime, GitHub API, credentials, merge, deployment,
  release, or external mutation was invoked.

Machine-readable result evidence compatible with `result.schema.json`:

```json
{
  "task_id": "T-010",
  "base_sha": "21293443f591277c3415d62171a0abf95510885c",
  "status": "completed",
  "summary": "Staged lifecycle evidence and draft publication boundary passed local verification.",
  "changed_files": [".github/CODEOWNERS", ".github/workflows/agent.yml", "CODEOWNERS", "docs/verification/phase-2.md", "orchestration/policies/protected-paths.json", "src/ice_maker/orchestrator.py", "src/ice_maker/publisher.py", "src/ice_maker/runner.py", "tests/test_orchestration.py"],
  "commands_run": [{"command": "PYTHONPATH=src python3 -m unittest tests.test_orchestration -v && make check && git diff --check", "exit_code": 0, "output": "Focused: 7 tests OK; full offline suite: 66 tests OK; repository gate passed; diff check clean."}],
  "acceptance": [{"criterion": "Staged lifecycle and publication policy", "status": "pass", "evidence": "Focused temporary-Git tests passed."}],
  "tests": {"passed": 66, "failed": 0},
  "risks": ["Real self-hosted runner registration, rootless runtime execution, egress enforcement, scoped publisher credentials, and provider invocation remain external-pending."],
  "open_questions": [],
  "usage": {"provider": "local", "model": "not-invoked", "estimated_cost_usd": 0, "elapsed_seconds": 6}
}
```

## Documentation

Updated `docs/verification/phase-2.md` to describe staged/finalized lifecycle
evidence and retain explicit external-pending semantics.

## Risks and Follow-ups

Real self-hosted runner registration, rootless container execution, egress
enforcement, scoped publisher credentials, and provider invocation remain
external pending. No commit was made.
