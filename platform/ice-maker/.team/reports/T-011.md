STATUS: DONE

## Summary

Reworked the OpenCode builder as `builder_economy` bound to the `deepseek`
contract provider. It now uses the supported `run --pure -m --format --dir`
form and exclusively publishes bounded stdout evidence to a new result file.

Reworked Claude and Grok review adapters to validate their installed CLI version
shapes and use supported read-only, non-interactive argv. They receive only a
validated bounded artifact embedded in the positional prompt, never a workspace
or input/output file flags. `--tools=` is the empty-tools spelling compatible
with the Phase 2 argv primitive, which rejects empty argv elements.

## Verification

- RED: `PYTHONPATH=src python3 -m unittest tests.test_adapters -v` — failed as
  expected after the exact-argv tests were introduced: the shared executor
  rejects the empty argv element in `--tools ""` (exit 1, 2 errors).
- GREEN: `PYTHONPATH=src python3 -m unittest tests.test_adapters -v` — passed;
  5 tests (exit 0), including no-replace race, failed, truncated, empty, and
  redacted-secret output cases added during orchestrator review.
- `PYTHONPATH=src python3 -m unittest tests.test_runner -v` — passed; 11 tests
  (exit 0).
- `git diff --check` — passed (exit 0).

Machine-readable evidence:

```json
{"task_id":"T-011","status":"passed","checks":[{"command":"PYTHONPATH=src python3 -m unittest tests.test_adapters -v","exit_code":0,"tests":5},{"command":"PYTHONPATH=src python3 -m unittest tests.test_runner -v","exit_code":0,"tests":11},{"command":"git diff --check","exit_code":0},{"command":"make check","exit_code":0,"tests":72}]}
```

## Documentation

No documentation change was needed: the approved Phase 3 specification and plan
already describe this adapter scope.

## Risks and Follow-ups

No real provider, credential, or network call was made. Installed CLI doctor
versions and the local `--tools=` parser spelling were verified by the
orchestrator. External authentication and paid-provider evidence remain pending
under the approved specification.

Changed files: `src/ice_maker/adapters.py`, `tests/test_adapters.py`,
`.team/reports/T-011.md`. No commit was created.
