STATUS: DONE

## Summary

Completed REWORK 1 by replacing the raw `gate_argv`, caller-provided ledger,
and raw reviewer-string API with immutable `BuilderBoundary`, `GateBoundary`,
and `ReviewerBoundary` request values. Their only operation is a typed
`invoke` callback returning `CommandEvidence`.

The request accepts only the Phase 3 Codex/DeepSeek builder routes, local
zero-cost gate route, and Claude/Grok read-only reviewer routes. The pipeline
owns its alias-bound three-attempt ledger and accepts no workspace, argv,
shell, network, publisher, merge, deploy, or branch capability. A bounded
tuple of normalized prior signatures is the only prior-failure input.

## Verification

- RED: `PYTHONPATH=src python3 -m unittest tests.test_pipeline -v` — failed
  as expected (exit 1): the rewritten test imported missing sealed boundary
  types before their implementation.
- GREEN: `PYTHONPATH=src python3 -m unittest tests.test_pipeline -v` — passed
  (exit 0), 5 tests after orchestrator escalation. Covers success callback order;
  committed-route cost parity; failed/timed-out/truncated,
  empty, redacted, exception, malformed, and rejected evidence; forged,
  same-provider, and write-capable routes; repeated failures and budget stops.
- `make check` — passed (exit 0), 84 tests.
- `git diff --check` — passed (exit 0).

Machine-readable evidence:

```json
{"task_id":"T-013","status":"passed","checks":[{"command":"PYTHONPATH=src python3 -m unittest tests.test_pipeline -v","exit_code":0,"tests":5},{"command":"make check","exit_code":0,"tests":85},{"command":"git diff --check","exit_code":0}]}
```

## Documentation

Updated `docs/verification/phase-3.md` and `docs/runbooks/providers.md` to
describe sealed callback boundaries, the evidence failure rules, and exact
external-pending provider and gate-adapter checks.

## Risks and Follow-ups

No real provider, credential, network call, merge, deploy, or `main` mutation
was performed. Provider authentication/model resolution, rate-limit/health,
cost reconciliation, production read-only enforcement, and actual gate-adapter
configuration remain external-pending.
