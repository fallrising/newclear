STATUS: DONE

## Summary

Hardened duplicate intake validation: it now recomputes MIME, digest, findings, and eligibility from securely read quarantine bytes, rejecting forged provider-eligible manifests. Taxonomy now rejects byte-level drift and unsafe paths.

## Verification

- `PYTHONPATH=src python3 -m unittest tests.test_knowledge_store -v` — passed (12 tests)
- `PYTHONPATH=src python3 -m unittest discover -s tests -v` — passed (97 tests)
- `git diff --check` — passed

## Documentation

Updated [T-014.md](/home/ckc/test/codex/worktrees/ice-maker-t014/.team/reports/T-014.md).

## Risks and Follow-ups

No provider/network capability added. Production PDF/OCR remains intentionally out of scope.