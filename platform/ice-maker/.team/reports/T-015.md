STATUS: DONE

## Summary

Hardened the standard-library synthetic extraction cache. Cache-directory
creation now `lstat`s every component before use or creation and rejects
symlink/non-directory ancestors and symlinked entries. Cache reads use a
duplicate-key rejecting JSON decoder, exact schemas, strict primitive types,
finite confidence, ordered one-to-one pages/chunks, bounded region grammar,
safe normalized text, source/version/digest binding, and recomputed chunk IDs.
A no-replace publication collision is accepted only when its already-published
canonical result exactly equals this invocation's result; otherwise it fails
closed.

## Verification

- RED: `PYTHONPATH=src python3 -m unittest tests.test_extraction -v` — exit 1;
  expected `ModuleNotFoundError: No module named 'ice_maker.extraction'` before
  implementation.
- GREEN: `PYTHONPATH=src python3 -m unittest tests.test_extraction -v` — exit 0;
  7 tests passed. New regressions cover version/digest/schema/bool/NaN/page/
  region/method/text/chunk cache forgeries, duplicate JSON keys, intermediate
  and entry symlinks, and mismatched safe publication collisions. The existing
  counting OCR fixture proves a valid cache hit makes no second OCR call.
- `PYTHONPATH=src python3 -m unittest discover -s tests -v` — exit 0; 92 tests
  passed.
- `git diff --check` — exit 0.
- `git diff --no-index --check /dev/null src/ice_maker/extraction.py` — exit 0.

Machine-readable evidence:

```json
{"task":"T-015","red_exit":1,"focused_green_exit":0,"focused_tests":7,"full_green_exit":0,"full_tests":92,"diff_check_exit":0,"provider_calls":0,"production_ocr_claim":false}
```

## Documentation

No documentation files were changed because the extractor module documents its
strict synthetic-PDF/PBM and fixed-glyph limitations in the public module and
function docstrings; task scope excludes documentation paths.

## Risks and Follow-ups

This is deliberately not arbitrary PDF support or production OCR.  It accepts
only marked fixture PDFs, 3x5 uppercase glyphs `A,E,H,I,L,O`, and ASCII-safe
output.  Production parser/OCR, languages, noisy-image accuracy, and object
storage evidence remain external-pending.
