## Summary

Reworked the promotion API to require a retained `PromotionStateMachine`, closing the stateless replay bypass. Citations now require lowercase 64-hex SHA-256 chunk IDs. Added regressions for replay, terminal stages, hypotheses, malformed decisions, failure atomicity, and stale-digest mutation. Slotted frozen review values expose no mutable `__dict__`.

## Verification

- `PYTHONPATH=src python3 -m unittest tests.test_promotion -v` — passed, 10 tests
- `make check` — passed, 131 tests
- `git diff --check` — passed

## Documentation

Updated [T-019 report](/home/ckc/test/codex/worktrees/ice-maker-t019/.team/reports/T-019.md).

## Risks and Follow-ups

None. No external publishing, provider, Git, or network boundary was added.
