STATUS: DONE

## Summary

Implemented the T-022 rework in the assigned files:

- One pinned no-follow repository descriptor for every source read and output
  operation, including ancestor and whole-root swap rejection.
- Output confined strictly beneath `<repo>/.ice-maker/`.
- Atomic exclusive directory publication of `book.md` + `book.html`.
- Collision rejection; no pair reuse/overwrite.
- Fault-injection coverage for staged write and final publish failures.
- Silent argparse failures with stable JSON only.

## Verification

- `PYTHONPATH=src python3 -m unittest tests.test_publication_build -v` — passed (8 tests)
- `make check` — passed (158 tests)
- `git diff --check` — passed
- Independent root/overlap/non-canonical-path probes — all rejected
- Independent atomic-directory observation — staged pair complete before one
  rename; published pair complete after it
- Independent CLI marker probe — stable JSON, empty stderr, no marker echo

## Documentation

Updated [.team/reports/T-022.md](/home/ckc/test/codex/worktrees/ice-maker-t022/.team/reports/T-022.md).

## Risks and Follow-ups

Linux `renameat2(RENAME_NOREPLACE)` is required; unsupported platforms fail
closed. Real concurrent hostile-user stress remains an operational rather than
unit-test gate.
