STATUS: DONE

## Summary

Fixed the doctor's memory parsing, Docker reachability validation, independent
security-feature detection, and configurable OCR-language checks. Added
regression tests and recorded real-host evidence.

## Verification

- `bash -n scripts/document-ingestion-doctor.sh` — passed
- `PYTHONPATH=src python3 -m unittest tests.test_document_ingestion_doctor -v` — passed (8 tests)
- `git diff --check` — passed
- Real Docker-enabled run — `host-capable`, exit 1

## Documentation

Updated `docs/runbooks/production-document-ingestion.md`.

## Risks and Follow-ups

Host parser/OCR dependencies remain unavailable; the host is not
`runtime-ready`. The dated host observation must be refreshed on deployment
hosts.
