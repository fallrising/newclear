STATUS: DONE

## Summary

Implemented and hardened the production batch-contract security boundary.
Source discovery streams bounded chunks without retaining document bytes, pins
the input root and descendant opens, rejects filesystem aliases, verifies
descriptor identity, and records deterministic content duplicates. Manifest,
configuration, result, and checkpoint values are exact, bounded, immutable, and
content-addressed. Checkpoint publication uses pinned directory descriptors,
durable no-replace links, and exact binding/digest validation.

## Verification

- Worker RED: the prior implementation buffered complete sources, accepted a
  one-item hard link, and allowed relative checkpoint roots.
- Orchestrator RED: five additional focused regressions reproduced an
  intermediate-directory swap, directly forged source evidence, unbounded
  paths/policy type failures, a swallowed directory-sync error, and checkpoint
  publication outside a swapped directory.
- `PYTHONPATH=src python3 -m unittest tests.test_document_batch -v` — passed
  (16 tests).
- `PYTHONPATH=src python3 -m unittest tests.test_document_batch tests.test_document_ingestion_doctor -v` — passed (24 tests).
- `make check` — passed (212 tests).
- `python3 -m py_compile src/ice_maker/document_batch.py tests/test_document_batch.py` — passed.
- `git diff --check` — passed.

## Documentation

The approved SDD already defines these contracts; the tracked canonical
configuration now includes the required tile-height ceiling.

## Risks and Follow-ups

Parser, OCR, CLI, and service wiring remain deliberately outside this task.
These contracts require Linux no-follow and descriptor-relative filesystem
semantics, matching the documented supported host floor.
