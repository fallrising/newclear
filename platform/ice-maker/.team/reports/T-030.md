STATUS: DONE

## Summary

Reworked production PDF extraction into a bounded, page-scoped Poppler/OCR route. `pdftotext` now receives deterministic UTF-8, Unix-EOL, and no-page-break arguments per page. Scanned pages use `pdftoppm -singlefile -png`'s real absolute output-prefix contract; the private scratch directory accepts exactly one regular, unlinked PNG output, validates its bound, signature, and hash, then removes it before the next page.

PDF page evidence is sealed: each chunk ID binds normalized text plus native region or ordered OCR rectangles/confidence and OCR raster hash/dimensions. Page and document evidence recompute version/config/tool/language/cache bindings, reject malformed direct values, duplicate chunks, and out-of-range page counts. Strict `pdfinfo` parsing accepts only code-owned English labels, with unique positive `Pages` and `Encrypted: no` required.

Orchestrator escalation then made supported metadata values UTF-8-safe so ordinary non-ASCII titles do not reject otherwise valid files, and changed raster publication from path prechecks plus `read_bytes()` to one bounded `O_NOFOLLOW` descriptor read with before/after identity validation.

## Verification

- RED: 2 new regressions failed before the rework: the expected `pdftoppm` output file was absent because stdout was incorrectly treated as raster data, and `canonical_pdf_chunk_id` did not bind page content.
- GREEN: 15 focused tests pass, including all-native, fully scanned, mixed, blank/no-word, malformed, encrypted, zero/over-limit, page-specific, timeout/non-zero, bad/UTF-8 metadata, unsafe text, source-digest, oversized output, missing/extra/symlink raster-output, and forged-evidence cases.
- `PYTHONPATH=src python3 -m unittest tests.test_production_extraction -v` — passed (15 tests; exit 0).
- `PYTHONPATH=src python3 -m unittest tests.test_production_extraction tests.test_extraction tests.test_document_batch -v` — passed (38 tests; exit 0).
- `python3 -m py_compile src/ice_maker/production_extraction.py tests/test_production_extraction.py` — passed (exit 0).
- `make check` — passed (227 tests; exit 0).
- `git diff --check` — passed (exit 0).

```json
{"result_schema":"document-batch-result.v1","task":"T-030","status":"passed","evidence":{"focused_tests":15,"commands":[{"command":"PYTHONPATH=src python3 -m unittest tests.test_production_extraction -v","exit_code":0},{"command":"PYTHONPATH=src python3 -m unittest tests.test_production_extraction tests.test_extraction tests.test_document_batch -v","exit_code":0},{"command":"make check","exit_code":0},{"command":"git diff --check","exit_code":0}]}}
```

## Documentation

No documentation change: `specs/active/production-document-batch/spec.md` already specifies page-level native/OCR routing and bounded local tools.

## Risks and Follow-ups

The host has not supplied real Poppler, Tesseract, or Pillow fixture/container evidence. A later container task must prove actual Poppler output labels and PNG behavior, Tesseract language availability, process limits, and a representative mixed-corpus drill. This task has no cache persistence adapter; it seals the evidence/cache-key values that a later cache owner must validate.
