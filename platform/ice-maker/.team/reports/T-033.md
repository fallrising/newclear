STATUS: DONE

## Summary

Added a digest- and snapshot-pinned document-service image, sanitized idempotent
build, and conflict-safe two-container runtime. The parser has `--network none`,
one data mount, dropped capabilities, read-only root, and fixed limits. A
separate bounded relay has only the runtime socket mount and binds exactly host
loopback; Docker port publishing is not used. Real smoke findings corrected
daemon security-option ordering, source-file read modes, Docker health-command
escaping, stale owned socket recovery, and background-process lifetime.

## Verification

- `PYTHONPATH=src python3 -m unittest tests.test_document_service -v` — passed (20 tests; 4 sandbox-only optional skips)
- `/usr/bin/env PYTHONPATH=src python3 -m unittest tests.test_document_service.DocumentServiceContainerPolicyTests -v` — passed (6 tests, no skips, host socket permission)
- `bash -n scripts/build-document-service.sh && bash -n scripts/run-document-service.sh` — passed
- `python3 -m py_compile scripts/loopback-uds-proxy.py` — passed
- `scripts/build-document-service.sh` — passed; image revision `95149ca0d10eb85b6baaa191aef5078844edadeccd4dbd7ecbe99a58eeba32ef`
- `scripts/run-document-service.sh /tmp/ice-maker-service.hspOqpGe 18080` — passed
- `curl -fsS http://127.0.0.1:18080/healthz` — passed with `{"status":"ok"}`
- Parser inspect — running/healthy, `network=none`, no ports, read-only, 2 CPU, 8 GiB, 256 PIDs, cap-drop ALL, no-new-privileges, one `/data` mount
- Relay inspect — running, host network with no Docker-published ports, read-only, 0.25 CPU, 128 MiB, 64 PIDs, cap-drop ALL, no-new-privileges, only `/run/ice-maker` mounted
- Identical second run — passed and reported `reusing healthy parser and relay`
- `git diff --check` — passed

## Documentation

Updated the living specification and plan with the measured Docker 27 port
boundary and the parser/Unix-socket/relay design.

## Risks and Follow-ups

The relay necessarily receives uploaded HTTP bytes and uses host networking,
but its implementation has no AF_INET connect path and receives no data-root,
Docker socket, Git tree, credentials, or provider capability. Rootless sandbox
and real-corpus accuracy evidence remain external gates; this is not a
`PRODUCTION_READY` claim.
