STATUS: DONE

## Summary

Added one deterministic generated control journey for exactly 70 PDF-shaped
and 30 PNG/JPEG/WebP-shaped uploads. It composes direct coordinator resume,
content-addressed reuse, FTS5 provenance, durable service upload/status, policy
retention, isolated corrupt/denied outcomes, and unsupported/over-limit upload
rejection. Expanded the operator runbook and living SDD with the supported
capability, external gates, and exact operational workflow.

The Codex worker retry left no report. The Cursor escalation stopped after an
incomplete failing draft. Orchestrator RED evidence found an invalid positional
search limit and then a malformed chunk identity that caused duplicate binding
failure; both were corrected before acceptance.

## Verification

- `PYTHONPATH=src python3 -m unittest tests.test_production_ingestion_e2e -v` — passed; 1 journey, exactly 100 final item records
- `make check` — passed; 281 tests, 4 explicit environment-only skips
- `PYTHONPATH=src python3 -m ice_maker validate production-document-batch` — passed
- `python3 -m compileall -q src tests` — passed
- `git diff --check` — passed

## Documentation

Updated `README.md`, the production ingestion runbook, and the active SDD spec,
plan, tasks, and verification tables. Commands cover doctor, image build,
loopback start, browser/API upload, status, interruption, exact-container stop,
resume, backup/cleanup boundaries, pilot, and benchmark evidence.

## Risks and Follow-ups

The generated extractor is control evidence, not Poppler/Tesseract quality
evidence. Representative owner-corpus OCR accuracy, required languages, rights
and data-class review, quality thresholds, and rootless network-disabled sandbox
evidence remain external gates. No `PRODUCTION_READY` claim is made.
