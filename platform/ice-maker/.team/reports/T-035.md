STATUS: DONE

## Summary

Implemented deterministic, immutable local study export for one independently
reviewed study. The export binds selected sources to actual extraction chunks,
analysis claims, QA records, exact approvals, source/config/tool/batch digests,
and the evidence-ledger digest. It renders the native `doc_analysis_study`
contract without raw source text or absolute paths and publishes with pinned
no-follow descriptors plus atomic no-replace rename.

The first worker result was reworked because it had no chunk evidence, returned
an absolute output path, accepted weak approval sets, and did not close path or
collision races. The replacement adds strict canonical config/input parsing,
bounded Unicode-safe Markdown fields, Public/Internal/Restricted publication
rules, Prohibited and unconfirmed-rights denial, exact-tree idempotency checks,
targeted interruption cleanup, and a local-only CLI result containing immutable
identifiers rather than filesystem paths.

## Verification

- `PYTHONPATH=src python3 -m unittest tests.test_study_export -v` — passed, 13 tests.
- `PYTHONPATH=src python3 scripts/check_repo.py` — passed.
- `PYTHONPATH=src python3 -m unittest discover -s tests -v` — passed, 201 tests.
- `PYTHONPATH=src python3 -m py_compile src/ice_maker/study_export.py src/ice_maker/study_cli.py tests/test_study_export.py` — passed.
- `git diff --check` — passed.
- `git -C /home/ckc/test/codex/doc_analysis_study status --porcelain=v1` — empty before and after; the real target repository was not modified.

## Documentation

- Added `config/study-publication.json` as the strict publication policy.
- Exported artifacts implement the target repository's README, progress, source
  inventory, analysis overview, QA review, and five-column registry contracts.

## Risks and Follow-ups

- This task creates immutable bundles only. Applying one to a target checkout
  and any Git/GitHub action remain separate T-036/T-037 operator-controlled
  boundaries.
- Real corpus accuracy and target integration are not claimed by these unit and
  repository checks.
