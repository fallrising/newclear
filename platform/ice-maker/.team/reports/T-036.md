STATUS: DONE

## Summary

Replaced the partial local publisher with a bundle-digest, repository-identity,
base-commit, and target-contract-pinned boundary. It creates an offline Git
bundle snapshot that retains the true upstream base commit, applies exactly six
allowed paths, runs the target-native validator, emits the exact bounded diff,
and atomically retains a private staging checkout only on explicit `--apply`.
No clone, fetch, push, credential, branch, commit, or remote mutation exists in
this component.

## Verification

- `PYTHONPATH=src python3 -m unittest tests.test_study_publisher -v` — passed (9 tests)
- `python3 -m py_compile src/ice_maker/study_publisher.py src/ice_maker/study_cli.py` — passed
- `git diff --check` — passed
- Real-target isolated preview and apply from local `--no-hardlinks` clone — passed at base `ad28e5c1e9f0a7ccc10cad0e1f19fad00680d62e`, contract `526fbc45d564ad099b5f770529d24a85ca800c03a7b09361bd8874e20546700e`, bundle `4fec13b757c3eaeeb7347e7c22e3a2912ad7b6edb6ff8fe1db526eac2736fd5e`
- Target-native `python3 scripts/validate_repository.py` — passed (`studies=4, markdown_files=104`)
- Staging `git rev-parse HEAD` and exact cached path list — preserved expected base and six paths
- Real `/home/ckc/test/codex/doc_analysis_study` status before/after — unchanged and clean on `agent/analyze-singlesignon`

## Documentation

CLI now exposes `study target-contract` plus explicitly pinned
`study publish-local`; the operator runbook is completed by T-034/T-037.

## Risks and Follow-ups

GitHub branch/commit/push/draft-PR capability is deliberately absent here and
belongs to T-037. The real target was only read; all mutation occurred below
`/tmp/ice-maker-study-publish.GC4pCK9Y`.
