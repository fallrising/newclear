STATUS: DONE

## Summary

Added a separate, host-only GitHub publisher for one validated T-036 staging
checkout. It defaults to dry-run and, only with explicit `--publish`, verifies
remote `main`, creates a deterministic unique study branch and commit, performs
one exact non-force push, creates or reuses one draft PR, and retains bounded
local evidence. Parser/upload/export code has no access to this capability.

The GitHub configuration is intentionally separate from the exact T-035 export
config so Git capability does not enter the bundle boundary. Code-owned values
fix repository, SSH URL, base, study/registry paths, branch prefix, absolute
executables, timeout, and output limits.

## Verification

- `PYTHONPATH=src python3 -m unittest tests.test_study_publisher -v` — passed; 14 tests
- `bash -n scripts/publish-study.sh` — passed
- `make check` — passed; 286 tests, 4 explicit environment-only skips
- `PYTHONPATH=src python3 -m ice_maker validate production-document-batch` — passed
- `python3 -m compileall -q src tests` — passed
- `git diff --check` — passed
- Fake bare remote — one exact branch, deterministic commit, one push, one draft-PR create; replay reused both and left `main` unchanged
- Real T-036 isolated staging — shell dry-run passed at base `ad28e5c1e9f0a7ccc10cad0e1f19fad00680d62e`, bundle `4fec13b757c3eaeeb7347e7c22e3a2912ad7b6edb6ff8fe1db526eac2736fd5e`, exact six paths; no remote mutation
- Actual `/home/ckc/test/codex/doc_analysis_study` checkout — remained clean on `main`

## Documentation

Added `docs/runbooks/study-publication.md` with export, isolated base, local
preview/apply, credential preflight, dry-run, explicit publish, evidence,
review, abort, retry, and recovery procedures. Updated README and the living
SDD status/evidence.

## Risks and Follow-ups

No real GitHub action was attempted. Current `gh auth status` reports an invalid
credential; the operator must re-authenticate and review least-privilege access
before an explicit publish. Fake-remote evidence does not prove GitHub branch
protection, organization policy, or connectivity. Merge remains manual and out
of scope. Real-corpus and production-sandbox gates also remain external.
