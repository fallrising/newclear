STATUS: DONE

## Summary

Fixed an intermittent real two-worker failure by holding a bounded
process-safe shared/exclusive lock on the validated SQLite index inode for each
complete connection lifetime. Concurrent initialization and publication now
serialize before SQLite open; existing-index reads remain non-creating.

## Verification

- Deterministic concurrent-constructor regression before fix — failed for the
  expected reason: the second constructor entered SQLite early
- Pre-fix service reproducer — failed on attempt 7 of 50 with `index_failed`
- Post-fix service reproducer — passed 50/50 runs
- `PYTHONPATH=src python3 -m unittest tests.test_knowledge_index -v` — passed
  (19 tests, including bounded lock timeout)
- `make check` — passed (303 tests; 5 explicit environment-only skips)
- Pinned network-disabled/read-only container selection — passed (13 tests,
  no skips)

## Documentation

Updated the production-batch living spec, task mapping, verification evidence,
and orchestration plan.

## Risks and Follow-ups

Lock contention fails closed after five seconds. Representative real
100-document capacity and rootless-host evidence remain external gates.
