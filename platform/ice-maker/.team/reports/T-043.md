STATUS: DONE

## Summary

Rebuilt the current source image, preserved the previous private state, and ran
the same representative ten-file corpus against a fresh isolated service. The
new listing, browser preview, Markdown download, safe error, and idempotent
resubmission paths passed with aggregate-only evidence.

## Verification

- Fresh upload — passed: 10 processed, 0 duplicate/rejected/failed in about
  546 seconds
- Readable HTTP smoke — passed: 10 documents, 54 pages, 16,168 chunks;
  2,458-byte listing; maximum Markdown 112,254 bytes; maximum HTML 113,420 bytes
- Human-review signal — 5,405 sub-0.80 OCR markers exposed
- Isolation — unknown source returned stable HTTP 404
- Idempotency — same completed batch returned in 0.304 seconds without another
  extraction attempt
- Runtime — healthy loopback relay; parser network disabled and root read-only

## Documentation

Updated the production runbook, readable-result verification, and team plan
with aggregate evidence only.

## Risks and Follow-ups

The 546-second observation is one loaded-host sample, not a throughput claim.
Formal body/table accuracy, a real 70-PDF/30-image benchmark, and rootless-host
evidence remain open.
