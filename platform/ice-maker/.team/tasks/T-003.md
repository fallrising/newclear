ROLE: WORKER
AGENT: codex-cheap
ID: T-003
Goal: Create Phase 0 reusable SDD templates and strict task/result JSON Schemas.
Why: Phase 1 and adapter work need deterministic, versioned contracts before implementation.
Inputs to read first: AGENTS.md, .team/tasks/T-003.md, docs/sdd/02-SDD-WORKFLOW.md, specs/active/full-build/spec.md
Scope (may touch): specs/templates/spec.md, specs/templates/plan.md, specs/templates/tasks.md, specs/templates/verification.md, orchestration/schemas/task.schema.json, orchestration/schemas/result.schema.json, .team/reports/T-003.md
Out of scope (must not touch): .team/PLAN.md, .github/**, CODEOWNERS, orchestration/policies/**, docs/sdd/**, src/**, tests/**, every path not listed above
Definition of done:
- [ ] Templates cover required SDD sections and acceptance/verification mapping.
- [ ] JSON Schemas reject missing identifiers, mutable base refs, unbounded execution, invalid paths, and incomplete evidence.
- [ ] Schemas are valid JSON and use a declared JSON Schema draft.
- [ ] Report uses the required Final Handoff format with actual command output.
Verify with: `python3 -m json.tool orchestration/schemas/task.schema.json && python3 -m json.tool orchestration/schemas/result.schema.json && git diff --check`
Budget: 14 tool calls / 20 minutes; if exceeded, stop and report PARTIAL.
Commit permission: no.
