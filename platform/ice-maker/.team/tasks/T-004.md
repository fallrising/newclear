ROLE: WORKER
AGENT: codex-cheap
ID: T-004
Goal: Add a dependency-free repository-native Phase 0 validation gate that makes the configured CI executable now.
Why: The protected CI workflow invokes `make check`; Phase 0 may not checkpoint with a locally fixable missing target.
Inputs to read first: AGENTS.md, .team/tasks/T-004.md, .github/workflows/ci.yml, CODEOWNERS, orchestration/policies/data-providers.json, orchestration/policies/protected-paths.json, orchestration/schemas/task.schema.json, orchestration/schemas/result.schema.json, docs/execution/sdd-source.sha256, specs/active/full-build/spec.md
Scope (may touch): Makefile, scripts/check_repo.py, tests/test_phase0_contracts.py, .team/reports/T-004.md
Out of scope (must not touch): .team/PLAN.md, .github/**, CODEOWNERS, orchestration/policies/**, orchestration/schemas/**, docs/sdd/**, every path not listed above
Definition of done:
- [ ] RED is recorded by running the focused test before implementation and observing the expected missing-check failure.
- [ ] `make check` uses only Python 3.11 standard library and validates tests, source hashes, JSON, trailing whitespace, secret-like tracked content, CODEOWNERS/policy coverage, and SHA-pinned workflow actions.
- [ ] Scanner output does not print credential values and excludes explicit synthetic test markers without weakening production-file scanning.
- [ ] Focused tests cover success plus malformed JSON, secret detection, unpinned action, and SDD hash mismatch.
- [ ] Report starts `STATUS: DONE` and includes Red/Green evidence with exit codes.
Verify with: `python3 -m unittest tests.test_phase0_contracts -v && make check && git diff --check`
Budget: 18 tool calls / 25 minutes; if exceeded, stop and report PARTIAL.
Commit permission: no.
