ROLE: WORKER
AGENT: codex-cheap
ID: T-005
Goal: Implement the dependency-free constrained front-matter parser and SDD validator for SDD-0001.
Why: Phase 1 requires deterministic schema/content validation before the lifecycle CLI can create specifications.
Inputs to read first: AGENTS.md, .team/tasks/T-005.md, specs/active/sdd-cli/spec.md, specs/templates/*.md, specs/active/full-build/spec.md
Scope (may touch): pyproject.toml, src/ice_maker/__init__.py, src/ice_maker/sdd.py, tests/test_sdd_validation.py, .team/reports/T-005.md
Out of scope (must not touch): .team/PLAN.md, other .team/tasks/**, specs/**, docs/sdd/**, .github/**, orchestration/policies/**, .git/**, any network or external provider
Definition of done:
- [ ] RED test demonstrates a required field/type/enum or traversal failure before implementation.
- [ ] Valid current governed SDDs and templates are parsed without a third-party dependency.
- [ ] Missing files/front matter, duplicate keys, unsupported syntax, required fields/types/enums, task identifiers, and unsafe allowed/forbidden paths fail with stable diagnostics.
- [ ] Public API returns structured validation issues and never mutates the input tree.
- [ ] Report is written in required format and lists exact evidence.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_sdd_validation -v && git diff --check`
Budget: 12 tool calls / 20 minutes; if exceeded, stop and report PARTIAL.
Commit: do not commit; orchestrator reviews and commits accepted work.
