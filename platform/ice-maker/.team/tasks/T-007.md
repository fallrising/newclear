ROLE: WORKER
AGENT: codex-cheap
ID: T-007
Goal: Make the repository CI-equivalent gate validate every active SDD.
Why: Phase 1 is complete only when malformed active specifications fail the same offline command used by CI.
Inputs to read first: AGENTS.md, .team/tasks/T-007.md, specs/active/sdd-cli/spec.md, specs/active/sdd-cli/plan.md, Makefile, .github/workflows/ci.yml, scripts/check_repo.py, src/ice_maker/sdd.py, .team/reports/T-006.md
Scope (may touch): Makefile, .github/workflows/ci.yml, scripts/check_repo.py, src/ice_maker/sdd.py, tests/test_sdd_validation.py, tests/test_phase1_ci.py, .team/reports/T-007.md
Out of scope (must not touch): .team/PLAN.md, other .team/tasks/**, specs/**, docs/sdd/**, orchestration/policies/**, CODEOWNERS, other src/**, README.md, .git/**, any network or external provider
Protected-path authorization: the master build prompt explicitly authorizes `.github/workflows/**` for Phase 0-2 only to strengthen validation/security, keep SHA-pinned actions, least privileges, bounded timeouts, and no secret exposure. Do not weaken any control.
Definition of done:
- [ ] RED test proves the current CI-equivalent gate does not correctly run source-tree tests and/or does not block an invalid active SDD.
- [ ] `make check` runs repository checks and all tests with the repository `src` directory importable, without installation or network access.
- [ ] The repository-native gate deterministically validates every non-symlink active SDD and exits nonzero with an actionable diagnostic for an invalid fixture.
- [ ] Symlinked `specs/active` or active child directories fail closed instead of being silently skipped.
- [ ] CI continues to invoke the one repository-native `make check` command, uses SHA-pinned actions, read-only contents permission, concurrency control, and a bounded timeout.
- [ ] No production dependency is added and no existing governance/security check is weakened.
- [ ] Report uses the required Final Handoff format with exact RED/GREEN evidence.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_phase1_ci -v && make check && git diff --check`
Budget: 12 tool calls / 20 minutes; if exceeded, stop and report PARTIAL.
Commit: do not commit; orchestrator reviews and commits accepted work.

## REWORK 1 — governed-file symlink boundary

Decision: REWORK. The main gate passes, but a `spec.md`, `plan.md`, `tasks.md`,
or `verification.md` symlink is still followed by the shared validator and can
read outside the repository.

- Add a RED regression proving `validate_sdd()` rejects a symlinked governed
  file without reading its target; cover the CLI/CI path through the shared
  result as appropriate.
- Make the shared validator reject a symlinked SDD directory and any symlinked
  governed file with a stable actionable issue before reading it.
- Preserve all current gate behavior, run the original required verification
  plus `PYTHONPATH=src python3 -m unittest tests.test_sdd_validation -v`, and
  update the report with exact RED/GREEN evidence.
