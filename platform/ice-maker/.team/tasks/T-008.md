ROLE: WORKER
AGENT: codex-strong escalation via gpt-5.6-terra worker
ID: T-008
Goal: Implement the fail-closed contract, process, redaction, and changed-path policy core for SDD-0002.
Why: Every later adapter depends on deterministic enforcement that cannot be expanded by repository or model input.
Inputs to read first: AGENTS.md, .team/tasks/T-008.md, specs/active/secure-execution/spec.md, specs/active/secure-execution/plan.md, orchestration/schemas/task.schema.json, orchestration/schemas/result.schema.json, orchestration/policies/data-providers.json, orchestration/policies/protected-paths.json, docs/adr/0001-local-runtime.md, docs/adr/0003-credentials-and-data.md
Scope (may touch): src/ice_maker/execution.py, tests/test_execution.py, .team/reports/T-008.md
Out of scope (must not touch): .team/PLAN.md, other .team/tasks/**, specs/**, docs/**, orchestration/**, .github/**, README.md, pyproject.toml, other src/**, other tests/**, .git/**, network, external providers, or real credentials
Definition of done:
- [ ] RED tests fail before implementation for the smallest required public API.
- [ ] A frozen task contract validates exact required/unknown fields, task/repo/SHA/paths/role/network/bounds/commands, reads the current provider-data policy, verifies the spec is a regular in-repo file, computes spec SHA-256, and exposes identity `task_id:base_sha:spec_hash`.
- [ ] Unknown providers/classes, disallowed data/provider pairs, stale/nonexistent base commits, malformed glob paths, protected/forbidden path overlap, zero/over-limit budgets, and shell-like command ambiguity fail before execution.
- [ ] Bounded execution accepts argv only (`shell=False`), uses a minimal explicit environment, starts a new session, enforces timeout and byte caps, kills the process group on timeout, and returns deterministic command evidence.
- [ ] Redaction covers supplied secret values and common token/key/password forms before output truncation/serialization; diagnostics never echo a detected secret.
- [ ] Post-run path policy obtains NUL-delimited Git changes against the immutable base, rejects invalid UTF-8/path traversal, directories/files/symlinks resolving outside the worktree, forbidden/protected paths, and anything not matching an allowed glob.
- [ ] Tests cover success plus malformed contract, provider denial, stale base, timeout cleanup, non-zero exit, redaction, truncation, forbidden path, symlink escape, and allowlist success using only temporary local fixtures.
- [ ] Report starts with `STATUS: DONE | PARTIAL | BLOCKED`, follows Final Handoff, and records exact RED/GREEN output.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_execution -v && git diff --check`
Budget: 20 tool calls / 35 minutes; if exceeded, stop and report PARTIAL.
Commit: do not commit; orchestrator reviews and commits accepted work.

## Reassignment evidence

Claude fable/high and the protocol escalation fable/xhigh each produced no
output, report, or worktree change for about five minutes, then returned
`Execution error` when interrupted. Treat these as provider transport stalls,
not implementation attempts. The orchestrator reassigned the unchanged task to
Codex Terra; all original scope, RED/GREEN, and acceptance requirements remain.

## REWORK 1 — orchestrator security findings

Decision: REWORK. Add focused regressions first and prove each fails for its
named reason; do not accept a generic later failure as coverage.

- Fix the contract test helper so every malformed-field/provider/path/command
  subcase starts with the actual valid current HEAD. Assert an actionable error
  category/message for each case. The existing default fake SHA currently masks
  multiple missing validations.
- Keep the raw task mapping compatible with the committed
  `task.schema.json`: do not require undeclared `provider` or `data_class`
  properties. Accept the selected provider as an explicit trusted function
  argument and derive `data_class` from the validated spec front matter before
  applying `data-providers.json`.
- Define stale base as a valid commit that is not the worktree's current HEAD;
  add an advanced-HEAD regression and reject before execution.
- Redact before retained-output truncation. Add a regression whose supplied
  secret crosses the byte-cap boundary and another for a recognizable token
  form. Raw buffering must remain bounded; an output-limit termination must have
  a deterministic failure exit rather than an OS signal value.
- Strengthen the timeout test with a spawned child that would create a marker
  after the parent is killed; prove the process group is gone by showing the
  marker never appears.
- Diff with rename detection disabled (or equivalent) so both sides of a rename
  are gated; add a forbidden-to-allowed rename regression.
- Remove unused imports, preserve all prior behavior, run the full required
  command, and update the report with exact REWORK RED/GREEN evidence.

## REWORK 2 — orchestrator boundary findings

Decision: REWORK and escalate to `codex-strong`. Add the regressions before
editing production code.

- Prove supplied-secret redaction when a child writes a secret one byte at a
  time across the configured byte cap. The raw reader must retain a bounded
  look-ahead window instead of killing at the first over-cap byte, redact the
  completed bounded buffer, and only then truncate retained evidence.
- Ensure retained redacted payload is capped deterministically (status markers
  may be appended separately) and incomplete recognizable private-key blocks
  redact from their header through the end of the bounded buffer.
- Reject allowed globs whose wildcard prefix can overlap protected/forbidden
  paths, including a broad extension pattern such as `**/*.md` overlapping
  `.team/**`; conservative fail-closed rejection is acceptable.
- Preserve all REWORK 1 behavior and record separate REWORK 2 RED/GREEN output.
