ROLE: WORKER
AGENT: cursor-grok high/xhigh, reassigned to codex-terra medium
ID: T-009
Goal: Add a disposable Git worktree boundary, deny-by-default sandbox command, and doctor-checked Codex adapter for SDD-0002.
Why: The execution core needs a concrete isolated workspace and first real CLI adapter before orchestration can safely publish evidence.
Inputs to read first: AGENTS.md, .team/tasks/T-009.md, specs/active/secure-execution/spec.md, specs/active/secure-execution/plan.md, src/ice_maker/execution.py, orchestration/schemas/task.schema.json, orchestration/schemas/result.schema.json, orchestration/policies/data-providers.json, docs/adr/0001-local-runtime.md, docs/execution/preflight.md, .team/reports/T-008.md
Scope (may touch): src/ice_maker/runner.py, src/ice_maker/adapters.py, tests/test_runner.py, runner/sandbox-policy.json, docs/runbooks/runner.md, .team/reports/T-009.md
Out of scope (must not touch): .team/PLAN.md, other .team/tasks/**, specs/**, docs/sdd/**, other docs/**, orchestration/**, .github/**, README.md, pyproject.toml, other src/**, other tests/**, .git/**, network, provider prompts, or real credentials
Definition of done:
- [ ] RED tests fail before implementation for worktree isolation and adapter/sandbox contracts.
- [ ] A context-managed worktree job accepts only an existing immutable commit in a local Git repository, creates a fresh detached worktree beneath a caller-owned job root, never checks out or writes `main`, and cleans the worktree plus Git registration on success, exception, and explicit cleanup.
- [ ] Job names and all paths reject absolute/traversal/symlink escape; a prior failed job cannot leave files visible in the next job.
- [ ] Sandbox policy is machine-readable and command construction requires an OCI image pinned by SHA-256 digest; it sets remove-on-exit, network none, read-only root, all capabilities dropped, no-new-privileges, bounded CPU/memory/PIDs, bounded tmpfs, one resolved worktree RW mount, and no Docker socket/host-root/credential mount.
- [ ] Unsupported runtime, unpinned image, unsafe mount, forbidden environment key, or shell string fails closed before starting a container. Runtime execution is not claimed without real rootless infrastructure evidence.
- [ ] Codex adapter exposes stable alias `builder_primary`, runs a bounded local `codex --version` doctor through the execution core, verifies the exact configured model ID, constructs non-interactive argv with workspace sandbox and output file, redirects stdin from the null device at invocation, and never places credentials in argv/prompt/result.
- [ ] Adapter input is the governed contract/minimal task prompt only; data/provider policy is checked by the execution core before argv construction. Tests use synthetic fixtures and no provider/network call.
- [ ] Runner runbook documents bootstrap prerequisites, rootless requirement, registration/egress/credential external gates, cleanup/recovery, and exact local doctor/test commands without claiming production readiness.
- [ ] Report begins with STATUS and includes exact RED/GREEN evidence; do not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_runner -v && git diff --check`
Budget: 20 tool calls / 35 minutes; if exceeded, stop and report PARTIAL.
Commit: do not commit; orchestrator reviews and commits accepted work.

## Retry evidence

The first `cursor-grok-4.6-high` dispatch produced no output, report, or
worktree change for over five minutes and was interrupted with exit 130. Treat
this as a provider transport stall, not an implementation attempt. Retry the
unchanged task once with `cursor-grok-4.6-xhigh`; all scope and acceptance rules
remain identical.

The prescribed xhigh retry repeated the same behavior with a valid Cursor
login and installed model ID: over five minutes, no output/report/code, then
exit 130 when interrupted. Do not retry Cursor a third time. The orchestrator
reassigns this unchanged task to Codex Terra medium; no Cursor result is
accepted.

## REWORK 1 — orchestrator boundary findings

Decision: REWORK. Add focused regressions first and prove the current code
fails for each named boundary before editing implementation.

- Worktree cleanup must be verified after success, body exception, and an
  explicit `cleanup()` call by checking both directory absence and
  `git worktree list --porcelain`. Do not run repository-wide `git worktree
  prune`. If remove/registration cleanup fails, keep the object in a retryable
  created state and raise; a later explicit retry must be able to complete. If
  `worktree add` partially fails, remove only the target registration/path or
  fail with actionable cleanup evidence.
- Preserve original paths before resolution. Reject repository/job-root/
  worktree/output paths with a symlink in any supplied component, job roots
  that contain or alias the source repository, and targets outside the
  canonical caller-owned job root. A prior failed job must remain invisible to
  a fresh name.
- Treat sandbox policy as an enforced contract, not informational JSON:
  validate exact version/keys/limit types and values, and prove constructed
  argv agrees with every control. Require remove-on-exit, network none,
  read-only root, drop-all, no-new-privileges, CPU/memory/PID bounds,
  `noexec,nosuid,nodev` bounded tmpfs, one worktree RW mount, and no
  host-root/Docker-socket/credential mount.
- Reject `/`, non-worktree directories, symlink-parent worktrees,
  option/path-confused OCI image references, shell executables including
  absolute paths, newline/NUL arguments, unknown or secret-like environment
  keys, and recognizable secret values. Limit environment keys to an explicit
  harmless allowlist.
- Adapter output must be a non-existing regular path strictly beneath the
  canonical worktree, never a sibling/outside path or symlinked parent. Prompt
  validation must reject recognizable secret forms without rejecting ordinary
  prose that mentions security terms.
- `doctor()` must reject nonzero, timed-out, truncated, or non-Codex version
  output; `/bin/echo` is not valid doctor evidence. Keep model selection
  alias/config-driven and reject a configured-model mismatch.
- Add a bounded `invoke()` path through the execution core so stdin is
  actually `/dev/null`, startup/timeout/output failure is preserved, and
  tests use a local fake executable only—no real provider or network call.
- Expand beyond four happy-path tests to cover every boundary above, update
  runbook/recovery and report with exact REWORK RED/GREEN evidence, and
  preserve the no-production-readiness claim.

## REWORK 2 — codex-strong escalation

Terra's first REWORK still misses verifiable acceptance boundaries. Add RED
coverage for cleanup failure followed by a successful explicit retry, an add
failure after target registration, the installed `codex-cli N.N.N` doctor
format, symlinked output parents before resolution, `../image@sha256:...`,
and sandbox construction with a malformed policy. Make policy input mandatory
for command construction and derive its exact limit/tmpfs controls from the
validated policy. Reject secret-like values in command args and environment.
On a successful adapter invocation require a newly created regular in-worktree
result file; preserve nonzero evidence without claiming success. Then run the
full focused suite plus compile and diff checks and replace the report's
machine-readable result with the final evidence.
