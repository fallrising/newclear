ROLE: WORKER
AGENT: codex-cheap
ID: T-010
Goal: Orchestrate the accepted Phase 2 boundaries and produce fail-closed branch/PR publication evidence without making external mutations.
Why: SDD-0002 is complete only when contract, isolated execution, result validation, repository gates, cleanup, and human-review publication policy compose into one observable synthetic path.
Inputs to read first: AGENTS.md, .team/tasks/T-010.md, specs/active/secure-execution/spec.md, specs/active/secure-execution/plan.md, src/ice_maker/execution.py, src/ice_maker/runner.py, src/ice_maker/adapters.py, orchestration/schemas/task.schema.json, orchestration/schemas/result.schema.json, orchestration/policies/protected-paths.json, .team/reports/T-008.md, .team/reports/T-009.md
Scope (may touch): src/ice_maker/orchestrator.py, src/ice_maker/publisher.py, tests/test_orchestration.py, .github/workflows/agent.yml, orchestration/policies/protected-paths.json, docs/verification/phase-2.md, .team/reports/T-010.md
Out of scope (must not touch): .team/PLAN.md, other .team/tasks/**, specs/**, docs/sdd/**, other docs/**, other orchestration/**, other .github/**, README.md, pyproject.toml, other src/**, other tests/**, .git/**, network, GitHub API, provider calls, real credentials, main mutation, merge, release, or deploy
Definition of done:
- [ ] Add the smallest RED tests first for one synthetic success path and important failure/policy paths; no test may invoke a real provider, network, container, GitHub API, or credential.
- [ ] Compose existing public boundaries so a valid immutable contract can become publish-ready only after adapter/result evidence, every required command, changed-path policy, and disposable-worktree cleanup all succeed. Any missing/malformed/mismatched/non-zero/truncated/timed-out evidence fails closed.
- [ ] Validate the governed result shape and bind task ID/base SHA/changed files/commands/acceptance/tests/usage to the submitted contract without silently dropping unknown fields or accepting duplicate paths.
- [ ] Publication is a pure, reviewable request boundary: allow only a scoped non-main branch, the integration base `build/full-sdd`, a draft pull request, and no merge/deploy/release operation. Reject `main`, dirty/unaccepted paths, failed orchestration, ambiguous refs, and any credential/secret in title/body/branch/evidence before any injected mutation callback can be reached.
- [ ] The synthetic end-to-end fixture proves one allowed file can reach a draft-PR request and that forbidden paths, command failure, stale identity, failed cleanup, direct-main target, and secret-bearing evidence produce no publication request.
- [ ] Add `.github/workflows/agent.yml` as manually triggered evidence only, with least-privilege read permissions, concurrency cancellation, job timeout, pinned action SHAs, `persist-credentials: false`, a non-default self-hosted/rootless label, and repository-native validation. It must not use `pull_request_target`, expose secrets, push, merge, or deploy.
- [ ] Add the new workflow path to `orchestration/policies/protected-paths.json`; tests must parse/check the policy and statically reject mutable action tags, broad permissions, missing timeout/concurrency, main mutation, or unsafe self-hosted triggers.
- [ ] `docs/verification/phase-2.md` records exact local commands/results and labels real self-hosted runner registration, rootless container execution, egress enforcement, scoped publisher credential, and actual provider invocation as external pending. It must not claim production readiness.
- [ ] Report starts with `STATUS: DONE | PARTIAL | BLOCKED`, follows Final Handoff, includes exact RED/GREEN evidence, and does not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_orchestration -v && make check && git diff --check`
Budget: 20 tool calls / 35 minutes; if exceeded, stop and report PARTIAL.
Commit: do not commit; orchestrator reviews and commits accepted work.

## REWORK 1 — orchestrator evidence findings

Decision: REWORK. The master prompt authorizes the required protected-path
change, so this rework additionally permits synchronized edits to `CODEOWNERS`
and `.github/CODEOWNERS`; no other scope changes are authorized.

- Add RED tests before implementation for an actual temporary Git repository:
  orchestration must derive changed paths through `validate_changed_paths`,
  reject a real forbidden/unaccepted dirty path, and verify the disposable
  worktree path is absent and unregistered. A caller-provided boolean or claimed
  path list is not cleanup/diff evidence.
- Require separate successful adapter invocation evidence and exact successful
  evidence for every required command. Reject wrong types, nonzero, timeout,
  truncation, missing/extra/reordered/mismatched evidence, and malformed quoted
  command text without leaking values.
- Bind all result fields to the contract: enforce finite cost not above
  `contract.max_cost_usd`, elapsed time, provider, task/base identity, safe
  unique paths, exact schema/nested shapes, and secret-free strings. Malformed
  unhashable input must fail with `ContractError`, not an incidental exception.
  Do not retain mutable result structures in publish authorization; retain a
  deterministic digest or an equivalently immutable summary.
- Bind the publication branch to the task (`agent/<task_id>`), validate Git-ref
  edge cases such as `..`, trailing dot, and `.lock`, and reject recognizable
  secrets in branch/title/body/evidence. `publish_draft` must revalidate a
  directly constructed request so the callback is never reachable by bypassing
  `build_publication_request`.
- Strengthen the workflow test to reject write permissions, non-manual triggers,
  unsafe runner labels, mutable action tags, and push/merge/deploy/release or
  secret-bearing steps. Add the exact workflow entry to both CODEOWNERS copies
  and prove all three governance files stay synchronized.
- Replace four broad tests with focused success/failure coverage for every named
  boundary. After the orchestrator-authorized CODEOWNERS fix, rerun the exact
  required command and replace PARTIAL/blocked report and verification text with
  final GREEN evidence. Keep all real infrastructure/provider gates external.

## REASSIGN — Terra evidence-state escalation

Luna's REWORK 1 is rejected despite its green tests. Independent probes proved
that an arbitrary never-created absent path with `cleanup_succeeded=False`
still returns `publishable=True`, a normal newline-bearing command output is
rejected even though `execute_argv` and the result schema permit it, and a
wrong adapter-evidence type raises incidental `AttributeError`. The rework also
derives changes from the source repository after deleting an unrelated empty
worktree, so it does not prove the changed workspace was the one cleaned.

Reassign to Codex Terra medium. In addition to the prior scope, Terra may touch
`src/ice_maker/runner.py` to expose the smallest read-only lifecycle evidence
needed by orchestration. No other scope changes are authorized.

- Replace caller-asserted cleanup with an explicit two-stage state transition:
  stage only while the exact `WorktreeJob` is active/registered at the contract
  base and derive the diff from that worktree; finalize only after that same job
  is absent/unregistered following cleanup. A never-entered job, unrelated
  absent path, false/true boolean, source-repository diff, or swapped job must
  fail. Pending evidence is never publishable.
- Preserve the staged changed-file/result/command/adapter evidence immutably and
  use canonical sorted JSON for a deterministic digest. Revalidate it at final
  publication without re-reading a deleted worktree. Reject post-stage input or
  evidence mutation.
- Validate every `CommandEvidence` type and field before access. Accept ordinary
  JSON-schema text including newline/tab while rejecting NUL/unsafe controls and
  unredacted secret forms. Bind finite cost to the contract and require exact
  ordered required commands plus distinct successful adapter evidence.
- Add focused RED cases for never-entered, still-active, swapped and failed
  cleanup; real active-worktree allowed/forbidden diffs; pending publication;
  false booleans being irrelevant/removed; wrong evidence types; newline output;
  cost over budget/NaN; raw secret in changed path/result/evidence/branch; and
  direct PublicationRequest forgery before callback.
- Keep synchronized CODEOWNERS/workflow changes and external-pending semantics.
  Replace the report with Terra RED/GREEN evidence and run the exact gate.
