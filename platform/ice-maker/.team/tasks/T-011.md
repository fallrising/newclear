ROLE: WORKER
AGENT: codex-terra
ID: T-011
Goal: Add secure OpenCode execution and Claude/Grok read-only review adapters.
Why: Phase 3 requires two executable adapter routes and an independent reviewer without model IDs, credentials, network calls, or write escalation.
Inputs to read first: `AGENTS.md`, `.team/tasks/T-011.md`, `specs/active/multi-provider-routing/spec.md`, `specs/active/multi-provider-routing/plan.md`, `src/ice_maker/adapters.py`, `src/ice_maker/execution.py`, `tests/test_runner.py`
Scope (may touch): `src/ice_maker/adapters.py`, `tests/test_adapters.py`, `.team/reports/T-011.md`
Out of scope (must not touch): every other path, especially `.team/PLAN.md`, `docs/sdd/**`, `.github/**`, `orchestration/policies/**`, credentials, network/provider calls, commits, merge, deploy
Definition of done:
- [ ] Start RED with focused fake-executable tests proving the same synthetic result fixture can be invoked through existing Codex and new OpenCode builder adapters.
- [ ] OpenCode doctor validates an actual OpenCode version shape, exact injected model, and non-interactive argv; success requires a safe in-worktree result file.
- [ ] Add a Claude/Grok read-only review adapter that accepts only a bounded regular input artifact and new result path, never a writable source workspace, and validates provider-specific doctor/version/argv evidence.
- [ ] Reject unsupported provider, alias, model, symlink/path escape, existing output, secret-like input/model/path, missing result, malformed/failed/truncated doctor, and mismatched contract provider before invocation.
- [ ] Reuse Phase 2 execution/path primitives; keep business routing out of adapters; standard library only.
- [ ] Write `STATUS: DONE | PARTIAL | BLOCKED` report with RED/GREEN evidence and exact changed files. Do not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_adapters -v && git diff --check`
Budget: 20 tool calls / 30 minutes; if exceeded, stop and report PARTIAL.

## REWORK 1 — orchestrator evidence

The four fake-CLI tests pass, but the implementation is rejected because its
version strings and invocation flags do not match the installed CLIs. Read-only
orchestrator probes observed:

- `/tmp/phark-opencode-v1.18.9/opencode --version` -> `1.18.9`; `opencode run
  --help` supports `--pure`, `-m/--model`, `--format`, `--dir`, and positional
  message, but no `--output` and no `--` result-file contract.
- `claude --version` -> `2.1.259 (Claude Code)`; non-interactive review uses
  `-p/--print`, positional prompt, `--model`, `--permission-mode plan`,
  `--permission-prompts none`, `--tools ""`, `--safe-mode`,
  `--no-session-persistence`, and `--output-format json`. There is no local
  `--input`/`--output` pair.
- `grok --version` -> `grok 1.0.13 (5e9a58528b76) [stable]`; single review uses
  `--single` or `--prompt-file`, `--model`, `--output-format json`,
  `--disable-web-search`, `--no-subagents`, `--permission-mode plan`, and
  `--tools ""`. There is no `--input`/`--output` pair.

Required rework:

- [ ] Add focused tests using these real version shapes and exact supported
  argv forms; fake CLIs must reject unknown flags rather than teaching invented
  flags.
- [ ] Capture OpenCode/reviewer stdout through `CommandEvidence`, then publish
  it to the already-validated new result path with exclusive/no-replace
  semantics; fail closed on empty, truncated, failed, missing, raced, symlinked,
  or unsafe output. Do not pass a fictitious output flag.
- [ ] Treat OpenCode as an adapter, not a data provider. Use alias
  `builder_economy`; bind it to a configured known provider and require the
  task contract provider to match (the Phase 0 matrix uses `deepseek`).
- [ ] Use `critic_independent` for Grok and `reviewer_claude` for Claude; prove
  both provider-specific argv forms and actual doctor versions.
- [ ] Review input may be read only after regular-file/symlink/size/secret checks.
  Do not give reviewers a workspace, write tools, web, subagents, session reuse,
  or implicit interactive mode.
- [ ] Remove the unused import, rerun focused and Phase 2 adapter regression
  tests, and replace the report with exact REWORK RED/GREEN evidence.
