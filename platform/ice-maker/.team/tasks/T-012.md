ROLE: WORKER
AGENT: codex-cheap
ID: T-012
Goal: Implement deterministic alias routing, explicit fallback, and an immutable task usage ledger.
Why: Phase 3 must fail closed on unhealthy/rate-limited/unsafe/over-budget providers and make every selection and attempt auditable.
Inputs to read first: `AGENTS.md`, `.team/tasks/T-012.md`, `specs/active/multi-provider-routing/spec.md`, `specs/active/multi-provider-routing/plan.md`, `orchestration/policies/data-providers.json`, `src/ice_maker/execution.py`
Scope (may touch): `src/ice_maker/routing.py`, `config/provider-routing.json`, `tests/test_routing.py`, `.team/reports/T-012.md`
Out of scope (must not touch): every other path, especially `.team/PLAN.md`, `src/ice_maker/adapters.py`, `docs/sdd/**`, `.github/**`, `orchestration/policies/**`, credentials, network/provider calls, commits, merge, deploy
Definition of done:
- [ ] Start RED with focused tests for primary selection, fail-closed unavailable route, one explicit equal-tier fallback, rate limits, unsafe data class, and cost ceiling.
- [ ] Validate exact alias-only configuration schema; do not commit actual model IDs or secret values.
- [ ] Route by role, health, rate-limited state, data class, and remaining task budget; unknown/malformed values fail closed.
- [ ] Fallback is opt-in, bounded to configured aliases at the same policy tier, and never bypasses the provider/data matrix.
- [ ] Stop automatic retry when a normalized failure signature repeats twice or attempt ceiling is reached.
- [ ] Record immutable usage entries containing provider, alias, attempt, latency, finite nonnegative cost, outcome, and failure signature; totals cannot exceed the task budget.
- [ ] Standard library only; deterministic/offline behavior; write a `STATUS:` report with RED/GREEN evidence. Do not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_routing -v && git diff --check`
Budget: 18 tool calls / 30 minutes; if exceeded, stop and report PARTIAL.

## REWORK 1 — orchestrator evidence

The six focused tests pass, but the current repository configuration has only
Codex/local builder aliases and cannot route either required OpenCode provider
or an independent reviewer. Usage identity and configuration-boundary coverage
are also incomplete.

Required rework:

- [ ] Make `config/provider-routing.json` represent the Phase 3 routes without
  model IDs: `builder_primary`/Codex, `builder_economy`/DeepSeek via OpenCode,
  `critic_independent`/Grok, and `reviewer_claude`/Claude. Configure explicit
  same-tier builder and reviewer fallbacks; fallback targets must never become
  implicit primaries.
- [ ] Test the committed configuration against the real read-only
  `orchestration/policies/data-providers.json` and prove no key/value contains a
  model ID or secret-like data.
- [ ] Validate and freeze the complete provider-policy subset consumed by
  routing, including boolean egress and disjoint known allowed/blocked classes;
  caller mutation after load must not change selection.
- [ ] Validate provider/alias identity in usage entries with the same strict
  name grammar, bound ledger attempt ceiling to the configuration maximum, and
  reject controls/secret-like failure signatures without echoing them.
- [ ] Add cases for malformed health/rate-limit values, policy mutation,
  fallback tier/role errors, reviewer fallback, and NaN/Inf/negative usage.
- [ ] Keep the API small and deterministic; replace the report with exact
  REWORK RED/GREEN evidence and rerun the full suite.
