ROLE: WORKER
AGENT: codex-terra
ID: T-020
Goal: Compose retrieval and promotion into an independently reviewed synthetic horizontal-comparison journey.
Why: Phase 5 acceptance requires an observable pattern formed from compared experiences, while preserving the truth that the fixtures are not real production evidence.
Inputs to read first: `AGENTS.md`, `.team/tasks/T-020.md`, `specs/active/knowledge-synthesis/spec.md`, `specs/active/knowledge-synthesis/plan.md`, `src/ice_maker/knowledge_graph.py`, `src/ice_maker/promotion.py`, `.team/reports/T-018.md`, `.team/reports/T-019.md`
Scope (may touch): `src/ice_maker/synthesis.py`, `tests/test_synthesis_e2e.py`, `docs/runbooks/knowledge-synthesis.md`, `.team/reports/T-020.md`
Out of scope (must not touch): every other path, especially `.team/PLAN.md`, existing implementation modules, `knowledge/**`, `docs/sdd/**`, `.github/**`, credentials, network/provider calls, commits, merge, deploy
Definition of done:
- [ ] Start RED with a marked-synthetic two-experience horizontal comparison and independent critic/human approval journey.
- [ ] Compose only accepted T-018/T-019 typed boundaries; do not duplicate or bypass their validators.
- [ ] Produce a pattern candidate that names both experience notes, records meaningful comparison evidence, and cites both exact source chunks.
- [ ] Keep unsupported inference structurally marked as a hypothesis and reject any attempt to present fixtures as real production evidence.
- [ ] Prove rejected critic, missing/stale human approval, duplicate/contradiction/orphan findings, and input reordering all fail or report deterministically as specified.
- [ ] Update the local-only runbook and report exact external-pending production corpus/identity gates. Standard library only; do not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_synthesis_e2e -v && make check && git diff --check`
Budget: 24 tool calls / 35 minutes; if exceeded, stop and report PARTIAL.

## REWORK 1 — orchestrator findings

The first implementation is locally green, but the independent retry probe
returned `promotion was rejected` and then `review decision was replayed`: a
valid knowledge decision was consumed before the invalid pattern decision was
rejected. Correct all of the following within the original scope:

- Make the two-step approval failure-atomic by preflighting both transitions
  through the accepted T-019 state machine before consuming either retained
  decision. A corrected retry after valid knowledge plus rejected/stale pattern
  review must succeed; do not duplicate promotion validation rules.
- Remove `allow_integrity_findings`. `compose_pattern` must always fail closed
  on duplicate, contradiction, or orphan findings; callers may use
  `inspect_experiences` for deterministic diagnosis only.
- Reject mutable or unbounded experience inputs at the public boundary. Require
  the exact immutable two-item tuple already used by the acceptance journey.
- Make directly constructed `SynthesisJourney` values fail closed unless all
  cross-field invariants match a canonical composed journey: exact owned types,
  empty integrity findings, comparison/note identity agreement, stage and parent
  chain, identical builder/content/citations across grounded artifacts, a
  correctly shaped optional hypothesis, and the synthetic-only marker.
- Add focused RED regressions for each finding, update the runbook, rerun the
  original verification, and overwrite the report with the new exact evidence.
