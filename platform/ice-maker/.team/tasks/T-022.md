ROLE: WORKER
AGENT: codex-terra
ID: T-022
Goal: Add a confined filesystem and CLI boundary that publishes Markdown and HTML as one reproducible pair.
Why: Pure rendering is insufficient when path traversal, symlink swaps, partial writes, or stale collisions could detach generated books from tracked knowledge.
Inputs to read first: `AGENTS.md`, `.team/tasks/T-022.md`, `specs/active/publication-compiler/spec.md`, `specs/active/publication-compiler/plan.md`, `src/ice_maker/publication.py`, `.team/reports/T-021.md`, `src/ice_maker/knowledge_store.py`
Scope (may touch): `src/ice_maker/publication_build.py`, `src/ice_maker/publication_cli.py`, `tests/test_publication_build.py`, `.team/reports/T-022.md`
Out of scope (must not touch): every other path, especially `publication.py`, tracked fixtures/manifests, `.team/PLAN.md`, `docs/sdd/**`, `.github/**`, credentials, network, Git mutation, merge, deploy
Definition of done:
- [x] Start RED with a temp-repository build, output deletion/rebuild, collision, symlink, and injected second-format failure scenarios.
- [x] Load a strict manifest and every declared knowledge source beneath one exact repository root without following symlink components or accepting generated-output sources.
- [x] Bind loaded bytes to the T-021 compiler and a caller-supplied exact source commit; do not invoke Git or infer mutable environment state.
- [x] Publish Markdown and HTML below a confined output root as one complete pair using private staging and no-follow operations; on any failure retain the prior complete pair or publish neither.
- [x] Expose a non-interactive argparse CLI with stable JSON success/error output that does not echo unsafe content. Standard library only; do not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_publication_build -v && make check && git diff --check`
Budget: 28 tool calls / 40 minutes; if exceeded, stop and report PARTIAL.

## REWORK 1 — orchestrator findings

The first pass reported 153 green tests, but independent probes produced:

```
ancestor_symlink ACCEPTED
source_output_overlap ACCEPTED:.../repo/knowledge/book.md
pair_visibility_during_publish [(True, False), (True, True)]
argparse ... unrecognized arguments: --definitely-secret LEAK-ME
```

Correct all of the following within the original scope:

- Reject a repository root or output root reached through any symlink component,
  not only when the final component is a link. Keep descriptor-relative,
  no-follow source reads and stable identity checks.
- Enforce the SDD boundary that generated output is strictly below the exact
  repository's `.ice-maker/` subtree. Reject output/source overlap and any
  caller-selected location outside that generated subtree.
- Publish a private staged *directory* containing both complete files as one
  atomic, exclusive, no-replace operation (the existing Linux standard-library
  `renameat2(RENAME_NOREPLACE)` pattern may be reused; fail closed when the
  primitive is unavailable). A concurrent observer must never see exactly one
  format, and a collision must never be overwritten. Return paths inside the
  published book directory.
- Change the same-build test to prove collision rejection unless the prior pair
  was explicitly deleted. Add RED fault injection for the second staged-format
  write and the final directory publication; both must leave either the old
  complete directory or no published directory. Cover ancestor/root symlinks,
  output confinement/overlap, and collision races.
- Override argparse failure behavior so missing/unknown/malformed invocations
  emit only the stable JSON error and never usage text or untrusted argument
  values on stdout/stderr. Add a subprocess or redirected-stream regression
  containing a recognizable secret marker.

Rerun the original verification and update the report with the rework evidence.

## STRONG ESCALATION — post-rework findings

REWORK 1 closed the original findings, but the orchestrator reproduced three
remaining fail-closed violations:

```
mixed_repository_snapshot True
output_creation_escaped_before_failure True
noncanonical books//book.json ... ACCEPTED
noncanonical .../.ice-maker/tmp/../publications ACCEPTED
```

The strong escalation must add RED regressions and then:

- Open and pin one exact no-follow repository directory descriptor for the
  whole build. All manifest/chapter reads and output-directory creation must be
  relative to that descriptor; never reopen the repository by pathname between
  stages. Reject if its public pathname identity changes before publication.
- Create every output-root component with descriptor-relative `mkdir/open`
  operations so a path swap cannot cause even a preparatory directory outside
  the pinned repository.
- Reject non-canonical repository, manifest, and output paths containing
  traversal or repeated separators before normalization.
- Preserve the accepted atomic no-replace book-directory publication and silent
  JSON CLI behavior, then rerun focused/full/diff gates and independent probes.
