ROLE: WORKER
AGENT: codex-cheap
ID: T-023
Goal: Prove a clean two-book rebuild that reuses one tracked synthetic knowledge source.
Why: Phase 6 acceptance is the observable deletion-and-rebuild journey, not merely unit-level renderer behavior.
Inputs to read first: `AGENTS.md`, `.team/tasks/T-023.md`, `specs/active/publication-compiler/spec.md`, `specs/active/publication-compiler/plan.md`, `src/ice_maker/publication.py`, `src/ice_maker/publication_build.py`, `src/ice_maker/publication_cli.py`, `.team/reports/T-021.md`, `.team/reports/T-022.md`
Scope (may touch): `books/system-design.json`, `books/reliability-patterns.json`, `knowledge/50-patterns/synthetic-idempotency.json`, `tests/test_publication_e2e.py`, `docs/runbooks/publication.md`, `.team/reports/T-023.md`
Out of scope (must not touch): every other path, especially compiler/build implementation, `.team/PLAN.md`, `docs/sdd/**`, `.github/**`, credentials, network, Git mutation, merge, deploy
Definition of done:
- [ ] Start RED with a subprocess journey that deletes the ignored output root, builds both tracked manifests, snapshots both format bytes, deletes them, and rebuilds byte-identically.
- [ ] Both manifests reference the exact same marked-synthetic Phase 5 pattern record; every rendered book includes that chapter, both SHA-256 chunk citations, and the caller-supplied source commit SHA.
- [ ] Prove generated files are not tracked inputs, neither manifest can relabel the fixture as real production evidence, and a missing/tampered shared source produces no partial new output.
- [ ] Document exact local rebuild commands, generated-file policy, and external-pending rights/signing/publishing gates. Standard library only; do not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_publication_e2e -v && make check && git diff --check`
Budget: 22 tool calls / 30 minutes; if exceeded, stop and report PARTIAL.

## REWORK 1 — orchestrator review

The functional journey passed, but the test recursively deletes
`output.parent`, which is the entire `.ice-maker` runtime-state directory.
That exceeds the authorized disposable output boundary.

- Delete only `.ice-maker/publications`; never recursively delete its parent.
- Put an unrelated sentinel under `.ice-maker` and prove it survives initial
  cleanup, deletion/rebuild, and final cleanup.
- Keep the exact-manifest-key/shared-chapter assertions and missing/tampered
  fail-closed assertions. Also prove those failures preserve unrelated state.
- Make the runbook's deletion target equally narrow.
- Rerun the original verification and replace the report with current evidence.
