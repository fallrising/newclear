ROLE: WORKER
AGENT: claude
ID: T-025
Goal: Implement strict hardening configuration and safe local operational drill primitives.
Why: Phase 7 must test ephemeral isolation, short-lived identity, deny-by-default egress, restore, rotation, and compromise response without touching real infrastructure or secrets.
Inputs to read first: `AGENTS.md`, `.team/tasks/T-025.md`, `specs/active/operational-hardening/spec.md`, `specs/active/operational-hardening/plan.md`, `src/ice_maker/runner.py`, `tests/test_runner.py`, `docs/sdd/06-SECURITY-OPERATIONS.md`
Scope (may touch): `src/ice_maker/hardening.py`, `tests/test_hardening.py`, `ops/hardening/runner.json`, `ops/hardening/identity.json`, `ops/hardening/egress.json`, `.team/reports/T-025.md`
Out of scope (must not touch): every other path, especially `.team/PLAN.md`, observability/evidence implementation, `.github/**`, `orchestration/policies/**`, credentials, network, Git mutation, merge, deploy
Definition of done:
- [ ] Begin RED for strict duplicate-key/unknown-field JSON and reject persistent/multi-job/privileged runners, writable roots, added capabilities, static or overlong credentials, and direct/default-allow egress.
- [ ] Validate tracked contracts requiring one-job ephemeral rootless execution, read-only root, no-new-privileges, dropped capabilities, bounded resources, metadata-only OIDC at no more than 15 minutes, and HTTPS allowlists enforced only through a named proxy.
- [ ] Implement deterministic local drills: hash-exact backup/restore with tamper, traversal, symlink, collision, and partial-state rejection; opaque metadata-only credential rotation; ordered detect/revoke/destroy/rebuild/canary compromise response; deny unlisted or ambiguous egress; fail closed at a finite cost ceiling.
- [ ] No network, shell, timestamps, randomness, secret values, external deletion, deployment, or new dependency. Inputs/results are bounded and immutable; write `STATUS: DONE|PARTIAL|BLOCKED` report; do not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_hardening -v && make check && git diff --check`
Budget: 28 tool calls / 40 minutes; if exceeded, stop and report PARTIAL.

## REWORK 1 — orchestrator review

The orchestrator ran the previously denied command: all 13 focused tests pass.
Independent probes still reject the implementation:

- A directly constructed default-allow/direct `EgressPolicy` containing only
  `evil.invalid` is accepted by `evaluate_egress` and returns `allowed=True`.
- `respond_to_compromise(..., list(COMPROMISE_STEPS))` and
  `enforce_cost_ceiling(..., [1])` accept mutable inputs despite the contract.
- A deterministic race that creates the destination immediately before
  `Path.rename` replaces that directory and reports a successful restore.
- Path pre-check plus later `read_bytes`/`write_bytes` remains vulnerable to
  ancestor/source swaps and mixed backup snapshots.

Add isolated RED regressions, then seal and revalidate every public contract and
result. Require exact tuple steps/charges. Load configs and all backup entries
through pinned no-follow descriptors, stage relative to one pinned destination
parent descriptor, and publish the complete restore directory with Linux atomic
no-replace semantics (fail closed when unavailable). A collision/race must keep
the existing destination and clean only task-owned staging state. Do not use a
path-based recursive cleanup that can delete a swapped directory. Rerun focused,
full, and diff gates; update the report to DONE only when verified.
