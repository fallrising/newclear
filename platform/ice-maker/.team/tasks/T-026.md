ROLE: WORKER
AGENT: codex-cheap
ID: T-026
Goal: Prove the complete deterministic local Phase 7 hardening journey and document the operator response paths.
Why: Accepted observability/evidence and hardening primitives must compose into one honest DEVELOPMENT_COMPLETE result while preserving every real production dependency as external evidence.
Inputs to read first: `AGENTS.md`, `.team/tasks/T-026.md`, `specs/active/operational-hardening/spec.md`, `src/ice_maker/operations.py`, `src/ice_maker/hardening.py`, `tests/test_operations.py`, `tests/test_hardening.py`, `docs/sdd/06-SECURITY-OPERATIONS.md`, `ops/observability/*.json`, `ops/hardening/*.json`
Scope (may touch): `tests/test_hardening_e2e.py`, `docs/runbooks/operational-hardening.md`, `.team/reports/T-026.md`
Out of scope (must not touch): every other path, especially production code, `.team/PLAN.md`, `specs/**`, `docs/sdd/**`, `.github/**`, `orchestration/policies/**`, credentials, network, Git mutation, merge, deploy
Definition of done:
- [ ] Begin RED with one end-to-end test that loads every tracked hardening/metrics/dashboard contract, creates a temporary hash-bound backup, performs an exact restore, metadata-only credential rotation, ordered compromise response, explicit allowed/denied egress checks, a finite cost-ceiling check, and a complete healthy metric snapshot.
- [ ] Bind each required local control to immutable `LocalEvidence` using one exact 40-lowercase-hex synthetic commit and SHA-256 artifact digests derived from actual local outputs. Evaluate the exact tuple of controls and assert readiness is exactly `DEVELOPMENT_COMPLETE`, with `EXTERNAL_PRODUCTION_GATES` unchanged and no `PRODUCTION_READY` outcome.
- [ ] Prove a corrupt restore, over-cost/threshold snapshot, missing/replayed/malformed evidence, or incomplete control cannot become development-complete. Repeat the healthy journey deterministically and compare results.
- [ ] Add an actionable runbook for runner offline/unhealthy; provider auth, rate limit, or model removal; cost spike/stuck agent; secret detection in tree/history/log; compromised runner rebuild; knowledge-source takedown/redaction; and ledger/object restore. Include exact local verification commands, recovery/escalation steps, external evidence checklist, and the explicit no-production-claim rule.
- [ ] Standard library, local temporary state, no network/shell/timestamp/randomness/secret value/new dependency. Write `STATUS: DONE|PARTIAL|BLOCKED` report and do not commit.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_hardening_e2e -v && make check && git diff --check`
Budget: 20 tool calls / 30 minutes; if exceeded, stop and report PARTIAL.

## REWORK 1 — orchestrator review

The focused and full gates pass, but the negative evidence claim is not proved.
After observing threshold and over-cost failures, the test creates a separate
complete tuple of arbitrary passed evidence; an independent probe confirms that
exact tuple returns `DEVELOPMENT_COMPLETE`. Refactor the test-only composed
journey so it appends each control's evidence only after that actual validation
or drill succeeds, and add corrupt-restore, halted-over-cost, and threshold-
violation journey cases whose readiness is `LOCAL_EVIDENCE_INVALID` because the
corresponding evidence was never emitted. Bind runner, identity, dashboard, and
egress decision values—not merely their tracked file bytes—into actual artifact
digests. Keep the healthy repeated journey deterministic. In the runbook, give
exact repository-native commands for secret scanning and knowledge takedown/
rebuild verification. Update the report so its claims match the assertions,
then rerun all three gates. Do not change production code or scope.
