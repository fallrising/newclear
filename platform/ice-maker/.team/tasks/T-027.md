ROLE: WORKER
AGENT: codex-cheap
ID: T-027
Goal: Add a reproducible, read-only document-ingestion capability doctor and record the measured host floor.
Why: SDD-0008 needs evidence-based capacity classification before parser/OCR/service implementation; the owner explicitly requested a shell-like evaluator and local infra scripts.
Inputs to read first: AGENTS.md; /home/ckc/test/codex/ice-maker/specs/active/production-document-batch/spec.md; /home/ckc/test/codex/ice-maker/specs/active/production-document-batch/plan.md; runner/sandbox-policy.json
Scope (may touch): scripts/document-ingestion-doctor.sh; tests/test_document_ingestion_doctor.py; docs/runbooks/production-document-ingestion.md; .team/reports/T-027.md
Out of scope (must not touch): every other path; installing packages; changing host configuration; Docker image build; production extraction; service code; .team/PLAN.md
Definition of done:
- [ ] RED tests cover CPU, available-memory, disk, Linux/x86-64, Python 3.11+, Docker daemon/security features, parser/OCR commands, Tesseract languages, GPU observation, stable JSON, and no-write behavior through injected fixture commands/data rather than host assumptions.
- [ ] The shell script supports readable default output plus `--json`; classifies `unsupported`, `host-capable`, and `runtime-ready` without treating GPU absence as failure.
- [ ] Thresholds are the SDD floor: 4 CPUs, 8 GiB available RAM, 20 GiB free data-volume space; all numeric comparisons use bytes and fail closed on malformed observations.
- [ ] `host-capable` requires the host floor and reachable Docker with cgroup resource limits, seccomp, and AppArmor; `runtime-ready` additionally requires Poppler commands, Tesseract, configured OCR languages, and Pillow/FastAPI/Uvicorn/python-multipart availability.
- [ ] The script is read-only, does not print secrets or environment values, uses no network, creates no persistent files, and returns 0 only for runtime-ready, 1 for host-capable, and 2 for unsupported.
- [ ] The runbook records this machine's observed 6 CPU, 25 GiB total/16 GiB available RAM, 147 GiB free disk, no GPU, usable Docker security/cgroups, Python 3.11, and missing host parser/OCR/pip as a dated observation—not a hard-coded result.
- [ ] Focused tests and shell syntax checks pass.
Verify with: `bash -n scripts/document-ingestion-doctor.sh && PYTHONPATH=src python3 -m unittest tests.test_document_ingestion_doctor -v`
Budget: 12 tool calls / 30 minutes; if exceeded, stop and report PARTIAL.

## Orchestrator review: REWORK 1

The focused fixture tests pass, but the real-host run is incorrect and the
implementation does not yet satisfy the evidence contract:

- `/proc/meminfo` conversion uses awk's default numeric formatting and emits
  scientific notation (`1.70322e+10` here), so a capable 16 GiB-available host
  is classified with `available_memory_bytes: null`. Emit an exact integer and
  add a regression test using a realistic meminfo fixture without the direct
  byte override.
- Docker reachability is inferred from non-empty stdout after `docker info`
  even when the daemon command fails; Docker can print client details before a
  daemon permission error. Require a successful daemon query. Test failure with
  non-empty stdout.
- Detect and report cgroup version/limits, seccomp, and AppArmor independently;
  do not copy one combined boolean into all three JSON fields. Use the observed
  Docker 27 format (`SecurityOptions` contains `name=apparmor`,
  `name=seccomp,profile=builtin`, `name=cgroupns`; cgroup version is `2`) and
  add a negative test where one feature is missing.
- Configured OCR languages must be explicit and configurable (default
  `eng,chi_tra` for this pilot), all must be present, and JSON must report the
  required and observed language lists without leaking unrelated environment
  values. Add success and one-language-missing tests.
- The actual host execution must classify this machine as `host-capable` when
  run with Docker daemon access, while truthfully reporting that host parser/OCR
  dependencies are absent. Record that exact execution in the report.

Re-run the original verification command plus `git diff --check`.
