ROLE: WORKER
AGENT: codex-terra
ID: T-028
Goal: Add strict production batch manifest, configuration, source-discovery, and immutable checkpoint/result contracts.
Why: The real PDF/image adapters, resumable CLI, and upload service need one fail-closed data contract before any untrusted parser is invoked.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; src/ice_maker/knowledge_store.py; src/ice_maker/knowledge_cli.py; tests/test_knowledge_store.py
Scope (may touch): config/document-ingestion.json; src/ice_maker/document_batch.py; tests/test_document_batch.py; .team/reports/T-028.md
Out of scope (must not touch): every other path; production parser/OCR calls; CLI/service wiring; package dependencies; host or Docker configuration; .team/PLAN.md
Definition of done:
- [ ] RED tests first cover one valid canonical manifest and failures for duplicate JSON keys, non-canonical bytes, unknown/missing fields, 0 or 101 items, duplicate normalized paths, invalid rights/data class/languages, absolute/traversal/NUL paths, symlinked root/components/files, non-regular/empty/unsupported-signature/oversized inputs, alias/hard-link duplicates, and source mutation during descriptor read.
- [ ] A tracked canonical JSON configuration declares item/file/aggregate/PDF-page/decoded-pixel/tile/overlap/OCR-output/timeout/worker/error ceilings. Code-owned maxima reject tracked or caller overrides above the supported envelope; the baseline allows 100 items and at most two workers.
- [ ] Accepted signatures are PDF, PNG, JPEG, and WebP independent of extension. Discovery is relative to one explicit absolute input root, resolves no links, opens no-follow regular descriptors, verifies identity before/after bounded streaming, computes SHA-256, and never stores absolute paths or raw bytes in batch evidence.
- [ ] The validated manifest and ordered item contract are immutable/frozen and canonical by normalized relative path. Duplicate content is explicit without re-reading via an unsafe path; policy metadata remains per item.
- [ ] Define stable bounded item states `processed`, `duplicate`, `rejected`, `failed`; transition rules cannot rewrite terminal success or convert failure into success without a new validated run. Canonical batch result counts exactly match item records and contain no document/extracted text.
- [ ] Content-addressed checkpoints use canonical JSON, validate their digest/schema/config/source binding on reuse, publish atomically without replacement, reject symlink/path escape/forgery/partial bytes, and make same-input re-entry idempotent.
- [ ] The module performs no parser/OCR/network/provider/Git action. Tests use only generated local fixtures and deterministic seams; no host dependency is assumed.
- [ ] Focused tests and `git diff --check` pass, and the report records RED then GREEN evidence.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_document_batch -v && git diff --check`
Budget: 16 tool calls / 30 minutes; if exceeded, stop and report PARTIAL.

## Orchestrator review: REWORK 1

The three aggregate tests pass, but the task is not accepted: the requested
adversarial matrix is largely absent and review found contract violations.
Address all items below with focused regressions before changing production
code, then rerun the original gate.

- Expand tests to cover the explicit Definition of Done rather than collapsing
  it into three happy-path methods. At minimum prove 0/101 items, duplicate and
  unsorted normalized paths/languages, every unsafe path form, unknown fields,
  all four signatures, empty/unsupported/oversized sources, aggregate limit,
  final/intermediate/root symlinks, hard-link aliases, short read/mutation via a
  deterministic seam, config unknown/negative/bool/above-max values, result
  schema/bounds, every illegal transition, checkpoint schema/field/hash/status/
  digest/binding forgery, symlinked checkpoint paths, and no-replace re-entry.
- Enforce `config.limits.max_items` when validating a manifest, not only the
  hard-coded 100 maximum. Add a baseline `tile_height_pixels` limit and validate
  `tile_overlap_pixels < tile_height_pixels`; area and height are different
  units.
- Do not use a fixed nine-language allowlist. Validate canonical Tesseract
  language identifiers syntactically; installed/allowed availability belongs
  to the doctor/runtime boundary. Keep unique sorted tuples.
- Detect duplicate content by SHA-256 and expose a deterministic `duplicate_of`
  relative path on later descriptors so downstream work can reuse extraction.
  Reject filesystem aliases by `(st_dev, st_ino)` separately.
- `_read_file` must loop over regular-file reads and verify descriptor identity
  before/after. Source discovery must verify bytes read equals the original
  size. Add injected read/open seams or an equivalent deterministic regression
  for short read and mutation; do not rely on timing.
- `make_result` currently accepts missing/unbounded/malformed fields and mutable
  nested dictionaries. Define an exact per-status record schema, validate SHA
  and bounded safe reason codes (not arbitrary extracted text), reject nested
  text keys, verify counts on serialization, and return an immutable public
  contract or canonical bytes only.
- `transition_status` currently permits `failed -> duplicate` and
  `rejected -> processed/duplicate`. No rejected/failed terminal outcome may
  become either success status in the same run.
- Checkpoint publication currently validates only `schema_version`; checkpoint
  loading accepts unknown/missing fields and malformed hashes/status. Enforce
  one exact schema and bindings both before publication and after load. Create
  checkpoint directories with the repository's no-symlink directory pattern;
  reject traversal/symlink/non-directory targets and preserve no-replace
  semantics.
- Keep the implementation readable and reviewable; split compound statements
  and long lines. The report must state the expanded test count and actual RED
  failure(s), not classify the adversarial matrix as future work.

## Orchestrator review: REASSIGN after failed REWORK 1

Nine tests pass, but the first rework remains below the explicit contract and
is reassigned to Codex Terra. Preserve the correct fixes, add focused RED
regressions for every finding below, and then make the smallest maintainable
changes needed to satisfy them.

- Source discovery must hash in bounded chunks without retaining an entire
  256 MiB file in memory. Preserve only the signature prefix, byte count, and
  digest, and prove multi-read streaming plus short/extra-read mutation cases.
- Reject a source descriptor whose `st_nlink != 1`, even when only one alias is
  present in the manifest; retain the repeated-inode check as defense in depth.
- Prove configured file and aggregate byte limits, empty and non-regular input,
  and root/intermediate/final symlink rejection. The current tests cover only a
  final symlink and omit the named bounds.
- Bound raw in-memory manifest bytes before JSON parsing. Bound language IDs
  and result item cardinality; directly constructed invalid manifest/result
  values must fail with `BatchContractError`, not `AttributeError` or an
  unbounded serialization.
- Checkpoint publication/loading must reject relative or `..`-containing paths
  and symlinked root/intermediate/final paths, validate exact missing/unknown
  fields, hashes and statuses, reject partial/digest-forged bytes, preserve an
  existing different destination, and prove identical re-entry is idempotent.
  Use a confined absolute checkpoint directory/path contract and make durable
  atomic publication explicit (including directory sync where supported).
- Complete the configuration matrix for zero, negative, bool, unknown, every
  above-code maximum, and invalid overlap/height relationships. Do not leave
  the requested cases implicit in one `max_items` assertion.
- Refactor one-line compound statements and overlong expressions so the
  security boundary is independently reviewable. Do not alter the accepted
  doctor files or any downstream parser/service code.
