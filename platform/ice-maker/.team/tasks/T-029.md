ROLE: WORKER
AGENT: codex-terra
ID: T-029
Goal: Add bounded PNG/JPEG/WebP decoding, long-image tiling, Tesseract TSV parsing, overlap deduplication, and original-coordinate evidence.
Why: The corpus includes about 30 long screenshots; their text is useful only if hostile image inputs are bounded and every OCR word remains traceable to its original rectangle.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; config/document-ingestion.json; src/ice_maker/document_batch.py; src/ice_maker/extraction.py; tests/test_document_batch.py
Scope (may touch): pyproject.toml; src/ice_maker/production_extraction.py; tests/test_production_extraction.py; .team/reports/T-029.md
Out of scope (must not touch): every other path; PDF routing; batch CLI/service; Docker/host configuration; provider/network/Git behavior; .team/PLAN.md
Definition of done:
- [ ] RED tests first cover accepted PNG/JPEG/WebP signatures independent of extension, wrong/mismatched/truncated signatures, zero/over-limit dimensions or pixels, Pillow decompression-bomb/error paths, unavailable codec/dependency, and source digest mutation/binding.
- [ ] Pillow is an explicit pinned optional production dependency and imported only at the adapter boundary; supported formats are allowlisted and decoded pixels/mode are checked against code-owned and tracked limits before full conversion. Tests remain deterministic through injected decoder/runner seams when the dependency is absent on the host.
- [ ] A pure tiler deterministically emits complete vertical coverage with configured bounded tile height and overlap, never emits a zero/negative/out-of-range rectangle, and handles images shorter than/equal to/taller than one tile.
- [ ] Tesseract is invoked as a configured absolute executable using an argv sequence (`shell=False`), minimal deterministic environment, private temporary directory, bounded timeout/output, new process group, at most configured concurrency, and cleanup/kill of the process group on timeout. No input value becomes a shell fragment.
- [ ] Strict TSV parsing requires the supported header/column count, UTF-8, finite confidence, bounded integer coordinates, non-empty safe text, in-tile boxes, and total output/word ceilings; malformed or secret-like output publishes no success.
- [ ] Accepted word boxes translate to exact original-image coordinates. Only exact duplicate `(normalized text, absolute rectangle)` words caused by overlap are removed; canonical reading order and confidence aggregation are deterministic.
- [ ] Public frozen result values include source SHA-256, method/version/config digest, image dimensions, tile/word regions, normalized bounded text, and confidence without raw image bytes, absolute paths, temporary names, timestamps, or locale-dependent fields.
- [ ] Missing tools/codecs/languages, non-zero exit, timeout, malformed stdout, excessive stderr/stdout, and unsafe text raise stable bounded adapter errors and leave no cache/success artifact.
- [ ] Legacy synthetic extraction remains unchanged. Focused tests and `git diff --check` pass, and the report records RED then GREEN evidence plus any untested real-runtime path.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_production_extraction -v && git diff --check`
Budget: 20 tool calls / 35 minutes; if exceeded, stop and report PARTIAL.

## Orchestrator review: REWORK 1

The six tests pass but do not establish the requested adapter. Add focused RED
regressions for every item below before production changes, then rerun the gate.

- Provide one public integrated raster operation that revalidates bounded
  source bytes against the T-028 descriptor/hash, decodes, checks actual codec
  against signature, crops/encodes every tile in a private directory, invokes
  OCR, strictly parses each TSV, translates/deduplicates, and returns one
  validated result. Any tile failure publishes no result.
- Remove raw raster data from public/repr/equality evidence. Every public frozen
  dataclass (`DecodedImage`, `OcrWord`, `RasterExtraction` as applicable) must
  reject direct construction with invalid type/hash/format/dimension/mode/
  method/version/config/tool/confidence/text/box/tile values. Result evidence
  must include the verified Tesseract version digest/value required by the
  original task and use confidence compatible with downstream 0..1 chunks.
- A decoder must return its actual codec. Reject signature/codec mismatch,
  truncated data, missing Pillow/codec, `DecompressionBombWarning` and
  `DecompressionBombError`, wrong mode, zero/oversized dimensions, decoded
  pixels, tile pixels, and source/hash mutation. Use the reviewed current pin
  `Pillow==12.3.0`, not stale 10.4.0.
- Use only exact normalized text plus exact absolute rectangle for overlap
  deduplication. `Same` and `same` at one rectangle are distinct. Bound input
  cardinality before building dictionaries or joined output.
- Parse all 12 TSV columns and supported hierarchy rows, including legitimate
  `conf=-1` non-word rows, while requiring canonical integers, page/box bounds,
  supported levels, bounded rows/bytes, word confidence, UTF-8, and safe Unicode
  without bidi/surrogate/noncharacter/secret-like content. Unknown/malformed
  hierarchy or header-only success must fail in the integrated path.
- Validate the executable and input as absolute no-link regular paths, canonical
  unique sorted language tuples, and an explicit verified installed-language
  set/tool version. Injected runners must return exact byte fields and stable
  errors for timeout/nonzero/malformed/oversized results.
- The real runner must impose output file limits while the process is running,
  kill/reap the entire new process group on timeout, nonzero, output violation,
  and completion cleanup, and leave no scratch artifacts. Tests use a process
  factory/poll seam to prove cleanup; do not claim group cleanup from
  `subprocess.run` alone.
- Enforce at most configured `max_workers` (hard maximum two) in the public
  integrated adapter with a deterministic concurrency seam/test. Caller limits
  may only lower the exact T-028 code-owned/tracked envelope; unknown, missing,
  bool, negative, or above-max values fail closed.
- Expand tests for the complete Definition of Done, plus legacy synthetic
  extraction. Keep files readable and record the actual RED failures and test
  count in the report.
