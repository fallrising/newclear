ROLE: WORKER
AGENT: codex-terra
ID: T-030
Goal: Extend the production extractor with bounded native, scanned, and mixed PDF page routing and exact page provenance.
Why: About 70 corpus files are PDFs and may mix searchable text with scanned pages; page-level routing avoids unnecessary OCR while preserving evidence.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; config/document-ingestion.json; src/ice_maker/document_batch.py; src/ice_maker/production_extraction.py; tests/test_production_extraction.py
Scope (may touch): src/ice_maker/production_extraction.py; tests/test_production_extraction.py; .team/reports/T-030.md
Out of scope (must not touch): every other path; batch CLI/service; dependency/container setup; legacy synthetic extraction; provider/network/Git behavior; .team/PLAN.md
Definition of done:
- [ ] RED tests first cover a native-text PDF, fully scanned PDF, mixed native/scanned PDF, blank page, encrypted PDF, malformed/truncated PDF, zero/over-limit pages, tool unavailable/non-zero/timeout/malformed output, page-count disagreement, oversized text/raster output, and source mutation/digest mismatch.
- [ ] PDF identity and bounded bytes come from the T-028 source contract. `pdfinfo`, `pdftotext`, and `pdftoppm` are configured absolute tools invoked only through the T-029 no-shell bounded process boundary with deterministic locale/environment and complete process-group cleanup.
- [ ] `pdfinfo` output is strictly parsed for a positive page count within config bounds and encryption status; unknown, duplicate, contradictory, locale-dependent, or out-of-range values fail closed.
- [ ] Each page is routed independently in order: accept bounded normalized native UTF-8 text only when it crosses an explicit useful-text threshold; otherwise rasterize only that page and send it through the existing image/OCR adapter. No full-document OCR fallback hides a page-specific failure.
- [ ] A mixed PDF result records one immutable entry per page with source SHA-256, 1-based page, method (`pdf-text` or `ocr`), text region or OCR rectangles, confidence, extractor/config/tool-version digests, and deterministic chunk IDs. No absolute path, temp name, timestamp, raw bytes, or unbounded tool output is retained.
- [ ] Native and OCR text undergo the same bounded normalization and secret-like rejection; any failed page makes that item fail with no item-level success artifact, while the caller can continue unrelated items.
- [ ] Cache keys bind source hash, production extractor/config digest, exact Poppler/Tesseract versions, and language tuple; forged, partial, mismatched, or stale cache evidence is rejected instead of silently trusted.
- [ ] Existing long-image and synthetic extractor tests remain green. Focused tests and `git diff --check` pass, and the report records RED/GREEN plus paths requiring later real-container evidence.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_production_extraction -v && git diff --check`
Budget: 20 tool calls / 35 minutes; if exceeded, stop and report PARTIAL.

## Orchestrator review: REWORK 1

Eleven focused tests pass, but only two cover PDF behavior and several required
boundaries are either absent or contradicted. Add RED regressions before fixes.

- Correct the actual Poppler contract: `pdftoppm` writes
  `<absolute-output-prefix>.png`; it does not publish PNG bytes to stdout. Run
  it with `-singlefile -png`, enforce RLIMIT/live process bounds, then open
  exactly the expected no-link regular output, prove its size/hash/signature,
  reject missing/extra/symlinked outputs, and clean the private directory.
  The fake runner must create that output file so the test mirrors production.
- Bind each `chunk_id` to its bounded normalized text and native region or
  canonical OCR rectangles/confidence, in addition to source/page/method/config/
  tools. The independent probe constructed two different page texts with the
  same accepted chunk ID. Retain raster SHA-256 and image dimensions for OCR
  page provenance, and validate them against every rectangle.
- Seal every field of `PdfPageExtraction` and `PdfExtraction`: direct malformed
  types must raise the stable adapter error rather than `TypeError`; page count
  must stay within the code maximum; pages/chunks/words must be canonical and
  unique; config/tool/cache/language/version bindings and safe metadata must be
  recomputed rather than trusted. Add forged/partial/stale evidence tests.
- Expand the RED matrix for fully scanned, blank/no-word, all-native, and mixed
  PDFs; malformed/truncated signature; page-specific native/raster/OCR failure;
  timeout/nonzero/malformed/oversized stdout and stderr; unsafe Unicode/secret
  native text; output page disagreement; missing/unavailable/symlinked tools;
  language/version mutation; source mutation; and no partial result/scratch.
- Use explicit deterministic `pdftotext` arguments including UTF-8, Unix EOL,
  and no page breaks. Strictly validate each `pdfinfo` line against a documented
  code-owned label set (or a narrowly justified forward-compatible grammar),
  required unique `Pages`/`Encrypted` values, and bounded metadata bytes. The
  current parser silently accepts `Unknown: value`, contradicting the task.
- Refactor the PDF addition into readable helpers. Keep all Poppler operations
  on the T-029 no-shell/output-RLIMIT/process-group boundary and record the
  actual RED and GREEN counts in the report.
