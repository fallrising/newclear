ROLE: WORKER
AGENT: codex-cheap
ID: T-031
Goal: Wire resumable `knowledge batch`, production FTS5 compatibility, atomic per-item progress, and bounded canonical reporting.
Why: The owner needs one command that processes a mixed corpus, survives interruption, reuses validated extraction, and makes Chinese/long-image results searchable without bypassing review gates.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; src/ice_maker/document_batch.py; src/ice_maker/production_extraction.py; src/ice_maker/knowledge_cli.py; src/ice_maker/knowledge_index.py; tests/test_document_batch.py; tests/test_knowledge_index.py
Scope (may touch): src/ice_maker/document_batch.py; src/ice_maker/knowledge_cli.py; src/ice_maker/knowledge_index.py; tests/test_document_batch.py; tests/test_knowledge_index.py; .team/reports/T-031.md
Out of scope (must not touch): every other path; HTTP/service/container code; cloud/provider calls; promotion; study/Git publication; .team/PLAN.md
Definition of done:
- [ ] RED tests first prove `knowledge batch --manifest <path> --input-root <absolute> [--config <path>]` processes a mixed ordered batch through injected production extraction, indexes only successful items, returns canonical JSON, and exits 0 only when every item is `processed` or `duplicate`.
- [ ] The coordinator validates manifest/config/root before parsing, reads/hash-binds each source through T-028, respects per-item rights/data class/languages, identifies content duplicates, and never invokes extraction or index work for rejected/duplicate items.
- [ ] Each item is failure-isolated with stable bounded safe reason codes. Reports contain exactly one canonical-order record per manifest item and exact status counts, hashes/evidence IDs only—never raw bytes, extracted text, exception repr, absolute paths, temporary names, environment values, or secrets.
- [ ] Successful extraction/cache/index evidence and progress checkpoints publish atomically without replacement under the existing ignored local state root. Resume validates manifest/config/tool/source bindings and skips completed/duplicate work; forged/partial/stale/symlinked checkpoints are rejected, and interrupted work cannot appear successful.
- [ ] Same-input re-entry is deterministic and produces identical canonical result bytes/evidence IDs. Changed source/config/tool/languages cannot reuse old extraction. Duplicate content reuses the first validated extraction and citations while retaining its own relative path/policy record.
- [ ] Extend `KnowledgeIndex` surgically to accept bounded NFKC-normalized printable Unicode (including Traditional Chinese), production `ocr` provenance, and large bounded text/pixel rectangles while continuing to accept and exactly validate legacy `pdf-text`/`ocr-pbm` chunks. Reject controls, bidi overrides, surrogates/noncharacters, secret-like content, invalid coordinates/methods/confidence, and chunk-ID mismatch.
- [ ] FTS5 insertion/search of generated Chinese and long-screenshot chunks is deterministic and traceable. Existing index/proposal rules, taxonomy, unpromoted status, source/chunk identities, and synthetic CLI commands remain compatible.
- [ ] A deterministic interruption seam proves resume does not call OCR again for validated completed items. A cache-forgery test proves no persisted success is trusted by filename or status alone.
- [ ] Tests prove one rejected/corrupt item does not block independent later items, but final exit is non-zero and counts remain exact. No production provider/network/Git capability is reachable from this command.
- [ ] Focused batch/index/legacy E2E tests and `git diff --check` pass; report records RED/GREEN and any remaining real-container evidence gate.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_document_batch tests.test_knowledge_index tests.test_knowledge_e2e -v && git diff --check`
Budget: 22 tool calls / 40 minutes; if exceeded, stop and report PARTIAL.

## Orchestrator review — REWORK 1

The first pass is rejected. Replace manual smoke claims with persisted RED
tests and address every item below; do not report DONE from the old 33 tests.

- Make the normal CLI path usable. Introduce an immutable, validated explicit
  production toolchain binding (absolute Poppler/Tesseract paths, exact version
  evidence, installed languages, and a digest), and compose `extract_pdf` or
  `extract_raster` based on the validated descriptor. An injected extractor may
  remain only as a deterministic test seam. No network/provider fallback.
- Do not change the shared `canonical_json` encoder. Canonicalize immutable
  mappings locally into ordinary exact dictionaries.
- Discover the full manifest once. Reject rights=`denied` before extraction;
  retain each item's policy/language binding; avoid extraction/index calls for
  rejected and duplicate entries.
- Replace `_read_source` with a bounded descriptor-relative no-follow read loop
  that rejects hard links, short reads, source swaps, and any before/after
  identity/hash/size/signature mismatch.
- Preserve production evidence. Native text may be deterministically split into
  bounded chunks; every OCR word must retain its exact original pixel rectangle,
  text, confidence, page, source digest, and recomputed standard chunk ID.
- Define a strict, bounded cache record with exact fields and canonical bytes.
  Its key and validated contents must bind source, config, toolchain/version,
  requested languages, policy, extraction evidence, and chunks. Publish and
  load by pinned no-follow directories, without replacement, with directory
  sync. Reject partial, forged, stale, symlinked, aliased, or non-canonical
  cache/checkpoint values; never trust a filename/status alone.
- Mark an item complete only after idempotent indexing succeeds. A validated
  cache may resume extraction after interruption, but a forged/missing index
  cannot be represented as completed. Add an explicit interruption seam and
  prove rerun does not invoke OCR for already validated extraction.
- Use owned exception types/context for a closed set of bounded reason codes;
  do not inspect exception text or catch `BaseException`. Results must never
  expose extracted text, exception repr, absolute paths, temp names, environment
  values, or secrets.
- Index validation must enforce page/UTF-8/text bounds, NFKC, printable Unicode,
  no controls/bidi/surrogates/noncharacters/secrets, exact method/confidence/ID,
  and rectangles whose positive extents and sums stay within the coordinate
  limit. Add deterministic Traditional Chinese insertion/search coverage.
- Persist tests covering success, duplicate/policy handling, mixed failure
  isolation and CLI exit, real extractor selection, exact OCR rectangles,
  Unicode, source short-read/swap, changed config/tool/languages, interruption,
  cache/checkpoint forgery/symlink/partial state, canonical safe output, and
  network/Git/provider unreachability. Record actual RED then GREEN evidence.
