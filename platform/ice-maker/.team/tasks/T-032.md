ROLE: WORKER
AGENT: codex-terra
ID: T-032
Goal: Add a loopback-only multipart batch-upload service backed entirely by durable batch state and the accepted resumable coordinator.
Why: The owner needs to start one local service, upload up to 100 real PDFs/screenshots, and inspect bounded progress across process restarts without weakening the CLI or local-only security boundaries.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; config/document-ingestion.json; src/ice_maker/document_batch.py; src/ice_maker/knowledge_cli.py; tests/test_document_batch.py
Scope (may touch): pyproject.toml; src/ice_maker/document_service.py; tests/test_document_service.py; .team/reports/T-032.md
Out of scope (must not touch): every other path; parser/OCR semantics; Docker/scripts; provider/network clients; Git/study publication; promotion; .team/PLAN.md
Definition of done:
- [ ] RED API/component tests cover `GET /healthz`, one valid multi-file `POST /api/batches` returning 202 promptly, `GET /api/batches/{id}`, process restart/recovery, unknown IDs, and the smallest useful browser upload page.
- [ ] Pin only the owner-approved FastAPI 0.141.1, Uvicorn 0.52.4, and python-multipart 0.0.32 production extras in the repository's existing dependency style; imports remain at the service boundary so non-service legacy tests still run without them.
- [ ] Upload streams/spools each file into one caller-owned absolute state root without reading the full request/file into memory, enforces exactly 1–100 files plus per-file/aggregate bounds while copying, derives no filesystem path from an untrusted filename, validates signature, and atomically publishes either one complete immutable batch or nothing.
- [ ] Reject empty, duplicate/Unicode-confusable/absolute/traversal/control/overlong filenames, unsupported signatures, incomplete streams, declared/actual size disagreement, limit overrun, symlinked state paths, and malformed policy metadata with stable non-echoing 4xx errors and no retained partial upload/manifest.
- [ ] Batch ID is deterministic/content-addressed from the canonical manifest and source digests, contains no path/time/secret, and retry of an identical complete upload is idempotent without replacement; a conflicting collision fails closed.
- [ ] Accepted data is handed to the same production batch coordinator used by the CLI. Submission persists `queued` before returning 202; status truth is reconstructed from canonical state/checkpoints/results, never an in-memory Future. Restart converts abandoned running work to resumable queued work without claiming success.
- [ ] One process owns at most the configured two workers, concurrent submissions cannot exceed or bypass this bound, and item errors stay isolated. Status responses expose only bounded IDs/counts/stable reasons, never original bytes, extracted text, absolute paths, tracebacks, temporary names, or environment values.
- [ ] Startup accepts only a literal loopback bind (`127.0.0.1` or `::1`) and refuses wildcard/non-loopback/hostname binding before state mutation. The app exposes no auth/TLS/remote-production claim and has no provider, shell, Git, or GitHub capability.
- [ ] All public request/state/result values validate exact fields and immutable bounds. Atomic writes are no-replace and durable; adversarial tests cover stale/tampered/partial state, symlinks, restart during queued/running/completed work, and cleanup preserving unrelated state.
- [ ] Focused tests and `git diff --check` pass; report records RED then GREEN evidence and identifies any HTTP/runtime behavior tested only through dependency-injected seams.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_document_service -v && git diff --check`
Budget: 24 tool calls / 40 minutes; if exceeded, stop and report PARTIAL.
