ROLE: WORKER
AGENT: codex-cheap
ID: T-033
Goal: Package the document service in a pinned, constrained, reproducible local container with idempotent build and run scripts.
Why: This host meets the capacity floor but lacks parser/OCR/Python service packages; the owner needs scripted infrastructure that does not modify the host or expose the service remotely.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; docs/runbooks/production-document-ingestion.md; scripts/document-ingestion-doctor.sh; src/ice_maker/document_service.py; tests/test_document_service.py
Scope (may touch): docker/document-service.Dockerfile; scripts/build-document-service.sh; scripts/run-document-service.sh; scripts/loopback-uds-proxy.py; src/ice_maker/document_service.py; tests/test_document_service.py; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; .team/reports/T-033.md
Out of scope (must not touch): every other path; host package installation; corpus data; provider/network calls at runtime; Git/study publication; deployment; .team/PLAN.md
Definition of done:
- [ ] RED static-policy tests parse the Dockerfile and scripts and fail for floating base/dependency inputs, shell interpolation of untrusted values, non-loopback ports, excess mounts, writable root, retained capabilities, missing resource controls, network-enabled runtime, root user, or absent health check.
- [ ] Build from a digest-pinned Python 3.11 Debian base and apt snapshot/version inputs approved by the spec; install only Poppler CLI, Tesseract, `eng` plus `chi_tra` data, Pillow/service dependencies, and the application. Record exact tool/package versions and license references as labels or build evidence without embedding secrets.
- [ ] The image uses a non-root numeric user, deterministic locale, no package caches/compiler/runtime shell dependency beyond what tools require, read-only application files, explicit entrypoint, and a health check that reaches only local `/healthz`.
- [ ] `scripts/build-document-service.sh` is strict, idempotent, takes no secret, uses a stable local image tag plus immutable source/config digest label, validates Docker daemon/security prerequisites, and leaves a failed build unclaimed.
- [ ] `scripts/run-document-service.sh` validates one explicit absolute non-symlink data directory, refuses broad/home/root paths, binds only `127.0.0.1:<validated unprivileged port>`, and invokes Docker with `--network none` while still publishing loopback port through the supported container networking design. If Docker cannot combine those semantics, document and enforce the smallest loopback-only network that permits the port; do not silently weaken either claim.
- [ ] Runtime drops all capabilities, sets no-new-privileges, read-only root, bounded CPU/memory/PIDs, tmpfs scratch, init/process cleanup, at most one explicit writable data mount, and a deterministic restart/name policy. Re-entry inspects and reuses an identical healthy container or reports an actionable conflict; it never deletes an unrelated container/volume.
- [ ] Scripts never use `eval`, download at runtime, pass credentials, mount Docker/Git/SSH sockets, mount the source corpus outside the owned data root, or grant the upload service Git/provider capability.
- [ ] Fixture/static tests cover missing Docker, unsafe directories/ports/container collisions, exact argv construction, and idempotency. A real image build/smoke is attempted only when dependency download is authorized and evidence is recorded distinctly from static pass.
- [ ] Focused tests, `bash -n` for both scripts, and `git diff --check` pass; report records RED/GREEN plus any skipped real build reason.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_document_service -v && bash -n scripts/build-document-service.sh && bash -n scripts/run-document-service.sh && git diff --check`
Budget: 20 tool calls / 35 minutes; if exceeded, stop and report PARTIAL.
