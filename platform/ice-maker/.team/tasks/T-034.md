ROLE: WORKER
AGENT: codex-cheap
ID: T-034
Goal: Prove the generated 70-PDF/30-image service journey and publish exact operator, benchmark, and limitation instructions.
Why: Unit boundaries are insufficient evidence for the owner's real workflow; one generated 100-item journey must compose upload, extraction, resume, indexing, status, and compatibility without pretending it proves real-corpus OCR quality.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; specs/active/production-document-batch/verification.md; docs/runbooks/production-document-ingestion.md; src/ice_maker/document_service.py; tests/test_document_service.py; tests/test_document_batch.py; tests/test_production_extraction.py
Scope (may touch): tests/test_production_ingestion_e2e.py; docs/runbooks/production-document-ingestion.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; specs/active/production-document-batch/tasks.md; specs/active/production-document-batch/verification.md; README.md; .team/reports/T-034.md
Out of scope (must not touch): production implementation; other tests/docs; real corpus; host installs; provider/network/Git actions; status above DEVELOPMENT_COMPLETE; .team/PLAN.md
Definition of done:
- [ ] RED BDD-style E2E tests generate exactly 70 minimal PDF and 30 PNG/JPEG/WebP inputs, including native/scanned/mixed pages, one long image, duplicate content, and isolated corrupt/unsupported/over-limit cases, without repository fixtures or network.
- [ ] The journey exercises the public service upload/status boundary and the same CLI coordinator, interrupts after deterministic completed checkpoints, reconstructs the app/coordinator, and proves completed extraction is not repeated while unfinished items resume in canonical order.
- [ ] Final safe result contains exactly 100 items and exact counts; every successful chunk is searchable with a source hash plus page or original-image rectangle citation; duplicate bytes reuse content-addressed extraction while preserving per-item policy metadata.
- [ ] Prove no provider/Git/network callback is available, raw bytes/extracted text/absolute paths do not enter summaries/logs/Git, confidential/restricted candidates remain local and unpromoted, and mixed failures return non-zero without erasing independent successes.
- [ ] Prove existing synthetic `knowledge ingest/extract/index/propose`, full test suite, SDD validation, and publication behavior remain compatible.
- [ ] Runbook gives copy-paste doctor/build/start/upload/status/stop/resume commands; manifest metadata examples; writable-state paths; backup/cleanup boundaries; 10-document pilot; and a 100-document benchmark command that records duration, non-empty page rate, sampled OCR accuracy, coordinate validity, cache reuse, failures, peak resources, and tool versions.
- [ ] Runbook clearly labels generated tests as control evidence only. It states that rootless/network-disabled sandbox evidence plus owner-attested real-corpus limits, languages, rights/data class, quality thresholds, and review remain external gates; no `PRODUCTION_READY` claim is made.
- [ ] Update the living spec/task/verification tables with actual commands, counts, exit codes, skipped real-runtime gates, and a truthful DEVELOPMENT_COMPLETE or lower assessment.
- [ ] Focused E2E, `make check`, SDD validation, byte compilation, and `git diff --check` pass; report records exact runtime evidence.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_production_ingestion_e2e -v && make check && PYTHONPATH=src python3 -m ice_maker validate production-document-batch && python3 -m compileall -q src tests && git diff --check`
Budget: 24 tool calls / 40 minutes; if exceeded, stop and report PARTIAL.
