ROLE: WORKER
AGENT: codex-terra
ID: T-036
Goal: Apply a validated study bundle into a base-pinned isolated `doc_analysis_study` checkout and return exact local diff evidence without touching the owner's active checkout.
Why: Local preservation should be directly usable, but repository mutation is a separate capability from parsing/export and must be reviewable, target-native, and safe around an already dirty working tree.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; src/ice_maker/study_export.py; src/ice_maker/study_cli.py; tests/test_study_export.py; /home/ckc/test/codex/doc_analysis_study/docs/repository-structure.md; /home/ckc/test/codex/doc_analysis_study/scripts/validate_repository.py
Scope (may touch): src/ice_maker/study_publisher.py; src/ice_maker/study_cli.py; tests/test_study_publisher.py; .team/reports/T-036.md
Out of scope (must not touch): every other path; `/home/ckc/test/codex/doc_analysis_study/**`; bundle contents; Git remote/push/commit/PR; service/parser/provider/network code; .team/PLAN.md
Definition of done:
- [ ] RED component/E2E tests use generated temporary Git repositories and prove preview then apply of one valid bundle, exact study tree/registry diff, idempotent re-entry, target-native validator execution, and no mutation to a separately dirty checkout.
- [ ] Accept only an explicit absolute isolated checkout root plus exact expected repository identity, base commit SHA, clean detached/owned staging branch state, target contract digest, and bundle digest. Refuse current working repository, dirty/untracked/staged state, symlinked roots/components/files, wrong remote/repository, stale/diverged base, submodules, unsafe hooks/config, alternates, replace refs, or unexpected worktrees.
- [ ] Never clone or fetch here. Invoke only an absolute allowlisted Git executable with fixed argv/minimal environment/no shell/time/output bounds. Disable hooks, credential helpers, signing, pagers, external diff, filters, aliases, replace objects, and network-capable Git operations.
- [ ] Preflight the complete target snapshot through pinned no-follow descriptors. Apply only `studies/<slug>/**` and the one deterministic README registry row; reject an existing conflicting slug, path collision, case/Unicode alias, link, special file, changed target identity, or any bundle path/content/hash mismatch.
- [ ] Default operation is preview: materialize in a publisher-owned temporary isolated checkout/worktree, run `python3 scripts/validate_repository.py`, `git diff --check`, secret/path policy checks, and return a frozen bounded exact file list plus canonical diff digest without mutating the supplied base checkout.
- [ ] Explicit local apply publishes one complete validated staging checkout/directory atomically and no-replace under a caller-owned state root. It never modifies the owner's active checkout or base branch/index; same bundle/base re-entry is byte-identical, while different content at the same identity fails closed.
- [ ] After apply, recalculate tree/diff and reject changes outside the exact allowlist, raw/private/extracted directories, executable/binary/symlink entries, registry duplicates, or target-validator warnings/errors. On any failure retain the immutable source bundle, remove only publisher-owned temporary state, and preserve unrelated files.
- [ ] CLI exposes explicit `study publish-local --bundle ... --base ... --staging-root ... --expected-base ...`; output is bounded evidence only. It has no push/remote/credential/GitHub action and is not importable from the HTTP service.
- [ ] Tests cover command timeout/non-zero/malformed output, base mutation races, destination collision, partial write, validator failure, dirty/stale/wrong target, out-of-scope diff, and exact cleanup boundaries using deterministic seams.
- [ ] Focused tests and `git diff --check` pass; report records RED/GREEN and confirms the real dirty target checkout remained unchanged.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_study_publisher -v && git diff --check`
Budget: 24 tool calls / 40 minutes; if exceeded, stop and report PARTIAL.
