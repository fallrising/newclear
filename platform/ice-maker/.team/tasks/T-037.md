ROLE: WORKER
AGENT: codex-terra
ID: T-037
Goal: Add an explicit host-side GitHub publisher that turns one validated isolated study checkout into a unique branch and draft PR without main, merge, or deployment authority.
Why: The owner wants optional GitHub upload, but credentials and remote mutation must remain absent from upload/parsing/export and require a separate deliberate operator action with retained exact evidence.
Inputs to read first: AGENTS.md; specs/active/production-document-batch/spec.md; specs/active/production-document-batch/plan.md; config/study-publication.json; src/ice_maker/study_publisher.py; src/ice_maker/study_cli.py; tests/test_study_publisher.py; docs/runbooks/production-document-ingestion.md
Scope (may touch): config/study-github-publication.json; scripts/publish-study.sh; src/ice_maker/study_publisher.py; src/ice_maker/study_cli.py; tests/test_study_publisher.py; docs/runbooks/study-publication.md; README.md; specs/active/production-document-batch/spec.md; .team/reports/T-037.md
Out of scope (must not touch): every other path; `/home/ckc/test/codex/doc_analysis_study/**`; upload/parser/service code; raw/extracted inputs; GitHub Actions; main mutation; merge/release/deploy; credential management; real remote publication during tests; .team/PLAN.md
Definition of done:
- [ ] RED fake-remote E2E proves one explicit publication creates exactly one unique valid `study/<slug>-<bundle-prefix>` branch, one commit with exact approved paths, one push to that branch, and one draft-PR request targeting `main`; no test contacts GitHub or reads real credentials.
- [ ] Tracked config allowlists only remote identity `fallrising/doc_analysis_study`, base branch `main`, study path/registry row, branch prefix, absolute Git and `gh` executables, and bounded time/output. Config/caller values cannot broaden repository, path, ref, command, or capability ceilings.
- [ ] Entry requires the T-036 validated staging identity: clean checkout, exact base/tree/diff/bundle digests, target-native validator pass, exact file allowlist, publication approval, and no raw/private/prohibited content. Re-run discovers matching immutable branch/PR evidence and is idempotent; conflicting remote state fails closed.
- [ ] Git/GitHub commands use fixed argv, no shell/eval, minimal environment, disabled hooks/helpers/signing/pagers/aliases/filters/replace refs, no token arguments/logging, bounded timeout/output, and stable non-echoing errors. The publisher never reads document bytes or OCR/cache/index state.
- [ ] Before mutation, fetch/verify the allowlisted remote main commit explicitly and refuse stale/diverged/unrelated history, dirty worktree, alternate object store, submodule, unsafe remote URL, branch collision, changed diff, or unauthorized path. Commit identity/message are deterministic and contain only safe slug/bundle digest metadata.
- [ ] Push uses an exact full source/destination refspec with no force/delete/tags; draft PR uses exact head/base/repository/title/body flags. There is no code path or script option for direct-main push, force, merge, auto-merge, ready-for-review, release, deploy, workflow change, or repository creation.
- [ ] Retain canonical bounded evidence for local base/tree, commit SHA, remote URL/owner/repo, branch, pushed SHA, PR number/URL/draft state, exact paths, command exit classes, and bundle digest without credentials or raw/derived private text. A failed GitHub step never deletes the local bundle/staging evidence or rewrites main.
- [ ] `scripts/publish-study.sh` performs strict preflight and calls only the explicit publisher CLI. Default is `--dry-run`; real `--publish` is a separate opt-in and prints the exact repository/base/branch/file list before action. It refuses non-interactive ambiguity and broad/home/root paths.
- [ ] Runbook documents export → preview → isolated local apply → dry-run → explicit publish, review/abort/retry, credential scope, evidence locations, and recovery. It states that the existing dirty `doc_analysis_study` checkout is never targeted and merge remains manual/out of scope.
- [ ] Tests cover fake bare remote success plus wrong remote/base, stale SHA, changed files, secret/credential redaction, command injection, timeout, push/PR failure, branch/PR collision, replay, partial evidence, and absence of forbidden Git/GitHub argv. Confirm `document_service` has no import/call path to this module.
- [ ] Focused tests, shell syntax, full repository checks, and `git diff --check` pass; report records RED/GREEN and states no real GitHub mutation was made.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_study_publisher -v && bash -n scripts/publish-study.sh && make check && git diff --check`
Budget: 26 tool calls / 45 minutes; if exceeded, stop and report PARTIAL.
