ROLE: ORCHESTRATOR
AGENT: codex-strong
ID: T-042
Goal: Prevent two document-service workers from transiently failing valid items when they initialize or update the same local FTS5 index concurrently.
Why: A required repeated full gate exposed a real intermittent `index_failed`; an independent 50-run reproducer failed on attempt 7.
Inputs to read first: AGENTS.md, specs/active/production-document-batch/spec.md, src/ice_maker/knowledge_index.py, src/ice_maker/document_batch.py, tests/test_knowledge_index.py, tests/test_document_service.py
Scope (may touch): specs/active/production-document-batch/spec.md, specs/active/production-document-batch/tasks.md, specs/active/production-document-batch/verification.md, src/ice_maker/knowledge_index.py, tests/test_knowledge_index.py, .team/PLAN.md, .team/reports/T-042.md
Out of scope (must not touch): schemas, extraction behavior, dependencies, private data, Git operations, runtime deployment
Definition of done:
- [x] Add a deterministic RED test proving a second constructor cannot enter SQLite while the first owns the same index.
- [x] Serialize complete index connection lifetimes with a bounded process-safe inode lock and preserve read-only existing-index behavior.
- [x] Prove 50 consecutive three-batch/two-worker reproductions without item failure.
- [x] Pass focused, full repository, container HTTP, SDD, compile, and diff gates.
Verify with: `PYTHONPATH=src python3 -m unittest tests.test_knowledge_index tests.test_document_service tests.test_readable_results -v && make check`
Budget: one strong implementation cycle; if the final gate fails for the same reason, stop and redesign.
