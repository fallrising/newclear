STATUS: DONE

## Summary

The independent-image architecture and engine split are sound. Five issues required
resolution before implementation: exact cross-engine schema semantics, bounded waiting
cardinality, authentication before body parsing, deterministic image validation, and a
clear separation between build-time acquisition and offline runtime proof.

Required decisions:

1. Normalize OCR text at line granularity while keeping Paddle layout blocks separate.
2. Bound admitted requests as well as active inference and acquire admission before
   reading or decoding the body.
3. Authenticate and reject oversized `Content-Length` before parsing request content;
   fail configuration without an API key and keep engine ports off public interfaces.
4. Validate actual image signatures, dimensions, full decoding, EXIF orientation, and
   reject animated/multi-frame inputs deterministically.
5. Bake immutable model/language artifacts into images, record checksums, and describe
   network-disabled operation as evidence rather than proof that no socket was attempted.

## Verification

- Inspection of `AGENTS.md`, `.team/tasks/T-001.md`, the specification, and plan — passed
- Contract, security, concurrency, and offline-claim review against T-001 criteria — passed

## Documentation

No source files were modified by the reviewer. The orchestrator records accepted
decisions in the living specification and plan.

## Risks and Follow-ups

Runtime accuracy, latency, memory use, and PP-Structure air-gap behavior remain
unverified until target-server testing. Optional later work includes metrics, API-key
rotation, SBOM/model-license manifests, and target-specific tuning.
