STATUS: DONE

## Summary

The T-002 rework resolves both original must-fix findings and closes the two test/documentation gaps that were actionable on this host. No remaining must-fix defect was found in the reviewed rework.

- `src/ocr_service/concurrency.py:47-62` now shields the non-cancellable thread task and
  releases its inference semaphore only from the task's completion callback. The task is
  retained until completion and late exceptions are consumed. The cancellation regression
  at `tests/test_concurrency.py:49-76` proves a second operation cannot acquire the slot
  before the cancelled caller's worker actually finishes. The revised semantics are also
  explicit in `docs/specs/deployable-ocr-services.md:117-125` and
  `docs/operations.md:61-63`.
- `.env.example:1-2` now uses `change-me`, which fails the existing minimum-length
  validation. `README.md:14-20` and `docs/operations.md:16-22` generate and export a real
  value before Compose and curl use it, so the documented path neither starts with a
  public credential nor silently sends an empty shell variable.
- The original admission-evidence gap is closed by
  `tests/test_api.py:354-392`: with one active and one queued request, a third streamed
  body receives 503 and its generator is never consumed.
- The offline acceptance criterion now claims successful operation with networking
  unavailable, while `docs/specs/deployable-ocr-services.md:142-145` retains the accurate
  warning that this does not prove absence of socket attempts.
- `src/ocr_service/backends/tesseract.py:20-32` now reports the actual Tesseract binary
  version rather than the Python wrapper version, covered by
  `tests/test_adapters.py:63-78`.

## Verification

- Re-inspection of the concurrency implementation, cancellation regression, streamed-body admission regression, safe-key quick start, offline wording, and Tesseract version metadata — passed
- Orchestrator final test evidence: 38 tests and the complete `make check` gate — passed
- Orchestrator rebuilt-image evidence: RapidOCR and Tesseract images, network-disabled real-engine warmups, and authenticated HTTP OCR smokes — passed
- `test -s .team/reports/T-002.md && grep -q '^STATUS:' .team/reports/T-002.md` — passed

## Documentation

Only this review report was updated. The revised specification, README, and operations
guide now match the implemented cancellation, credential, and offline guarantees.

## Risks and Follow-ups

- Paddle Structure remains explicitly deferred. Its hand-written adapter fixtures do not
  replace building the exact Paddle image and exercising real PaddleOCR 3.7 result objects,
  model-cache completeness, authenticated structured OCR, and `--network none` operation
  with representative text, table, and layout documents on the target server.
- Target-server accuracy, p50/p95 latency, memory, CPU, and saturation measurements remain
  necessary for all engines before production promotion.
- The Python base image is digest-pinned, but apt packages, transitive Python dependencies,
  and downloaded Paddle model inputs are not fully reproducible. Record final image
  digests and complete license/SBOM review before redistribution.
