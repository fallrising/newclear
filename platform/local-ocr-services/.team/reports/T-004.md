STATUS: DONE

## Summary

Decision: **REWORK**.

The exact rebuilt image identity is unambiguous across S-005, S-006, and S-007:
`sha256:2a80e84b499149db3a20c620e247a5c99dff82d93b4ae6ce8f86b8ffef663409`.
The recorded build, hardened HTTP smoke, real `--network none` warmup, and
same-network-namespace HTTP results are mutually consistent. Archive hashes are correctly
labelled as archive identities rather than registry digests; `RepoDigests` is correctly
empty. Historical/current evidence, non-representative synthetic fixtures, performance
limitations, remaining promotion gates, and the production authorization boundary are
also kept distinct.

The following evidence defects must be corrected before M2 acceptance:

1. The document-level CURRENT interval ends at `2026-09-04T07:50:31+02:00`, but M2 starts
   at `07:56:22`, the build runs at `08:05:55` through `08:06:24`, and later runtime
   evidence follows. Extend the interval using a recorded exact timestamp command and
   exit code so the M2 evidence is inside the declared window.
2. S-006 says the secret “remained only in the mode-0600 env file.” Docker `--env-file`
   injects the value into the container process environment and container configuration
   metadata for the container lifetime. Replace this with the narrower supported claim:
   the key was sourced from the protected file, was not placed literally in the command
   line or recorded output, and temporary containers were removed. The PLAN phrase
   “redacted test credential” should likewise describe redacted *credential evidence*,
   not the credential itself.
3. Several required S-005/S-006 claims have summarized results but no visible exact
   command. Record the exact commands and exit status for the build-window sampler and
   before/after RAM/disk observations; the image source/dependency/entrypoint/model
   verification (including Python/OS/package observations); and the S-006 readiness,
   rootfs/tmpfs, process-count, log, and resource probes. These claims are material to
   artifact provenance, resource evidence, and runtime hardening and cannot be accepted
   from prose alone.
4. Both HTTP results depend on temporary harness programs. The invocations and redacted
   output are present, but the record supplies neither an auditable harness identity nor
   its assertion logic. Record a SHA-256 and preserve/review the non-secret harness source
   (or document the exact equivalent request/assertion commands) before deleting the
   temporary stage directory. This is necessary to substantiate raw-body submission,
   header/body request-ID matching, schema checks, expected-text matching, and the
   statement that credentials/payloads were absent from logs without exposing OCR text
   or credentials.

## Verification

- `git diff --check && git diff -- .team/PLAN.md docs/verification-target-de1.md docs/specs/target-server-verification.md` (exit 0; requested tracked paths were limited to the M2 plan and target verification record) — passed
- `git rev-parse HEAD` and SHA-256 checks for the RapidOCR Dockerfile, requirements, Compose file, and entrypoint (HEAD `5e48987b71818fd834fddb71e4bda1a311757368`; all five hashes matched S-004) — passed
- Full read-only inspection of the original 1,061-line target verification record, M2 plan diff, specification, Dockerfile, entrypoint, and direct requirements (no OCR text or credential emitted) — passed

## Documentation

Only this report was added. No plan, specification, verification record, source,
fixture, Git, Docker, host, or external state was modified.

## Risks and Follow-ups

The observed runtime results may be valid, but the missing commands/harness identity
prevent independent reconstruction of several completion claims after temporary evidence
is deleted. RapidOCR must remain `CONDITIONAL`; S-010, S-011, and human-approved S-016
quality fixtures remain outstanding. Repository quality-gate execution and GitHub
persistence remain orchestrator-owned milestone-close steps.

## Re-review — 2026-09-04

Decision: **ACCEPT**.

No must-fix evidence defect remains. All four original T-004 findings are resolved.

The declared CURRENT interval now closes at `2026-09-04T08:28:13+02:00` with an exact
`date --iso-8601=seconds` result and exit 0. Credential wording now acknowledges the
temporary Docker process-environment/configuration copy and limits the claim to protected
file sourcing, absence from literal command/output and captured logs, and removal of
disposable metadata.

The preserved evidence programs match the documented hashes:
`run-rapidocr-build.sh` is
`1bb848ecdea4ff2f3511111adeb3ac19e3b2d727433b80e65956c3781711a89a`
and `rapidocr_http_smoke.py` is
`57d32221bdc686621958dcaffc9cefbddfe30643debf21602b346c506b3c5410`.
The build wrapper exposes the exact sampler/build method, while the HTTP harness visibly
asserts raw-byte submission, health, both 401 paths, schema/engine/dimensions, request-ID
header/body consistency, normalized regions, expected-result hashes, runtime security,
loopback-only networking, and log redaction.

Read-only source inspection found no literal real API key, fixture text, or recognized
OCR text in the harness; it stores only normalized expected-result SHA-256 values and a
clearly invalid authentication test value. The current HEAD remains
`5e48987b71818fd834fddb71e4bda1a311757368`; the current source aggregate
`d5e83c2ffe1f5db50d92c5a5ac83836753c57e49b440a3a7811029aae44edd5a`
and dependency/entrypoint hashes match the provenance record. Final S-006 and S-007 use
the same exact image
`sha256:2a80e84b499149db3a20c620e247a5c99dff82d93b4ae6ce8f86b8ffef663409`
and the checksum-addressed harness; their redacted HTTP/auth/schema and network-none
evidence is consistent with its assertion logic.

Re-review verification:

- Actual SHA-256 and full read-only source inspection of both `.team/evidence/M2/` programs, including boolean scans for CJK and literal key assignment — passed
- Current HEAD, source aggregate, direct requirements, entrypoint, evidence-program hashes, and absence of source/runtime-input diffs — passed
- Four original REWORK findings mapped to the corrected time, credential, exact-command, and durable-harness evidence — passed

RapidOCR remains `CONDITIONAL`; M2 acceptance does not promote it past the outstanding
S-010, S-011, and human-approved S-016 gates. Final repository checks, cleanup, plan
acceptance, commit, and GitHub persistence remain orchestrator-owned.
