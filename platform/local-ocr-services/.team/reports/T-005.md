STATUS: DONE

## Summary

Decision: **REWORK**.

The M3 record consistently uses exact image
`sha256:25b27d10815f4c167b7915829b010514fff5d9a48c32271bf05154e9469aec77`
for S-008 artifact verification and every S-009 runtime. The visible output separates
Tesseract engine 5.5.0 from pytesseract wrapper 0.3.13, reports `eng`, `chi_sim`, and
`chi_tra` plus `osd`, keeps `RepoDigests` empty, and labels archive hashes correctly.
Online and network-none HTTP/auth/schema/security evidence is internally consistent, and
the narrow offline and `CONDITIONAL` promotion claims respect the production boundary.

The preserved build wrapper and HTTP harness match their documented SHA-256 values. The
harness visibly submits raw bytes, covers health, both 401 paths, engine/schema/image and
request-ID checks, validates normalized regions, and checks same-namespace security and
loopback-only networking. Read-only inspection found no real API key, fixture text, or
recognized OCR text literal; expected results are stored only as SHA-256 values. Its
expected-result match is reported rather than asserted, matching the document's explicit
description of that result as observable quality evidence rather than a wiring gate.

The following exact-command evidence gaps must be fixed before M3 acceptance:

1. S-008 historical engine/language/manifest inspection and historical tar/source
   comparison show results and exit status but omit their exact commands. Record the
   hardened commands or a checksum-addressed preserved evidence program, including the
   comparison algorithm that establishes newline-only differences.
2. S-008 exact-image provenance is central to completion, but the record shows only the
   output of the hardened source/dependency/entrypoint/manifest/engine/language check.
   Record its exact command and exit status so engine 5.5.0, wrapper 0.3.13, all required
   languages, and embedded-source equality are independently reconstructable.
3. The build wrapper establishes the exact build and one-second raw sampler, but the M3
   record omits the exact aggregation command and the before/after RAM, filesystem,
   Docker-image-usage, container-count, and load commands that produced the reported
   values. Add those commands and exit statuses. Likewise, add the exact `stat` and
   `sha256sum` commands/exit status for both transfer archives rather than only their
   resulting sizes and hashes.
4. The M3 credential and regenerated-fixture mode/checksum claims lack their exact
   creation, permission, redacted shape-check, generation, file-type, and checksum
   commands. Record them without disclosing the key or fixture content.
5. S-009 documents log sizes/hashes and negative key/text scans, but does not show the
   exact log-capture, permission/stat, checksum, `grep -Fq -f`, or controlling assertion
   commands. The offline harness/mount copies are also said to have matching hashes
   without the exact `sha256sum` command. Add these command records and exit semantics;
   preserve the distinction between expected `grep` exit 1 and the controlling check's
   exit 0.

## Verification

- T-005 required diff command completed with exit 0; current tracked M3 changes are the plan, living specification, and target verification record, with M3 evidence sources untracked pending acceptance — passed
- HEAD `f6c21904ada9a30da4954d67c33df36099229ee3`, source aggregate `d5e83c2ffe1f5db50d92c5a5ac83836753c57e49b440a3a7811029aae44edd5a`, Dockerfile/dependency/Compose/entrypoint hashes, and both evidence-program hashes match the S-008 record — passed
- Full read-only inspection of the 69-line build wrapper and 320-line HTTP harness found the asserted behavior and no literal real credential or OCR text — passed
- Document-level CURRENT interval closes at the recorded final M3 observation `2026-09-04T08:49:28+02:00`; historical/current, performance, fixture-scope, promotion, and authorization limitations remain distinct — passed

## Documentation

Only this report was added. No plan, specification, verification record, evidence source,
Git, Docker, runtime, host, network, fixture, credential, or external state was modified.

## Risks and Follow-ups

The visible S-009 behavior may be valid, but missing command provenance prevents an
independent reviewer from reconstructing several required S-008 identity/resource and
S-009 privacy assertions after temporary stage artifacts are removed. Tesseract must
remain `CONDITIONAL`; S-010, S-011, and human-approved S-016 remain outstanding. Final
repository gates, cleanup, PLAN acceptance, commit, and GitHub persistence remain
orchestrator-owned.

## Re-review after command-provenance rework

Date: 2026-09-04

Decision: **REWORK**.

All five original command-provenance findings are substantively resolved:

1. Historical image identity and tar/source comparison now include exact hardened
   commands, a checksum-addressed comparison program, output, and exit semantics. The
   failed `sed` attempt is visible as exit 1 and is not used as acceptance evidence.
2. Exact rebuilt-image provenance now includes the complete host/image command and
   sufficient output for source, dependencies, entrypoint, four-file manifest,
   Tesseract 5.5.0, pytesseract 0.3.13, and `chi_sim,chi_tra,eng,osd`.
3. Build-window aggregation, before/after host observations, build evidence identities,
   and archive creation/stat/checksum commands now include exit status and preserve the
   shared-host attribution limitation.
4. Credential and fixture handling now includes protected creation/mode/shape commands
   plus checksum-addressed rendering logic. Private input content remains deliberately
   absent while the generated PNG identities are fixed and verified. The initial
   unreadable-log inspection failure and the corrected permission check are explicit.
5. Online log capture, permission/hash, raw `grep` exit 1 versus controlling exit 0,
   and offline harness/fixture mount-copy checksum and mode commands are now present.

One must-fix evidence issue remains: the added section is labelled **CURRENT** and
contains reviewer-rework Docker, resource, provenance, fixture, and privacy commands
which necessarily ran after the initial T-005 review, but the document-level CURRENT
interval still ends at `2026-09-04T08:49:28+02:00`, the pre-review final S-009
observation. Record an exact `date --iso-8601=seconds` closure with exit 0 and extend the
top-level interval, or relabel any purely review-only material consistently. M3 must not
be accepted while current evidence is outside its declared time window.

Read-only checks confirmed all four preserved source hashes:
`run-tesseract-build.sh` `1987f626...b571`, `compare_image_source.py`
`c88b55d6...a90f`, `generate_functional_fixtures.py` `baa1b749...f819`, and
`tesseract_http_smoke.py` `4715f37d...e67b`. HEAD remains
`f6c21904ada9a30da4954d67c33df36099229ee3`; the source aggregate remains
`d5e83c2f...d5a`. No real API key, fixture text, or recognized OCR text literal was
found in the preserved Python sources.

The ignored `.team/evidence/M3/__pycache__/` files created by `py_compile` are not in
Git status and are not evidence inputs; remove them with orchestrator-owned cleanup
before milestone close. Tesseract otherwise remains correctly `CONDITIONAL`, with
S-010, S-011, and human-approved S-016 still outstanding.

## Re-review after command-provenance rework — final timestamp correction

Date: 2026-09-04

Decision: **ACCEPT**.

The sole remaining must-fix is resolved. The target verification record's top-level
CURRENT interval now ends at `2026-09-04T09:13:40+02:00`, and its closing block records
the exact `date --iso-8601=seconds` command returning the identical value with exit 0.
The closure explicitly distinguishes later Git review/acceptance bookkeeping from target
runtime evidence.

The latest PLAN decision accurately records that all five command-provenance groups were
accepted in the prior re-review and that only this timestamp confirmation remained. The
M3 evidence directory contains the four intended source files, while generated
`__pycache__` is absent from both the filesystem check and Git status.

No evidence defect remains for T-005. This acceptance does not change Tesseract's
`CONDITIONAL` promotion state or satisfy S-010, S-011, or human-approved S-016. Final
repository gates, temporary-stage cleanup, PLAN acceptance bookkeeping, commit, and
private GitHub persistence remain orchestrator-owned.
