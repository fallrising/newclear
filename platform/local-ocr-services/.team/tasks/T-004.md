ROLE: WORKER
AGENT: codex-strong-readonly
ID: T-004
Goal: Independently audit M2 S-004 through S-007 RapidOCR evidence before milestone acceptance.
Why: The orchestrator must not accept build, security, HTTP, or network-disabled claims without an independent evidence check.
Inputs to read first: `/home/ckc/test/codex/AGENTS.md`, `AGENTS.md`, `.team/PLAN.md`, `docs/specs/target-server-verification.md`, `docs/verification-target-de1.md`, `docker/Dockerfile.rapidocr`, `docker/entrypoint.sh`, `requirements/base.txt`, `requirements/rapidocr.txt`, and the current `git diff`.
Scope (may touch): `.team/reports/T-004.md` only.
Out of scope (must not touch): every other file; do not run OCR requests, builds, containers, Git mutations, cleanup, or network operations.
Definition of done:
- [ ] Check that every S-004 through S-007 completion claim is supported by exact commands, exit codes, sufficient output, and one unambiguous artifact identity.
- [ ] Check source/image/archive identities, build/resource labels, security controls, HTTP/auth/schema evidence, `--network none` warmup and same-namespace HTTP evidence.
- [ ] Check that failures, skips, inferences, performance limitations, historical/current evidence, secrets, fixture scope, and promotion status are represented honestly.
- [ ] Check the diff for contradictions with the specification and task requirements.
- [ ] Return `ACCEPT` only if no must-fix evidence defect remains; otherwise list precise `REWORK` items.
Verify with: `git diff --check && git diff -- .team/PLAN.md docs/verification-target-de1.md docs/specs/target-server-verification.md`
Budget: 8 tool calls / 15 minutes; if exceeded, stop and report PARTIAL.

Write the report to `.team/reports/T-004.md` using the repository Final Handoff format,
prefixed with `STATUS: DONE | PARTIAL | BLOCKED`, and include an `ACCEPT` or `REWORK`
decision. Do not include API keys, OCR text, or fixture contents.
