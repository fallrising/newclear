ROLE: WORKER
AGENT: codex-strong-readonly
ID: T-005
Goal: Independently audit M3 S-008 and S-009 Tesseract evidence before milestone acceptance.
Why: Tesseract build, engine/language identity, security, real HTTP, and network-disabled claims require independent evidence review.
Inputs to read first: `/home/ckc/test/codex/AGENTS.md`, `AGENTS.md`, `.team/PLAN.md`, `docs/specs/target-server-verification.md`, `docs/verification-target-de1.md`, `docker/Dockerfile.tesseract`, `docker/entrypoint.sh`, `requirements/base.txt`, `requirements/tesseract.txt`, `.team/evidence/M3/run-tesseract-build.sh`, `.team/evidence/M3/tesseract_http_smoke.py`, and the current `git diff`.
Scope (may touch): `.team/reports/T-005.md` only.
Out of scope (must not touch): every other file; do not run OCR requests, builds, containers, Git mutations, cleanup, or network operations.
Definition of done:
- [ ] Check S-008 baseline, historical mismatch, exact build, source/dependency/manifest/archive/image identity, resource labels, Tesseract engine 5.5.0, wrapper 0.3.13, and `eng`/`chi_sim`/`chi_tra` evidence.
- [ ] Check S-009 security controls, one-worker process, health/auth/schema/request IDs, Traditional Chinese/English functional evidence, real warmup, and same-namespace `--network none` evidence against one image ID.
- [ ] Check exact commands/exit status, current/historical/inference distinctions, credential/log handling, fixture privacy/scope, performance limitations, promotion status, and production authorization boundary.
- [ ] Check the preserved evidence sources match documented behavior and contain no API key, fixture text, or recognized text literal.
- [ ] Return `ACCEPT` only if no must-fix evidence defect remains; otherwise list precise `REWORK` items.
Verify with: `git diff --check && git diff -- .team/PLAN.md docs/specs/target-server-verification.md docs/verification-target-de1.md .team/evidence/M3/run-tesseract-build.sh .team/evidence/M3/tesseract_http_smoke.py`
Budget: 8 tool calls / 15 minutes; if exceeded, stop and report PARTIAL.

Write the report to `.team/reports/T-005.md` using the repository Final Handoff format,
prefixed with `STATUS: DONE | PARTIAL | BLOCKED`. Verification bullets must end exactly
with `— passed`, `— failed`, or `— skipped`. Include an `ACCEPT` or `REWORK` decision.
Do not include API keys, OCR text, or fixture contents.
