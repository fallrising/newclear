ROLE: WORKER
AGENT: codex-cheap
ID: T-006
Goal: Implement auditable M4 fixture, benchmark, saturation, and container-wrapper evidence programs without running target load.
Why: S-010/S-011 need checksum-addressed real-HTTP measurement tooling that preserves API-key and OCR-input privacy and follows the committed M4 protocol.
Inputs to read first: AGENTS.md, .team/tasks/T-006.md, docs/specs/target-server-verification.md, .team/PLAN.md, compose.yaml, src/ocr_service/app.py, src/ocr_service/concurrency.py, .team/evidence/M2/rapidocr_http_smoke.py, .team/evidence/M3/tesseract_http_smoke.py
Scope (may touch): .team/evidence/M4/generate_synthetic_fixtures.py, .team/evidence/M4/benchmark_http.py, .team/evidence/M4/saturation_http.py, .team/evidence/M4/run_engine_validation.sh, .team/reports/T-006.md
Out of scope (must not touch): .team/PLAN.md, docs/**, src/**, tests/**, compose.yaml, Dockerfiles, requirements/**, Git state, Docker state, network, /tmp runtime stages, production or host configuration
Definition of done:
- [ ] Fixture generator creates private random text plus two different-size PNGs and a larger saturation resize without printing text, and emits only safe file metadata/checksums.
- [ ] Benchmark harness waits for readiness, records first cold OCR, performs exactly three excluded warmups and twenty measured sequential requests per base fixture, computes nearest-rank p50/p95 for client and response timings, validates/redacts response metadata, and never prints key or OCR text.
- [ ] Saturation harness separately exercises two slow-body admission holders plus pre-body overflow, at least two consistent real-engine queue timeout/recovery iterations, exact Retry-After, and no automatic retry; it fails rather than overclaiming inconclusive timing.
- [ ] The saturation program also supports an exact-image component mode that loads the real backend, observes the worker start, cancels its awaiting asyncio task, proves the inference slot stays occupied until worker completion, and then proves recovery; raw HTTP disconnect is never presented as equivalent proof.
- [ ] Shell wrapper accepts only the two frozen exact image IDs/engine profiles, starts one hardened loopback container with one worker and fixed resources, samples Docker stats once per second, labels maxima as sampled rather than true peaks, captures mode-protected logs/results, and provides trap-based cleanup limited to its named container/sampler.
- [ ] Evidence states that admission covers authenticated upload/decode/running/waiting requests, Retry-After is a static value rather than an ETA, and OCR_ENGINE_THREADS is currently not wired into either engine adapter.
- [ ] All programs expose useful --help or usage, avoid literal fixture text/API keys, and write machine-readable redacted evidence suitable for later checksum capture.
- [ ] Worker report records design choices, exact static verification commands/output, limitations, and does not claim real runtime success.
Verify with: `python3 -m py_compile .team/evidence/M4/generate_synthetic_fixtures.py .team/evidence/M4/benchmark_http.py .team/evidence/M4/saturation_http.py && sh -n .team/evidence/M4/run_engine_validation.sh && python3 .team/evidence/M4/benchmark_http.py --self-test && python3 .team/evidence/M4/saturation_http.py --self-test`
Budget: 15 tool calls / 30 minutes; if exceeded, stop and report PARTIAL.

## Orchestrator REWORK addendum — 2026-09-04

The initial delivery is rejected. Preserve the assigned scope and do not run Docker,
network, or target load. Replace the implementation so all of these are statically
auditable and self-tested:

- Render genuinely OCR-readable private randomized Traditional-Chinese and English
  text into the two base PNGs (Pillow plus an operator-supplied font path is acceptable),
  then create the saturation fixture as a deterministic resize of one base. Never print
  or commit the text. Public metadata may contain image paths/names, dimensions, byte
  sizes, and image SHA-256 only; do not publish a hash of the private text file.
- Benchmark readiness as elapsed time from a wrapper-supplied monotonic container-start
  timestamp. Record the wrapper's `docker run` return duration separately. Only the
  first OCR after readiness is cold. Perform exactly three excluded warmups and twenty
  measured sequential requests for each base fixture; retain per-fixture raw timings and
  per-fixture nearest-rank p50/p95 (N=20 means zero-based indexes 9 and 18, the 10th
  and 19th ordered samples).
- Validate the expected exact engine name, full response schema, image dimensions,
  region structure, and request-ID header/body agreement. Compare normalized recognized
  text with the protected expected-text file in memory, but emit only match booleans and
  redacted region/timing metadata.
- Parse raw HTTP responses through the complete headers and declared body length and
  parse the JSON error code. For pre-body admission overflow, assert the third request is
  503 `capacity_exhausted` with literal `Retry-After: 1` before sending its body; close
  the holder sockets and prove quiescent recovery before queue timing.
- Run exactly two real-engine queue-timeout iterations. Each must show one 200 and one
  503 `capacity_exhausted`, literal `Retry-After: 1`, timeout timing consistent with the
  configured 0.2 seconds, followed by a successful recovery request. Fail on
  inconclusive scheduling; do not cap or silently alter the requested iteration count.
- Exact-image component mode must load the selected real backend and call
  `backend.recognize()` on the supplied saturation fixture inside the observed worker.
  Cancel only the awaiting asyncio task, prove a second operation times out while real
  recognition continues, observe real worker completion, and prove capacity recovery.
- The wrapper must refuse an existing output directory or container name, track whether
  it created its container before cleanup, never export the API key, and run component
  mode in a separate exact-image `--network none` container. Capture exact image/security/
  process state, pre/during/post host contention, one-second sampled Docker stats,
  cold-start/readiness/benchmark/saturation/component results, logs, artifact hashes,
  and negative scans showing key/private input/recognized text are absent. Label sampled
  maxima as sampled rather than true peaks.
- Keep readable, maintainable Python/shell rather than compressed one-line statements.
  Report every attempted verification and failure. The final `.team/reports/T-006.md`
  must start with `STATUS: DONE | PARTIAL | BLOCKED` and use the required handoff format.

Rework verification remains the original command plus fixture-generator self-tests that
prove the two base images contain rendered text and the saturation image is the declared
resize without revealing that text.

## Orchestrator REASSIGN addendum — 2026-09-04

The prior worker's single rework remains rejected. A stronger worker now owns one
bounded repair of the same five-file scope. Rewrite freely inside that scope; do not run
Docker, network traffic, target load, or Git operations.

- Implement nearest rank exactly as `sorted(values)[ceil(p * n) - 1]`. For N=20,
  self-tests must prove p50 selects zero-based index 9 and p95 index 18. The earlier
  rework wording that said indexes 10 and 19 was an orchestrator defect and is void.
- Record exactly one cold OCR observation globally: the first OCR request immediately
  after readiness. Still perform three excluded warmups and twenty measured sequential
  requests independently for each base fixture, with separate raw values and summaries.
- Generate each final fixture string at runtime from randomized common Traditional-
  Chinese glyphs or tokens and randomized English tokens. Do not embed a complete final
  fixture sentence or API-key value in source. Render high-contrast readable text with
  an operator-supplied CJK-capable font; keep expected and recognized text only in
  protected temporary files needed for later negative scanning.
- In saturation HTTP mode, validate `OCR_API_KEY=<nonempty>` without printing it. Close
  both pre-body holders without sending bodies, then require a quiescent authenticated
  200 before starting queue measurements. Obtain exactly two accepted timeout/recovery
  iterations in no more than three recorded controlled attempts; every accepted 503
  must assert `capacity_exhausted`, literal `Retry-After: 1`, and timing consistent with
  0.2 seconds. Propagate thread failures and fail cleanly on incomplete/EOF responses.
- In component mode, create a non-literal valid placeholder with `secrets`, perform real
  `backend.recognize`, and account for the possible event-before-admission-release race.
  Prove the cancelled waiter did not release capacity before real worker completion and
  that admission subsequently recovered. Support reading the private fixture from stdin
  so a UID-10001 `--network none` container does not require a world-readable bind.
- The wrapper must remain readable and capture sanitized exact-command/exit-status
  evidence, exact image inspect data, container inspect/top and `/proc/1/status`
  (`Uid`, `Gid`, `CapEff`, `NoNewPrivs`), OOM state, and host pre/during/post observations.
  It must work when `rg` is unavailable. Its negative scan must compare artifacts with
  the actual protected API key, expected input strings, and recognized strings without
  printing them; do not claim arbitrary recognized text is absent unless it was retained
  privately and scanned. Use stdin for component fixture input and stdout for its
  redacted JSON result. Clean only unique containers the wrapper proved it created.
- Preserve the one-worker, loopback, 2-CPU, per-engine memory, read-only, dropped-cap,
  no-new-privileges, PID-limit, fixed-image, one-second stats, and limitation-label
  requirements. Require `--engine` in HTTP benchmark mode and keep output mode-protected.
- The host has no Pillow and no `rg`. The worker must report the real fixture-render
  self-test as `SKIPPED` for that exact environmental reason; the orchestrator will run
  it inside an authorized disposable Docker environment before accepting the tools.
  All pure/static self-tests must pass. The report must be factual, readable, and begin
  with exactly one status line.

Checkpoint state: the escalated repair was interrupted on human request before
completion. Resume from the retained local T-006 worktree, re-read this entire task, and
do not treat any existing candidate program or prior self-test as accepted evidence.
