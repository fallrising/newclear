STATUS: PARTIAL

## Summary

G0 RE-REVIEW RESULT: **FAIL**.

The dependency-order portion of the T-007 status blocker is repaired: `.team/PLAN.md` lists T-008
as `Ready`, T-010 as `Pending`, and T-010 depends on T-001..T-008. The OpenAPI candidate also now
contains Attention and Impact queries/schemas, expanded WorkItem detail, typed projection/reset SSE
schemas, and conditional operation-to-payload mappings.

The required stale-decision repair is incomplete. `products/hai-taskboard/docs/SDD.md` still has
`## 14. Open decisions before G0 acceptance` and still lists freezing exact REST/OpenAPI schemas,
selecting the UI information architecture, and freezing the first acceptance subset as open. Those
items conflict with the accepted decisions and artifacts recorded in `.team/PLAN.md`, so the first
T-008 definition-of-done item does not pass.

The orchestrator directed this reviewer to publish the current result immediately and run no
additional checks. Therefore the not-yet-executed JSON Schema instance validation and broader
OpenAPI/spec-graph checks remain explicitly NotRun; they are not converted into passing evidence.

## Verification

- Read the required evidence-gate skill, `.team/tasks/T-008.md`, `.team/PLAN.md`, `.team/reports/T-007.md`, HAI `AGENTS.md`, OpenAPI, SDD, UI interaction mini-SDD, traceability, and HTB-P0A-001/002 contracts — passed
- `sed -n '1,320p' .team/PLAN.md` inspection: T-008 is `Ready`, T-010 is `Pending`, and T-010 depends on T-001..T-008; command exited 0 — passed
- SDD stale-decision inspection: SDD §14 still labels already-selected REST/OpenAPI, UI IA, and first acceptance subset decisions as open; read command exited 0 — failed
- OpenAPI read inspection: `/attention` and `/impact-plans/{impact_plan_id}` queries and `AttentionSnapshot`, `AttentionItem`, `ImpactPlan`, expanded `WorkItemDetail`, `ProjectionEvent`, and `ProjectionResetRequired` schemas are present — passed
- OpenAPI read inspection: `CommandRequest` enumerates 21 operations and visibly supplies 21 `if`/`then` payload bindings to named payload schemas; the inspected payload schemas use `additionalProperties: false` — passed
- Available-tool probe: Python reported `yaml=True`, `jsonschema=True`, and `openapi_spec_validator=False`; command exited 0 — passed
- Legal and mismatched operation/payload JSON Schema instance validation — skipped (available `jsonschema` was identified, but the orchestrator then directed immediate publication with no additional checks)
- YAML/OpenAPI/spec-graph parse and graph checks for this re-review — skipped (not executed before the orchestrator's immediate-publication instruction)
- Strict OpenAPI validation/lint — skipped (`openapi_spec_validator` and standalone OpenAPI validator commands were unavailable; dependency installation was forbidden)
- Product build, test, container, CI, restore, and failure-injection checks — skipped (outside this narrow document re-review and retained as NotRun by the existing design evidence)

## Documentation

Added only `.team/reports/T-008.md`. No product/API/spec/task/plan file, other report, dependency,
implementation, credential, runtime/external system, or Git ref was changed.

## Risks and Follow-ups

- Reconcile SDD §14 with the accepted G0 decisions without creating a competing mutable execution
  status source, then independently re-review the resulting text.
- Execute and retain at least one legal and one mismatched operation/payload validation using the
  already-available JSON Schema implementation.
- Re-run the available YAML/OpenAPI/spec-graph checks and retain exact exits. Keep unavailable strict
  OpenAPI validation and pre-bootstrap product checks explicitly NotRun.
