STATUS: DONE

## Summary

Implemented the P0-A Board-first React fixture shell under `products/hai-taskboard/web/`.
It has typed static projections for all six phase lanes and one orthogonal Blocked card, WorkItem
detail, Attention, and Impact Preview surfaces. It contains no backend calls or live mutations.

Movement uses one typed `TransitionIntent` path for ordinary click, Alt+Arrow keyboard operation,
and an explicit drag test seam. Rejection preserves the fixture's authoritative lane, restores the
origin control's focus, and makes an associated polite live announcement. The fixtures make
Disconnected, Version conflict, Stale input, OutcomeUnknown, CancelRequested, and Done · Stale
inspectable as distinct text-labelled states. CSS tokens supply light/dark themes and a narrow
layout with a 320px minimum viewport and list/detail presentation.

The committed `components.json`, source-owned `Button`/`Badge`/`Card` primitives, recipe, and
SHA-256 register in `web/SHADCN_BASELINE.md` record the controlled shadcn-compatible baseline.
The pinned package root and `pnpm-lock.yaml` use Node 24.20.0, pnpm 11.25.0, React 19.2.8,
Vite 8.2.2, Tailwind 4.3.3, and the accepted test/lint pins. No `pnpm-workspace.yaml` was retained.

## Verification

- `docker pull node:24.20.0-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e` (exit 0; resolved digest matched the reproducibility contract) — passed
- `docker run --rm --user 1000:1000 -v /home/ckc/test/codex/worktrees/hai-taskboard-p0a/products/hai-taskboard/web:/workspace -w /workspace node:24.20.0-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e sh -lc 'node --version && corepack pnpm --version && corepack pnpm@11.25.0 install --frozen-lockfile --config.minimumReleaseAge=0 --store-dir /tmp/hai-taskboard-pnpm-store'` (exit 0; `v24.20.0`, `11.25.0`, 214 packages, frozen lockfile) — passed
- `docker run --rm --user 1000:1000 -v /home/ckc/test/codex/worktrees/hai-taskboard-p0a/products/hai-taskboard/web:/workspace -w /workspace node:24.20.0-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e sh -lc './node_modules/.bin/prettier --check . --ignore-path .gitignore && ./node_modules/.bin/eslint . && ./node_modules/.bin/tsc --noEmit'` (exit 0) — passed
- `docker run --rm --user 1000:1000 -v /home/ckc/test/codex/worktrees/hai-taskboard-p0a/products/hai-taskboard/web:/workspace -w /workspace node:24.20.0-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e sh -lc './node_modules/.bin/vitest run'` (exit 0; 6 files/8 tests; full `axe.run(document)` violations empty; jsdom emitted its known canvas warning, so browser colour contrast is not claimed) — passed
- `docker run --rm --user 1000:1000 -v /home/ckc/test/codex/worktrees/hai-taskboard-p0a/products/hai-taskboard/web:/workspace -w /workspace node:24.20.0-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e sh -lc './node_modules/.bin/vite build'` (exit 0; Vite 8.2.2; 22 modules) — passed
- `docker image inspect mcr.microsoft.com/playwright:v1.62.1-noble@sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e` (exit 1 expected: the image was absent and browser verification is conditional on an available/pulled image) — passed
- `rm -rf products/hai-taskboard/web/node_modules products/hai-taskboard/web/dist products/hai-taskboard/web/.pnpm-store` then `find products/hai-taskboard/web -maxdepth 1 \( -name node_modules -o -name dist -o -name .pnpm-store -o -name pnpm-workspace.yaml \) -print` (exit 0; no generated dependency/build/cache/workspace artifact remains) — passed
- `git diff --check` and `git status --short -- .team/reports/T-030.md products/hai-taskboard/web` (both exit 0; status names only the two permitted scope roots) — passed

## Documentation

- Added `web/SHADCN_BASELINE.md` as the controlled committed-source/bootstrap record, including the
  precise refresh recipe and post-format SHA-256 values.
- Product task and mini-SDD remain authoritative for the UI contract; no product specification was
  changed.

## Risks and Follow-ups

- **NotRun browser/Playwright/axe at 320px —** The exact Playwright image was not locally available
  and this task did not pull it because the task makes browser execution conditional on availability.
  Run the `@playwright/test@1.62.1` and `@axe-core/playwright@4.13.0` critical flow in
  `mcr.microsoft.com/playwright:v1.62.1-noble@sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e`
  when that image is deliberately provisioned; this is also needed for rendered colour contrast,
  zoom and coarse-pointer evidence.
- The shell deliberately consumes only typed static fixtures. T-040 must connect its same command
  intent to the authoritative API/SSE projections without introducing client-side lifecycle mutation.
