STATUS: PARTIAL

## Summary

**Decision: FAIL.** T-030's fixture source supplies the Board-first shell, static four-surface
projection, six primary phases, single-card Blocked projection, failure/attention labels, typed
click/keyboard/drag-seam intents, rejected-transition focus recovery, and no live API/provider
path. The independently repeated pinned Node checks passed.

T-030 cannot be accepted unconditionally: `package.json` and `pnpm-lock.yaml` omit the required
`@playwright/test@1.62.1` and `@axe-core/playwright@4.13.0` pins, while the candidate currently
contains ignored `web/node_modules` and `web/dist`. In addition, the controlled shadcn baseline
does not record the registry URL or icon choice required by `docs/reproducibility.md`. The mandated
browser/Playwright, rendered colour-contrast and zoom evidence remains NotRun; the jsdom axe result
is not browser accessibility acceptance.

## Verification

- Read the evidence-gate instructions, plan, T-030 task/report, T-003/T-006 reports, product
  instructions, accepted SDD/UI mini-SDD, reproducibility contract, OpenAPI, and every non-generated
  file under `products/hai-taskboard/web/` — passed
- Read-only source inspection found the four required surfaces; Draft/Ready/Developing/Review/QA/Done
  plus a single `Blocked` precedence projection; textual Disconnected, Version conflict, Stale input,
  OutcomeUnknown, CancelRequested and Done · Stale fixtures; subject/guard/cause-path detail; and no
  KPI, agent-progress, or chat-first presentation — passed
- `src/app.tsx` and `board-transition-accessibility.spec.tsx` show one `TransitionIntent` shape for
  click, Alt+Arrow keyboard, and drag seam, with only `source` differing; the rejection test confirms
  authoritative phase retention, polite announcement, and trigger focus recovery — passed
- Read-only scan of web source/manifests found no fetch, XMLHttpRequest, WebSocket, EventSource,
  provider, credential, or live mutation path — passed
- `docker image inspect node:24.20.0-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e`
  resolved `node@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e` — passed
- In an isolated temporary copy using that exact image: `node --version && corepack pnpm --version &&
  corepack pnpm@11.25.0 install --frozen-lockfile --config.minimumReleaseAge=0 --store-dir
  /tmp/hai-taskboard-pnpm-store` exited 0 (`v24.20.0`, `11.25.0`, 214 packages) — passed
- In the same isolated copy, `./node_modules/.bin/prettier --check . --ignore-path .gitignore &&
  ./node_modules/.bin/eslint . && ./node_modules/.bin/tsc --noEmit` exited 0 — passed
- In the same isolated copy, `./node_modules/.bin/vitest run` exited 0 (6 files, 8 tests); axe's
  jsdom run had zero violations but emitted the known canvas warning — passed
- In the same isolated copy, `./node_modules/.bin/vite build` exited 0 (Vite 8.2.2, 22 modules) — passed
- The required `@playwright/test@1.62.1` and `@axe-core/playwright@4.13.0` package pins are absent
  from both `package.json` and `pnpm-lock.yaml`, contrary to the reproducibility contract; the exact
  Playwright image cannot establish package-image equality for this candidate — failed
- `SHADCN_BASELINE.md` has matching hashes for its listed source files, but its refresh record omits
  the required registry URL and icon choice — failed
- Final candidate artifact audit found ignored `products/hai-taskboard/web/node_modules` and
  `products/hai-taskboard/web/dist`; this fails the required no generated dependency/build/cache
  output condition. I did not remove them because this reviewer did not create them — failed
- Browser/Playwright critical-flow, rendered colour-contrast, zoom, keyboard-only and coarse-pointer
  evidence — NotRun (the accepted image was unavailable to T-030; jsdom axe is not substituted) — skipped
- Temporary review-copy `node_modules` and `dist` were removed before this audit; no candidate source,
  manifest, lockfile, Git ref, provider, or live API state was changed by T-031 — passed

## Documentation

- Added this report only. No candidate files were edited.

## Risks and Follow-ups

- Add and lock the exact Playwright/axe packages, then provision and run the specified matching
  Playwright image for browser a11y, 320px, colour-contrast, zoom, keyboard and coarse-pointer
  critical-flow evidence.
- Complete the shadcn source-generation record with the registry URL and icon choice, retaining
  reviewed hashes.
- Remove the existing ignored `web/node_modules` and `web/dist` from the candidate before a fresh
  scope audit. Re-run this independent gate after those changes.
