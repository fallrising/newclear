STATUS: DONE

## Summary

Repaired only the three blockers from T-031. `package.json` and the pnpm 11.25.0 lockfile now pin
`@playwright/test@1.62.1` and `@axe-core/playwright@4.13.0` exactly. The controlled shadcn record now
names official registry identity `https://ui.shadcn.com/r`, preset/style `base`, base colour `slate`,
icon choice `lucide`, exact CLI 4.21.0 refresh recipe, and the fact that no registry request or
response supplied this source-equivalent bootstrap. `components.json` records `lucide` explicitly,
and its updated digest is recorded beside the unchanged UI-source digests.

No UI behavior, source, test, product specification, API, backend, task, plan, other report, or Git
reference was changed. Generated dependency/build/cache/workspace paths were removed after the
reproduced checks.

## Verification

- `docker image inspect node:24.20.0-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e --format '{{index .RepoDigests 0}}'` printed the accepted digest (exit 0) — passed
- `docker run --rm --user 1000:1000 -v /home/ckc/test/codex/worktrees/hai-taskboard-p0a/products/hai-taskboard/web:/workspace -w /workspace node:24.20.0-bookworm-slim@sha256:ba849c60be29959425b8734d57b8b4b7d56f98edd9504c9af091d5281095a71e sh -lc 'node --version && corepack pnpm@11.25.0 --version && corepack pnpm@11.25.0 install --lockfile-only --config.minimumReleaseAge=0 --store-dir /tmp/hai-taskboard-t032-pnpm-store'` printed `v24.20.0` and `11.25.0` and regenerated the lockfile (exit 0) — passed
- A clean run of the same pinned container with `corepack pnpm@11.25.0 install --frozen-lockfile --config.minimumReleaseAge=0 --store-dir /tmp/hai-taskboard-t032-pnpm-store` resolved 218 packages and passed lockfile supply-chain policy verification (exit 0) — passed
- `./node_modules/.bin/prettier --check . --ignore-path .gitignore && ./node_modules/.bin/eslint . && ./node_modules/.bin/tsc --noEmit` in the pinned Node container completed without findings (exit 0) — passed
- `./node_modules/.bin/vitest run` in the pinned Node container passed 6 files and 8 tests (exit 0; the existing jsdom canvas warning remains and is not browser evidence) — passed
- `./node_modules/.bin/vite build` in the pinned Node container built 22 modules with Vite 8.2.2 (exit 0) — passed
- `node -e "const p=require('./node_modules/@playwright/test/package.json'); const a=require('./node_modules/@axe-core/playwright/package.json'); console.log('@playwright/test', p.version); console.log('@axe-core/playwright', a.version); if(p.version!=='1.62.1'||a.version!=='4.13.0') process.exit(1)"` printed both required exact versions (exit 0) — passed
- `sha256sum products/hai-taskboard/web/components.json products/hai-taskboard/web/src/components/ui/button.tsx products/hai-taskboard/web/src/components/ui/badge.tsx products/hai-taskboard/web/src/components/ui/card.tsx products/hai-taskboard/web/src/styles.css` matched every digest in `SHADCN_BASELINE.md`, including updated `components.json` digest `98d222b34ddb07037d5077718cd403dc802e87431c0821aa1efab6feccec0919` (exit 0) — passed
- `grep` checks found the registry, base style, slate colour, lucide icon, exact CLI recipe, no-registry-response statement, and exact lockfile importer pins (exit 0) — passed
- `rm -rf products/hai-taskboard/web/node_modules products/hai-taskboard/web/dist products/hai-taskboard/web/.pnpm-store products/hai-taskboard/web/pnpm-workspace.yaml` followed by the exact generated-path `find` check returned no path (exit 0) — passed
- Exact-pin/config assertions, generated-path absence tests, `git diff --check`, report validation, and `git status --short --untracked-files=all -- products/hai-taskboard/web/package.json products/hai-taskboard/web/pnpm-lock.yaml products/hai-taskboard/web/components.json products/hai-taskboard/web/SHADCN_BASELINE.md .team/reports/T-032.md` all exited 0; status named only the five T-032 scope files — passed

The first post-resolution Prettier check identified only `pnpm-lock.yaml` formatting and exited 1;
the pinned formatter was applied to the four allowed web files, after which the required complete
format/lint/typecheck gate above passed.

## Documentation

- Updated only `web/SHADCN_BASELINE.md` with the missing shadcn registry/icon provenance and current
  source hash register.
- No normative product documentation changed.

## Risks and Follow-ups

- **Browser/Playwright NotRun:**
  `docker image inspect mcr.microsoft.com/playwright:v1.62.1-noble@sha256:dcc5531e97840b9b5e794f2814476b21571c5124a3fca2267d73041f56e7580e`
  exited 1 because the accepted image is not local. Per T-032 it was not pulled. Browser
  accessibility, rendered colour contrast, zoom, keyboard-only and coarse-pointer checks therefore
  remain visible later-gate work; jsdom axe is not substituted for them.
