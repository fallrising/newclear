STATUS: DONE

## Summary

Completed the narrow approval-consumption repair. `port.Completion` now carries a one-to-one consumption slice for its Approval IDs, validates exact set equality before writing, and binds every immutable consumption to its project, CompletionRecord, Approval and command result. Done rehydration now requires exactly one project/subject-correct consumption for each joined Approval, including explicit per-Approval cardinality rather than aggregate count alone, while approval-free completions require zero. No application command, Fake, transport, provider or external behavior was added.

## Verification

- `sh /home/ckc/.codex/plugins/cache/goland-codex-marketplace/modern-go-guidelines/1.1.1/skills/use-modern-go/scripts/run-tool.sh list --go-version 1.27` exited 0 and its complete unfiltered output was read before the first T-058 Go edit — passed
- Digest-pinned network-disabled Go 1.27.1 commands `go version`, `go mod verify`, `gofmt -l .`, and `go vet ./...` exited 0 with `go version go1.27.1 linux/amd64`, `all modules verified`, no unformatted file and no vet diagnostic — passed
- Digest-pinned network-disabled uncached named SQLite command ran the prior nine named tests plus `TestStoreCompletion_ApprovalConsumptionSetValidationBeforeWrite`; it exited 0, reported SQLite 3.53.4, and passed all named tests — passed
- `TestStoreCompletion_ApprovalConsumptionSetValidationBeforeWrite` passed nine duplicate/missing/extra/unjoined/wrong-project/wrong-subject/wrong-completion cases and observed zero CompletionRecord, join or consumption writes before rollback — passed
- `TestUnitOfWork_PublicPortSuccessReplayAndFailureInjection` passed a two-Approval write/load and ten rollback seams; the second consumption's missing command FK failed after the first consumption insert and rolled back the CompletionRecord, all joins, both consumptions, command result and aggregate — passed
- `TestSQLiteLoad_DoneRequiresMatchingCompletionRecord` passed nineteen corruption cases with typed `domain.ErrStorageCorruption` and zero WorkItem, including a balanced two-Approval attack with two consumptions for one Approval and none for the other, plus missing, unjoined, wrong-project and wrong-subject consumption and valid two-Approval/approval-free loads — passed
- The final `TestSQLiteProjectAssociationsFailClosed` and success-path immutable update/delete attacks exited 0, proving composite project binding for consumption-to-CompletionRecord/Approval/command result and preserving INSERT-only triggers with clean foreign keys — passed
- Digest-pinned network-disabled command `go test -count=1 ./...` exited 0 for application port, domain, nested rehydration authority, SQLite and reconciliation packages — passed
- Digest-pinned network-disabled command `go test -count=1 -race ./...` exited 0; SQLite including real-lock, blocker-race, consumption rollback and corrupt-load coverage passed in 15.519s — passed
- Digest-pinned network-disabled module/license/import inventory exited 0, reported exact `modernc.org/sqlite v1.58.0`, read its BSD-style LICENSE, and listed no production shell, executor, provider, HTTP-client or network-client import — passed
- The focused regression command `go test -count=1 ./internal/domain/sqlite -run '^TestSQLiteLoad_DoneRequiresMatchingCompletionRecord/(missing-approval-consumption|duplicate-approval-consumption)$' -v` exited 0, proving both the T-057 missing-consumption state and balanced duplicate/missing cardinality attack fail closed — passed
- `git diff --check` exited 0; the exact T-058 write audit contains only the authorized persistence port, V1 migration, SQLite store/tests and this report, while modules, specs, plan, tasks, prior reports and Git refs were not edited by this worker — passed
- `python3 /home/ckc/.codex/plugins/cache/fallrising-private/codex-team-superpowers/0.1.0+codex.20260904040057/scripts/teamctl.py validate-report .team/reports/T-058.md` exited 0 with `validate-report validation passed: .team/reports/T-058.md` after the final report edit — passed

## Documentation

Added this validator-compatible evidence report and updated only the four authorized persistence implementation/test artifacts. The repaired fixtures now include valid command results and consumptions unless consumption corruption is the intended attack.

## Risks and Follow-ups

T-058 completes its bounded repair and evidence envelope but does not accept itself. A fresh independent reviewer must inspect and rerun the final candidate before the orchestrator records acceptance; application commands, Fake execution, HTTP/SSE and vertical integration remain later work outside this task.
