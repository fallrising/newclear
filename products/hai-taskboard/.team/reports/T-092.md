STATUS: PARTIAL

## Summary

Independent decision: unconditional FAIL for the exact T-091 runnable-runtime candidate. The process starts, authenticates, persists and replays command results across a clean same-root/same-port restart, and the complete pinned Go gate passes, but fresh attacks found four acceptance blockers: broad system/public roots are accepted as controlled data roots; semantically equal but textually different Origin hosts/ports are accepted; a trivially predictable token satisfies the claimed strength check; and an artifact root replaced by a symlink after construction permits an out-of-root write. T-091 therefore requires bounded repair and a fresh independent review; this report does not update PLAN or accept itself.

## Verification

- Read-first chronology completed before executable probes: the reviewer read both required skills and evidence-gate contract, PLAN/reviewer contract, T-091 task/report, AGENTS/HANDOFF/SDD, ADR-001/002/003/005/006, runtime-relevant mini-SDDs and local-development guide, complete `backend/cmd/taskboard/**`, OpenAPI and accepted application-port/Fake/HTTP/SQLite open-close boundaries; the complete T-091 candidate was inspected before tests or process execution — passed
- Before any Go verification or disposable Go attack, `sh /home/ckc/.codex/plugins/cache/goland-codex-marketplace/modern-go-guidelines/1.1.1/skills/use-modern-go/scripts/run-tool.sh list --go-version 1.27` exited 0 and its complete unfiltered output was read — passed
- Candidate lineage was bound at local/origin HEAD `c37a57d147bdfeb3ef43e6faae681f81a42d00b6`; T-091 task/report SHA-256 were `4ee76ec51997b8eff787bc420cf4d14379d8d5a773856093c3e632c16733e957`/`9e0b4e96cac1e1a6676550c87f0e0da39573564aa06a1c7cf7d1fca65bd85ebb`, and T-092 task SHA-256 was `64ba559d41f22bd254055f6547fddba5e3c5813302cf3ad600ec6ddf026d6e19` — passed
- T-091 candidate hashes remained mini-SDD/local-development `496001110a1ac9dcd955694297df8b8a79edb7358766571495d12ebc4d87f97d`/`0a5c71e708e5017ba962a07f5987d4687d5f5cabb89f314c8228c41b0f4a5a0e`, main/config `f25a822b3e9bab04ed41f23cbd7aa9e67dfde528542628e013eabce5283bd487`/`f073c9ceced83505ed4eb5d55e005464a2b2341dd04aa05a10bbe55ebbd2f032`, local-dependencies/runtime `60ace9d38217b1fc43a19c04fb7ed942607144b8879addf51758a463b9ca4854`/`1e848a2ca8f8b4b1594b94ae080dd493f10835fe1fc933e713163322ef92b6bc`, and runtime-test `d587c9a42cdc8adf32c4159ab2e9ff57bb9179c5d1bb351fb3dfcd6074cd6755` — passed
- Protected predecessor hashes remained go.mod/go.sum `8c66a51fe39944f0a30e0952756dfd4b35824483af60a826ab9620d751691226`/`59d6f9f0590f841c826c0c2003f906b038b16397a886eaef7a562ac031b68d6b`, wiring `532121121fa48326349ce952f50489248546f6f4f30cdb5de2a39d62d638d332`, SQLite `3b000590cc720f5da715fe0064ba9341f584dbf8b131ab389979b1a250cf1741`, Fake adapter `bb5cbcb6f176f5abd450c4b288f4222b94c837fe5e2b93730b81e4e0d2aed1cc`, HTTP server `ae5adb54f1c2ae6cdaa665c920e404ab816cae8e348c3a52f29071b3eb21c71a`, migration `57d96955d3351de47b1a81696398cf9ddb843394eb5c004c6f79841117c7745c` and OpenAPI `08f5566f9fbfefb1949e5008119c8973e039a253d3c105daa8ab88145632b8fe`; `git diff --exit-code HEAD` over all predecessor/module/API/migration paths was empty — passed
- Fresh isolated-copy `TestT092Config_RejectsBroadPublicRoot` exited 1 because `runtimeConfig.validate` accepted both `/` and `/tmp`; source inspection proves `startRuntime` then calls `ensurePrivateDirectory(config.DataRoot)`, whose `os.Chmod(path, 0700)` would mutate either broad root rather than reject it, so the controlled-root and public-root attack requirement is unmet — failed
- Fresh isolated-copy `TestT092Config_RejectsNonExactOriginAddress` exited 1 because `http://127.0.0.1:018080` and `http://[::ffff:127.0.0.1]:18080` were accepted for listener `127.0.0.1:18080`; only the true IPv6 mismatch `http://[::1]:18080` was rejected, so numeric `ParseUint`/`net.IP.Equal` does not enforce the specified exact Origin/listener host-and-port spelling — failed
- Fresh isolated-copy `TestT092Config_RejectsTriviallyPredictableToken` exited 1 because 43 repeated `a` characters passed `validSessionToken`; the candidate checks only length/alphabet and therefore does not establish the task's sufficiently-strong-token boundary — failed
- Fresh isolated-copy `TestT092ArtifactStore_RejectsRootReplacement` exited 1 after a successfully constructed artifact store had its object directory renamed and replaced with a symlink: `Put` succeeded and the digest object appeared in the outside target, proving validation is time-of-check only and the required symlink-escape denial is bypassable — failed
- Fresh isolated-copy `TestT092HTTP_RejectsDuplicateCookieAndOrigin` exited 0: two `__Host-hai_session` cookies produced 401 and two exact Origin headers produced 403; accepted-source inspection also confirmed SHA-256 fixed-size `subtle.ConstantTimeCompare`, session-derived actor identity and generic secret-free errors — passed
- The existing configuration/artifact/session exact tests freshly rejected wildcard, empty-host, hostname, non-loopback and zero-port listeners; path/query/userinfo/HTTPS/port-mismatched Origin; relative/unclean roots; missing/short/invalid-alphabet tokens; initial symlink roots/objects, traversal/absolute/invalid artifact names and corrupt existing objects; wrong token/Origin requests; the independent failures above are additional uncovered cases rather than replacements for these passing checks — passed
- The reviewer-built process binary SHA-256 `684c163c674215545675f591e8ae7b5b8c0b8ed5e1129390e0999e27b064592d` ran with a new private `/tmp` root, non-production token and `127.0.0.1:18092`; health was 200 with automatic execution/projections unavailable, wrong token was 401, wrong Origin was 403, duplicate cookie was 401, CreateProject/lookup were 200, SIGTERM exited 0, same-root/same-port restart lookup and idempotent replay were 200 and byte-exact at result SHA-256 `4f447e269250d1c7210a43ad29990a5e9639d0acad86129efde9f4c882e25626` — passed
- Both real process sessions emitted exactly `taskboard: listening on 127.0.0.1:18092` and nothing else at combined log SHA-256 `59e453c98bfd29f46dca122e4343e97dce02778194c3291aeb7e839021389acd`; automated checks found no token, cookie value, request body or result in stdout/stderr, and restart on the exact port/root proves HTTP and SQLite were released after shutdown — passed
- The complete authoritative gate used `golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b` with `--network none`, read-only source/module mounts and container root, isolated executable tmpfs caches, `GOTOOLCHAIN=local`, `GOPROXY=off` and `GOSUMDB=off`; `go version` returned `go1.27.1 linux/amd64`, `go mod verify` returned `all modules verified`, complete `gofmt -l .` was empty and `go vet ./...` exited 0 — passed
- Exact pinned `go test -count=1 -v ./cmd/taskboard -run '^(TestRuntimeConfig_RejectsUnsafeExposure|TestLocalArtifactStore_RejectsEscape|TestLocalSessionAuthority_EnforcesTokenAndOrigin|TestRuntime_HTTPPersistenceAcrossRestart)$'` exited 0 with all four required tests passing and no skip; `go test -count=10 ./cmd/taskboard -run '^TestRuntime_HTTPPersistenceAcrossRestart$'` exited 0 — passed
- Complete pinned `go test -count=1 ./...` exited 0 across every backend package including SQLite in 6.184s, and `go test -count=1 -race ./...` exited 0 across every backend package including SQLite in 29.860s with no failure, skip or race — passed
- Pinned `go build -o /tmp/taskboard ./cmd/taskboard`, complete `go test -list . ./...`, `go list -deps ./cmd/taskboard` and `go list -m all` all exited 0; inventories included the four required runtime names and no new module, real provider or unauthorized adapter — passed
- The reviewer task's `ROLE: REVIEWER` intentionally remains outside the worker-only task-validator grammar, so `teamctl.py validate-task .team/tasks/T-092.md` exited 1 with exact compatibility diagnostic `task ROLE must be exactly WORKER`; the forward-only reviewer contract requires report validation, not rewriting the orchestrator-owned task — passed
- Automatic persistent outbox/Fake execution, persisted board/SSE projections, restore/backup, browser/live UI, root CI, real provider/credential/agent execution and wider G1/G2 acceptance remain explicit NotRun/Deferred boundaries and were not counted as passing runtime evidence — skipped
- Evidence-gate reconciliation mapped every T-091/T-092 Definition-of-done item to the source, unit, adversarial, process, pinned-suite, inventory, scope or NotRun evidence above; the four fresh required failures prevent DONE/PASS even though all worker-authored and process gates passed — failed
- The exact reviewer-owned attack and smoke processes were stopped, and exact-path cleanup removed only `/tmp/hai-taskboard-t092-attack.PjTdHA`, `/tmp/hai-taskboard-t092-smoke.isnu1y` and the disposable protected-diff file; final nonexistence checks printed `reviewer-temp-clean` — passed
- `python3 /home/ckc/.codex/plugins/cache/fallrising-private/codex-team-superpowers/0.1.0+codex.20260904040057/scripts/teamctl.py validate-report .team/reports/T-092.md` exited 0 with `validate-report validation passed: .team/reports/T-092.md`; final `git diff --check` exited 0 and scope inspection showed this report as the reviewer's only durable write — passed

## Documentation

Added only `.team/reports/T-092.md`. No candidate source/test/specification, task, PLAN, HANDOFF, prior report, module, migration, API, Git ref or external state was modified. All reviewer-owned attack, build, runtime, data and log material under `/tmp` was removed after exact results and hashes were recorded.

## Risks and Follow-ups

T-091 must not be accepted. Route a bounded repair that rejects broad system/public roots without chmod side effects, canonicalizes or textually binds Origin to the configured listener without IPv4-mapped aliases, enforces or generates a genuinely strong local session secret, and makes artifact operations resistant to post-validation root replacement and object-open races. Preserve the passing lifecycle, persistence, authentication and pinned-suite evidence, then require a fresh independent security/restart review. All explicitly skipped later-gate surfaces remain unavailable.
