ROLE: WORKER

AGENT: reproducibility-and-ci-reviewer

ID: T-006

Goal: Freeze a current, reproducible Go/React/shadcn/Tailwind toolchain and define the root path-scoped
CI gate needed for this monorepo.

Why: “Latest” versions and component-local workflows are not reproducible acceptance evidence.

Inputs to read first: `.team/PLAN.md`; `products/hai-taskboard/AGENTS.md`;
`products/hai-taskboard/docs/SDD.md`; `.team/reports/T-001.md`; root `README.md`;
`docs/specs/monorepo-ci.md`; all current root `.github/workflows/*.yml`.

Scope (may touch): `.team/reports/T-006.md` only.

Out of scope (must not touch): Product or CI code/docs; every other report; Git refs; dependencies;
runtime services; credentials; external mutation.

Definition of done:

- [ ] Record verified current stable Go, React, Vite, Tailwind, shadcn approach, TypeScript, test/lint
      families and compatible Node/pnpm baseline, distinguishing exact pins from moving CLI channels.
- [ ] Recommend lockfile/package layout and pinned container gates for backend/frontend/E2E.
- [ ] Specify one root path-scoped workflow contract consistent with `docs/specs/monorepo-ci.md`.
- [ ] Identify dependency/audit/license/update evidence that remains NotRun before bootstrap.

Verify with: named primary sources and repository paths; re-read `.team/reports/T-006.md`; verify no
file outside the report changed.

Budget: One bounded reproducibility review; no installs, code/workflow edits, commit or delegation.
