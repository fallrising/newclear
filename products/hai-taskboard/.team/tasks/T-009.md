ROLE: REVIEWER

AGENT: final-g0-verifier

ID: T-009

Goal: Independently verify that the final repaired HAI Taskboard P0-A design set satisfies the G0
gate, with every prior T-007/T-008 blocker either repaired or still visibly NotRun for a later gate.

Why: Two failed reviews are retained as evidence. The orchestrator's SDD rework cannot accept itself.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/reports/T-007.md`; `.team/reports/T-008.md`; `products/hai-taskboard/AGENTS.md`;
`products/hai-taskboard/docs/SDD.md`; `products/hai-taskboard/api/openapi.yaml`;
`products/hai-taskboard/docs/spec-graph.yaml`; `products/hai-taskboard/docs/traceability.md`.

Scope (may touch): `.team/reports/T-009.md` only.

Out of scope (must not touch): Product/API/spec/task/plan files; other reports; dependencies;
implementation; Git refs; credentials; runtimes/external systems.

Definition of done:

- [ ] Confirm T-008 is failed, T-009 is Ready, T-010 is Pending on T-001..T-009, and SDD §14 no
      longer presents accepted choices as open.
- [ ] Confirm exact Attention/Impact queries, complete WorkItem detail, typed SSE projection/reset,
      and all 21 strict operation-to-payload bindings remain present.
- [ ] Validate at least one legal and one mismatched command instance with the available JSON Schema
      implementation; mismatched input must be rejected.
- [ ] Parse OpenAPI YAML and spec graph, confirm all declared graph paths exist and the graph is
      acyclic. Run the already-available pinned Redocly container lint without installing tools.
- [ ] Audit report-only scope and return an unconditional PASS or FAIL. Product/runtime checks that
      belong to later gates must remain explicit NotRun rather than being implied to pass.

Verify with: read-only commands, exact exits, hashes and scope audit; re-read the report. No fixes.

Budget: One narrow final re-review turn; no implementation, install, commit or delegation.
