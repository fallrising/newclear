ROLE: REVIEWER

AGENT: pinned-g0-verifier

ID: T-011

Goal: Independently rerun the complete final G0 checks now that an immutable Redocly lint environment
is locally available, and accept or reject the repaired HAI Taskboard P0-A design set.

Why: T-009 passed its executed design checks but correctly failed on a NotRun required lint. The
orchestrator's environment bootstrap is evidence, not acceptance.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/reports/T-007.md`; `.team/reports/T-008.md`; `.team/reports/T-009.md`;
`products/hai-taskboard/AGENTS.md`; `products/hai-taskboard/docs/SDD.md`;
`products/hai-taskboard/docs/reproducibility.md`; `products/hai-taskboard/api/openapi.yaml`;
`products/hai-taskboard/docs/spec-graph.yaml`; `products/hai-taskboard/docs/traceability.md`.

Scope (may touch): `.team/reports/T-011.md` only.

Out of scope (must not touch): Product/API/spec/task/plan files; other reports; dependencies;
implementation; image pulls/installs; Git refs; credentials; mutable runtimes/external systems.

Definition of done:

- [ ] Confirm T-009 remains failed, T-011 is Ready, T-010 is Pending on T-011, and SDD §14 records
      resolved G0 choices without hiding later NotRun evidence.
- [ ] Validate one legal and one mismatched command instance; the mismatch must be rejected. Parse
      both YAML files, assert every declared graph path exists, and prove the graph acyclic.
- [ ] Independently run `redocly/cli@sha256:2dcc3939c2180e1da96db06a40aa079cb32c4ef3bac8b35ff061f2140322da64`
      with telemetry/update checks disabled and `api/` mounted read-only; verify version 2.51.2 and
      require lint exit 0 with no warning/error.
- [ ] Confirm Attention/Impact, complete WorkItem detail, typed SSE reset/events and 21 strict
      operation/payload bindings remain present.
- [ ] Audit report-only scope and return an unconditional PASS or FAIL. Later product/runtime checks
      remain explicit NotRun.

Verify with: exact read-only commands/exits/hashes and scope audit; re-read report. No fixes or pulls.

Budget: One narrow independent re-review turn; no implementation, install, commit or delegation.
