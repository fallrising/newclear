ROLE: WORKER

AGENT: domain-kernel-implementer

ID: T-020

Goal: Implement and verify `HTB-P0A-001` as a deterministic pure-Go domain/reconciliation kernel.

Why: The subject/evidence/transition and old+new graph rules must be executable before persistence,
HTTP or executor adapters can safely depend on them.

Inputs to read first: `.team/PLAN.md`; `products/hai-taskboard/AGENTS.md`;
`products/hai-taskboard/docs/tasks/HTB-P0A-001-domain-kernel.md`; accepted SDD/ADR-002/ADR-004;
domain/reconciliation mini-SDDs; `docs/traceability.md`; `docs/reproducibility.md`; Modern Go skill.

Scope (may touch): `products/hai-taskboard/backend/go.mod`;
`products/hai-taskboard/backend/internal/domain/**`;
`products/hai-taskboard/backend/internal/reconcile/**`; `.team/reports/T-020.md`.

Out of scope (must not touch): go.sum or external dependencies; persistence, HTTP, filesystem,
executor and web code; specs/API/CI; other reports/tasks/plan; Git refs; credentials/external systems.

Definition of done:

- [ ] Run/read the Modern Go Guidelines full `list` for the exact Go module before Go edits.
- [ ] Strong typed IDs/digests, phases/conditions, immutable subject/evidence/review records and stable
      rejection codes compile without a third-party dependency.
- [ ] Table-driven transitions and exhaustive Done-gate negative tests implement named contracts.
- [ ] Deterministic graph validation, old+new reverse closure, plan identity and reuse fingerprint tests
      cover AC-23..31 core without reusing production logic as the oracle.
- [ ] P0-A code exposes no DB/network/filesystem/shell/provider authority.

Verify with: pinned Go 1.27.1 container `gofmt` check, `go vet ./...`, `go test ./...`,
`go test -race ./...`; retain exact command/image/exit output in `.team/reports/T-020.md` and verify
scope with Git status/diff.

Budget: One bounded kernel implementation; no commit or delegation.
