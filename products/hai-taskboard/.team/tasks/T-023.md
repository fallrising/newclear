ROLE: REVIEWER

AGENT: done-authority-rework-verifier

ID: T-023

Goal: Independently verify the T-022 authority repair and decide T-020 domain-kernel acceptance.

Why: The original T-021 failure must remain visible while the exact bypass and regression evidence
are rerun by a non-authoring reviewer.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/tasks/T-020.md`; `.team/tasks/T-022.md`; `.team/reports/T-020.md`;
`.team/reports/T-021.md`; `.team/reports/T-022.md`; `products/hai-taskboard/AGENTS.md`;
ADR-002; domain mini-SDD; and every backend domain/reconciliation source/test file.

Scope (may touch): `.team/reports/T-023.md` only. Read-only `/tmp` probes are allowed.

Out of scope (must not touch): Backend/web/spec/API/plan/tasks/other reports; dependencies; Git refs;
credentials or external systems.

Definition of done:

- [ ] Confirm `NewWorkItem` rejects Done and every public `Transition` input rejects Done without
      changing the original aggregate; confirm no other public constructor/rehydration path exists.
- [ ] Independently execute the external-package boundary test: direct paths fail, while the sole
      successful Done path returns a non-zero exact-subject CompletionRecord with the same WorkItem.
- [ ] Confirm T-021's previously passing domain/reconciliation findings and independent oracles were
      not regressed by the narrow source/test changes.
- [ ] Rerun pinned Go 1.27.1 gofmt, vet, test, race, test-list, stdlib/module/authority and go.sum
      absence checks.
- [ ] Audit report-only scope and return unconditional PASS or FAIL for T-020/T-022 acceptance.

Verify with: complete source inspection, exact commands/versions/exits/hashes and optional `/tmp`
probe. No fixes, installs, delegation, commits or Git refs.

Budget: One narrow independent domain-kernel re-review.
