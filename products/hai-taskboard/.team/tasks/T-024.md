ROLE: REVIEWER

AGENT: domain-inventory-verifier

ID: T-024

Goal: Independently run the three T-023 NotRun inventory checks against the unchanged repaired
domain candidate and decide final T-020/T-022 acceptance.

Why: T-023's source/boundary and primary Go checks passed, but required NotRun inventory checks
cannot be inferred from prior reports or the orchestrator's reproduction.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/tasks/T-023.md`; `.team/reports/T-021.md`; `.team/reports/T-022.md`;
`.team/reports/T-023.md`; `products/hai-taskboard/AGENTS.md`; backend `go.mod` and exact current file
list/hashes.

Scope (may touch): `.team/reports/T-024.md` only.

Out of scope (must not touch): Any candidate source/test/module, web/spec/API/plan/tasks/other
reports, dependencies, Git refs, credentials or external systems.

Definition of done:

- [ ] Confirm T-023-reviewed backend file hashes have not changed since its checks.
- [ ] In the exact pinned Go 1.27.1 image with network disabled/read-only mount, run `go test -list
      . ./...` and confirm every T-020/T-022 named oracle including the public boundary test.
- [ ] Run `go list` package imports and `go list -m all`; confirm stdlib plus local domain only, with
      no DB/network/filesystem/shell/executor/provider or third-party authority.
- [ ] Confirm `go.sum` remains absent.
- [ ] Audit report-only scope and return unconditional PASS or FAIL for T-020/T-022 acceptance,
      relying only on T-023's explicitly passing checks plus these newly executed checks.

Verify with: exact digest-pinned read-only commands, versions, exits, hashes and scope audit.
No fixes, installs, delegation, commits or Git refs.

Budget: One very narrow inventory review.
