ROLE: WORKER

AGENT: web-contract-shell-implementer

ID: T-030

Goal: Implement and verify `HTB-P0A-002` as an accessible Board-first React/shadcn fixture shell.

Why: UI hierarchy and failure states should be executable independently of persistence/API work,
while all movement affordances emit the same command intent.

Inputs to read first: `.team/PLAN.md`; `products/hai-taskboard/AGENTS.md`;
`products/hai-taskboard/docs/tasks/HTB-P0A-002-web-contract-shell.md`; accepted SDD/UI mini-SDD;
`api/openapi.yaml`; `docs/reproducibility.md`; `.team/reports/T-003.md`; `.team/reports/T-006.md`.

Scope (may touch): `products/hai-taskboard/web/**`; `.team/reports/T-030.md`.

Out of scope (must not touch): Backend, product specs/API/CI, other reports/tasks/plan, Git refs,
credentials, live API/provider/external systems.

Definition of done:

- [ ] Generate/record the controlled shadcn 4.21.0 source baseline or document an exact equivalent
      committed-source bootstrap; exact package pins and pnpm lockfile follow reproducibility.md.
- [ ] Implement the four P0 surfaces using typed static fixtures; Board-first selection/detail works
      at wide and 320px layouts with light/dark tokens.
- [ ] Draft/Ready/Developing/Review/QA/Done plus single-card Blocked projection and every required
      disconnected/conflict/stale/unknown/cancel/done-stale state are inspectable.
- [ ] Drag intent (a semantic test seam is acceptable in this slice), keyboard and ordinary click
      controls share one transition intent; rejection preserves server lane and focus/announcement.
- [ ] No ungrounded KPI, agent self-reported progress, chat-first UI or live mutation exists.

Verify with: pinned Node 24.20.0/pnpm 11.25.0 install; format, ESLint, TypeScript, Vitest/Testing
Library accessibility tests and Vite production build; use the matching Playwright image only if it
is available/pulled in scope. Record exact commands, image/version, exits and NotRun items in the
report; verify Git scope.

Budget: One bounded fixture-shell implementation; no backend, commit or delegation.
