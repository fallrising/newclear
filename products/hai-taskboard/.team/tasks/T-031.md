ROLE: REVIEWER

AGENT: web-shell-verifier

ID: T-031

Goal: Independently inspect and verify the T-030 Board-first React/shadcn fixture-shell candidate.

Why: A worker report is evidence, not acceptance; UI state semantics, accessibility claims, exact
pins and absence of live mutations require independent source and executable review.

Inputs to read first: codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/tasks/T-030.md`; `.team/reports/T-030.md`; `products/hai-taskboard/AGENTS.md`;
the accepted SDD and UI mini-SDD; `docs/reproducibility.md`; `api/openapi.yaml`; every file under
`products/hai-taskboard/web/` except generated ignored directories.

Scope (may touch): `.team/reports/T-031.md` only. Exact lockfile installation may temporarily create
ignored `web/node_modules`, `web/dist` or cache data, but they must be removed before the scope audit.

Out of scope (must not touch): Web source/manifests/lockfile; backend/spec/API/plan/tasks/other
reports; Git refs; provider credentials; live API/external mutation.

Definition of done:

- [ ] Confirm all four surfaces, six phases plus orthogonal single-card Blocked projection, exact
      failure/attention states, subject/guard/cause-path detail and no fabricated KPI/chat-first UI.
- [ ] Confirm click, Alt+Arrow and drag seam emit the same typed intent except source; rejected moves
      keep the authoritative lane, announce the reason and restore focus.
- [ ] Confirm exact pins and lockfile, controlled source-owned shadcn baseline/hash recipe, light/dark
      semantic tokens and 320px layout; no workspace/cache/generated output remains in candidate.
- [ ] Confirm source has no live fetch/provider/mutation path. Independently run frozen install,
      Prettier, ESLint, TypeScript, Vitest/full fixture axe and Vite build in the pinned Node image.
- [ ] Keep exact Playwright/browser/colour-contrast/zoom evidence NotRun if the accepted image was
      not available to T-030; do not generalize jsdom axe into browser accessibility acceptance.
- [ ] Audit report-only durable scope and return unconditional PASS or FAIL for T-030 acceptance.

Verify with: exact commands, versions, exits/hashes, read-only source inspection and scope audit.
No fixes, commits, delegation or candidate edits.

Budget: One bounded independent web-shell review.
