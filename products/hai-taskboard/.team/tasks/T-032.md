ROLE: WORKER

AGENT: web-bootstrap-reworker

ID: T-032

Goal: Repair only the three T-031 blockers in the T-030 web-shell candidate and reproduce its
existing checks without widening UI behavior.

Why: The functional shell passed review, so only dependency pins, source-generation provenance and
generated-output hygiene should be recomputed.

Inputs to read first: codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/reports/T-030.md`; `.team/reports/T-031.md`; `products/hai-taskboard/AGENTS.md`;
`docs/reproducibility.md`; `web/package.json`; `web/pnpm-lock.yaml`; `web/components.json`;
`web/SHADCN_BASELINE.md`.

Scope (may touch): `products/hai-taskboard/web/package.json`;
`products/hai-taskboard/web/pnpm-lock.yaml`; `products/hai-taskboard/web/components.json`;
`products/hai-taskboard/web/SHADCN_BASELINE.md`; exact generated ignored paths under `web/`;
`.team/reports/T-032.md`.

Out of scope (must not touch): Other web behavior/source/tests; backend/spec/API/plan/tasks/other
reports; Git refs; credentials; live APIs/providers.

Definition of done:

- [ ] Add exact dev pins `@playwright/test@1.62.1` and `@axe-core/playwright@4.13.0` and regenerate
      the existing pnpm 11.25.0 lockfile without ranges or a workspace file.
- [ ] Record the official shadcn registry identity `https://ui.shadcn.com/r`, preset/style `base`,
      base colour `slate`, icon choice `lucide`, exact CLI 4.21.0 refresh recipe, the fact that no
      registry response was used for this source-equivalent bootstrap, and current source hashes.
- [ ] Remove only generated `web/node_modules`, `web/dist`, `web/.pnpm-store` and any accidental
      `web/pnpm-workspace.yaml`; confirm none remains after verification.
- [ ] Re-run pinned frozen install, format, lint, typecheck, Vitest and build. Browser checks remain
      NotRun unless the exact accepted Playwright image is already locally available; do not pull it.
- [ ] Write exact evidence and scope audit. Do not change UI behavior to make bootstrap checks pass.

Verify with: pinned Node/pnpm commands, hash checks, exact generated-path absence and Git scope.

Budget: One narrow rework turn; no delegation, commit, Git ref or external application mutation.
