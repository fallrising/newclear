ROLE: WORKER

AGENT: fake-vertical-slice-designer

ID: T-041

Goal: Turn the accepted T-040 product contract into an executable, non-overlapping implementation
contract for SQLite persistence, application commands, resumable SSE and deterministic Fake execution.

Why: T-040 crosses authority, transaction, rehydration, recovery and transport boundaries. Exact
package ownership, schema invariants and child acceptance oracles must be frozen before Go changes.

Inputs to read first: codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; `products/hai-taskboard/AGENTS.md`; `docs/SDD.md`;
all accepted ADRs and mini-SDDs; `docs/traceability.md`; `docs/reproducibility.md`;
`docs/spec-graph.yaml`; `docs/tasks/HTB-P0A-003-fake-vertical-slice.md`; accepted reports T-024 and
T-033; every current backend Go file/go.mod; and the full OpenAPI contract.

Scope (may touch): `products/hai-taskboard/docs/sdd/fake-vertical-slice-implementation.md`;
`products/hai-taskboard/docs/spec-graph.yaml`; `products/hai-taskboard/docs/traceability.md`;
`.team/reports/T-041.md`.

Out of scope (must not touch): Production Go/TypeScript/OpenAPI; dependencies/lockfiles;
`.team/PLAN.md`; task envelopes; existing reports/contracts; Git refs; credentials; real providers,
shell/network execution, Slack/Lark, deployment or release.

Definition of done:

- [ ] Freeze package/dependency direction for domain, application ports/services, SQLite adapter,
      Fake adapter, HTTP/SSE transport and composition root; adapters never become authority.
- [ ] Specify migration V1 tables, keys/checks/immutability constraints and PRAGMA assertions for
      Project, WorkItem/blockers, Run dimensions/lease, Candidate, Review/Evidence/Approval,
      CompletionRecord, artifact metadata, idempotency/command result, audit, outbox and stream state.
- [ ] Specify guarded rehydration that preserves the public Done-authority invariant: a Done
      WorkItem cannot be restored without a matching immutable CompletionRecord, while ordinary
      construction/transition still cannot enter Done.
- [ ] Specify exact transaction ordering, expected-version/idempotency replay semantics and atomic
      completion behavior, including the first bounded persistence checkpoint and failure seams.
- [ ] Specify Fake adapter inputs/capabilities/observations, lease/restore fencing, start-ack loss,
      cancellation uncertainty and the rule that unknown effects never auto-retry.
- [ ] Specify HTTP `/api/v1` routing/auth/canonicalization boundaries and SSE `(epoch,sequence)`
      snapshot high-water, gap replay, retention/wrong-epoch reset and bounded slow-client behavior.
- [ ] Propose serial child tasks with non-overlapping writable scopes, explicit dependencies and
      named tests. The first code task must be independently useful/acceptable without claiming the
      entire T-040 vertical slice.
- [ ] Update traceability/spec graph only for the new contract and still-NotRun oracles. Produce a
      validator-compatible T-041 report; no implementation claim.

Verify with: YAML parse/path/acyclic checks; clause/test/decomposition inventory; current report
validator; `git diff --check`; exact scope audit.

Budget: One bounded design/decomposition turn; no delegation, commit, Git ref or external mutation.
