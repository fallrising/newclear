ROLE: WORKER

AGENT: sqlite-foundation-implementer

ID: T-043

Goal: Implement and prove the independently accepted SQLite V1 foundation, persistence port and
guarded Done rehydration boundary as the first independently useful T-040 code checkpoint.

Why: Later application/Fake/HTTP slices require a durable schema and load boundary that cannot
fabricate Done or weaken the accepted pure domain kernel.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; product AGENTS; SDD; ADR-001/002/003/005;
domain/persistence and accepted fake-vertical-slice implementation contracts; reproducibility;
traceability; HTB-P0A-003; T-024/T-041/T-042/T-048/T-049/T-051/T-052 reports; backend `go.mod`
and every existing domain Go file/test.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27. Apply all relevant guidance; use `explain` for any returned
rule that appears relevant but would be skipped.

Scope (may touch): `products/hai-taskboard/backend/go.mod`;
`products/hai-taskboard/backend/go.sum`;
`products/hai-taskboard/backend/internal/application/port/persistence.go`;
`products/hai-taskboard/backend/internal/domain/rehydration.go`;
`products/hai-taskboard/backend/internal/domain/rehydration_test.go`;
`products/hai-taskboard/backend/internal/domain/internal/rehydrationcap/**`;
`products/hai-taskboard/backend/internal/domain/sqlite/**`; `.team/reports/T-043.md`.

Out of scope (must not touch): Existing domain/reconcile source/tests; application commands;
Fake/HTTP/SSE/composition/web/OpenAPI; specs/traceability/plan/tasks/other reports; CI/deploy;
Git refs; credentials; shell/network/provider execution or external systems.

Definition of done:

- [ ] Pin `modernc.org/sqlite@v1.58.0` with a complete `go.sum`; retain Go 1.27/go1.27.1 and prove
      actual linked SQLite is at least 3.51.3 in the pinned container.
- [ ] Embed/apply a checksummed transactional V1 migration for every table/constraint/append-only
      trigger named by the accepted implementation contract, including immutable unique
      `approval_consumptions` and the CompletionRecord-before-Done trigger.
- [ ] Open only an absolute controlled DB path; configure and read back foreign_keys=1, WAL,
      synchronous=FULL and busy_timeout=5000 on each usable connection; bound writers/busy errors.
- [ ] Implement the persistence port and concrete Project/WorkItem/blocker create/load/update subset
      needed by this checkpoint, with composite project scoping and optimistic versions.
- [ ] Implement bare-lowercase-64-hex SQLite/domain digest conversion and reject prefixed/uppercase/
      malformed storage values; do not implement the future HTTP prefix codec.
- [ ] Implement nested-internal opaque rehydration authority so only domain + `domain/sqlite` can
      reconstruct `(Done WorkItem, CompletionRecord)` from matching authoritative rows. Missing,
      duplicate, wrong item/version/subject or corrupt joins fail closed without a partial aggregate.
- [ ] Pass the exact named tests `TestSQLiteV1_MigrationPragmasAndConstraints`,
      `TestSQLiteLoad_DoneRequiresMatchingCompletionRecord`, and
      `TestSQLiteDigestStorageDomainRoundTrip`, plus existing format/vet/unit/race gates.
- [ ] Prove no unexpected provider/network/shell/runtime import or production behavior was added;
      write a validator-compatible report with exact image/commands/exits and scope audit.

Verify with: digest-pinned Go 1.27.1 container; module download/verify; gofmt check; `go vet ./...`;
named tests; `go test ./...`; `go test -race ./...`; dependency/license/engine inventory;
report validator; `git diff --check`; exact scope audit.

Budget: One bounded implementation turn; no delegation, commit, Git ref, real executor, credential,
HTTP call, deployment or external mutation.
