ROLE: WORKER

AGENT: application-command-implementer

ID: T-044

Goal: Implement the accepted T-044 application-command slice over the public persistence port:
Create Project/WorkItem, MarkReady, DispatchRun and CompleteWorkItem with authenticated-principal
identity, exact idempotency replay and one atomic state/result/audit/outbox/event transaction.

Why: T-064 accepts the SQLite predecessor and its allocator-safe canonical result ordering. The next
serial child must prove application-owned command decisions and atomicity before any Fake adapter or
HTTP/SSE implementation begins.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; product AGENTS; SDD HAI-BOUNDARY-003,
HAI-AUTH-001/002, HAI-DOMAIN-004/005, HAI-STATE-001/002/004, HAI-DONE-001..004 and HAI-API-001;
ADR-001/002/003/005; `domain-and-gates.md`, `persistence-and-recovery.md`, `executor-and-security.md`
and accepted `fake-vertical-slice-implementation.md` sections 1, 4 and 7; OpenAPI command/result/
error schemas; reproducibility/traceability; T-041/T-052/T-062/T-063/T-064 tasks/reports; backend
go.mod/go.sum, all domain source/tests and the accepted application persistence port. SQLite source
and tests are read-only predecessor evidence.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 and apply all relevant guidance; explain any relevant rule
that would otherwise be skipped.

Scope (may touch): `products/hai-taskboard/backend/internal/application/command/**`; `products/hai-taskboard/backend/internal/application/service/**`; `products/hai-taskboard/backend/internal/application/port/clock.go`; `products/hai-taskboard/backend/internal/application/port/id_source.go`; `products/hai-taskboard/backend/internal/application/port/executor.go`; `products/hai-taskboard/backend/internal/application/port/artifact.go`; `products/hai-taskboard/backend/internal/application/port/projection.go`; `.team/reports/T-044.md`.

Out of scope (must not touch): `backend/go.mod`, `go.sum`, the accepted `port/persistence.go`, domain/
reconcile/SQLite source or tests; Fake/HTTP/SSE/composition/web/OpenAPI; specs/traceability/plan/tasks/
prior reports; CI/deploy; Git refs; real executor, credentials, shell/network or external state.

Definition of done:

- [ ] Define application command DTOs and canonical V1 request bytes for CreateProject,
      CreateWorkItem, MarkReady, DispatchRun and CompleteWorkItem. The authenticated principal is a
      separate trusted service input and is the sole actor source; payload DTOs MUST NOT carry an
      actor override. Canonical bytes/digests are deterministic and project/operation scoped.
- [ ] Define narrow Clock, IDSource, Executor declaration, ArtifactStore and ProjectionSink ports
      using application/domain values only. The command service depends on `port.UnitOfWork`, these
      ports/configuration and domain APIs; it imports no SQLite, transport, Fake or concrete adapter.
- [ ] Within the write transaction, look up idempotency by `(principal,project,operation,key)`.
      Same canonical request returns an alias-safe byte-identical stored success or failure without
      a second mutation/Run/audit/event; different bytes conflict; expired/tombstoned records fail
      closed. Response loss after commit is replay, never command re-execution.
- [ ] Create and MarkReady use domain constructors/transitions. MarkReady derives accepted-AC facts
      from persisted material and specification validity from trusted application policy/config,
      never a caller boolean. DispatchRun requires current Ready/version/no blocker or unknown work,
      creates a new Run and outbox intent, and changes to Developing atomically; it does not invoke
      an executor inside the transaction.
- [ ] CompleteWorkItem loads project-scoped current Candidate/Run/AC/graph/Evidence/Review/Approval/
      artifact facts, reconstructs domain values including persisted verifier role, rebuilds the
      current subject from authoritative material plus trusted policy/recipe configuration, and calls
      `domain.CompleteWorkItem`. Every unmet, stale, nonpassing, unavailable, non-independent,
      expired or active/unknown condition returns stable codes with zero authoritative mutation.
- [ ] A successful completion writes every exact Evidence/Review/Approval join, one immutable
      consumption per selected Approval, CompletionRecord and QA->Done update in the same UnitOfWork.
      It then appends audit/event (and any outbox) first, constructs canonical CommandSuccess bytes
      from the returned real audit sequence/cursor, stores the immutable result and idempotency, and
      commits. It MUST NOT predict allocator values, use a placeholder result or expose SQLite.
- [ ] Canonical success bytes conform to OpenAPI CommandSuccess and include command identity,
      operation, recorded status, replay marker, typed result, real audit sequence/cursor and bounded
      correlation ID. Recorded failures conform to CommandFailure with stable code/message/retryable
      fields. Stored bytes are the replay authority; adapters do not reconstruct them from mutable
      state. Application digest values remain bare; HTTP `sha256:` encoding remains T-046 NotRun.
- [ ] Add and pass `TestCommandTransaction_FailureInjectionRollsBackAllWrites`,
      `TestCommand_IdempotencySameRequestAndConflict` and
      `TestCompleteWorkItem_PersistentAtomicity`, plus response-loss/no-duplicate-Run,
      authenticated-actor, allocator-not-predicted, exact-subject negative and storage-corruption
      cases. Use an application-owned transactional test double; application tests MUST NOT import
      concrete SQLite or weaken the independently accepted real-store evidence.
- [ ] Keep T-045 Fake, T-046 HTTP/SSE, backup/restore, browser and integration families explicitly
      NotRun. Pass pinned network-disabled Go 1.27.1 module/format/vet/named/full/race, existing
      SQLite regression, source/import/license inventory, report validator, exact scope audit and
      `git diff --check` with no skipped required check.

Verify with: accepted digest-pinned Go 1.27.1 container and read-only module cache; named application
tests and all backend/race tests; architecture/import inventory; current report validator.

Budget: One bounded application-command implementation turn; no delegation, commit, Git ref,
dependency/spec/persistence change, Fake/HTTP/SSE behavior, real executor, credential, deployment or
external mutation.
