ROLE: WORKER

AGENT: digest-oracle-scope-reworker

ID: T-051

Goal: Repair only T-049's digest-codec oracle ownership mismatch in the T-041/T-048 contract.

Why: SQLite can prove bare storage/domain fidelity, but only the later HTTP adapter can prove the
real `sha256:` wire boundary; a test-only simulation is not acceptance evidence.

Inputs to read first: codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-042/T-048/T-049 reports; candidate implementation
contract; OpenAPI SHA256 schema; backend domain digest implementation.

Scope (may touch): `products/hai-taskboard/docs/sdd/fake-vertical-slice-implementation.md`;
`.team/reports/T-051.md`.

Out of scope (must not touch): All other files, including production code, other specs, plan/tasks,
existing reports, dependencies, Git refs, credentials, providers or external state.

Definition of done:

- [ ] Assign T-043 only a real bare storage/domain digest round-trip oracle within its SQLite scope.
- [ ] Assign the real prefixed HTTP wire codec and all positive/missing/double/uppercase rejection
      cases to T-046 under `internal/transport/httpapi/**`; T-046 must use production codec code.
- [ ] Preserve every other T-048 repair and existing Fake/SSE/NotRun boundary unchanged.
- [ ] Write a validator-compatible T-051 report and exact scope audit.

Verify with: child/oracle ownership inventory; report validator; `git diff --check`; scope audit.

Budget: One minimal document repair; no delegation, code, commit, Git ref or external mutation.
