ROLE: REVIEWER

AGENT: sqlite-foundation-verifier

ID: T-053

Goal: Independently attack-review T-043's SQLite foundation and decide whether it is correct,
forward-compatible with T-044 and acceptable without broadening its evidence.

Why: Passing happy-path migrations can hide historical-completion loss, an unusable transaction
port, forgeable/incomplete rehydration, weak busy classification or cross-project gaps.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-043 task/report; accepted T-041/T-048/T-051
contract and T-052 review; product AGENTS; SDD; ADR-001/002/003/005; domain/persistence mini-SDDs;
traceability/reproducibility; backend go.mod/go.sum and every T-043 file plus all existing domain tests.

Scope (may touch): `.team/reports/T-053.md` only. Read-only candidate mounts and removable `/tmp`
probes/copies are allowed; remove them before scope audit.

Out of scope (must not touch): Candidate source/dependencies/specs; plan/tasks/other reports;
Git refs; credentials; providers/external state. Do not fix findings.

Definition of done:

- [ ] Re-run pinned module verify, format, vet, full unit/race and all three named T-043 tests.
- [ ] Inspect every migration table/FK/check/trigger against the accepted schema, especially multiple
      immutable CompletionRecords across reopen/re-completion, approval consumption, composite
      project scope, Done insert/update ordering and append-only rows.
- [ ] Prove T-044 can implement one transaction containing aggregate/idempotency/result/audit/outbox/
      event writes through the T-043-owned port without importing concrete SQLite or editing T-043.
- [ ] Attack guarded rehydration with missing/duplicate/wrong item/version/subject/corrupt joins and
      verify application/transport/external domain code cannot obtain the capability.
- [ ] Check controlled-path semantics, all-connection PRAGMAs, typed/bounded SQLITE_BUSY behavior,
      optimistic blocker mutations and deterministic inputs; do not accept string-matching as typed
      error classification.
- [ ] Check dependency/module/engine/license evidence exactly and keep application/Fake/HTTP/SSE/
      restore evidence NotRun.
- [ ] Validate the reviewer report and report-only scope; return unconditional PASS or FAIL.

Verify with: pinned Go 1.27.1 offline container and existing module cache where available; SQL/Go
inspection; disposable negative probes; exact report validator; `git diff --check`; temp cleanup.

Budget: One independent code gate; no fixes, delegation, commits, Git refs or external mutation.
