ROLE: WORKER

AGENT: sqlite-adversarial-completer

ID: T-056

Goal: Complete the explicitly skipped T-054 adversarial and public-port work so the unchanged
T-043/T-054 SQLite candidate is ready for a fresh independent acceptance review.

Why: T-054 fixed the principal defects but truthfully stopped PARTIAL. The remaining work is now
bounded to a complete application-facing transaction seam, real failure evidence, and exhaustive
fail-closed storage/project-scope tests rather than another schema redesign.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-043/T-053/T-054 tasks and reports;
T-041/T-048/T-051/T-052 accepted design chain; product AGENTS; SDD; ADR-001/002/003/005;
`domain-and-gates.md`, `persistence-and-recovery.md`, `fake-vertical-slice-implementation.md`;
reproducibility/traceability; every current backend Go/module/migration/test file.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27. Apply all relevant guidance; use `explain` for any returned
rule that appears relevant but would be skipped.

Scope (may touch): `products/hai-taskboard/backend/go.mod`;
`products/hai-taskboard/backend/go.sum`;
`products/hai-taskboard/backend/internal/application/port/persistence.go`;
`products/hai-taskboard/backend/internal/domain/rehydration.go`;
`products/hai-taskboard/backend/internal/domain/rehydration_test.go`;
`products/hai-taskboard/backend/internal/domain/internal/rehydrationcap/**`;
`products/hai-taskboard/backend/internal/domain/sqlite/**`; `.team/reports/T-056.md`.

Out of scope (must not touch): Existing non-rehydration domain/reconcile source/tests; application
command/service implementation; Fake/HTTP/SSE/composition/web/OpenAPI; specs/traceability/plan/tasks/
prior reports; CI/deploy; Git refs; credentials; providers/external state. Do not weaken accepted
constraints to make an attack fixture pass.

Definition of done:

- [ ] Finish the public `port.Transaction` contract needed by T-044 without SQL/concrete adapter
      types: exact idempotency/result replay reads with a port-owned not-found result; full Run
      fields instead of hard-coded adapter/scenario/state/attempt values; current completion material
      reads sufficient to compare persisted WorkItem/Candidate/Run/AC/graph facts and load exact-
      subject Evidence/Review/Approval/artifact availability; immutable completion joins and optional
      approval consumption; aggregate/result/idempotency/audit/outbox/projection writes.
- [ ] Audit insertion writes both group and ordered entry, returns its allocated sequence, preserves
      nullable before/after digests, and is project-bound. Projection insertion atomically allocates
      `(stream_epoch,event_sequence)` by updating `instance_state`, binds the returned audit sequence
      in the same project, ignores/rejects caller-forged sequence state, and returns the durable cursor.
- [ ] Through `port.UnitOfWork` only, prove one success/replay flow and forced failure after each
      write category. Every forced failure must roll back aggregate, Run, CompletionRecord and joins/
      consumption, command result, idempotency, audit group/entry, outbox, projection event and
      `instance_state.next_event_sequence`; no test may pass merely because an earlier FK failed.
- [ ] Prove typed bounded busy behavior with two real independent SQLite connections/process-visible
      locks: elapsed time is bounded around the configured five-second ceiling, `errors.Is(err,
      ErrBusy)` is true, and a synthetic non-SQLite error containing `SQLITE_BUSY`/`database is
      locked` is not classified. No retry loop may be added.
- [ ] Execute the full Done load matrix with a fresh fixture per case: missing; duplicate current row
      even when simulating a malformed legacy/corrupt table; wrong project/item/version; invalid or
      mismatched subject; missing/mismatched Evidence, Review and Approval joins; missing/non-Present
      artifact. Every case must return typed `domain.ErrStorageCorruption` and a zero WorkItem.
- [ ] Add a source/import architecture oracle that permits the nested rehydration capability only in
      `internal/domain` and `internal/domain/sqlite`; retain opaque mixed-capability denial. The next
      reviewer will independently compile an external-module denial probe.
- [ ] Close and attack project association keys: AC requirement->AC revision, dependency edge->graph,
      WorkItem->Run->Completion, project command->idempotency, approval consumption, audit, outbox and
      projection event. A row from project B must never satisfy or attach to project A; use composite
      unique keys/FKs or an equally strong declared constraint and verify `PRAGMA foreign_key_check`.
- [ ] Prove explicit root authority against relative/root-escape/symlink-escape/query/fragment/encoded
      PRAGMA spellings; prove the injected clock owns migration/completion/blocker timestamps. Remove
      or clearly demote any convenience opener that can silently choose its own trust root.
- [ ] Prove blocker stale-version, missing, already-resolved and racing resolution behavior: exact
      blocker and aggregate row counts are checked and every failed path rolls back timestamp/version.
- [ ] Preserve two CompletionRecords across reopen/re-completion, current-version exact load, all V1
      append-only triggers, bare digest rules, per-connection asserted PRAGMAs, exact modernc v1.58.0,
      linked SQLite >=3.51.3, and no external/provider/shell/network production behavior.
- [ ] Pass pinned offline Go 1.27.1 module verification, gofmt, vet, named/adversarial/full/race tests,
      engine/license/import inventory, report validator, exact scope audit and `git diff --check`.

Verify with: digest-pinned Go 1.27.1 container; network disabled after using the existing read-only
module cache; all exact tests listed in the report; disposable corrupt-schema/lock/import probes;
current report validator; temp cleanup; exact scope audit.

Budget: One bounded completion turn; no delegation, commit, Git ref, application service, Fake,
HTTP/SSE, real executor, credential, deployment or external mutation.
