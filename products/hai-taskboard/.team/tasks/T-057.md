ROLE: REVIEWER

AGENT: sqlite-foundation-final-verifier

ID: T-057

Goal: Independently decide whether the unchanged T-043/T-054/T-056 SQLite foundation now closes
every T-053 finding and is a safe, usable predecessor for T-044.

Why: The repair worker reports all gates green, but acceptance requires fresh attack review of the
actual final bytes, public application port, transactional semantics and corrupt-state behavior.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-043/T-048/T-051/T-052/T-053/T-054/
T-056 tasks and reports; product AGENTS; SDD; ADR-001/002/003/005; domain/persistence/fake vertical
slice mini-SDDs; reproducibility/traceability; backend go.mod/go.sum and every backend source/test/
migration file.

Scope (may touch): `.team/reports/T-057.md` only. Read-only candidate mounts plus removable `/tmp`
compile/lock/corruption probes are permitted; remove them before scope audit.

Out of scope (must not touch): Candidate code/dependencies/specs/tasks/plan/prior reports; Git refs;
credentials/providers/external state. Do not fix findings and do not accept your own report.

Definition of done:

- [ ] Re-run pinned Go 1.27.1 module verify, format, vet, all nine named SQLite/adversarial tests,
      full unit and race against the final candidate; verify modernc v1.58.0/license and linked
      SQLite >=3.51.3.
- [ ] Inspect every V1 table/key/FK/CHECK/append-only trigger and run `PRAGMA foreign_key_check`.
      Attack retained completion history, exact current-version Done trigger/load, AC/graph/Run/
      completion/command/approval/audit/outbox/event project associations and immutable joins.
- [ ] Compile or implement a disposable application-only probe using `port.UnitOfWork` and domain
      values, with no SQLite import or edit, proving T-044 can perform scoped idempotency replay,
      Create/MarkReady/Dispatch/Complete material reads and one atomic aggregate/result/idempotency/
      Run/completion/audit/outbox/event write. Fail if required values are hard-coded by the adapter,
      a SQL error escapes as the not-found contract, or sequence allocation is caller-forgeable.
- [ ] Independently rerun the real-lock timeout and synthetic-message tests; require typed base/
      extended `SQLITE_BUSY`, bounded completion near five seconds and no retry loop.
- [ ] Re-run/inspect all fourteen corrupt Done subcases. Missing/duplicate/wrong project/item/version/
      subject, bad/missing joins and artifact states must return typed storage corruption and zero
      aggregate; independently compile an external-module rehydration-capability import denial.
- [ ] Inspect explicit root/symlink/DSN controls, injected timestamps, blocker missing/resolved/stale/
      race rollback, digest spelling and per-connection PRAGMAs. Reject tests that pass because an
      unintended earlier constraint fires or that do not inspect every relevant row/cursor.
- [ ] Confirm the source/import inventory adds no application command, Fake, HTTP/SSE, provider,
      network-client or shell behavior and keeps all later evidence explicitly NotRun.
- [ ] Validate a one-line-compatible unconditional PASS or FAIL report, exact report-only scope,
      temp cleanup and `git diff --check`.

Verify with: digest-pinned network-disabled Go 1.27.1 container and existing read-only module cache;
SQL/source inspection; disposable external application/internal-import probes; exact report validator.

Budget: One independent final code gate; no fixes, delegation, commits, Git refs, real executor,
credentials, HTTP calls, deployment or external mutation.
