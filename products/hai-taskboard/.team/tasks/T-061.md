ROLE: WORKER

AGENT: persistence-forward-contract-repairer

ID: T-061

Goal: Close the orchestrator's final T-044 forward-contract audit by durably preserving canonical
command result bytes, Evidence verifier run role, and append-only identity fields.

Why: A digest-only command result cannot satisfy response-loss replay, and an Evidence row without
`VerifierRole` cannot reconstruct the accepted domain Done gate after restart. The current migration
also leaves several contractually immutable identities writable even though their state fields may
legitimately advance.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-056/T-057/T-058 reports; SDD HAI-DOMAIN-002,
HAI-DOMAIN-005, HAI-DONE-003 and HAI-EXEC-001; `domain-and-gates.md`,
`persistence-and-recovery.md`, `fake-vertical-slice-implementation.md`; OpenAPI CommandSuccess/
CommandFailure/getCommandResult and Evidence; current domain completion code plus persistence port,
migration, store and all SQLite tests.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 and apply all relevant guidance.

Scope (may touch): `products/hai-taskboard/backend/internal/application/port/persistence.go`;
`products/hai-taskboard/backend/internal/domain/sqlite/migrations/0001_v1.sql`;
`products/hai-taskboard/backend/internal/domain/sqlite/store.go`;
`products/hai-taskboard/backend/internal/domain/sqlite/store_test.go`; `.team/reports/T-061.md`.

Out of scope (must not touch): Modules, domain source/tests, application services, specs/OpenAPI/
traceability/plan/tasks/prior reports, Fake/HTTP/SSE/web/CI/deploy, Git refs, providers/external state.

Definition of done:

- [ ] Add nonempty canonical result bytes to the application-owned `CommandResult` port DTO and V1
      `command_results` row. On write, require its declared digest equals SHA-256(payload); on read,
      return a safe byte copy and fail with typed `domain.ErrStorageCorruption` if stored bytes and
      digest differ. Project-scoped idempotency replay must recover byte-identical CommandSuccess or
      CommandFailure material without re-executing or reconstructing it from mutable state.
- [ ] Persist nonempty Evidence `VerifierRole` separately from verifier actor/class, return it in
      `CompletionMaterial`, and prove it can be passed unchanged to `domain.NewEvidence` so
      `ProhibitedVerifierRunRole` remains enforceable after restart.
- [ ] Make `command_results` and `projection_events` INSERT-only with update/delete triggers. For
      mutable `runs`, `idempotency_records` and `outbox`, add triggers that reject changes to immutable
      identity/input/result/payload fields and reject delete while still allowing only their declared
      state/lease/tombstone transitions. Preserve all existing append-only triggers.
- [ ] Add adversarial tests for response-loss exact result replay, digest/payload mismatch on write,
      corrupt stored result on read, verifier-role round trip/domain reconstruction, and each new
      immutable-field trigger with a positive control for an allowed state-field update.
- [ ] Keep the complete T-058 approval-consumption cardinality, public UnitOfWork, project scoping,
      real typed busy, guarded rehydration, controlled path, history, blocker and digest suites green.
- [ ] Pass pinned network-disabled Go 1.27.1 module/format/vet/named/full/race/engine/license/import
      gates, report validator, exact scope audit and `git diff --check`.

Verify with: existing digest-pinned Go 1.27.1 image/read-only module cache; uncached SQLite named
tests plus focused replay/role/immutability tests; full/race/inventory; current report validator.

Budget: One narrow persistence-contract repair; no delegation, commit, Git ref, new dependency,
application command/Fake/transport behavior, credentials, deployment or external mutation.
