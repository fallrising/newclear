ROLE: REVIEWER

AGENT: command-result-ordering-final-verifier

ID: T-064

Goal: Independently decide whether T-063 closes T-062's allocator/result ordering failure while the
combined T-043/T-054/T-056/T-058/T-061 persistence guarantees remain intact and safe for T-044.

Why: The repair worker reports commit-deferred transaction-local command-result foreign keys and a
non-predictive success oracle, but it cannot accept its own schema/test changes. A fresh reviewer
must attack the real public port and final combined bytes.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-043/T-048/T-051/T-052/T-053/T-054/
T-056/T-057/T-058/T-061/T-062/T-063 tasks and reports; product AGENTS; SDD; ADR-001/002/003/005;
domain, persistence and Fake vertical-slice mini-SDDs; OpenAPI CommandSuccess/CommandFailure;
reproducibility/traceability; backend go.mod/go.sum and every backend source/test/migration file.

Scope (may touch): `.team/reports/T-064.md` only. Read-only candidate mounts plus removable `/tmp`
application/import/schema probes are permitted; remove them before scope audit.

Out of scope (must not touch): Candidate code/dependencies/specs/OpenAPI/tasks/plan/prior reports;
Git refs; credentials/providers/external state. Do not fix findings and do not accept your own report.

Definition of done:

- [ ] Re-run digest-pinned, network-disabled Go 1.27.1 module verify, format, vet, the new T-063
      named oracle, all SQLite tests, full unit and race; verify exact modernc v1.58.0/license and
      linked SQLite >=3.51.3.
- [ ] Build an independent disposable application-only probe importing only application port/domain
      plus standard library. Through the real Store as `port.UnitOfWork`, advance allocator state
      beyond `1`, write completion/consumptions/Done/audit/event/outbox before the result, construct
      canonical CommandSuccess bytes only from returned sequence/cursor, store result/idempotency,
      commit and prove project-scoped byte-identical replay without SQLite access or prediction.
- [ ] In a separate probe transaction omit the final result, require callback completion followed by
      commit-time FK failure, and prove rollback of aggregate/completion/consumption/audit/outbox/
      event and event sequence. Cross-project and permanently dangling references must still fail.
- [ ] Inspect all four command-result FKs: only Approval consumption, audit group and outbox may be
      commit-deferred; idempotency remains immediate. Confirm composite project scope, `foreign_key_check`
      cleanliness, nonempty digest-verified result bytes and command-result INSERT-only triggers.
- [ ] Re-run T-062's canonical success/failure replay, alias/digest/corruption attacks; all nineteen
      corrupt-Done/Approval-cardinality and verifier-role/domain-gate checks; every V1 FK/CHECK/trigger
      attack and legal state control; busy/project/path/symlink/DSN/clock/blocker/rollback/race and
      external rehydration-capability import denial.
- [ ] Confirm source/import inventory adds no application command, Fake, HTTP/SSE, provider,
      network-client or shell behavior and keeps every later acceptance family NotRun.
- [ ] Produce one-line-compatible unconditional PASS or FAIL, validate it, audit exact report-only
      scope/temp cleanup and run `git diff --check`.

Verify with: exact digest-pinned Go container/cache from reproducibility; independent application
and internal-import probes; SQL/source inspection; report validator; diff/scope cleanup audit.

Budget: One fresh independent combined review; no fixes, delegation, commits, Git refs, real
executor, credentials, HTTP calls, deployment or external mutation.
