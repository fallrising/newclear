ROLE: WORKER

AGENT: application-command-boundary-repairer

ID: T-066

Goal: Repair only T-065's three T-044 blockers: real-Store completion ordering, executor declaration
timing and strict canonical result/error validation.

Why: T-065 independently proved the otherwise-green application candidate cannot complete against
the accepted SQLite trigger, calls the executor inside UnitOfWork, and permits stored wire bytes
outside the closed OpenAPI schema. The narrow repair must preserve every passing T-044 gate.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-044/T-065 tasks and reports; product AGENTS;
SDD HAI-AUTH-001/002, HAI-DONE-001..004, HAI-EXEC-001/003 and HAI-API-001; ADR-001/002/003/005;
domain, persistence, executor and Fake vertical-slice mini-SDDs; OpenAPI CommandSuccess,
CommandFailure, ErrorBody and StreamCursor; all current application source/tests; accepted
persistence port, migration/Done trigger, Store completion methods/tests and T-063/T-064 evidence.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 and apply all relevant guidance; explain any relevant rule
that would otherwise be skipped.

Scope (may touch): `products/hai-taskboard/backend/internal/application/command/result.go`; `products/hai-taskboard/backend/internal/application/service/service.go`; `products/hai-taskboard/backend/internal/application/service/service_test.go`; `.team/reports/T-066.md`.

Out of scope (must not touch): every other application file; go.mod/go.sum; persistence port,
domain/reconcile/SQLite; Fake/HTTP/SSE/composition/web/OpenAPI; specs/traceability/plan/tasks/prior
reports; CI/deploy; Git refs; credentials/providers/external state.

Definition of done:

- [ ] In successful CompleteWorkItem, keep every read/domain guard and all ID construction before
      authoritative writes, then call `StoreCompletion` (including exact joins/consumptions) before
      `UpdateWorkItem(...Done...)` so the accepted SQLite trigger observes the matching record. Any
      later failure still rolls back both; the application double must model and test this trigger.
- [ ] Snapshot and validate a cloned immutable `ExecutorDeclaration` during Service construction.
      Dispatch uses that snapshot and never invokes any Executor method while UnitOfWork is active.
      Add a call-tracking/blocking oracle that would fail if Declaration is called after construction.
- [ ] Make canonical success/failure decoding reject unknown top-level or nested members,
      noncanonical key order/whitespace/representation, mismatched identity/operation, malformed
      required values and all failure codes outside the exact OpenAPI ErrorBody enum. Make
      CanonicalFailure itself reject out-of-enum codes; expose the complete closed enum needed by
      later application commands without adding transport behavior.
- [ ] Preserve byte-exact replay of valid stored success/failure, real returned audit/cursor metadata,
      application-bare digests, alias safety, recorded-failure semantics, project/operation scope,
      trusted completion/artifact gates and stable messages.
- [ ] Add named regressions for real-Store-compatible completion order, zero executor calls inside
      transaction and strict canonical/OpenAPI result rejection. In an isolated removable copy,
      rerun T-065's actual Service.CompleteWorkItem over sqlite.Store and prove Done/version 6,
      immutable result/idempotency and byte-exact replay; this probe is repair evidence, not T-047.
- [ ] Pass pinned network-disabled Go 1.27.1 module/format/vet, all T-044/T-066 named/application/
      SQLite/full/race tests, modernc/license/import inventory, unchanged predecessor hashes, task/
      report validators, exact scope/temp cleanup and `git diff --check`; no required skip or failure.
- [ ] Keep T-045 Fake, T-046 HTTP/SSE, T-047 vertical integration, backup/restore, browser, real
      providers and broader G1/G2 gates explicit NotRun/Deferred.

Verify with: exact digest-pinned Go container/cache; named regressions; disposable real-Store
application completion/replay probe; full/race/import/scope/report gates.

Budget: One narrow three-file repair turn; no delegation, commit, Git ref, dependency/spec/persistence
change, Fake/HTTP/SSE behavior, real executor, credential, deployment or external mutation.
