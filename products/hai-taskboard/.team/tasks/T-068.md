ROLE: REVIEWER

AGENT: deterministic-fake-executor-final-verifier

ID: T-068

Goal: Independently attack-review the final T-045 deterministic Fake candidate and decide whether it
satisfies the accepted scope without broadening into application persistence, HTTP/SSE, T-047 or a
real provider.

Why: The candidate passes its author-run gates, but its task report cannot accept itself. A fresh
reviewer must verify immutable deterministic scripting, fail-closed capabilities, fence/recovery
semantics and the no-shell/network/external-write boundary against the actual bytes.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045 task/report; T-041/T-044/T-052/T-065/
T-066/T-067 tasks and reports; product AGENTS; `docs/SDD.md` HAI-BOUNDARY/EXEC/SEC;
ADR-003; `docs/sdd/executor-and-security.md`, `docs/sdd/persistence-and-recovery.md` and accepted
`docs/sdd/fake-vertical-slice-implementation.md` sections 1, 5 and 7; reproducibility/traceability;
HTB-P0A-003; backend go.mod/go.sum; application executor/clock/artifact ports and declaration
validation; every final `internal/executor/fake/**` source/test byte. Note transparently that two
delegated worker attempts stalled without durable writes and the orchestrator integrated the
candidate; decide technical/process acceptance from visible evidence rather than the T-045 claim.

Mandatory Go preflight: before writing any disposable Go attack, run and read the complete
unfiltered `modern-go-guidelines` list for Go 1.27 and apply every relevant rule.

Scope (may touch): `.team/reports/T-068.md` only. Read-only candidate mounts and removable `/tmp`
copies/probes/caches are permitted and must be removed before final scope audit.

Out of scope (must not touch): Candidate Fake files/tests, modules, all other backend source/tests,
specifications/OpenAPI/traceability/plan/tasks/prior reports, HTTP/SSE/T-047, Git refs, credentials,
real providers, PR, merge, deployment, release or external state. Do not fix findings and do not
accept your own report.

Definition of done:

- [ ] Bind the candidate to exact source/test/report SHA-256, confirm branch/baseline ancestry and
      independently rerun digest-pinned network-disabled Go 1.27.1 module verify, empty format, vet,
      the five exact T-045 named tests, repeated Fake tests, full backend and full race.
- [ ] Inspect and attack `port.Executor` satisfaction and declaration identity/version/capability
      normalization. Unknown, duplicate or scenario-undeclared capabilities and unsupported
      lookup/cancel operations must fail before session cursor, tick, transcript or rejection state
      changes; caller mutation cannot change later declarations.
- [ ] Attack scenario immutability and validation: input/returned step, capability, artifact and
      observation aliases; invalid/duplicate IDs, unordered ticks, repeated dispatch, unsupported
      observation/capability, absolute/traversal/URL/Windows/NUL names, invalid media/digest and
      missing staging writer. Same scenario must yield byte/deep-identical ordered transcripts under
      repetition and concurrent tick calls without sleeps, randomness or scheduling-dependent order.
- [ ] Independently attack the worker fence with wrong Run, input digest, holder, lease epoch and
      restore generation; duplicate/out-of-order/regressed-tick/invalid-artifact observations; and
      terminal or stale/late publication after lease expiry. Rejections must be visible while
      accepted sequence/lifecycle/effect/artifacts remain unchanged.
- [ ] Exercise dispatch receipt, start acknowledgement/loss, heartbeat, checkpoint staging, terminal
      success/failure, lookup running/unknown, timeout/unknown and cancel request/acknowledgement.
      Absent lookup/cancel capability fails closed; unconfirmed cancellation and expiry/unknown
      retain uncertainty; no path schedules or reports automatic redispatch.
- [ ] Inspect every production Fake import and API. Require no `os`, `os/exec`, `syscall`, `net/**`,
      database/SQLite, HTTP/transport, provider/client or Git/shell API; artifact bytes may leave Fake
      only through the bounded staging writer with validated relative name, copied bytes and verified
      digest. Keep this labelled a Fake-boundary denial, not real OS isolation.
- [ ] Confirm exact T-045 writable scope, unchanged predecessor/module/port bytes, explicit later-gate
      NotRun inventory and no worker-report self-acceptance. Validate a one-line-compatible report,
      remove every temporary path and run `git diff --check` plus report-only scope audit.
- [ ] Return an unconditional visible PASS or FAIL. Any required failure or skipped/NotRun reviewer
      check makes the review FAIL and remains visible; the reviewer must not repair the candidate.

Verify with: exact digest-pinned Go 1.27.1 image/cache from reproducibility; full source/API/import
inspection; disposable deterministic/concurrent/fence/artifact attacks; current report validator;
temp cleanup; exact report-only scope audit.

Budget: One fresh independent review turn; no delegation, candidate fix, commit, Git ref, HTTP/SSE/
T-047 work, real executor, credential, deployment or external mutation.
