ROLE: WORKER

AGENT: deterministic-fake-boundary-repairer

ID: T-069

Goal: Repair only the four T-068 deterministic Fake failures while preserving every passing T-045
capability, determinism, fencing, recovery, artifact and scope gate.

Why: T-068 independently proved that media-type validation, capability-before-fence ordering,
visible invalid-observation rejection and lifecycle ordering are not yet fail-closed. T-045 remains
unaccepted until these exact defects are repaired and freshly reviewed.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045/T-068 tasks and reports; product AGENTS;
SDD HAI-EXEC/SEC; ADR-003; executor/security, persistence/recovery and Fake implementation mini-SDDs;
current Fake source/tests and unchanged application executor port.

Mandatory Go preflight: before editing Go, run and read the complete unfiltered Modern Go list for
Go 1.27 and apply every relevant rule.

Scope (may touch): `products/hai-taskboard/backend/internal/executor/fake/types.go`;
`products/hai-taskboard/backend/internal/executor/fake/adapter.go`;
`products/hai-taskboard/backend/internal/executor/fake/worker.go`;
`products/hai-taskboard/backend/internal/executor/fake/fake_test.go`; `.team/reports/T-069.md`.

Out of scope (must not touch): Every other file, including modules, application ports/services,
domain/persistence/SQLite, specifications/traceability/plan/tasks/prior reports, HTTP/SSE/T-047,
Git refs, real providers, credentials, PR, merge, deployment, release or external state.

Definition of done:

- [ ] Validate media types with the standard-library MIME parser and reject syntactically invalid
      values including `text/`, `/plain` and `text /plain`; retain bounded length/newline/NUL rules
      and existing valid `application/json`/`text/plain` behavior.
- [ ] In Session lookup/cancel handling, test the declared capability before comparing or recording
      a request fence. An unsupported operation always returns `capability_unsupported` and leaves
      cursor, tick, transcript and rejection state byte/deep-identical even when its request also has
      a stale Run/input/holder/epoch/generation; supported operations retain visible fence rejection.
- [ ] Make every structurally invalid observation, including artifact digest/bytes/name mismatch,
      append one visible rejection while preserving accepted sequence, tick, lifecycle, effect and
      accepted artifacts. Nil worker remains an ordinary invalid-request error because it has no
      state on which to record evidence.
- [ ] Enforce an explicit lifecycle transition table before applying observations: queued accepts
      only dispatch receipt; starting accepts start acknowledgement/loss; running accepts heartbeat,
      checkpoint, terminal success/failure, timeout/unknown; reconciliation accepts lookup running/
      unknown and current cancellation acknowledgement where requested. At minimum, terminal success
      or failure before dispatch/start acknowledgement must visibly reject without mutation.
- [ ] Add regressions equivalent to all four T-068 failing disposable probes, plus valid media,
      supported stale-fence, invalid-observation alias, legal terminal success/failure and lost-start/
      lookup/cancel positive controls. Preserve all prior exact named tests and ten-repeat determinism.
- [ ] Pass pinned network-disabled Go 1.27.1 module/format/vet, focused/named/repeated Fake, full
      backend and race, production import/API inventory, report validator, exact scope audit and
      `git diff --check` with no skipped required check.

Verify with: exact digest-pinned Go image and read-only module cache; T-068 attack regressions; all
T-045 tests; full/race/import inventory; current task/report validators; exact scope audit.

Budget: One narrow repair turn; no delegation, commit, Git ref, new dependency, application/
persistence/domain/HTTP/SSE/T-047 change, real executor, credential, deployment or external mutation.
