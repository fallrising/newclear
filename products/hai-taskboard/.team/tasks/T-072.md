ROLE: REVIEWER

AGENT: deterministic-fake-final-attack-verifier

ID: T-072

Goal: Independently execute the missing attack evidence and decide whether the unchanged T-045/T-069
deterministic Fake candidate satisfies its bounded contract.

Why: T-070 correctly returned PARTIAL because its fresh attacks were skipped. T-071 completed
preflight but stalled at a disposable placeholder and was interrupted without a report. A new
report-only reviewer must perform, not merely plan, every required check.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045/T-068/T-069/T-070/T-071 tasks and existing
reports; product AGENTS; SDD HAI-BOUNDARY/EXEC/SEC; ADR-003; executor/security,
persistence/recovery and accepted Fake implementation mini-SDDs; reproducibility/traceability;
HTB-P0A-003; backend module, unchanged application executor port/declaration validation and every
final Fake source/test byte.

Mandatory Go preflight: before any disposable Go attack, run and read the complete unfiltered
Modern Go list for Go 1.27 and apply every relevant rule.

Scope (may touch): `.team/reports/T-072.md` only. Read-only candidate mounts plus removable `/tmp`
copies/probes/caches are permitted and must be deleted before handoff.

Out of scope (must not touch): Candidate source/tests, modules, application ports/services,
domain/persistence/SQLite, specifications/OpenAPI/traceability/plan/tasks/prior reports, HTTP/SSE/
T-047, Git refs, real providers, credentials, PR, merge, deployment, release or external state. Do
not fix findings and do not accept your own report.

Definition of done:

- [ ] Bind exact candidate and prior-report hashes; rerun digest-pinned, network-disabled Go 1.27.1
      module verify, empty format, vet, five exact T-045 tests, T-068 regression, ten-repeat Fake,
      full backend and full race gates.
- [ ] In a fresh disposable external-package test, reproduce all four T-068 findings after repair:
      malformed MIME is rejected; unsupported lookup/cancel wins over each stale fence dimension
      without mutation; invalid artifact name/bytes/digest shape visibly rejects without accepted
      mutation; terminal success/failure reject from Queued and Starting and accept from Running.
- [ ] In disposable external-package tests, cover every explicit lifecycle transition plus
      out-of-order, duplicate, regressed, late, stale and expired rejection/state preservation;
      lost-start lookup and cancellation uncertainty/confirmation; and zero redispatch.
- [ ] In disposable external-package tests, cover declaration/scenario/returned-observation/artifact
      alias safety, repeated and concurrent finite explicit-tick transcript determinism, staging
      digest mismatch, supported stale-fence rejection and hostile artifact names. Use no sleeps or
      scheduling-dependent assertion.
- [ ] Inspect production imports/API for the bounded no-host-filesystem/shell/Git/network/HTTP/
      SQLite/transport/credential/provider capability claim. Confirm exact repair scope, unchanged
      predecessors/historical reports, explicit later-gate NotRun inventory, report validation,
      temp cleanup, `git diff --check` and report-only scope audit.
- [ ] Return unconditional PASS or FAIL. Any required failure or skipped reviewer check is FAIL;
      never modify or repair the candidate.

Verify with: exact pinned Go image/read-only module cache; fresh disposable external-package attack
tests (separate focused files are encouraged); source/API/import inspection; current report
validator; cleanup and report-only scope audit.

Budget: One fresh independent evidence review; no delegation, candidate fix, commit, Git ref,
HTTP/SSE/T-047, real executor, credential, deployment or external mutation. Start executing the
probe promptly after preflight; do not stop at a placeholder.
