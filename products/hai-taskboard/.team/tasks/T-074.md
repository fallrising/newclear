ROLE: REVIEWER

AGENT: deterministic-fake-determinism-and-security-verifier

ID: T-074

Goal: Independently attack deterministic scripting, alias safety, staging and forbidden-capability
boundaries of the unchanged T-045/T-069 candidate.

Why: T-070 skipped fresh attacks, while two all-in-one retry reviewers stalled before creating a
probe. This smaller report-only review owns the determinism/artifact/security half of the evidence.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045/T-068/T-069/T-070 tasks/reports; product
AGENTS; SDD HAI-BOUNDARY/EXEC/SEC; ADR-003; executor/security and accepted Fake implementation
mini-SDDs; reproducibility/traceability; HTB-P0A-003; backend module, unchanged executor port and all
final Fake source/test bytes. Run and read the complete unfiltered Modern Go list for Go 1.27 before
writing disposable Go.

Scope (may touch): `.team/reports/T-074.md` only. Read-only candidate mounts and removable `/tmp`
probes/caches are permitted and must be removed.

Out of scope (must not touch): Candidate/tests, modules, all other product/spec/plan/task/prior-report
files, Git refs, HTTP/SSE/T-047, real providers, credentials, PR, merge, deploy or external state.

Definition of done:

- [ ] Bind exact candidate/predecessor hashes and freshly verify `port.Executor`, exact adapter
      identity/version, capability sorting/fail-closed behavior and immutable returned declaration.
- [ ] Run fresh disposable external-package tests for input/returned scenario, step, observation and
      artifact aliases; repeated and concurrent finite explicit-tick transcript determinism; staging
      writer copied bytes/digest mismatch; supported stale-fence rejection; hostile artifact names;
      invalid/duplicate scenario data and no scheduling-dependent expected transcript.
- [ ] Inspect every production import and exported API and prove the bounded Fake has no host
      filesystem, shell, Git, network/HTTP, database/SQLite, transport, credential or provider/client
      capability. Keep this claim limited to the Fake boundary, not real OS isolation.
- [ ] Run pinned network-disabled Go 1.27.1 module verify, empty format, vet, Fake tests, full backend
      and full race; validate an unconditional PASS/FAIL report, remove temp paths, run
      `git diff --check`, and prove report-only scope. Any failed/skipped required item is FAIL;
      never repair the candidate.

Budget: One focused independent reviewer, no delegation or candidate/Git/external mutation.
