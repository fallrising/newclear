ROLE: REVIEWER

AGENT: deterministic-fake-parent-path-final-verifier

ID: T-076

Goal: Independently decide whether T-075 closes T-074's exact-parent artifact-name bypass while
preserving the complete accepted-candidate evidence set.

Why: T-075 is worker evidence and cannot accept itself. The reviewer that reproduced the original
failure must attack the repaired bytes without modifying them.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-045/T-068/T-069/T-070/T-073/T-074/T-075
tasks/reports; product AGENTS; SDD HAI-BOUNDARY/EXEC/SEC; ADR-003; executor/security and accepted
Fake implementation mini-SDDs; reproducibility/traceability; HTB-P0A-003; backend module, unchanged
executor port and every repaired Fake source/test byte. Run and read the complete unfiltered Modern
Go list for Go 1.27 before writing disposable Go.

Scope (may touch): `.team/reports/T-076.md` only. Read-only candidate mounts and removable `/tmp`
probes/caches are permitted and must be removed.

Out of scope (must not touch): Candidate/tests, modules, every other product/spec/plan/task/prior-
report file, Git refs, HTTP/SSE/T-047, real providers, credentials, PR, merge, deploy or external
state. Do not repair findings or accept your own report.

Definition of done:

- [ ] Bind T-075 report and repaired file hashes; confirm its exact two-code-file diff and unchanged
      adapter/worker/predecessor/historical-report bytes.
- [ ] Freshly reproduce T-074's exact `..` attack and require rejection before any staging-writer
      call; rerun the other hostile-name and valid nested-name controls, staging-digest, alias,
      supported-fence and deterministic/concurrent controls against repaired bytes.
- [ ] Rerun pinned network-disabled Go 1.27.1 module verify, empty format, vet, all exact T-045 tests,
      T-068 regression, ten-repeat Fake, full backend and full race; inspect imports/API and retain the
      bounded Fake-only security claim.
- [ ] Confirm T-073's lifecycle-covered adapter/worker hashes are unchanged and no later-gate
      NotRun claim was broadened. Validate an unconditional PASS/FAIL report, clean temp paths, run
      `git diff --check` and prove report-only scope. Any failed/skipped required check is FAIL.

Verify with: full Modern Go list; fresh disposable exact-parent plus preserved T-074 attacks;
digest-pinned offline standard/full/race gates; hash/import/API/scope inspection; current report
validator; cleanup; `git diff --check`.

Budget: One final report-only review; no delegation, fix, commit, Git ref, HTTP/SSE/T-047,
real-provider, credential, deploy or external mutation.
