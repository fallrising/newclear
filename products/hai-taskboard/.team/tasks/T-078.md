ROLE: WORKER

AGENT: http-sse-boundary-narrow-repairer

ID: T-078

Goal: Repair only the three deterministic T-077 acceptance failures in the exact T-046 candidate:
canonical validation of immutable stored command-result bytes, contiguous durable replay, and
OpenAPI-valid cursor-bound SSE projection data.

Why: T-077 independently reproduced that digest-correct malformed result bytes return 200, replay
can jump from sequence 1 to 3, and arbitrary single-line payloads lacking the required ProjectionEvent
shape are emitted. T-046 remains rejected until these exact failures are repaired and independently
re-reviewed; already-passing auth, routing, queue, revocation and composition behavior must remain
unchanged.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; `.team/tasks/T-046.md`;
`.team/reports/T-046.md`; `.team/tasks/T-077.md`; `.team/reports/T-077.md`; product `AGENTS.md`;
`docs/HANDOFF.md`; `docs/SDD.md` HAI-API-001..005 and HAI-BOUNDARY-003; ADR-001/003/005;
`docs/sdd/fake-vertical-slice-implementation.md` section 6; `docs/reproducibility.md`;
`docs/traceability.md`; complete `api/openapi.yaml` CommandSuccess, CommandFailure, ProjectionEvent,
ProjectionResetRequired and StreamCursor schemas; accepted application `command/result.go`,
application projection port/service event encoder; and every current T-046 transport source/test.
Treat all inputs except the five paths in Scope as read-only.

Mandatory Go preflight: before editing Go, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27. Apply each relevant rule; use `explain` before skipping an
apparently applicable rule.

Scope (may touch): `products/hai-taskboard/backend/internal/transport/httpapi/server.go`;
`products/hai-taskboard/backend/internal/transport/httpapi/server_test.go`;
`products/hai-taskboard/backend/internal/transport/httpapi/sse.go`;
`products/hai-taskboard/backend/internal/transport/httpapi/sse_test.go`;
`.team/reports/T-078.md`.

Out of scope (must not touch): Every other candidate or predecessor byte, including T-046 task and
report, T-077 task and FAIL report, `types.go`, `digest.go`, composition root, go.mod/go.sum,
application/port/domain/reconciliation/SQLite/Fake, OpenAPI/specification/traceability/
reproducibility/HANDOFF/PLAN; T-047/integration; restore/backup; real provider/credentials/agent
execution; Git refs, PR, merge, deployment, release, network or external state.

Definition of done:

- [ ] Before returning any stored result, preserve the existing ID/project/digest/byte checks and
      additionally call the accepted application canonical decoder with the success operation
      discriminator extracted from the payload; for failures use any accepted operation because the
      canonical failure envelope is operation-independent. Reject invalid discriminator, unknown or
      duplicate members, noncanonical formatting, wrong command identity, invalid operation/result,
      or otherwise malformed success/failure bytes with the same non-disclosing fail-closed response.
      Valid canonical success and failure bytes must remain byte-for-byte unchanged; lookup never
      invokes a command.
- [ ] Validate a durable replay as one exact contiguous sequence: events begin at cursor+1, advance
      by exactly one, stay in the same project/epoch, and finish exactly at declared high-water;
      empty replay is valid only when high-water equals cursor. Reject a 1-to-3 gap, missing tail,
      duplicate, reversal, wrong project/epoch or out-of-range event before beginning SSE.
- [ ] Decode each accepted application durable change-event payload strictly, reject duplicate or
      unknown members, require `api_version:v1`, exact event/resource-kind pair, matching project,
      bounded nonempty resource ID and resource version >=1, then canonically encode the OpenAPI
      ProjectionEvent with a `cursor` exactly equal to the SSE `id`. The accepted application event
      carries notification metadata rather than an entity body, so emit an empty `projection` object
      permitted by OpenAPI; do not invent mutable state or broaden into T-047. Invalid data must fail
      closed before replay headers or close an already-started live stream without emitting it.
- [ ] Never emit a live sequence greater than last-delivered+1. Duplicate/older notifications remain
      discarded. A live gap or out-of-order publication must fail closed without emitting the gapped
      event; do not invent a reset reason outside the OpenAPI enum and do not block or write a socket
      under the hub mutex.
- [ ] Add deterministic regression tests that make each T-077 probe pass: digest-correct malformed
      stored success/failure rejected while canonical bytes are exact; 1-to-3/missing-tail replay
      rejected; valid SSE data strictly decodes to the required OpenAPI shape with cursor equal to id;
      malformed payload and live gap emit no corrupt/gapped projection. Preserve every existing named
      auth/digest/snapshot/slow-consumer/revocation/import/composition test.
- [ ] Run the complete pinned Go 1.27.1 gate with exact digest, `--network none`, read-only candidate
      and module-cache mounts, `GOPROXY=off`, `GOSUMDB=off`: module verify, empty gofmt, vet, all four
      T-046 names, fresh T-077 regressions, all transport/composition, ten repeats, full backend and
      full race. Preserve all FAIL/skipped attempts visibly.
- [ ] Produce a validator-compatible T-078 worker report with exact commands/exits, candidate hashes,
      retained T-077 FAIL and T-046 repaired/interrupted history, scope audit and explicit NotRun
      boundaries. Worker evidence cannot accept itself; do not commit or push.

Verify with: complete Modern Go list; current teamctl task/report validators; digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under the existing network-disabled read-only toolchain; uncached focused/named/repeated/full/race
tests; source/hash/scope audit; `git diff --check`.

Budget: One bounded repair turn; no delegation, scope expansion, dependency/predecessor/report edit,
T-047, restore/backup, real provider/credential, network, commit, push or external mutation.
