ROLE: REVIEWER

AGENT: http-sse-boundary-repair-final-verifier

ID: T-079

Goal: Independently re-review the exact T-078 repair and the complete final T-046 HTTP/SSE candidate,
reproducing every T-077 blocker and completing the reviewer checks that T-077 retained as skipped.

Why: T-077 rejected T-046 on three deterministic defects and T-078 reports a bounded repair. Only a
fresh report-only reviewer can determine whether those exact bytes now satisfy the original envelope
without regressions or scope expansion.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-041/T-046/T-052/T-064/T-067/T-068/T-069/
T-070/T-073/T-074/T-075/T-076/T-077/T-078 tasks/reports; product AGENTS; HANDOFF; SDD HAI-BOUNDARY/
AUTH/API/SEC; ADR-001/003/005; persistence/recovery, executor/security and accepted Fake mini-SDDs;
reproducibility/traceability; HTB-P0A-003; complete OpenAPI; backend module; accepted application
command result/service/projection-port APIs; public Store/Fake construction; and every final T-046/
T-078 source/test byte. Bind T-078 report SHA-256
`21d2929e084f1aea32b6ea26f546922b63c5616474eea3da18e3cafb6ed1a9a1` and repaired hashes
`0bd3bec2e0724be84fdc54acaa598e8ee1bce42600b9f6c171282b1a21646c4b` server.go,
`4cc7a46b596f7ba8055f53bd3e76c427f49836029ea89153740dc5c51d2c14f2` server_test.go,
`2523a2ed1a91c8cc33303dca979997c8a4c9ce017104da5d229917cea26d5cb8` sse.go and
`424e4771bcb185ebb7022644b8b9108ad9e4f6fd8eea93ff4aa032fd4d60c187` sse_test.go. Before writing
disposable Go, run and read the complete unfiltered Modern Go list for Go 1.27.

Scope (may touch): `.team/reports/T-079.md` only. Read-only candidate/module mounts and removable
isolated `/tmp` probes/copies/caches are allowed and must be removed before handoff.

Out of scope (must not touch): Candidate source/tests, modules, every prior task/report, application/
port/domain/reconciliation/SQLite/Fake, OpenAPI/spec/docs/PLAN/HANDOFF, T-047/integration, restore/
backup, Git refs, real providers/credentials, PR, merge, deployment, release or external state. Do
not fix findings and do not accept your own report.

Definition of done:

- [ ] Confirm exact branch/local/remote/baseline identity, final candidate/task/report hashes,
      preserved T-068/T-074 FAIL and T-070 PARTIAL hashes, and absent T-071/T-072 reports. Rerun the
      complete T-078 digest-pinned, network-disabled, read-only candidate/module Go 1.27.1 gate:
      module/format/vet, four original names, three T-077 regressions, transport/composition,
      ten-repeat, full backend, full race and inventory.
- [ ] Freshly reproduce the three T-077 attacks outside candidate tests. Require digest-correct
      malformed/noncanonical/wrong-command result bytes to fail closed while canonical success and
      failure stay exact; replay gaps and missing declared tails to fail before SSE; and emitted
      projection data to strictly match OpenAPI with cursor equal to id, exact project, allowed
      event/resource pair, positive version and required projection object.
- [ ] Complete the previously skipped HTTP attacks: method-aware canonical route spellings,
      percent/double/backslash/NUL paths, duplicate session/Origin/Last-Event-ID headers, duplicate
      JSON/query cursor, wrong method/content type/origin, exact body limit, all five accepted command
      decoders and unsupported families. Require auth before sensitive reads, exact one configured
      Origin before mutation, project/result authorization before lookup, no payload actor authority,
      non-disclosing fail-closed lookup and command-free result replay.
- [ ] Freshly attack snapshot/replay/live ordering with deterministic commits around source reads,
      duplicates and deliberate live 3-before-2. Require contiguous monotonic output or fail-closed
      before any gapped/corrupt data; current replay is exactly greater than cursor, minimum-1 is
      resumable, and older/wrong-epoch cursors emit one canonical reset then close.
- [ ] Freshly exercise both subscriber limits independently in a disposable in-package test: event
      129 and bytes 1,048,577 must make publication return promptly, attempt exactly one
      `slow_consumer` reset and close. Verify revocation produces exactly one canonical reset and no
      later data, projects are isolated, and no hub-mutex path writes a socket or blocks a command.
- [ ] Inspect imports and public composition. Transport imports no concrete adapter; only
      cmd/taskboard wires Store, Fake declaration, Service, result/projection/session sources, Hub and
      Server. Do not require listener/runtime/T-047/restore behavior and do not accept invented
      application state in the empty OpenAPI-permitted projection object.
- [ ] Preserve T-046 repaired/interrupted gate history and T-077 FAIL verbatim. Keep T-047,
      Fake-to-application persistent worker execution, backup/restore, browser, providers/credentials
      and broader G1/G2 explicit NotRun/Deferred. Remove temp paths; validate the reviewer report;
      run `git diff --check`; prove report-only scope.
- [ ] Return unconditional PASS only if every required reviewer check passes with none failed or
      skipped; otherwise unconditional FAIL with STATUS PARTIAL/BLOCKED. Never repair, accept, commit
      or push.

Verify with: complete Modern Go list; exact pinned Go image/network-none/read-only mounts; fresh
external/in-package HTTP/SSE/OpenAPI/backpressure probes; source/import/API/hash/scope inspection;
teamctl validate-report; temp cleanup; `git diff --check`.

Budget: One fresh independent report-only review; no delegation, repair, candidate edit, commit,
Git ref, T-047, restore/backup, provider/credential, deployment or external mutation.
