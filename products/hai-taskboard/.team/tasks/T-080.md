ROLE: WORKER

AGENT: http-origin-revocation-narrow-repairer

ID: T-080

Goal: Repair only the T-079 duplicate-Origin and revocation-precedence failures, and prove that a
wrong-epoch live notice reports the already-read authoritative source epoch.

Why: T-079 accepted the three T-078 repairs and full pinned suite but independently reproduced that
two Origin values can execute CreateProject and that an already-revoked session with queued data can
receive a projection before its reset. Its source audit also identified a nearby live wrong-epoch
reset using the untrusted notice epoch rather than the authoritative snapshot/replay epoch.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-task-worker;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-046/T-077/T-078/T-079 tasks and reports;
product AGENTS; HANDOFF; SDD HAI-AUTH-002, HAI-API-001..005 and HAI-SEC-001/003/005; ADR-001/003/005;
`docs/sdd/fake-vertical-slice-implementation.md` section 6; OpenAPI ProjectionEvent and
ProjectionResetRequired; and every final transport source/test. Bind T-079 report SHA-256
`84e01d1b3f6a150841dc7a7e358157f9c991844d0b781dbfd01fbdbd818b8e68` and T-078 report SHA-256
`21d2929e084f1aea32b6ea26f546922b63c5616474eea3da18e3cafb6ed1a9a1`.

Mandatory Go preflight: before editing Go, rerun and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 and apply every relevant rule.

Scope (may touch): `products/hai-taskboard/backend/internal/transport/httpapi/server.go`;
`products/hai-taskboard/backend/internal/transport/httpapi/server_test.go`;
`products/hai-taskboard/backend/internal/transport/httpapi/sse.go`;
`products/hai-taskboard/backend/internal/transport/httpapi/sse_test.go`;
`.team/reports/T-080.md`.

Out of scope (must not touch): Every other candidate/predecessor/task/report byte, modules,
composition root, application/port/domain/reconciliation/SQLite/Fake, OpenAPI/spec/docs/PLAN/
HANDOFF, T-047/integration, restore/backup, real providers/credentials/agent execution, Git refs,
PR, merge, deployment, release, network or external state.

Definition of done:

- [ ] Every state-changing request requires exactly one Origin header field value and it must equal
      the configured Origin byte-for-byte. Missing, duplicate (even if the first value matches),
      comma-combined or wrong Origin fails before body read and command invocation. Do not change GET
      behavior or accept browser/user authority from a payload.
- [ ] Obtain the session revocation channel exactly once per SSE request. After authoritative
      snapshot/replay state is known, check revocation before starting replay/data and before each
      queued event write; an already-closed revocation channel must deterministically emit exactly one
      canonical `authorization_revoked` reset and no projection data, including when a valid event is
      already queued. Recheck after receiving an event so ready-channel select fairness cannot leak it.
- [ ] Preserve context cancellation, slow-consumer terminal handling, replay/live deduplication and
      fail-closed live-gap behavior without blocking Hub publication or writing a socket under its
      mutex. If a queued live notice has a wrong epoch, emit one `epoch_mismatch` reset whose
      `current_epoch` is the authoritative snapshot/replay epoch, not the notice's untrusted epoch.
- [ ] Add deterministic T-079 regression tests for two Origin values, comma-combined Origin,
      revoked-plus-queued data, one Revoked call per request, and authoritative current epoch on a
      wrong-epoch live notice. Preserve all T-046/T-077 tests and passing 129-event/1,048,577-byte
      behavior; no sleeps or probabilistic retry loops.
- [ ] Pass the complete T-079/T-078 pinned Go 1.27.1 network-disabled/read-only gate: module verify,
      empty gofmt, vet, four original names, three T-077 regressions, new T-079 regressions,
      transport/composition, ten repeats, full backend, full race and inventory. Preserve every
      setup failure, FAIL or skipped entry visibly.
- [ ] Produce a validator-compatible T-080 worker report with exact commands/exits and final hashes,
      preserving T-077/T-079 FAIL, T-070 PARTIAL, T-068/T-074 FAIL and T-071/T-072 absent history.
      Worker evidence cannot accept itself; do not commit or push.

Verify with: complete Modern Go list; current teamctl task/report validators; exact pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under network-none/read-only candidate/module mounts; focused/named/repeated/full/race tests;
source/hash/scope audit; `git diff --check`.

Budget: One bounded repair turn; no delegation, scope expansion, prior-report edit, T-047,
restore/backup, provider/credential, network, commit, push or external mutation.
