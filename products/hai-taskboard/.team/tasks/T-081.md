ROLE: REVIEWER

AGENT: http-sse-boundary-final-acceptance-verifier

ID: T-081

Goal: Perform the final independent report-only acceptance review of the exact T-080-repaired T-046
HTTP/SSE boundary, with fresh attacks for every T-077/T-079 failure and no required skipped check.

Why: T-077 and T-079 remain preserved FAIL evidence; T-078 and T-080 are worker evidence only. The
orchestrator cannot accept T-046 until an independent reviewer binds the final bytes and proves both
repair sets plus the original bounded gates.

Inputs to read first: modern-go-guidelines:use-modern-go; codex-team-superpowers:codex-evidence-gate;
`.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`; T-046/T-077/T-078/T-079/T-080 tasks/reports;
product AGENTS; HANDOFF; SDD HAI-BOUNDARY/AUTH/API/SEC; ADR-001/003/005; accepted Fake mini-SDD
section 6; reproducibility/traceability; HTB-P0A-003; complete OpenAPI; application command-result and
projection contracts; and every final transport/composition source/test. Bind T-080 report SHA-256
`22bf843b930b9bcbff9a750c880d226778266dca40a7b8645909d1f92102eae0` and final repaired hashes
`ae5adb54f1c2ae6cdaa665c920e404ab816cae8e348c3a52f29071b3eb21c71a` server.go,
`f96a5e16adb3cc7d67180ccefc8fa9ed8d69709b780c7152ee6052b4083413a5` server_test.go,
`f95210165656c5965174578c9b562d96ddf884f47de03e6141ff4467d1b0d71e` sse.go and
`2c03d5abaa9899fe580029fa65be4795973f830b594508e86e7df40a95d4ad1f` sse_test.go. Before any
disposable Go, run and read the complete unfiltered Modern Go list for Go 1.27.

Scope (may touch): `.team/reports/T-081.md` only. Read-only candidate/module mounts and removable
isolated `/tmp` probes/copies/caches are permitted and must be removed before handoff.

Out of scope (must not touch): Candidate source/tests/modules, every prior task/report, application/
port/domain/reconciliation/SQLite/Fake, OpenAPI/spec/docs/PLAN/HANDOFF, T-047/integration, restore/
backup, Git refs, real provider/credential/agent execution, PR, merge, deployment, release or
external state. Do not fix findings and do not accept your own report.

Definition of done:

- [ ] Confirm branch/local/remote/baseline identity, exact final task/report/source hashes, preserved
      T-068/T-074/T-077/T-079 FAIL and T-070 PARTIAL hashes, and absent T-071/T-072 reports.
- [ ] Independently rerun the exact digest-pinned, network-disabled, read-only candidate/module Go
      1.27.1 final gate: module verify, empty gofmt, vet, four original T-046 names, three T-077 and all
      T-079 regressions, transport/composition, ten-repeat, full backend, full race and inventory.
- [ ] Fresh disposable probes, independent of candidate tests, must prove: digest-correct malformed
      stored results fail closed while canonical bytes remain exact; durable gap/missing-tail replay
      fails before SSE; valid projection data matches OpenAPI and cursor/id while corrupt data and
      live gap are never emitted.
- [ ] Freshly prove mutation accepts exactly one configured Origin and rejects duplicate,
      comma-combined, missing and wrong values before body/command. Freshly prove each SSE request
      acquires Revoked once; a preclosed channel wins over queued and replay data with exactly one
      canonical authorization reset and no projection; wrong live epoch reports authoritative epoch.
- [ ] Freshly prove 129-event and 1,048,577-byte overflow each returns promptly, produces exactly one
      terminal slow-consumer reset and no later projection when exercised through a stream. Preserve
      project isolation, duplicate Last-Event-ID/query rejection, retention/minimum-1/wrong-epoch
      reset, method/path/body/five-command and import/composition guarantees through fresh source
      audit plus the final uncached/repeated suite.
- [ ] Preserve T-046 worker repaired/detached history and T-077/T-079 FAIL bytes. T-047,
      Fake-to-application persistent worker execution, backup/restore, browser, provider/credentials
      and broader G1/G2 remain explicit NotRun/Deferred, but these out-of-envelope boundaries are not
      required reviewer checks and must not be mislabeled as an acceptance skip.
- [ ] Remove all temp paths, validate report, run `git diff --check`, prove report-only scope. Return
      unconditional PASS with STATUS DONE only if every required item above passes and none is failed
      or skipped; otherwise return unconditional FAIL with STATUS PARTIAL/BLOCKED. Never repair,
      self-accept, commit or push.

Verify with: complete Modern Go list; exact pinned Go image/network-none/read-only mounts; fresh
external/in-package HTTP/SSE/OpenAPI/backpressure probes; source/import/API/hash/scope inspection;
teamctl validate-report; temp cleanup; `git diff --check`.

Budget: One bounded independent final review; no delegation, candidate edit, repair, commit, Git ref,
T-047, restore/backup, provider/credential, deployment or external mutation.
