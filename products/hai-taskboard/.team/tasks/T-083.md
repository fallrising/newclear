ROLE: WORKER

AGENT: vertical-authority-seam-repairer

ID: T-083

Goal: Repair only the predecessor production seams proven absent by T-047/T-082: application-owned
durable outbox/Run lease execution publication, exact Candidate/Review/Evidence/Approval-to-QA
completion preparation, and cancellation/reconciliation persistence with authoritative reads.

Why: The independent T-082 review proved every T-047 named oracle stops at a real exported-authority
gap. The accepted V1 schema already models Runs, leases, outbox, immutable completion material,
audit and projection state, and the accepted OpenAPI already names the required fixture/human
commands. A separate predecessor task must implement that authority before integration evidence can
legally remove its skips; T-047 may not emulate it through SQL or test-owned logic.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041 decomposition; T-043/T-044/T-045/T-046/T-054/T-056/
T-058/T-061/T-063/T-064/T-065/T-066/T-067/T-068/T-069/T-073/T-074/T-075/T-076/T-077/T-078/
T-079/T-080/T-081 tasks/reports; exact T-047 task/report/candidate and T-082 task/FAIL report;
product AGENTS; HANDOFF; SDD HAI-AUTH-001..002, HAI-DOMAIN-001..005, HAI-STATE-003..005,
HAI-DONE-001..004, HAI-EXEC-001..007, HAI-API-001/005 and HAI-SEC-004..006; ADR-001/002/003/005;
domain/gates, persistence/recovery, executor/security and accepted Fake vertical implementation
mini-SDDs; reproducibility/traceability; HTB-P0A-003; complete OpenAPI command/result/run/candidate/
review/evidence/approval schemas; backend go.mod/go.sum and every current backend source/test/
migration file. Treat T-047 integration and all non-scope predecessors as read-only.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 using the repository-approved wrapper. Apply every relevant
rule and call `explain <guideline-id>` before skipping a returned rule that appears applicable.

Scope (may touch): `products/hai-taskboard/backend/internal/application/command/**`;
`products/hai-taskboard/backend/internal/application/port/persistence.go`;
`products/hai-taskboard/backend/internal/application/service/**`;
`products/hai-taskboard/backend/internal/domain/sqlite/store.go`;
`products/hai-taskboard/backend/internal/domain/sqlite/store_test.go`; `.team/reports/T-083.md`.

Out of scope (must not touch): T-047 `backend/integration/**`, deterministic Fake source/tests,
HTTP/SSE and `cmd/taskboard`, domain/reconciliation source/tests, rehydration files, V1 migration,
go.mod/go.sum, web, OpenAPI/specification/traceability/reproducibility/HANDOFF/PLAN, task/prior-report
bytes, backup/restore, real provider/credential/agent execution, Git refs, commit, push, PR, merge,
network, deployment, release or external mutation. Do not weaken an accepted invariant or edit a
test oracle to hide a failure. If the accepted schema or an out-of-scope package truly prevents this
repair, preserve PARTIAL evidence and stop rather than widening the task.

Definition of done:

- [ ] Write named application/SQLite test skeletons before behavior. Extend the application-owned
      persistence port with domain/port DTOs and atomic methods sufficient to load a project-scoped
      Run, conditionally claim one Pending outbox+Run lease with a monotonically increasing epoch,
      inspect the current fence/input/restore generation, persist accepted fenced lifecycle and
      reconciliation observations, advance outbox/Run dimensions, and read the authoritative result.
      The port exposes no SQL/concrete adapter and cannot let a caller forge allocator/audit state.
- [ ] Implement the real SQLite transaction methods over the existing V1 tables without migration
      changes. Claim/update predicates must bind project, Run, immutable input digest, holder,
      current epoch and restore generation; stale/wrong publishers return a typed application-visible
      rejection before mutation. Run/outbox/lease update, immutable audit entry and projection event
      commit atomically. A missing final result or forced seam rolls all state/cursor changes back.
- [ ] Add application-owned runtime operations that claim a persisted dispatch, return an immutable
      executor envelope/fence bound to the stored Run input, and accept Fake observations as untrusted
      data only after current-fence, lifecycle, capability, digest and restore checks. Accepted start,
      terminal success/failure, timeout/lookup-unknown and stale publication update durable Run
      dimensions correctly; stale/late publication is auditable and cannot create Candidate, advance
      phase or redispatch. The application does not import concrete Fake or SQLite.
- [ ] Add only the already-specified application command subset required to prepare completion:
      `SubmitCandidate`, `PublishFixtureReview`, `PublishFixtureEvidence`, `ApproveSubject` and
      `RequestQA`. Each uses authenticated/call-context authority, expected version, idempotency and
      canonical command result; validates the persisted current Run/Candidate/exact subject and
      artifact digest/availability; enforces independent reviewer/verifier data; stores immutable
      material; and uses domain transitions to move Developing→Review→QA. No direct phase update,
      forged human actor or successful Run→Done shortcut is allowed.
- [ ] Add `RequestCancellation` plus the bounded unknown/reconciliation publication needed by T-047.
      Cancellation persists intent only. A timeout/lost contact/lookup-unknown becomes durable
      `OutcomeUnknown`/`NeedsReconcile`; it never becomes Canceled, confirmed stopped or redispatched.
      Only a current fenced cancel acknowledgement may establish Canceled where the existing domain
      contract permits. Expose project-scoped authoritative reads needed to verify Run/outbox/audit/
      projection effects without a raw table/query escape hatch.
- [ ] Preserve command atomicity: aggregate/result/idempotency/audit/projection/outbox/material and
      allocator cursor are all-or-nothing; response-loss replay is byte-exact and cannot repeat Fake
      dispatch or mutation. Canonical request/result decoding remains closed to unknown/duplicate or
      out-of-enum data and the HTTP transport remains unchanged, still exposing only its accepted
      five operations.
- [ ] Add and pass exact new tests `TestVerticalAuthority_ClaimAndPublishFencedRun`,
      `TestVerticalAuthority_PreparesSubjectBoundCompletion` and
      `TestVerticalAuthority_CancelUnknownRemainsUnconfirmed`, plus real-Store failure injection,
      stale/wrong fence, concurrent claim, response-loss/idempotency, subject/artifact/independence,
      cancel-ack positive control and zero-redispatch attacks. Tests use injected time/IDs and bounded
      synchronization, never sleeps, ambient randomness, direct SQL from application tests or a
      concrete Fake/HTTP import.
- [ ] Keep all accepted predecessor tests green and prove production application imports neither
      concrete SQLite nor Fake/HTTP/shell/network. Bind exact predecessor/new hashes and show the only
      writes are authorized paths. Preserve T-047 PARTIAL and T-082 FAIL bytes verbatim; this worker
      report cannot accept the repair.
- [ ] Pass the complete digest-pinned network-disabled Go 1.27.1 gate: module verify, empty gofmt,
      vet, three new names, all application and SQLite tests, full backend, full race and import/test
      inventory; validate task/report, run `git diff --check`, and retain every failure or skip.

Verify with: complete unfiltered Modern Go list; current teamctl task/report validators; exact pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under network-none/read-only module cache; uncached named/application/SQLite/full/race tests;
source/import/API/predecessor-hash/exact-scope inventory; `git diff --check`.

Budget: One bounded predecessor repair turn; no delegation, T-047/Fake/HTTP/module/migration/spec/
status-document edit, commit, push, Git ref, provider/credential, network, deployment, release or
external mutation.
