ROLE: REVIEWER

AGENT: vertical-authority-seam-verifier

ID: T-084

Goal: Independently accept or reject the exact T-083 predecessor-repair candidate that adds
application-owned durable Run/outbox lease publication, completion preparation and cancellation
reconciliation authority, without modifying candidate or predecessor bytes.

Why: T-083 reports DONE only after the preserved T-047 PARTIAL and T-082 FAIL proved real exported
authority gaps. Worker evidence cannot accept itself. A fresh report-only reviewer must adversarially
bind the candidate, rerun every mandatory gate, and decide whether it is safe to reopen T-047.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041 decomposition; T-047 task/PARTIAL report/candidate;
T-082 task/FAIL report; T-083 task/DONE report; T-045/T-046 and T-054/T-056/T-058/T-061/T-063/
T-064/T-065/T-066/T-067/T-068/T-069/T-073/T-074/T-075/T-076/T-077/T-078/T-079/T-080/T-081
tasks/reports; product AGENTS; HANDOFF; SDD; ADR-001/002/003/005; domain/gates,
persistence/recovery, executor/security and Fake vertical-slice mini-SDDs; reproducibility;
traceability; HTB-P0A-003; complete OpenAPI; backend module/migration and every current backend
source/test byte. Treat the candidate and all predecessors as read-only. Before any disposable Go
probe, run and read the complete unfiltered Modern Go list for Go 1.27.

Scope (may touch): `.team/reports/T-084.md` only. Read-only candidate/module mounts and removable
`/tmp` probes/copies/caches are permitted and must be removed before handoff.

Out of scope (must not touch): T-083 production/test/report/task bytes; T-047 integration/task/report;
every predecessor; Fake, HTTP/SSE, command composition, domain, reconciliation, rehydration,
migration, module, OpenAPI/specification/traceability/reproducibility/HANDOFF/PLAN; prior tasks/
reports; Git refs; real providers/credentials; network; PR; merge; deployment; release or external
state. Do not fix a finding and do not accept your own report.

Definition of done:

- [ ] Bind branch/local/remote/baseline identity, the exact eight T-083 source/test hashes, T-083
      report hash, preserved T-047/T-082 hashes and accepted T-081/Fake/HTTP/module/migration hashes.
      Prove the only T-083 writes are its authorized source/test files plus report and there is no
      mutation of T-047, accepted transport/Fake/module/composition bytes or Git refs.
- [ ] Freshly run the digest-pinned network-disabled Go 1.27.1 module verification, empty formatting
      inventory, vet, all three exact T-083 tests, complete application/SQLite/backend tests and full
      race suite against read-only candidate bytes. Retain every FAIL, PARTIAL or SKIP visibly even
      when a command exits zero.
- [ ] Attack durable claim/publication authority: project/Run/input/holder/epoch/restore binding;
      monotonically increasing one-winner claim; rollback of Run/outbox/lease/audit/projection and
      allocator effects; all stale-fence dimensions; invalid capability/digest/lifecycle; and late
      publication. Confirm rejected stale/late data is auditable but cannot change state, phase,
      Candidate, projection or redispatch authority.
- [ ] Attack completion preparation end to end through the exported application surface. Verify
      byte-exact response-loss replay; immutable verified Candidate/artifact binding; prospective QA
      completion subject; independent reviewer/verifier identity; accepted AC/recipe/environment;
      subject-bound approval; Developing->Review->QA through domain transitions; and the existing
      exact-subject Done gate. Check forged actor, wrong subject, stale version/fence and missing or
      conflicting artifact rejection are atomic and allocator-safe.
- [ ] Compare every new command/result/artifact field and enum to the accepted OpenAPI rather than
      accepting a locally self-consistent DTO. Confirm canonical input rejects invalid data and the
      accepted five-operation HTTP surface remains unchanged.
- [ ] Attack cancellation semantics: intent alone and timeout/lookup-unknown must remain durable
      `OutcomeUnknown`/`NeedsReconcile`, never Canceled or redispatchable; stale/wrong acknowledgements
      must fail without state mutation; only a current fenced cancel acknowledgement may reach the
      existing domain Canceled transition. Verify authoritative reads and audit/projection evidence.
- [ ] Inspect architecture boundaries: application imports no concrete SQLite/Fake/HTTP/shell/network;
      the optional persistence seam exposes no SQL/raw-query or allocator-forging escape; no copied
      Done predicate or direct phase update bypass exists; no migration/schema widening occurred.
- [ ] Map each T-083 Definition-of-done item to fresh evidence. Any required failure, skip, unverifiable
      assertion, contract drift, scope violation or test-insensitive path is an unconditional FAIL;
      recommend the smallest repair envelope without changing candidate bytes.
- [ ] Produce a forward-compatible report with one physical Verification line per result, run
      `teamctl.py validate-report`, remove every temporary path, run `git diff --check`, and prove
      report-only durable scope. Preserve T-047 PARTIAL, T-082 FAIL and T-083 worker evidence.

Verify with: exact digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
with network disabled and read-only candidate/module mounts; exact named/application/SQLite/full/race
tests; OpenAPI/source/export/import/hash/scope inspection; current report validator; temp cleanup;
`git diff --check`.

Budget: One bounded independent report-only review; no delegation, candidate/predecessor fix,
commit, push, Git ref, provider/credential, network, deployment, release or external mutation.
