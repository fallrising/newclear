ROLE: WORKER

AGENT: vertical-authority-review-finding-repairer

ID: T-085

Goal: Repair exactly the three independently confirmed T-084 failures in the unaccepted T-083
candidate: OpenAPI SHA256 wire values, causal expired-lease epoch succession without redispatch, and
terminal artifact publication outside the database write transaction.

Why: T-084 preserved an unconditional FAIL even though baseline tests passed. Its isolated probes
proved every new canonical digest is prefix-free, no legal path can create current epoch N+1 before
testing old N, and `ArtifactStore.Put` runs inside `UnitOfWork.Within`. These are predecessor defects
and must be repaired before T-047 can be reopened.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041 decomposition; exact T-047 task/PARTIAL report/test;
T-082 FAIL task/report; T-083 task/DONE report and all eight candidate files; T-084 task/PARTIAL FAIL
report; T-045/T-046/T-064/T-067/T-073/T-076/T-077/T-078/T-079/T-080/T-081 tasks/reports;
product AGENTS; HANDOFF; SDD HAI-DONE-001..004 and HAI-EXEC-001..007; ADR-001/002/003/005;
persistence/recovery, executor/security and Fake vertical-slice mini-SDDs; reproducibility;
traceability; HTB-P0A-003; complete OpenAPI SHA256/ArtifactLocator/Candidate/Review/Evidence/Approval
and command payload schemas; backend module/migration and every currently modified T-083
source/test byte. Treat T-047, Fake, HTTP/SSE and all non-scope predecessors as read-only.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 using the repository-approved wrapper. Apply every relevant
rule and call `explain <guideline-id>` before skipping a returned rule that appears applicable.

Scope (may touch): `products/hai-taskboard/backend/internal/application/command/vertical.go`;
`products/hai-taskboard/backend/internal/application/port/persistence.go`;
`products/hai-taskboard/backend/internal/application/service/vertical.go`;
`products/hai-taskboard/backend/internal/domain/sqlite/store.go`;
`products/hai-taskboard/backend/internal/domain/sqlite/store_test.go`; `.team/reports/T-085.md`.

Out of scope (must not touch): all other T-083 source files; T-047 `backend/integration/**`;
T-083/T-084 tasks/reports and every prior task/report; deterministic Fake; HTTP/SSE and composition;
domain/reconciliation/rehydration; migration; go.mod/go.sum; OpenAPI/specification/traceability/
reproducibility/HANDOFF/PLAN; backup/restore; real provider/credential/agent execution; Git refs;
commit; push; PR; merge; network; deployment; release or external mutation. Do not hide T-084 FAIL.
If a repair truly requires any out-of-scope byte, return PARTIAL rather than widening this task.

Definition of done:

- [ ] Write three exact test skeletons before behavior:
      `TestVerticalAuthority_CanonicalDigestsMatchOpenAPI`,
      `TestVerticalAuthority_ExpiredLeaseSuccessionFencesOldEpoch`, and
      `TestVerticalAuthority_ArtifactPublicationOutsideWriteTransaction`.
- [ ] Encode every digest value introduced by T-083 canonical commands with exactly one accepted
      `sha256:` wire prefix, including ArtifactLocator, Candidate/input, Review, Evidence, Approval
      and QA subject/revision/recipe/environment fields. Keep href canonical, reject double/missing or
      malformed external forms, preserve deterministic artifact ordering and prove actual values—not
      merely JSON field names—against the accepted OpenAPI patterns.
- [ ] Add a bounded application/persistence operation that succeeds only after a current nonterminal
      Run lease has expired, conditionally advances its formerly valid epoch N to N+1 under the same
      project/Run/input/restore authority, assigns the authenticated successor holder/deadline, and
      atomically records audit/projection plus `NeedsReconcile`/`OutcomeUnknown`.
- [ ] Expired-lease succession is reconciliation ownership, never redispatch: preserve existing
      outbox non-Pending state, Run/cancel intent and immutable input; do not invoke Fake, mark stopped,
      confirm side effects, or return a dispatchable executor envelope. Pending initial claim stays a
      separate path. Reject unexpired, terminal, wrong epoch/generation/scope/input or concurrent
      succession before mutation, with exactly one winner.
- [ ] Prove a causal fence sequence through exported application APIs: acquire epoch N, advance the
      injected clock beyond its deadline, legally acquire reconciliation epoch N+1, then publish with
      formerly valid N. The old publication must be rejected and audited without Run/outbox/lease/
      WorkItem/projection mutation; current N+1 reconciliation observations remain bounded by the
      existing lifecycle contract.
- [ ] For terminal-success observations, validate bounded bytes and requested digest, then seal/hash/
      publish through `ArtifactStore` before entering `UnitOfWork.Within`; the later transaction must
      revalidate current fence/lifecycle and bind only the verified digest. A transaction rejection
      may leave a recoverable immutable orphan, but no filesystem/object-store operation may occur
      inside the write transaction and no invalid digest may be staged.
- [ ] Preserve all T-083 success behavior and add tracking/failure attacks for: object-store call
      outside transaction, DB rejection after sealing without state advancement, digest/length
      mismatch, stale old epoch, unexpired succession, concurrency, zero redispatch and rollback.
      Do not weaken the exact completion or cancel-unknown oracles.
- [ ] Keep application free of concrete SQLite/Fake/HTTP/shell/network imports; preserve accepted
      Fake/HTTP/module/migration/composition and T-047/T-082/T-083/T-084 bytes exactly. Bind hashes and
      exact scope in the report; the worker cannot accept T-083/T-085 or reopen T-047.
- [ ] Pass the complete digest-pinned network-disabled Go 1.27.1 gate with no required skip: module
      verify, empty gofmt, vet, the three T-085 names, all six T-083/T-085 authority names,
      application, SQLite, full backend, full race, import/API/test inventory, task/report validators
      and `git diff --check`. Retain every failure or skip in the report.

Verify with: complete unfiltered Modern Go list; current teamctl task/report validators; exact pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under network-none/read-only module cache; uncached named/application/SQLite/full/race tests;
OpenAPI value-sensitive, tracking-UOW, causal lease, source/import/hash/exact-scope inspection;
`git diff --check`.

Budget: One bounded T-084 repair turn; no delegation, T-047/Fake/HTTP/module/migration/spec/status
document edit, commit, push, Git ref, provider/credential, network, deployment, release or external
mutation.
