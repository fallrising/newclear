ROLE: REVIEWER

AGENT: vertical-authority-final-acceptance-verifier

ID: T-086

Goal: Independently accept or reject the exact cumulative T-083 plus T-085 predecessor authority
candidate after T-084's three preserved failures, without modifying candidate or evidence bytes.

Why: T-084 rejected T-083 for OpenAPI SHA256 drift, absence of a causal old-epoch lease oracle and
object-store publication inside a database transaction. T-085 claims bounded repairs, but worker
evidence cannot accept itself or reopen T-047. A fresh report-only review must reproduce every repair
and the original vertical authority guarantees.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041 decomposition; exact T-047 task/PARTIAL report/test;
T-082 task/FAIL report; T-083 task/DONE report; T-084 task/PARTIAL FAIL report; T-085 task/DONE
report; T-045/T-046/T-064/T-067/T-073/T-076/T-077/T-078/T-079/T-080/T-081 tasks/reports;
product AGENTS; HANDOFF; SDD; ADR-001/002/003/005; persistence/recovery, executor/security and Fake
vertical-slice mini-SDDs; reproducibility; traceability; HTB-P0A-003; complete OpenAPI; backend
module/migration and every current backend source/test byte. Treat candidate and predecessors as
read-only. Before any disposable Go probe, run and read the complete unfiltered Modern Go list for
Go 1.27.

Scope (may touch): `.team/reports/T-086.md` only. Read-only candidate/module mounts and removable
isolated `/tmp` probes/copies/caches are permitted and must be removed before handoff.

Out of scope (must not touch): all production/test/module/migration bytes; T-047 integration;
T-083/T-084/T-085 tasks/reports and every prior task/report; Fake; HTTP/SSE and composition;
domain/reconciliation/rehydration; OpenAPI/specification/traceability/reproducibility/HANDOFF/PLAN;
Git refs; real providers/credentials; network; commit; push; PR; merge; deployment; release or
external state. Do not fix findings and do not accept your own report.

Definition of done:

- [ ] Bind branch/local/remote/baseline identity; all cumulative T-083/T-085 source/test and report
      hashes; preserved T-047/T-082/T-084 hashes; and accepted T-081/Fake/HTTP/composition/module/
      migration hashes. Prove exact worker scopes and no mutation of T-047 or accepted boundaries.
- [ ] Freshly run the digest-pinned network-disabled Go 1.27.1 module verification, empty formatting
      inventory, vet, the three exact T-085 names, all six T-083/T-085 authority names, complete
      application/SQLite/backend tests and full race suite. Retain every FAIL/PARTIAL/SKIP visibly
      even when a command exits zero.
- [ ] Independently compare actual canonical JSON values for every new Candidate/input/Artifact/
      Review/Evidence/Approval/QA digest to OpenAPI SHA256, href and enum constraints. Attack absent,
      double and malformed prefixes, href mismatch, duplicate/unsorted artifacts and unknown fields;
      fail if tests check names but not wire values.
- [ ] Independently establish a causal lease sequence through exported application APIs: epoch N,
      clock beyond deadline, legal reconciliation-only N+1, then formerly valid old-N publication.
      Verify old N is audited and rejected without Run/outbox/lease/WorkItem/projection mutation;
      verify full CAS dimensions, unexpired/terminal/wrong claims, concurrent one-winner, rollback,
      non-Pending outbox and zero redispatch. Succession must never return a dispatch envelope.
- [ ] With a reviewer-owned tracking UnitOfWork/ArtifactStore attack, prove terminal object validation
      and Put happen outside the write transaction, while fence/lifecycle and metadata binding happen
      atomically inside it. Attack DB rejection after seal, stale old epoch, requested/returned
      digest or length mismatch and oversize bytes; only a recoverable immutable orphan may remain.
- [ ] Reattack the original T-083 authority: persisted initial claim and fence dimensions;
      capability/lifecycle/digest checks; stale/late audit-only behavior; response-loss replay;
      immutable Candidate/artifacts; exact prospective-QA subject; independent Review/Evidence;
      subject approval; Developing->Review->QA through domain transitions; exact-subject Done; and
      cancellation intent/unknown/zero-redispatch/current-ack semantics.
- [ ] Inspect architecture and storage invariants: application imports no concrete SQLite/Fake/HTTP/
      shell/network; no SQL/raw-query or allocator-forging escape; no copied Done predicate/direct
      phase update; no migration widening; accepted five-command HTTP/Fake/module/composition bytes
      remain unchanged.
- [ ] Map every T-083 and T-085 Definition-of-done item and all T-084 findings to fresh evidence.
      Any required failure, skip, unverifiable assertion, scope violation or insensitive test is an
      unconditional FAIL; recommend the smallest repair without changing candidate bytes.
- [ ] Produce a forward-compatible report with one physical Verification line per result, run
      `teamctl.py validate-report`, remove all temporary paths, run `git diff --check`, and prove
      report-only scope. Return unconditional PASS with STATUS DONE only if every required check
      passes with no skip; preserve all historical FAIL/PARTIAL evidence.

Verify with: exact digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
with network disabled and read-only candidate/module mounts; fresh external/in-package OpenAPI,
causal-lease and transaction-boundary probes; named/application/SQLite/full/race tests; source/
import/API/hash/scope inspection; report validator; temp cleanup; `git diff --check`.

Budget: One bounded independent report-only review; no delegation, candidate/predecessor fix,
commit, push, Git ref, provider/credential, network, deployment, release or external mutation.
