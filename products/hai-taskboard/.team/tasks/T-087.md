ROLE: WORKER

AGENT: fake-vertical-integration-evidence-retry

ID: T-087

Goal: Replace T-047's three preserved NotRun boundaries with deterministic exported-seam vertical
integration evidence, using the independently accepted cumulative T-083/T-085 authority candidate
without modifying any predecessor byte.

Why: T-047 and T-082 correctly preserved PARTIAL/FAIL when application/persistence authority was
absent. T-083/T-085 added only those predecessor seams, T-084 preserved three repair findings, and
the independent T-086 report accepted their exact corrected bytes with no required failure or skip.
The original T-047 integration candidate may now be retried in its original production scope.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; original T-047 task/PARTIAL report/integration test; T-082
task/FAIL report; T-083 task/DONE report; T-084 task/PARTIAL FAIL report; T-085 task/DONE report;
T-086 task/DONE independent report; T-041 decomposition; T-045/T-046/T-064/T-067/T-073/T-076/
T-077/T-078/T-079/T-080/T-081 tasks/reports; product AGENTS; HANDOFF; SDD; all ADRs and mini-SDDs,
especially `docs/sdd/fake-vertical-slice-implementation.md`; reproducibility; traceability;
spec-graph; HTB-P0A-003; complete OpenAPI; backend go.mod/go.sum, migration and every current
backend source/test byte. Treat every file outside integration scope as read-only. Confirm T-081
HTTP/SSE acceptance and T-086 predecessor acceptance before editing.

Mandatory Go preflight: before editing any Go file, run and read the complete unfiltered
`modern-go-guidelines` list for Go 1.27 using the repository-approved wrapper. Apply every relevant
rule and call `explain <guideline-id>` before skipping a returned rule that appears applicable.

Scope (may touch): `products/hai-taskboard/backend/integration/**`; `.team/reports/T-087.md`.

Out of scope (must not touch): application command/service/port; persistence/domain/SQLite; Fake;
HTTP/SSE; `cmd/taskboard`; module/dependency; migration; web; OpenAPI/specification/traceability/
reproducibility/HANDOFF/PLAN; every task and prior report including T-047/T-082/T-084 evidence;
direct SQL/table access; reflection; copied application/Done predicates; test-owned authority;
automatic persistent worker/restore behavior not exposed by production; Git refs; commit; push; PR;
merge; real provider/credential/agent execution; network; deployment; release or external mutation.
If any named oracle still requires an out-of-scope predecessor fix, preserve the failure/NotRun and
return PARTIAL rather than repairing it here.

Definition of done:

- [ ] Keep exactly named external-package tests `TestVerticalFake_CompletionAndResponseLoss`,
      `TestRunLease_RejectsStaleEpochPublication` and
      `TestCancel_UnknownStopIsNotCanceled`; remove their old skips only after their complete real
      SQLite/application/Fake/HTTP assertions execute. Use only controlled temp roots, injected
      deterministic mutable clock/IDs, bounded synchronization and exported production seams.
- [ ] For completion, create Project, accepted AC/dependency revisions and WorkItem; MarkReady and
      DispatchRun through the real application/SQLite path. Deliberately lose the dispatch response,
      recover it through accepted HTTP command-result GET, replay by original identity byte-exactly,
      and prove no second Run/outbox/Fake dispatch or projection mutation.
- [ ] Claim the persisted Run, derive the Fake fence only from the returned immutable executor
      envelope, dispatch the configured deterministic Fake exactly once, accept each Fake observation
      in the Fake worker, translate it as untrusted data into application publication, and drive
      dispatch/start/terminal success with the exact staged artifact bytes. Never invent a Run input,
      lease epoch, holder or restore generation.
- [ ] Submit the exact Candidate through application authority; compute the prospective-QA
      completion subject from persisted Run/Candidate plus accepted AC/graph/policy/recipe inputs;
      publish independent Review and passing Evidence with sealed report; add exact human Approval;
      transition Review->QA and Complete through the real application/domain/SQLite transaction.
      Lose/recover/replay the completion response byte-exactly and prove no duplicate mutation.
- [ ] Verify Done and version through `Store.LoadWorkItem`, whose accepted load contract requires
      exactly one CompletionRecord and valid evidence/review/approval joins and exactly one Approval
      consumption. Also read exact completion material through the typed persistence port and assert
      Candidate/Run/artifact/evidence/review/approval identity and counts; assert command replay,
      runtime Run/outbox/audit/projection and recorder counts are unchanged by retries.
- [ ] For stale epoch, claim and start a persisted Fake Run at epoch N, advance the injected clock
      beyond the returned deadline, acquire reconciliation-only epoch N+1 through the accepted
      application seam, then let the old-N deterministic Fake session emit a terminal artifact.
      Application publication and Candidate submission must reject the formerly valid old epoch;
      audit must increase exactly as specified while Run/outbox/new lease/WorkItem/projection and
      completion material remain unchanged, and ClaimDispatch must prove zero redispatch.
- [ ] For cancel unknown, claim/start through the bound Fake fence, persist RequestCancellation,
      drive deterministic Fake timeout and lookup-unknown observations through both Fake worker and
      application publication, and assert durable desired action, `OutcomeUnknown`/
      `NeedsReconcile`, non-Pending outbox, unchanged Developing WorkItem, not Done/Canceled,
      unconfirmed side effects and zero redispatch. Do not use a cancel acknowledgement as the
      negative oracle.
- [ ] Keep translation mechanical and explicit: map only Fake observation kind/fence/artifact bytes
      to the corresponding application DTO, never reproduce lifecycle policy or mutate persistence
      directly. Assert Fake transcript/worker snapshots and application authority independently so a
      disconnected mock-only path cannot pass.
- [ ] Make every expected value deterministic: no wall-clock sleep, ambient randomness, network,
      shell, scheduling-dependent order or unbounded wait. All staging/object bytes stay beneath
      `t.TempDir()`. Preserve automatic persistent outbox polling, restore/backup, browser, real
      provider/credential, deployment and broader G1/G2 families as explicit out-of-envelope
      Deferred boundaries, not required skips in these three T-087 oracles.
- [ ] Pass with no required failure or skip the complete digest-pinned network-disabled Go 1.27.1
      gate: go version, module verify, empty gofmt, vet, the three exact integration names, ten
      integration repetitions, full backend, full race and test/import inventory. Validate task and
      report, run `git diff --check`, bind all T-083/T-085/T-086/predecessor and final integration
      hashes, and prove the only new writes are integration scope plus this report. Worker evidence
      cannot accept T-047/T-087.

Verify with: complete unfiltered Modern Go list; current teamctl task/report validators; exact pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under network-none/read-only module cache; uncached exact named, ten-repeat, full and race tests;
source/import/API/hash/exact-scope inventory; `git diff --check`.

Budget: One bounded integration retry; no delegation, predecessor/module/spec/status/prior-evidence
edit, direct SQL, commit, push, Git ref, provider/credential, network, deployment, release or
external mutation.
