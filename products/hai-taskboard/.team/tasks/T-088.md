ROLE: REVIEWER

AGENT: fake-vertical-integration-final-verifier

ID: T-088

Goal: Independently accept or reject the exact T-087 integration-only candidate for the original
T-047 vertical Fake evidence, without modifying integration or predecessor bytes.

Why: T-047/T-082 preserved the initial integration PARTIAL/FAIL, T-083/T-085 repaired predecessor
authority, T-084 preserved repair failures, and T-086 accepted the corrected predecessor. T-087 now
reports all three original named oracles execute without skips, but worker evidence cannot accept
itself. A fresh report-only reviewer must attack the exact composed behavior before the orchestrator
may accept T-047 and update status documents.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041 decomposition; original T-047 task/PARTIAL report and
historical test hash; T-082 task/FAIL report; T-083/T-085 tasks/DONE reports; T-084 task/PARTIAL FAIL
report; T-086 task/DONE independent report; T-087 task/DONE report and complete integration
candidate; T-045/T-046/T-064/T-067/T-073/T-076/T-077/T-078/T-079/T-080/T-081 tasks/reports;
product AGENTS; HANDOFF; SDD; all ADRs and mini-SDDs, especially Fake vertical implementation;
reproducibility; traceability; spec graph; HTB-P0A-003; complete OpenAPI; every current backend
source/test/module/migration byte. Treat all candidate and predecessor bytes as read-only. Before any
disposable Go probe, run and read the complete unfiltered Modern Go list for Go 1.27.

Scope (may touch): `.team/reports/T-088.md` only. Read-only candidate/module mounts and removable
isolated `/tmp` probes/copies/caches are permitted and must be removed before handoff.

Out of scope (must not touch): `backend/integration/**`; all production/test/module/migration bytes;
T-047/T-082/T-083/T-084/T-085/T-086/T-087 tasks/reports and every prior task/report; Fake;
application/persistence/domain; HTTP/SSE/composition; OpenAPI/specification/traceability/
reproducibility/HANDOFF/PLAN; Git refs; direct SQL; real provider/credential/agent execution;
network; commit; push; PR; merge; deployment; release or external state. Do not fix a finding and do
not accept your own report.

Definition of done:

- [ ] Bind branch/local/remote/baseline identity, final T-087 task/report/integration hashes,
      cumulative T-083/T-085/T-086 hashes, preserved T-047/T-082/T-084 evidence and accepted
      T-081/Fake/HTTP/composition/module/migration/OpenAPI hashes. Prove T-087 changed only the
      integration candidate plus its report and all predecessor bytes are unchanged.
- [ ] Freshly run the digest-pinned network-disabled Go 1.27.1 version/module/empty-format/vet gate,
      all three exact T-047 names verbosely, ten integration repetitions, complete backend and full
      race suite. Retain every FAIL/PARTIAL/SKIP; unconditional PASS is forbidden if any exact named
      test does not execute or any required command fails/skips.
- [ ] Adversarially inspect and, in a disposable copy, independently probe completion response loss:
      one persisted Run/outbox and Fake dispatch; original HTTP command-result recovery; byte-exact
      dispatch and completion replay with no counter/authority mutation; envelope-derived Fake fence;
      real staged/sealed Candidate; persisted AC/graph/policy/recipe/environment; independent Review
      and Evidence; exact Approval; Review->QA->Done through application/domain; typed material
      identity/counts; and `LoadWorkItem`'s one-record/join/one-consumption invariant.
- [ ] Prove no disconnected mock path can satisfy completion: Fake transcript/worker, staging bytes,
      application Run authority, SQLite material and HTTP result must bind the same Run/input/
      candidate/subject. Confirm translation is mechanical and does not reproduce lifecycle/Done
      policy. Attack wrong envelope/artifact/subject or a severed publication in disposable probes.
- [ ] Independently prove the stale epoch is causal: formerly valid epoch N drives Fake start, clock
      passes persisted deadline, exported reconciliation-only claim establishes N+1, then the old-N
      Fake terminal is locally produced but application-rejected. Verify exact audit-only effect,
      unchanged Run/outbox/N+1 lease/WorkItem/projection/material, rejected Candidate, recoverable
      orphan only and zero redispatch. Fabricated future epoch is not acceptable.
- [ ] Independently prove cancel unknown: application persists intent before timeout; bound Fake and
      application both process timeout/lookup-unknown; durable result remains `CancelRequested`,
      `Unknown`, `NeedsReconcile`, `OutcomeUnknown`, non-Pending and non-redispatchable; WorkItem and
      completion stay neither Canceled nor Done; no cancel acknowledgement is used in the negative
      path.
- [ ] Inspect determinism/security/scope: no sleeps, ambient randomness, network, shell, direct SQL,
      reflection, concrete table access, copied authority or writable path outside `t.TempDir`;
      synchronization is bounded and race-safe; integration imports and uses only exported accepted
      seams. Automatic persistent polling, restore/backup, browser, real provider/credential,
      deployment and broader G1/G2 remain explicit out-of-envelope Deferred boundaries, not hidden
      required skips.
- [ ] Map every T-047/T-087 Definition-of-done item to fresh evidence. Any required failure, skip,
      unverifiable assertion, insensitive test, predecessor drift or scope violation is an
      unconditional FAIL with the smallest recommended repair; do not alter candidate bytes.
- [ ] Produce a forward-compatible report with one physical Verification line per result, validate
      it, remove all temporary paths, run `git diff --check`, and prove report-only scope. Return
      STATUS DONE/unconditional PASS only when every required item passes; preserve all historical
      FAIL/PARTIAL evidence.

Verify with: exact digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
with network disabled and read-only candidate/module mounts; fresh reviewer-owned integration
attacks; exact/ten-repeat/full/race tests; source/import/API/hash/scope inspection; report validator;
temp cleanup; `git diff --check`.

Budget: One bounded independent report-only review; no delegation, candidate/predecessor fix,
commit, push, Git ref, provider/credential, network, deployment, release or external mutation.
