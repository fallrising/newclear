ROLE: REVIEWER

AGENT: fake-vertical-integration-read-first-verifier

ID: T-089

Goal: Perform a fresh, chronologically valid independent acceptance review of the exact T-087
integration candidate after T-088's preserved reviewer-process failure, without modifying any
candidate or predecessor byte.

Why: T-088 found no functional candidate defect, but it ran authoritative Go before completing its
mandatory backend/OpenAPI read and therefore cannot accept T-047/T-087. T-089 must repeat the review
from a provably read-first sequence; T-088 evidence is historical input only, never a substitute for
fresh checks.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-evidence-gate; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-041 decomposition; original T-047 task/PARTIAL report and
historical candidate; T-082 task/FAIL report; T-083/T-085 tasks/DONE reports; T-084 task/PARTIAL FAIL
report; T-086 task/DONE independent report; T-087 task/DONE report and complete candidate; T-088
task/PARTIAL process-FAIL report; T-045/T-046/T-064/T-067/T-073/T-076/T-077/T-078/T-079/T-080/
T-081 tasks/reports; product AGENTS; HANDOFF; SDD; all ADRs and mini-SDDs; reproducibility;
traceability; spec graph; HTB-P0A-003; complete OpenAPI; and every current backend source/test/module/
migration byte. Treat all inputs as read-only.

Mandatory read-first checkpoint: before any Go command, Docker command, disposable probe, candidate
execution or report write, read every input above completely, run and read the complete unfiltered
Modern Go list for Go 1.27, then send the orchestrator a checkpoint that explicitly confirms both
are complete and wait for its acknowledgement. Reading/status/hash/source-list shell commands are
allowed before that checkpoint; execution/build/test probes are not. The known absence of host `rg`
is already evidenced by T-088, so use an available read-only fallback directly rather than creating
another avoidable failed invocation.

Scope (may touch): `.team/reports/T-089.md` only. After the acknowledged read-first checkpoint,
read-only candidate/module mounts and removable isolated `/tmp` probes/copies/caches are permitted
and must be removed before handoff.

Out of scope (must not touch): `backend/integration/**`; all production/test/module/migration bytes;
every task and prior report; Fake; application/persistence/domain; HTTP/SSE/composition; OpenAPI/
specification/traceability/reproducibility/HANDOFF/PLAN; Git refs; direct SQL; real provider/
credential/agent execution; network; commit; push; PR; merge; deployment; release or external state.
Do not fix a finding and do not accept your own report.

Definition of done:

- [ ] Complete and checkpoint the mandatory read-first sequence before any executable probe. Bind
      branch/local/remote/baseline, final T-087 task/report/candidate hashes, cumulative T-083/T-085/
      T-086 hashes, preserved T-047/T-082/T-084/T-088 evidence and accepted T-081/Fake/HTTP/
      composition/module/migration/OpenAPI hashes. Prove exact integration-only worker scope.
- [ ] Only after orchestrator acknowledgement, freshly run the digest-pinned network-disabled Go
      1.27.1 version/module/empty-format/vet gate, all three exact T-047 names verbosely, ten
      integration repetitions, complete backend, full race and test/import inventory. Preserve every
      FAIL/PARTIAL/SKIP; PASS is forbidden if any named test does not execute or any required gate
      fails/skips.
- [ ] In a fresh disposable copy, independently probe completion response loss and vertical binding:
      one persisted Run/outbox/Fake dispatch; HTTP result recovery; byte-exact dispatch/completion
      replay with no mutation; envelope-derived fence; exact staged/sealed bytes; Candidate;
      AC/graph/policy/recipe/environment; independent Review/Evidence; exact Approval;
      Review->QA->Done; typed material identities/counts; and `LoadWorkItem` one-record/join/
      one-consumption invariant.
- [ ] Prove no disconnected Fake path can pass by attacking severed publication, wrong envelope,
      artifact/digest/media binding, Candidate artifact/input and completion subject. Fake transcript,
      staging, application Run, SQLite material and HTTP result must name the same Run/input/
      Candidate/subject; translation must remain mechanical and contain no copied lifecycle/Done
      authority.
- [ ] Independently prove causal stale epoch: real epoch N, injected time beyond persisted deadline,
      exported reconciliation-only N+1, old-N Fake terminal locally produced then application-
      rejected; exact audit-only increment; unchanged Run/outbox/N+1 lease/WorkItem/projection/
      material; Candidate rejection; orphan-only object effect; zero redispatch. A fabricated future
      mismatch is insufficient.
- [ ] Independently prove cancel unknown: application intent precedes bound Fake timeout and lookup-
      unknown; durable state is `CancelRequested`, `Unknown`, `NeedsReconcile`, `OutcomeUnknown`,
      non-Pending/non-redispatchable and neither Done nor Canceled, with absent completion and no
      cancel acknowledgement in the negative path.
- [ ] Inspect determinism/security/scope: no sleep, randomness, network, shell, SQL, reflection,
      concrete table access, copied predicate or write outside `t.TempDir`; bounded race-safe
      synchronization; only exported accepted seams. Keep automatic polling, restore/backup, browser,
      real providers/credentials/deployment and broader G1/G2 explicitly Deferred and out of this
      required acceptance gate.
- [ ] Map every T-047/T-087 Definition-of-done item to fresh evidence. Any candidate failure/skip,
      process-order breach, unverifiable assertion, insensitive test, predecessor drift or scope
      violation is an unconditional FAIL; do not alter candidate bytes.
- [ ] Produce a forward-compatible report with one physical Verification line per result, validate
      it, remove all temporary paths, run `git diff --check`, and prove report-only scope. Return
      STATUS DONE/unconditional PASS only if every required check and the read-first chronology pass;
      preserve T-088 and all earlier historical failures verbatim.

Verify with: completed/acknowledged read-first checkpoint; exact digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
with network disabled and read-only candidate/module mounts; fresh reviewer-owned integration
attacks; exact/ten-repeat/full/race tests; source/import/API/hash/scope inspection; report validator;
temp cleanup; `git diff --check`.

Budget: One bounded independent read-first report-only review; no delegation, candidate/predecessor
fix, commit, push, Git ref, provider/credential, network, deployment, release or external mutation.
