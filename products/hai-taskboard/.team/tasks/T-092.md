ROLE: REVIEWER

AGENT: local-fake-runtime-independent-reviewer

ID: T-092

Goal: Independently review the complete T-091 runnable-runtime candidate and its evidence, with
fresh runtime/security/restart attacks and no production changes.

Why: A worker report cannot accept itself. The new executable owns secrets, filesystem paths,
listener lifecycle and durable state; those boundaries need a read-first reviewer that is
independent of the implementation and preserves failures rather than repairing them in place.

Inputs to read first: codex-team-superpowers:codex-evidence-gate;
modern-go-guidelines:use-modern-go; `.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`;
`.team/tasks/T-091.md`; `.team/reports/T-091.md`; `products/hai-taskboard/AGENTS.md`;
`docs/HANDOFF.md`; `docs/SDD.md`; ADR-001/002/003/005/006; all runtime-relevant mini-SDDs,
especially `docs/sdd/local-fake-runtime.md`; `docs/local-development.md`; complete current
`backend/cmd/taskboard/**`; accepted HTTP/Fake/application port interfaces and SQLite open/close
boundary. Read the complete candidate before running acceptance commands.

Scope (may touch): `.team/reports/T-092.md` only.

Out of scope (must not touch): every production, test, module, specification, documentation,
PLAN, task, prior-report, Git-ref and external-system path; repair work; persistent worker or
projection implementation; commit, push, PR, merge, deployment, release, provider credentials or
real agent execution. Report any finding as FAIL/PARTIAL with exact evidence; do not fix it.

Definition of done:

- [ ] Establish read-first chronology and bind the T-091 task/report/candidate hashes. Verify exact
      scope and that accepted predecessor/module/API/migration bytes are unchanged.
- [ ] Review configuration and listener semantics for loopback-only exposure, HTTP Origin/listen
      equality, no accepted-but-unservable configuration, validation before listen, token strength,
      fixed-size comparison, actor validity and secret-free diagnostics. Attack wildcard, hostname,
      IPv4/IPv6 mismatch, HTTPS, path/query/userinfo Origin, duplicate/wrong cookies and Origin.
- [ ] Review storage confinement and durability. Attack relative/unclean/symlinked/public roots,
      artifact traversal/symlink/corrupt-existing objects, digest mismatch and restart on the exact
      same root/port. Confirm shutdown releases HTTP and SQLite and no task-owned process/temp data
      remains.
- [ ] Independently run the built binary with a new private temporary root and non-production token:
      health must name unavailable surfaces, authenticated exact-Origin CreateProject and lookup
      must succeed, wrong token/origin must fail, SIGTERM must exit 0, same-root restart must retain
      byte-exact lookup/replay, and stdout/stderr must not contain the token/cookie/body/result.
- [ ] Rerun the complete pinned/network-disabled Go 1.27.1 gate: version, module verify, empty
      formatting, vet, four exact T-091 tests, restart x10, full backend, full race, command build and
      inventories. No required failure or skip may be counted as passing.
- [ ] Reconcile every T-091 definition-of-done item to visible source/test/process evidence using
      the evidence-gate verdicts. Keep persistent worker/Fake execution, persisted board/SSE,
      restore/backup, browser/live UI, root CI and real providers as explicit NotRun/Deferred.
- [ ] Produce validator-compatible `.team/reports/T-092.md`, run report validation and
      `git diff --check`, and return PASS only if no acceptance blocker remains. The reviewer report
      does not update PLAN or accept itself.

Verify with: complete source/diff/hash inspection; current `teamctl.py validate-report`; exact
digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under network-none/read-only source and module cache; exact/repeated/full/race/build/inventory
commands; isolated loopback process smoke; `git diff --check`.

Budget: One report-only independent review; no delegation, implementation/repair, source/spec/status
edit, destructive cleanup outside reviewer-owned temporary paths, commit, push or external mutation.
