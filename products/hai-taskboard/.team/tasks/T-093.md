ROLE: WORKER

AGENT: local-fake-runtime-security-repairer

ID: T-093

Goal: Repair exactly the four T-092 blockers in the T-091 runnable-runtime candidate without
widening runtime functionality or changing accepted predecessor bytes.

Why: T-092 independently passed the process lifecycle, persistence restart and complete pinned Go
gate, but proved that broad existing roots can be chmod-mutated, non-canonical Origin aliases pass,
an obviously predictable token passes, and a post-construction artifact-root symlink replacement
can escape confinement. T-091 cannot be accepted until all four fail closed under fresh tests.

Inputs to read first: modern-go-guidelines:use-modern-go;
codex-team-superpowers:codex-task-worker; `.team/PLAN.md`;
`.team/REVIEWER_REPORT_CONTRACT.md`; T-091/T-092 tasks and reports; `products/hai-taskboard/AGENTS.md`;
`docs/sdd/local-fake-runtime.md`; `docs/local-development.md`; complete current
`backend/cmd/taskboard/**`; Go 1.27 standard-library documentation available from the pinned
toolchain for descriptor-confined filesystem APIs. Preserve T-092 as immutable FAIL evidence.

Mandatory Go preflight: before editing any Go file, run the complete unfiltered Modern Go
Guidelines list for Go 1.27, read the full output, apply every relevant rule, and use `explain` before
skipping any returned rule that appears applicable.

Scope (may touch): `products/hai-taskboard/docs/sdd/local-fake-runtime.md`;
`products/hai-taskboard/docs/local-development.md`; `products/hai-taskboard/backend/cmd/taskboard/**`;
`.team/reports/T-093.md`.

Out of scope (must not touch): accepted application/command/port, domain/SQLite, deterministic Fake,
HTTP/SSE, migrations, modules/dependencies, web, OpenAPI, traceability, reproducibility, HANDOFF,
PLAN, task and prior-report bytes; persistent worker/projection/restore implementation; direct SQL;
real providers/credentials/agents; Git refs, commit, push, PR, merge, deployment, release or
external mutation. If safe descriptor confinement is impossible without an out-of-scope dependency,
retain exact evidence and report PARTIAL/BLOCKED rather than approximating the boundary.

Definition of done:

- [ ] Amend the runtime mini-SDD/local guide before behavior to state canonical listen/Origin
      spelling, private-root ownership/mode behavior, canonical strong-token encoding and the
      descriptor-confined artifact lifecycle. Keep later runtime surfaces explicitly unavailable.
- [ ] Reject `/`, the platform temporary directory itself, existing group/other-accessible roots,
      symlink roots and other broad/uncontrolled roots without chmod or any other mutation. Permit a
      new child beneath an otherwise safe parent by creating runtime-owned components at mode 0700;
      permit an already-existing private nonsymlink task root without changing its mode. Add a
      negative test that snapshots mode/identity and proves rejection is side-effect free.
- [ ] Require the listen address to be the canonical `net.JoinHostPort(parsedIP.String(), decimal
      port)` representation and require Origin to be byte-exact `http://` plus that canonical listen
      address. Reject leading-zero ports, IPv4-mapped aliases, expanded/noncanonical IPv6 and every
      T-092 alias without opening storage or a listener.
- [ ] Require a canonical base64url-no-padding encoding of 32 decoded bytes and reject obvious
      low-entropy/patterned material, including T-092's repeated-character token. Update the guide's
      generation command to produce exactly this format without printing/logging it. Keep fixed-size
      digest comparison and never retain/log the raw configured token after authority construction.
- [ ] Make artifact operations descriptor-confined to the directory opened at construction using
      Go 1.27 standard-library root-relative APIs or an equally strong no-new-dependency mechanism.
      Bind and recheck directory identity so a pre-operation root rename/symlink replacement is
      rejected; a replacement racing after the check must still be unable to redirect reads/writes.
      Validate/hash the same opened object descriptor returned to consumers, preserve no-overwrite
      immutable digest semantics, sync successful publication and close artifact resources during
      startup failure and normal shutdown.
- [ ] Add exact tests `TestT093Config_RejectsBroadRootAndOriginAliases`,
      `TestT093SessionToken_RejectsPredictableSecret`, and
      `TestT093ArtifactStore_RejectsRootReplacement`. Preserve the four T-091 exact tests and extend
      their positive controls to canonical token/root behavior. Tests must use private `t.TempDir`
      descendants, snapshot any attacked directory mode/identity, and never target `/` or `/tmp`
      with a mutating call.
- [ ] Rerun a real built-process smoke with a fresh private temporary child and freshly generated
      canonical non-production token: health/create/lookup, wrong/duplicate auth and Origin,
      SIGTERM exit 0, same-root/same-port restart and byte-exact lookup/replay remain passing with no
      secret in output. Remove only task-owned temporary paths after processes stop.
- [ ] Pass the complete pinned/network-disabled Go 1.27.1 gate: module verify, empty format, vet,
      three exact T-093 tests, four exact T-091 tests, restart x10, full backend, full race, command
      build and inventories. Validate task/report, run `git diff --check`, bind T-092/protected/new
      hashes and prove exact scope with no required failure or skip for DONE.
- [ ] Produce a validator-compatible report that does not accept itself. Automatic worker/Fake
      execution, persisted board/SSE, restore/backup, browser/live UI, root CI, real providers and
      wider G1/G2 remain explicit NotRun/Deferred.

Verify with: complete unfiltered Modern Go 1.27 list; current teamctl task/report validators; exact
digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under network-none/read-only source/module cache; named/repeated/full/race/build/inventory gates;
isolated process smoke; exact hashes/scope; `git diff --check`.

Budget: One bounded four-finding repair; no delegation, predecessor/module/API/status edit, runtime
feature widening, commit, push, PR, merge, deployment, release or external mutation.
