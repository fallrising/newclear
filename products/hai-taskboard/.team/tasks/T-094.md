ROLE: REVIEWER

AGENT: local-fake-runtime-final-security-reviewer

ID: T-094

Goal: Freshly and independently re-review the exact T-093-repaired T-091 runnable runtime, reattack
all four T-092 failures and verify lifecycle/persistence evidence without modifying the candidate.

Why: T-092 remains immutable FAIL evidence and T-093 is worker evidence only. T-091/T-093 cannot be
accepted until a report-only reviewer proves that broad roots are side-effect-free rejections,
listener/Origin spelling is exact, predictable secrets fail, descriptor-confined artifacts cannot
escape after root replacement, and the previously passing runtime restart remains intact.

Inputs to read first: codex-team-superpowers:codex-evidence-gate;
modern-go-guidelines:use-modern-go; `.team/PLAN.md`; `.team/REVIEWER_REPORT_CONTRACT.md`;
T-091/T-092/T-093 tasks and reports; `products/hai-taskboard/AGENTS.md`; runtime SDD/local guide;
complete current `backend/cmd/taskboard/**`; accepted HTTP/Fake/application/SQLite boundaries and
the pinned Go 1.27 `os.Root` documentation. Read the complete repaired candidate before probes.

Scope (may touch): `.team/reports/T-094.md` only. Reviewer-owned isolated copies, binaries and data
under uniquely created `/tmp` paths are permitted and must be removed after their processes stop.

Out of scope (must not touch): every candidate production/test/spec/document/module/migration/API,
PLAN, task, prior report, Git ref and external system; repair work; commit, push, PR, merge,
deployment, release, real provider/credential or agent execution. Preserve any finding as
FAIL/PARTIAL; do not repair it.

Definition of done:

- [ ] Establish read-first chronology, exact T-091/T-092/T-093 task/report/candidate hashes,
      protected predecessor hashes and exact worker scope before executable probes.
- [ ] Freshly attack data-root validation with `/`, the platform temp root, task-owned 0755/0500,
      symlink and, where safely possible in reviewer temp space, wrong-owner roots. Snapshot
      identity/mode/owner before and after; every invalid root must be rejected before storage/listen
      without chmod/chown/mutation. Existing correct-owner 0700 and a missing child below such a
      parent must remain usable.
- [ ] Freshly attack canonical listener/Origin binding with leading-zero port, IPv4-mapped address,
      expanded IPv6, hostname/wildcard/non-loopback, scheme/path/query/userinfo and port mismatch.
      Only byte-exact `http://` plus canonical loopback listener may validate.
- [ ] Freshly attack token validation with T-092 repeated text and canonical encodings of zero,
      repeated-period and arithmetic-sequence bytes; all must fail while an independently generated
      canonical 32-byte token authenticates through fixed-size digest comparison. Verify diagnostics
      and process output contain no secret, cookie, body or result.
- [ ] Freshly reproduce artifact root rename plus outside-symlink replacement before Put/Open and
      prove both fail with no outside object/mode/identity mutation. Inspect/probe that root-relative
      creation/link/open/sync uses the retained `os.Root`, returned bytes are hashed from the same
      opened descriptor, no overwrite occurs, and artifact/root descriptors close after startup
      failure and normal runtime shutdown.
- [ ] Independently run a fresh built binary twice on the same private root and exact loopback port:
      health names unavailable surfaces, wrong/duplicate token and Origin fail, create/lookup pass,
      SIGTERM exits 0, restart lookup/replay are byte-exact, SQLite/listener are released, secrets do
      not appear, and no process/temp path remains.
- [ ] Rerun pinned/network-disabled Go 1.27.1 module/format/vet, three exact T-093 tests, four exact
      T-091 tests, restart x10, full backend, full race, build and inventories with no required
      failure or skip.
- [ ] Map every T-091/T-093 definition-of-done item to visible evidence. Keep persistent worker/Fake
      execution, persisted board/SSE, restore/backup, browser/live UI, root CI, real providers and
      wider G1/G2 explicit NotRun/Deferred.
- [ ] Write validator-compatible `.team/reports/T-094.md`, validate the report, run
      `git diff --check`, prove report-only scope, and return unconditional PASS only if no blocker
      remains; otherwise unconditional FAIL. The report does not accept itself or update PLAN.

Verify with: complete source/diff/hash inspection; current report validator; exact digest-pinned
`golang:1.27.1-bookworm@sha256:648f440f42a0958804efb24df176f806f9d353b41f1c0627f666428e40310f6b`
under network-none/read-only source/module cache; fresh attacks; isolated real-process smoke;
named/repeated/full/race/build/inventory gates; `git diff --check`.

Budget: One report-only independent re-review; no delegation, candidate edit, destructive cleanup
outside reviewer-owned exact temp paths, commit, push or external mutation.
