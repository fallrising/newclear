# obechow — delivery evidence（已退役）

`obechow` 是 `phark` 的前身：同樣是 Spring/React/SQLite 的 social deck，但 `phark` 已是功能後繼者。

這裡保留的是 obechow **獨有**的部分——完整的 VPS delivery bundle 與 CI/CD 交付證據
（P04–P06 的 SDD、實作計畫、測試計畫、驗證紀錄、rollout readiness、CI runbook 與 deploy workflow）。
程式碼沒有搬移，因為 `phark` 的實作已全面取代它。

**來源**：`fallrising/obechow` @ `8b2ab0ffe75e1491c2b1729b787de6a1f57abee4`（2026-07-30）
**原 repository**：已 archive，完整歷史保留於該處。
