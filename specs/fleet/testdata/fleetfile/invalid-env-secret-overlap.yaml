apiVersion: fleet.catalog/v1
kind: Service
metadata:
  name: overlap
spec:
  node: vps-hel-1
  image: ghcr.io/fallrising/hello:1
  expose:
    mode: public
    hostname: overlap.example.com
    port: 8080
  env:
    RELAY_TOKEN: not-a-secret-shape
  secrets:
    - RELAY_TOKEN
