apiVersion: fleet.catalog/v1
kind: Service
metadata:
  name: dups
spec:
  node: vps-hel-1
  image: ghcr.io/fallrising/hello:1
  expose:
    mode: private
    hostname: dups.fleet.internal
    port: 8080
  volumes:
    - name: blobs
      mount: /data
    - name: blobs
      mount: /other
