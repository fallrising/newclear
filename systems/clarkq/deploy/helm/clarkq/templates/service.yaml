apiVersion: v1
kind: Service
metadata:
  name: {{ include "clarkq.fullname" . }}
  labels:
    {{- include "clarkq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clarkq.selectorLabels" . | nindent 4 }}
